# Open backlog — Tier 1–6 full row bodies

> Relocated from `CLAUDE.md` (2026-07-25) to keep the always-loaded context under budget.
> `CLAUDE.md` keeps a one-line index of these rows; the full bodies live here.
> Load-bearing conventions (O27–O65) and Design Decisions stay in `CLAUDE.md`.

#### Tier 1 — structural gates (the most downstream work stacks on these; do first)

| # | Item | Notes |
|---|------|-------|
| O80 | **`[PART]`** User-customisable mode-transition schedule | Core data model + matcher shipped: `core/mode/ModeProfile.kt` carries an ordered `List<Rule>` where each Rule is `(Condition, targetMode, optional per-rule overrides)`. Conditions cover battery-above/below threshold, charging state, screen on/off, network (Wi-Fi/cellular/offline), time-of-day range (wraps across midnight), weekday set, and AND-composition. `ModeProfile.evaluate(state)` returns the first matching rule's target mode or the default. `ModeProfileTest` covers each condition + a realistic profile. **Orchestrator + hysteresis + pulses + transition log + starter template all shipped — see G39** (`AutoModeController`, `ModeOrchestrator`, 4-way Settings selector). **Remaining here:** the rule-editor UI (compose/edit/reorder rules), profile persistence (today the profile is the hardcoded `AutoModeController.starterProfile()` — nothing to persist until users can edit), and applying per-rule overrides (`scanIntervalMs`/`gossipCadenceMs`/`maxSessionMs`) on top of the active mode's envelope. Cross-references O33 (battery measurements help users pick threshold values), O62 (single source of truth for mode behaviour), G39. |
| O106 | **`[STRUCT]` `[PART]`** `:node` module — headless JVM runtime shared by desktop app and USB-bootable service** | **Headless test node SHIPPED 2026-07-22** (`docs/NODE_KICKOFF.md` both steps done): (a)-extraction landed as `core/runtime/MeshRuntime.kt` (host-agnostic orchestration — reseed, purges, coordinator + beacon/recompute loops, HLC persistence via `HlcStore` port, incoming-sink + backbone-realizer lambdas; MeshService now hosts it, byte-identical behavior, all suites green) and `:node` (4th Gradle module) runs the real engine over the G40 `LanTransport` with the in-memory repos (moved `:simulator/data/InMemoryRepos.kt` → `core/data/memory/`), file-backed identity+HLC (`~/.rumor-node`, unencrypted — TEST INSTRUMENT, the product node gets the O44 encrypted impl), and a localhost status page (JDK HttpServer, zero deps: peers/messages/events + send box; also curl-able for scripted driving). Verified: two nodes on one machine mDNS-discover, complete signed HELLO + gossip sessions, deliver an HTTP-composed broadcast end-to-end to overlap=1.0. Run: `./gradlew :node:installDist && node/build/install/node/bin/node [--bind ip] [--http port] [--data dir]`. **Remaining here = the product node** (2026-07-19 decision: waits until the phone is finished): The load-bearing decision (`: core` pure JVM, proven headless by `:simulator` for months) means most of the work is *assembly*, not porting. Pieces, each with an existing seam: (a) **`MeshRuntime` extraction** — the wiring in `MeshService.startMesh()` (engine ↔ transport config, reseed, flow collectors) is Android-hosted but not Android-shaped; extract to `:core` so MeshService, a desktop `main()`, and a systemd unit each just host it. (b) **Storage** — repos are already interfaces with Room + in-memory impls; :node needs a third: SQLDelight (one impl everywhere) vs JDBC-SQLite adapter (less work) — see re-scored O1; decide when this row starts. (c) **Identity** — file-based encrypted impl behind the existing `IdentityProvider` interface (see O44 platform-fork note). (d) **UI — GUI-first, never headless-facing (user correction 2026-07-15):** the median person in an austere scenario must be able to set this up, so the USB image boots *straight into* a dead-simple fullscreen GUI — big status ("Rumor running", peer count, network name), on/off, zero config, identity auto-created on first boot, AP auto-started; a terminal must never be visible. The *runtime* stays UI-agnostic (that's the :node/MeshRuntime split), but "headless daemon you ssh into" is not a product surface. Two implementation paths: kiosk-browser fullscreen on the localhost Ktor dashboard (cheapest, steals the `:simulator` pattern; localhost violates no offline/no-CDN rules) or a small dedicated Compose Desktop status app (a purpose-built ~3-screen UI — NOT a CMP migration of `:app`, which stays off the table until real demand). (e) **USB-boot packaging** (ops, not architecture): persistence partition on the stick — identity + message store MUST survive reboot (fresh identity per boot = broken); hostapd laptop-as-AP config per O104; autostart straight into the GUI. **Archimedes' O83 (desktop macOS/Linux/Windows relay-node targets) folded here** — same deliverable; macOS note from O104 discussion: Macs are a :node packaging target, not a reflash target (Apple Silicon can't live-USB boot; any Mac that boots macOS runs the JVM node natively). |
| O93 | **`[PART]`** Wi-Fi Direct discovery fails while STA-associated (field-confirmed)** | **Same-LAN mDNS transport SHIPPED — see G40** (`LanTransport` + `LanTransportManager`, active exactly while STA-associated, field check pending). **Remaining here:** different-LAN/no-LAN STA+P2P coexistence (GO forced to the current STA channel for single-radio concurrency), the "location off" UI warning surface. **Port-change re-target SHIPPED (2026-07-22, `aff4038`):** a peer that restarts gets a new ephemeral port; `LanTransport.onPeerLocated` now cancels the stale loop and re-targets on any mDNS re-resolve to a new endpoint (was dialing the dead port until the failure budget expired — minutes of blindness; field-hit on app reflash). **Follow-up (user-requested "optional, default-on automatic refresh"):** the reactive re-target only fires when JmDNS re-resolves — but an abrupt peer death sends no mDNS goodbye, so the stale record lingers by TTL. Add an optional periodic re-resolve nudge (default-on) so a silently-restarted peer is re-discovered fast without waiting out the TTL. Cross-ref O124 (search-button manual refresh is the on-demand cousin). **STA↔P2P coexistence — is churning the only fix? (user Q 2026-07-22, live-confirmed partition):** disconnecting a single-radio phone from the AP partitions it (LAN dead → falls back to Wi-Fi Direct; the LAN group can't reach it). Three fixes, best-first, churn is the *fallback* not the answer: **(A) same-channel STA+P2P concurrency** — force the P2P group to the *current STA channel* (the O98 channel-honoring `createGroup` path); most chipsets run STA + one P2P group concurrently on a *single* channel, so the phone is genuinely on BOTH LAN and Wi-Fi Direct at once and bridges them (GossipEngine relays across transports for free). This is the real fix; it's device-dependent and needs a per-chipset capability probe. **(B) infrastructure AP (O104)** — a wall-powered node runs an AP so everyone associates to *it* (all-LAN, no P2P needed at all); the austere-scenario answer, sidesteps the whole conflict. **(C) churn / DTN data-mule (fallback)** — time-slice: be on LAN for a window, sync, switch to P2P, sync, carry messages between the two. Rumor is delay-tolerant (O55) so this is *acceptable*, but it adds latency + connection thrash and should be the last resort when (A) fails and no (B) exists. **So: probe for same-channel concurrency first (A); deploy infra APs where possible (B); churn only when neither works (C).** **WIDE-AREA CORRECTION (user 2026-07-22): (A) only solves the LAN-EDGE bridge (one hop, AP↔P2P) — it does NOT solve a wide-area mesh.** Same-channel STA+P2P lets a node be on the AP + one P2P group at once, but a mesh spread over a large area needs to chain *multiple* P2P hops (node C reaches distant D via its own group while C is a client toward the LAN), and a single-radio phone can be in only ONE P2P group at a time (GO *or* client, not both, not two groups). So for wide-area the options are: **(D) GO+client (multi-group) concurrency** — some chipsets can be GO of one group AND client of another simultaneously (the harder cousin of STA+P2P); this is the non-churn wide-area answer and is exactly the unfinished O98 "multi-group backbone decomposition" work — device-dependent, needs a capability probe; **LoRa / transport-plugin tier (O54/S1) for genuinely long hops** — Wi-Fi Direct is the wrong radio for miles; the right architecture is Wi-Fi-Direct clusters stitched by LoRa bridges, not one Wi-Fi-Direct chain spanning the whole area; **(C) smart churn as the fallback** (user refinement — make it less bad, since sometimes it's unavoidable): pick mules by **screen-off first** (an idle node's downtime doesn't hurt anyone's active use) and **rotate mule duty** across eligible nodes to minimize per-user downtime — a fairness/scheduling layer on the data-mule model, not blind time-slicing. Cross-refs O98 (multi-group backbone = option D), O54/S1 (LoRa long-hop tier), O104 (laptop-as-AP = option B), O107 (transports already bridge at the engine layer). **File the smart-churn scheduler (screen-off-priority + rotation) as its own follow-up when the mule tier is built.** **PRIORITY ORDER (user 2026-07-22, for the wide-area/coexistence case): (D) GO+client multi-group concurrency is the BASELINE (default attempt) → LoRa if the hardware is present → (C) smart churn as fallback.** (A same-channel STA+P2P and B infra-AP remain the LAN-edge / infra-available cases; this D→LoRa→C order is specifically the wide-area multi-hop priority.) **Also owed: both SMART (automatic) switching AND MANUAL (user-override) switching between transports/roles must be figured out** — the automatic policy decides D-vs-LoRa-vs-C and who mules; the manual surface lets a user force a role/transport (pairs with O97's manual persistent-link control surface). Make both first-class, not just the auto policy. Original analysis: With both test phones associated to the same 5GHz AP, `discoverPeers()` succeeded but never produced a PEERS_CHANGED with results — on both a Motorola/Qualcomm and a OnePlus device. Disconnecting from the AP (radio still on) made discovery return the peer within ~3s. Users will not drop home Wi-Fi to use a mesh app, so this needs a real answer: when both devices are on the *same* LAN, mDNS transport (O73-style) sidesteps the radio conflict entirely and is the natural fix; different-LAN/no-LAN coexistence needs deeper work (listen-channel pinning, discovery-while-associated quirk table, or accepting BLE-only rendezvous with direct-connect). Also note the system Location toggle (`location_mode`) must be ON for P2P scan results on API 34 devices regardless of NEARBY_WIFI_DEVICES grant — surface a UI warning when it's off. **Field-confirmed (2026-07-13, 3-device run):** forming the P2P group doesn't just fail-to-discover while STA-associated — it *actively disrupts* nearby Wi-Fi. The OnePlus GO created the group on 5 GHz ch 36 (`mP2pFreq=5180`); a bystander's 5 GHz Wi-Fi dropped. On single-radio phones STA + P2P-GO on *different* channels can't coexist, so the OS tears down/moves STA. Two mitigations, both pointing the same way: (a) **primary fix for casual/home use — mDNS/NSD transport over the shared LAN (O73-style): when peers are already on the same Wi-Fi, never form a P2P group at all**, which sidesteps both the discovery failure and the interference; (b) force the GO to the *current STA channel* so STA+P2P run concurrently on one channel (most chipsets support same-channel concurrency; cross-channel is what forces the disruptive switch). Framing per O55: Wi-Fi Direct is the *infrastructure-down* transport (no AP to interfere with — the actual design target); mDNS is the *casual-use-on-existing-Wi-Fi* transport. Both should exist, selected by whether a usable shared LAN is present. **Archimedes' O73 row proposed the same mDNS/NSD transport (Android + iOS, JmDNS/NSD)** — folded here; one shared-JVM impl per O107. |
| O107 | **`[STRUCT]` `[PART]`** Transport SPI — extract WHEN the second transport lands, not before** | **2026-07-18, second transport landed (G40):** the extraction that actually fell out is the *boundary type*, not an interface — both transports independently construct `GossipSession` and emit `PeerExchangeResult` (now with `ExchangeSource.LAN`); MeshService wires each concrete's flow into `GossipEngine.onExchange`. A formal `Transport` interface still has no consumer that needs polymorphism (MeshService names both concretes for different lifecycles anyway); defer it to O106 :node, whose runtime hosting is the first caller that wants "a list of transports". Original rationale: There is no `Transport` interface today; `WifiDirectTransport` is a concrete class wired directly into `MeshService`, and that is *correct for now* (house rule: no premature abstractions — an interface with one impl is speculation). The trigger: **O93's mDNS/LAN transport**, which is pure JVM and wanted by BOTH Android (casual same-Wi-Fi mode) and :node (laptop-as-AP LAN per O104) — **write it in shared JVM code from day one, once, for both platforms**, and extract the Transport SPI at that moment, when its real shape is known from two concrete impls. The SPI then also serves O54's transport-plugin tier. Do not extract it speculatively before the mDNS transport exists. |
| O98 | **`[PART]`** Smart persistence — covering-set of persistent links under a per-device degree cap** | **Shipped + field-verified** (full phase-by-phase history archived in `docs/COMPLETED_GAPS.md` — G37, G38, O98 archive): Phase 1 deterministic degree-bounded backbone solver (`core/routing/PersistencePlanner.kt` + `PersistenceReconciler.kt` — determinism IS the election: identical view → identical link set, no coordinator; capacity by mode MOBILE=2/STATIC=4/FREE=8); Phase 2 20-node simulator validation (`SmartPersistenceScenarioTest`); MeshView substrate (`core/routing/MeshViewTracker.kt` — planner input assembled from SELF_PRESENCE beacons, LRU-bounded, recency-decayed); Phase 3a coordinator (`core/routing/PersistenceCoordinator.kt` + MeshService beacon/recompute loops, plan gates `isPriorityPeer`); Phase 3b radio realization (`GroupCredentials`/`BackboneRealizer`/`ChannelSelector` + `WifiDirectTransport.applyBackboneRole` — prompt-free credential joins, channel-honoring autonomous groups, hour-stable 3-phone star, negotiated pairing retired on API 29+; pre-10 devices keep the legacy flow); 0.6.6 dead-group client re-bootstrap (exhausted retries → rediscover + bootstrap-decision rerun, junior-yields resolves double-host — field check pending). **Hard-won radio facts (don't rediscover):** negotiated `connect()` IGNORES `setGroupOperatingFrequency` on every tested phone — autonomous `createGroup` is the ONLY channel-honoring path; `createGroup` joins need a manual WPS accept UNLESS the client joins by derived credentials as a legacy STA (SSID = `DIRECT-`+12hex(SHA-256(`rumor-o98-net-v1:`+hostUserId)), passphrase tag `rumor-o98-psk-v2:` — deliberately non-secret, the group is a transport lane, trust stays with HELLO Ed25519); plain `connect()` into a formed group raises the AOSP invitation prompt — never do it; host election from asynchronous discovery views fragments without the deterministic planner; channel choice: least-occupied non-DFS 5 GHz, UNII-3 first, co-channel with the strongest neighbor when saturated (never adjacent). **Remaining work:** scale beyond one star — multi-group backbones (today's realization converges the whole 3-phone mesh into ONE credentialed group; a 20-node building needs the star *decomposition* realized across multiple simultaneous groups → GO+client concurrency handling or time-slicing); coordinator-level role-flap hysteresis (roles wobble CLIENT↔NONE with beacon freshness; links held anyway); field-verify the 0.6.6 re-bootstrap fix; O102-style airtime work. |
| O100 | **`[PART]` `[TODO/SIM]`** Torrent-style multi-source chunk distribution (swarm fetch)** | **Content-addressed substrate SHIPPED 2026-07-23** (`:core`, the "hard design constraint (i)" wire decision, done before more chunk traffic accretes on transferId identity): `TransferMetadata.chunkHashes: List<String>?` (per-chunk Base64 SHA-256, inside the Ed25519-signed payload so a relay can't forge a chunk's expected hash — a poisoned chunk is now attributable, not just a whole-file-hash failure at the end) and `Chunk.contentHash: String?` (the content-group hash `(contentHash, index)` that makes a chunk from any seeder interchangeable; `transferId` stays the per-send session id). `Chunker.chunk()` populates both, `Chunker.chunkHash()`/`verifyChunk()` are the drop-a-bad-chunk-and-attribute primitives, `reassemble()` rejects a poisoned chunk at its index when hashes are present (legacy no-hash metadata still reassembles via the whole-file hash). Additive+nullable → no single-source regression; 6 new ChunkerTest cases + names reserved in `RENAMED_FIELDS_NEVER_REUSE.md` + wire-format.md §4.4. **Receipt-time verification SHIPPED 2026-07-24:** `TransferAssembler` retains the signed `metadata.chunkHashes` in memory for the transfer lifetime and verifies every inbound chunk at its index before storing (drops + logs a hash-mismatched chunk for the watchdog to re-NACK, instead of only catching it via the whole-file hash after the whole transfer landed); cleaned up on every terminal path; legacy no-hash metadata falls back to the whole-file check. `TransferChunkVerifyTest` (simulator, 2) drives real signed metadata+chunks over `SimTransport`: clean transfer reassembles, a poisoned chunk is dropped (blocks completion), a correct repair chunk for the same group+index then completes it (mechanism-B repair on the substrate). **Remaining:** (1) **multi-source assembler** — accept chunks for one content group from *many* senders (today still keyed on the single-send `transferId`; needs `TransferAssembler` to key its receive buffer on the `contentHash` group + persist `chunkHashes` on the `TransferRecord`, a repo/schema change) and evict a seeder that serves a hash-mismatched chunk (the receipt-time verifier already identifies which chunk/sender was bad — the eviction policy is the missing piece); (2) the **sim mechanism bake-off** (A active-query / B partial-repair / C passive-piggyback) — B is nearly free on the new substrate (CHUNK_REQUEST repair by `(group hash, index)`); (3) merkle-root variant if `chunkHashes` list size becomes a problem for very large files (today a hash list, fine under the 4 MB frame guard for realistic swarm targets). Original spec below. Most people who hold a file hold the *whole* file — so if chunking is deterministic, many peers can seed the same content to one requester, spreading load and minimizing single-node bandwidth. **What's missing today (verified):** `Chunker` keys chunks to a random per-send `transferId` (UUID), so the same file sent by two people produces two unrelated chunk sets, and `TransferAssembler` is single-source per transferId — **multi-source chunk recombination does not exist yet.** Required substrate: (a) **deterministic content-addressed chunking** — chunk identity = `(hash-of-completed-file, index)`; the completed-file hash is the "group name" every chunk carries, so a chunk from any seeder is interchangeable and a chunk always knows which group it belongs to; (b) **multi-source assembler** — accept chunks for one group from many senders. Three candidate fetch mechanisms (user-proposed), to be compared **in the simulator** before building for real: **(A) active query** — broadcast "who has hash H?"; holders reply; requester selects which chunk to pull from which peer; **(B) partial-transfer repair** — if someone hands you a file with chunks missing, request the specific missing chunk by `(group hash, index)` under that group name, from the original sender or anyone else; **(C) passive piggyback** — regular traffic (messages, relaying, handshakes) appends "while I have you: got any chunks of H?"; if the peer has some but not the ones you want, keep looking. (A fourth variant — provenance backtrace, asking who *they* got chunks from and querying up the chain — was considered and **rejected 2026-07-15**: who-got-what-from-whom is exactly the social-graph metadata O27/O58 works to limit, and O29 breadcrumbs already capture the reverse-path signal passively.) Deliverable: sim scenarios measuring delivery latency, per-node bandwidth, and load spread for A, B, C **and combinations** — the winner is chosen empirically, not by taste. Cross-refs: O26 (plugin distribution over the mesh is the first natural customer), O18 (cancel/pause must survive multi-source), O43 (heal-storm budgets), O19/O14 (same UI surfaces). **Analysis (agreed 2026-07-15):** (i) **hard design constraint — per-chunk hashes (hash list or merkle root) inside the signed TransferMetadata.** Whole-file-hash-at-the-end can't attribute poisoning once there are multiple seeders; per-chunk hashes make a bad seeder identifiable and evictable — without them one malicious node griefs every swarm it touches. (ii) Mechanism triage: **B is nearly free** (CHUNK_REQUEST repair exists; it just needs group-hash identity) — build regardless of the sim outcome; **C is the most Rumor-native** (extends the gossip summary to chunk inventory, rides existing machinery); **A is a flood-class spam surface** — rate-limit it. (iii) **Sequencing: content-addressed chunk identity is a wire-format decision — schedule immediately after the current reliability chain**, before more chunk traffic accretes on transferId identity. |
| O163 | **`[TODO/CODE]`** `:node` invisible to CI — not built, tested, or compiled (filed 2026-07-30, audit #5) | `.github/workflows/ci.yml` runs only `:core:test :app:testDebugUnitTest :simulator:test` + `:app:assembleDebug`; `:node` is in `settings.gradle.kts` with its own JUnit4+vintage scaffolding but is never referenced, and `:app` doesn't depend on it so nothing touches it transitively. `node/Main.kt`/`node/SybilDriver.kt` construct `GossipEngine`/`GossipSession`/`MeshRuntime`/`LanTransport.Config`/`IdentityProvider` positionally — any breaking `:core` signature change silently breaks `:node` until someone runs it by hand (same shape as the documented `:app`-JUnit4-never-ran incident: a module CI doesn't touch is a module CI can't protect). Fix: add `:node:test` (+ ideally `:node:assemble`/`:node:installDist`) to `ci.yml`. Today mostly buys compile-checking (no `node/src/test` yet — see O182) but that alone catches real breakage. Cross-ref O106. |
| O182 | **`[TODO/CODE]`** `:node` has no `src/test` despite declaring test scaffolding (filed 2026-07-30, audit #26) | `node/build.gradle.kts` declares junit + kotlinx-coroutines-test + junit-vintage-engine but `node/src/test` doesn't exist — scaffolding present, unused. Lower priority while the substantive logic (`MeshRuntime`) is tested from `:core`, but worth a note before the module grows per O106. Pairs with O163 (CI wiring). |

#### Tier 2 — security/crypto substrate (pre-ship blockers; Tier 3 is gated on the first two rows)

| # | Item | Notes |
|---|------|-------|
| O144 | **`[DONE 2026-07-23]`** Signature-transcript canonicalization — v2 length-prefixed framing (splice attack closed; filed + fixed 2026-07-23 from the parallel audit, user-approved hard cutover) | **`MessageStore.signableBytes` concatenates `id+senderId+senderPublicKey+sequenceNumber+sentAtMs+type.name+content+encryptedPayload+recipientId` with ZERO separators.** `senderId==SHA-256(pubkey)` protects the identity fields, but the trailing free strings (content/encryptedPayload/recipientId) are freely re-partitionable: a relay can move a signed broadcast's content suffix into `encryptedPayload` — byte-identical concatenation, so the sender's signature still verifies, but the displayed content (which reads only `payload.content`) is now **truncated** (e.g. "Evacuate at 9pm, NOT before" → "Evacuate at 9pm", validly signed). A real integrity break in the core auth scheme. **PARTIAL FIX SHIPPED 2026-07-23 (non-wire):** `MessageStore.ingest` now drops (a) any message whose `recipientId` isn't null-or-64-hex (`CryptoManager.isValidUserId`), and (b) any BROADCAST carrying `encryptedPayload`/`recipientId` — closes the demonstrated broadcast-truncation + the recipientId-splice without a wire bump (every legit `composeX` path satisfies both; verified + tested in `MessageStoreIngestTest` with a real splice whose sig genuinely verifies). **REMAINING — needs a user decision (wire-format-affecting):** the proper fix is a length-prefixed canonical transcript (`len:field` per field, not a bare delimiter which is itself forgeable) under a `rumor-msg-v1:`→`v2:` domain-tag bump, back-ported to the newer `'|'`-delimited helpers too. This changes the signed bytes → needs a coordinated fleet update + a transition decision (dual-verify v1&v2 during a window vs hard cutover — the fleet is on 0.6.13, all test devices, no shipped users). **Do not cut over the signing format without the user's call.** Cross-refs O65 (two wire layers), O112 (isValidUserId), `docs/RENAMED_FIELDS_NEVER_REUSE.md`. **REAL FIX SHIPPED (`0.6.14-o144-sigv2`, vc35):** `signableBytes` now writes each field as `<charLen>:<value>` (self-delimiting / prefix-free) under a `rumor-msg-v2:` domain tag — no two distinct field-sequences can share a transcript, so the splice is structurally impossible (test: the truncation splice's signature no longer verifies under v2). Hard cutover, no shipped users; v1 retired in `RENAMED_FIELDS_NEVER_REUSE.md` + wire-format.md §3.2/§6. Two stale hand-copied `signableBytes` duplicates (RbsrSimTransportTest, MessageGenerator) found + fixed to delegate to the real function so they can't drift again. The `'|'`-delimited newer helpers assessed splice-safe (fields are hex/base64/decimal/enum, can't contain `'|'`) — deliberately not re-framed. Belt-and-suspenders ingest shape guards (broadcast-must-not-carry-encryptedPayload, recipientId null-or-64hex) kept. **Fleet must reflash to `0.6.14` to interoperate** (v1 and v2 signatures never cross-verify — by design). |
| O20 | **`[TODO/EMU]`** Private key in Android Keystore (TEE-backed)** | Currently the Ed25519 private key lives in app storage — readable by any code running in Rumor's process, including DEX plugins. Migrate to Android Keystore so the key never leaves the Trusted Execution Environment. `PluginContext` exposes `signWithLocalKey(bytes): ByteArray` only — no raw key access. Pre-ship requirement before any plugin loader ships. Note: Keystore-backed keys are device-bound and non-exportable; backup/restore of identity requires a separate identity-backup protocol (seed phrase or similar). |
| O44 | **`[TODO/EMU]`** Keystore wrapping-key architecture (refines O20)** | Don't put the raw Ed25519 private directly in Android Keystore — it's non-exportable by hardware contract, which breaks every backup path. Instead: generate an AES-256-GCM wrapping key in Keystore (`setUserAuthenticationRequired` if biometric available, `setUnlockedDeviceRequired(true)` minimum, `setIsStrongBoxBacked(true)` on supported hardware). The Ed25519 private key lives encrypted-at-rest in app storage; decrypted into memory only inside `signWithLocalKey()`, zeroized on exit. Plugin context exposes `signWithLocalKey(bytes)` only — never a raw-key accessor; DEX plugins cannot exfiltrate. Surface `KeyInfo.isInsideSecureHardware()` in the UI so users know whether their device has hardware-backed protection or fell back to software Keystore. **Platform-fork note (2026-07-15, O106):** this whole row is Android-only by nature — the desktop/USB node gets a file-based encrypted identity impl behind the same `IdentityProvider` interface instead. The identity-at-rest story forks per platform by design; don't try to unify it. |
| O38 | **`[PART]`** Receiver-side forward secrecy via rotating recipient prekeys** — **wire shape + verifier shipped; rotation scheduler + sender cache + composeDirect selection open.** `MessageType.PREKEY_PUBLISH` (TRANSFER_SETUP class, MAX_BROADCAST_HOPS ceiling, relayed like other infrastructure broadcasts). `PrekeyPublish(publisherId, publisherPublicKey, prekeyPublic, validFromMs, validToMs, signature)`. Domain tag `rumor-prekey-v1:` binds publisherId + publisherPublicKey + prekeyPublic + validity window — a relay extending the window, swapping the prekey, or substituting another publisher's identity breaks the sig. `PrekeyVerifier` checks (1) publisherId hashes from publisherPublicKey, (2) signature verifies, (3) `validToMs > validFromMs` sanity. Window enforcement against wall clock is the caller's job (stateless verifier is time-free). 7 tests in `PrekeyVerifierTest` cover valid/tampered-prekey/tampered-window/tampered-publisherId/tampered-pubkey/bad-base64/inverted-window. O28 fuzz harness added for the new payload type. **Rotation scheduler + local prekey store SHIPPED 2026-07-24** (`core/protocol/PrekeyRotator.kt`, pure `:core`, `Clock`-injected for deterministic tests): `rotateIfDue(identity)` mints a fresh X25519 prekey when none is live or the cadence (default 1h) elapsed, signs it as a `PrekeyPublish` with the long-term Ed25519 key (bytes verified to match `PrekeyVerifier` exactly), and hands it back for the host to broadcast; `privateFor(prekeyPublic)` returns the X25519 private for DM decrypt; `purgeExpired()` zeroes + deletes each private past `validToMs + retentionGraceMs` — the FS property is structural (the private literally no longer exists). Validity window 24h, retention grace 1h for in-flight DMs. `PrekeyRotatorTest` (6): verifier-accepts-minted-prekey, cadence gating, **the money test — a sender's ephemeral DH against the published prekey public derives the same key the receiver derives from the stored private (the prekey is a usable X25519 static)**, purge-zeroes-after-grace, unknown-prekey→null, old-prekey-usable-across-rotation. **Sender-side cache already exists + wired:** `PrekeyCache` (`freshestFor(recipientId, nowMs)`, freshest-window-wins, no re-verify — caller's job) and GossipEngine already verifies inbound `PREKEY_PUBLISH` with `PrekeyVerifier` and populates `prekeyCache.put` on the receive path (`GossipEngine.kt:1087/1229`). **Not done (the cross-layer close-out — must land as ONE change or DMs break, and the decrypt half is `:app`, so it wants device verification — do NOT half-ship):** (1) host loop that calls `PrekeyRotator.rotateIfDue` on a timer and broadcasts the result as `PREKEY_PUBLISH` (MeshService/`:app`), plus wiring `PrekeyRotator` as a Koin `single` reachable from decrypt; (2) `composeDirect` selection — consult `prekeyCache.freshestFor(recipientId, clock.now())`; if present, DH the ephemeral against `prekey.prekeyPublic.fromBase64()` (raw X25519, NO Ed25519→X25519 conversion — the prekey is already X25519) and stamp the prekey public into `_ext` (reserve a key, e.g. `_ext.pk`) so the receiver knows which private to use; else the current long-term-static path (the honest offline-for-weeks fallback, FS regression documented); (3) decrypt-side selection in `ThreadViewModel.decryptPayload` (`:app`): when `_ext.pk` is set, decrypt with `PrekeyRotator.privateFor(pk)` (raw X25519 private) instead of `ed25519ToX25519PrivateSeed`; fall back if the prekey was already purged (bounded-FS loss, expected). A sim round-trip test becomes possible once a `:core`/sim decrypt seam exists; today DM decrypt is only in `:app`, so this close-out should be validated on-device. The cryptographic substrate + wire format is now bounded and tested; the remaining work is local-state and scheduler plumbing. Original spec: Current DM crypto (ephemeral sender X25519 + recipient static X25519 + AES-GCM + Ed25519 outer sig) provides **sender-side FS only**. A relay node holding stored ciphertext can decrypt every past DM the moment the recipient's static key leaks — equivalent to the original Briar-BHP gap (CVE-2023-33982), and Briar got away with it only because Tor sat underneath. We have no Tor layer. Fix: recipient periodically generates short-lived X25519 prekeys `R_t` signed by their long-term Ed25519 key, gossips them as small INFRASTRUCTURE messages; senders DH against the freshest `R_t` they hold rather than the long-term `R`; recipient deletes `R_t_priv` after expiry. Stored ciphertext encrypted to an expired `R_t` is unrecoverable even with the long-term key. Privacy by structure, not by code-comment — the private key actually does not exist anymore once deleted, which is what makes the property real against an adversary with full bit-awareness.  Ported in the merge. |
| O53 | **`[PART]`** Sealed-sender DMs — recipient-derivable delivery tag** — **crypto primitives + compose-side stamp shipped; receiver-side precompute + relay routing open.** `core/crypto/HmacSha256.kt` — pure-Kotlin HMAC-SHA-256 over the existing `Sha256` shim (no new platform actual; RFC 4231 vectors 1, 2, and the oversize-key path pinned in `HmacSha256Test`). `core/protocol/SealedSenderTag.kt` — `tagFor(sharedKey, messageId)` returns the 32-byte HMAC over `"rumor-dm-v1:" || messageId`. `core/protocol/SealedSenderKey.kt` — `derive(myUserId, myEd25519Priv, theirUserId, theirEd25519Pub)` returns the per-contact 32-byte HKDF key from X25519 ECDH of the Ed25519-derived static keys; userIds sorted into the HKDF info so direction doesn't matter. `core/wire/SealedSenderTagExt.kt` — `_ext.t` accessor + `withSealedSenderTag` helper. **Compose-side wired (this session):** `GossipEngine.composeDirect` derives the per-contact tag key, computes `SealedSenderTag.tagFor(tagKey, msg.id)`, stamps `_ext.t = base64(tag)`, zeros the tagKey in finally. Skipped for bridged DMs (recipient is foreign-network, not Rumor Ed25519). `_ext` is excluded from `signableBytes` so the outer Ed25519 sig stays valid. Simulator pin: `SealedSenderTagWireTest` (2 tests) drives a real `composeDirect` and proves the recipient (independently deriving the per-contact key) computes a matching tag for the same messageId — the property a relay's pre-match table will exploit. Different recipients on the same sender stamp different tags. Domain tags `rumor-dm-v1:` (tag) and `rumor-dm-recipient-tag-v1:` (key HKDF) reserved forever. **Not done:** receiver-side precompute store (per-contact `tagKey` cached at contact-load time, refreshed on contact-add/remove); relay routing path matches inbound `_ext.t` against the precomputed set instead of routing on plaintext `recipientId`; eventual wire-format-break to remove plaintext `recipientId` once the tag path is universally deployed. The remaining work is on the receive/routing layers; both substrate and compose-side are now bounded and tested. Original spec retained for the open work: Generalization of O52's broadcast tag to all DMs. Replace plaintext `recipientId` field with `T = HMAC(K_recipient, "rumor-dm-v1:" || messageId)`. Relays match incoming DMs against the small set of `T`s they've precomputed for keys they hold; non-recipients can't link DMs to recipientIds on the wire. Signal calls this "sealed sender"; their version trial-decrypts which is expensive — the tag pattern gives the same property cheaply. Closes the plaintext-recipientId leak on every DM (currently the dominant on-wire metadata leak); pairs with O29 breadcrumb routing — breadcrumb signals (recent-inbound-peer, ACK return path) work on tags too once the local node has computed them per-contact. Wire-format change: add tag field via O37's `_ext` until protocolVersion bump.  Ported in the merge. |
| O48 | **`[TODO/CODE]`** Bridge-asserted-pubkey constraint on synthetic userIds** | When a Meshtastic / MeshCore radio reports a new contact, the synthetic userId must be derived as `<bridgePrefix>:<hash-of-radio-asserted-pubkey>` (not as `<bridgePrefix>:<hash-of-name>` and not as a bridge-chosen string). A compromised or malicious radio can then substitute keys only for *new* contacts; existing pinned contacts whose userId is derived from a previously-seen pubkey surface a verification event when the underlying pubkey appears to change (O21 display-name pinning, generalized to pubkey pinning under the synthetic prefix). MeshCore current implementation uses `meshcore:<FNV-1a of name>` — that's spoofable; migrate to pubkey-hash before any DM bridging ships. |
| O43 | **`[TODO/SIM]`** Per-link heal-storm token bucket with traffic-class priority** | Round-robin scheduling guarantees fairness across senders but does not bound total throughput on partition heal. When two large clusters merge, every node tries to flush its delta to every newly-reachable peer; aggregate is still N×peers messages. Add per-outbound-link token bucket sized by traffic class: INFRASTRUCTURE / REALTIME pre-empt the bucket, BULK and reconciliation backlog share what's left. Combined with O42's lazy-pull-by-default (small fingerprint exchange before pushing bodies), this addresses both bandwidth spikes and the DTN-documented "storage exhaustion during long partition" failure mode. **Shares O32's prerequisite:** needs per-peer byte accounting, which only becomes meaningful once the scheduler/relay knows which peer each enqueue is targeted at. The breadcrumb substrate (4010501) and offer-ordering bias (4010501) shipped without this refactor because they operate at the "offer batch presented to the session" layer; O32 and O43 operate at the "decide which peer gets the next byte" layer below. |
| O112 | **`[TODO/CODE]`** Hostile-input hardening sweep (filed 2026-07-17, user request) | What happens when someone names themselves / sends / blocks the literal string `"null"`, a SQL fragment, format-string tokens, RTL-override or zero-width unicode, or a 10MB string? Audit + property-test every user-influenced string through: display layer (Compose rendering of names/message text), Room queries (parameterized by Room — verify no raw concat anywhere), blocklist + keyword-filter entries, wire parsers (extend the G14 fuzz seed corpus with these shapes), and every userId entry point. Post-§2 the wire enforces `senderId == SHA-256(senderPublicKey)` (structurally kills fake userIds in signed messages), but locally-typed userIds (block-by-id field, filter allowlists, room invites) need shape validation (64-hex) at the boundary. Deliverable: a canonical hostile-strings test corpus (null-likes, injections, unicode abuse, oversize) reused across suites. **2026-07-22 live-test progress:** protocol/storage layer PASSES the hostile-string battery (null/SQL/format-string/RTL/zero-width/emoji-control/json all ingest, verify, store, no crash). **Display layer FAILED on oversize + FIXED:** a single 500 KB unbroken-text broadcast wedged Android's native `LineBreaker.nComputeLineBreaks` on the main thread → ANR → feed unopenable (crash-looped; remotely triggerable by any signed broadcast). Root cause: Compose `Text` lays out the *entire* string before `maxLines` truncation. **Fixed 0.6.10 (`ui/DisplayText.kt` `capForDisplay()`, MAX_DISPLAY_CHARS=5000) at all three render sites (FeedScreen, MessagesScreen preview, ThreadViewModel body)** — field-verified feed reopens. **2026-07-23 progress:** `CryptoManager.isValidUserId` (64-lowercase-hex) added + enforced in `BlockManager.block` (the O110 typed-entry point); canonical `HostileStrings` test corpus shipped (`core/src/test/.../HostileStrings.kt`), reused by `UserIdValidationTest` + the BlockManager rejection test. **Still open in this row:** the RTL-override / zero-width-unicode *rendering* safety is unverified by eye (needs the O143 UI pass); wiring the userId validation into the filter-allowlist + room-invite entry points (those UIs not built yet). The oversize-broadcast *wire/storage* half (monolith with no size cap) is O132. |
| O115 | **`[PART]`** Identity lock lifecycle — core zeroize + KDF bump SHIPPED; UI surface open (2026-07-18 audit §2.2 + round 3) | **Shipped 2026-07-23 (overnight):** `lock()`/re-`unlock()` zero `privateKeyBytes` before dropping the reference; every derived AES wrapping key is `fill(0)`'d after use (create/unlock/changePassphrase/rewrap + BlockManager export/import); `PBEKeySpec.clearPassword()` + char-array zeroing in `PlatformCrypto.pbkdf2HmacSha256`; **iterations 100k→600k (OWASP PBKDF2-HMAC-SHA256)** with `kdf_iterations` prefs versioning — existing installs unlock at the legacy count and auto-rewrap at 600k on first unlock (fresh salt, invisible to user); BlockManager export blob gained a `v2:` version prefix, un-prefixed legacy blobs import at 100k (`BlockExportKdfVersionTest`, 3). **Remaining:** a Settings lock action + optional auto-lock timeout (UI — bundle with O143; lock() now actually works but still has zero callers), and threading CharArray from the entry UI end-to-end (the passphrase still rides a JVM String into pbkdf2; clearPassword covers the copies we control). O20/O44 Keystore later hardens the same surface. |
| O116 | **`[PART]`** Room migration chain — pre-first-release gate (2026-07-18 audit §2.4) | **Substrate shipped 2026-07-22:** `room.schemaLocation` KSP arg set, `app/schemas/…/11.json` exported + committed (schema history starts at v11 — earlier versions predate export, no shipped users), `room-testing` on androidTest for future `MigrationTestHelper` verification. **Remaining (activates at first tagged release):** author real `Migration` objects for every schema bump from the first release forward, verified with MigrationTestHelper against the committed JSONs; `fallbackToDestructiveMigration()` stays DEBUG-only. 100%-certain-to-fire once a release exists — the schema-bump discipline in the Coding conventions section now includes "commit the new schema JSON in the same commit". |
| O156 | **`[TODO/CODE]`** `keywordFilterListSignableBytes` signature-splice forgery + repo-wide `*SignableBytes` length-prefix sweep (filed 2026-07-30, audit #1 — HIGHEST) | Same bug class as O144 but NOT covered by that fix. `core/model/KeywordFilter.kt:136-156` transcript is `publisherId \| version \| name \| <entries> \| <allowlist>` with bare `\|`/`:`/`,` around unvalidated free text — `name` and `entries[].pattern` can contain `\|`, so anyone who observed one legitimately-signed list can craft a different `(name', entries', allowlist')` with a byte-identical transcript (shift content across the `name\|entries` or `entries\|allowlist` boundary via an embedded `\|`), reuse the original `list.signature` untouched, and rebroadcast under their own outer sig. `KeywordFilterVerifier.verify` resolves the pubkey from the subscriber's own pinned record, not from the object being verified — so it verifies. Impact: silently changes what gets BLOCK/WARN'd for every subscriber, or drops someone from `userIdAllowlist`, while the UI shows "validly signed by a trusted publisher." No private key needed. `BlockManager.block()` already closed the identical risk (gates through `CryptoManager.isValidUserId`) — not done here. Also LATENT (unwired today, live the moment O79/O89 land): `roomCreateSignableBytes`/`roomActionSignableBytes` (`core/model/Room.kt:142-193`) have the identical free-text-between-bare-delimiters shape, zero call sites today. Fix: length-prefix every field (`<len>:<value>`) under a fresh domain tag (per `RENAMED_FIELDS_NEVER_REUSE.md`), exactly like O144's `MessageStore.signableBytes` v2 — and grep for EVERY `*SignableBytes` helper with the same shape and generalize, don't spot-patch (second time this class shipped after the first "fix"). Cross-ref O144, O157 (retag shares the root class). |
| O157 | **`[TODO/CODE]`** OPEN-room routing tag not in signed transcript — keyless retag into a different room (filed 2026-07-30, audit #31 — HIGH) | `core/wire/RoomRoutingTagExt.kt:11-15` + `GossipEngine.kt:715-761` (`composeRoomMessage`) + `:1135-1152` (`handleRoomMessage`). The routing tag lives in `_ext.rt`, explicitly NOT covered by the Ed25519 signature; `RoomRoutingTag.openRoomTag(roomId)` is a pure public function (no secret needed for any roomId). The OPEN compose path puts no room-identifying data anywhere in the signed transcript (`payload.content` is raw text). So: take any validly-signed OPEN `ROOM_MESSAGE` (the attacker's own or merely observed), overwrite `_ext.rt` with `openRoomTag(otherRoomId)`, rebroadcast — signature still verifies (doesn't cover `_ext`), `RoomTagMatcher.match` matches on tag bytes alone, `handleRoomMessage` delivers unconditionally. Alice's Room-A content displays to every Room-B subscriber, attributed to Alice, attacker needs no key. Contradicts the file's own "tag tampering achieves nothing useful" claim (only true for a bogus tag, not a valid tag borrowed from elsewhere). Fix: bind the resolved room into the SIGNED transcript — an O89 posting-cert alone does NOT stop post-signing retagging. Same root class as O156. Cross-ref O79, O89. |
| O162 | **`[TODO/CODE]`** `RumorMessage.trustLevel` has no `MessageEntity` column — reverts to VERIFIED on Room reload (filed 2026-07-30, audit #6b) | Same failure class as the schema-v10 `_ext` incident (Critical bug history), on the field the headline invariant "Bridge traffic cannot forge VERIFIED trust" depends on. `MessageRepositoryAdapter.toEntity()`/`toMessage()` never read/write `trustLevel`; any message persisted with non-default trust (today only `BRIDGE_VOUCHED`) silently reverts to `VERIFIED` on the next load. Low live impact now (BRIDGED content isn't persisted at all — see O161; `BRIDGE_VOUCHED` isn't reseed-eligible) but O47's planned "via-bridge" per-message label depends on `trustLevel` surviving reload and will misrender the moment it ships. `MessageEntityExtRoundTripTest` — built after the original `_ext` incident to catch exactly this class — passes today only because its `msg()` fixture never sets a non-default `trustLevel`. Fix: add a `trustLevel` column (schema bump + exported JSON per O116) + a non-default case to that test. Bundle with O161 in one `MessageEntity` commit. Cross-ref O47, O17, O116. |
| O165 | **`[TODO/EMU]`** No `FLAG_SECURE` anywhere — passphrase entry + decrypted DMs are Recents/screenshot exposed (filed 2026-07-30, audit #3) | Grep confirms zero `FLAG_SECURE`/`window.addFlags` in `app/src/main/java`. Affected: `ui/onboarding/OnboardingScreen.kt` (identity passphrase + a "Show" cleartext toggle ~83-90), `ui/settings/ChangePassphraseScreen.kt` (current/new/confirm), `ui/blocks/BlockManagementScreen.kt` (export/import passphrase dialogs ~327-394), `ui/messages/ThreadScreen.kt` (decrypted DM bodies via `MessageBubble` ~140). Any home/recent-apps gesture snapshots the live screen into the OS app-switcher card; any screenshot/recording tool captures it freely. O45 already mandates FLAG_SECURE for the FUTURE mnemonic-export screen on this exact rationale — the equally-sensitive screens that exist today have none. Broader/more urgent than O45's scope. Verify on emulator. Cross-ref O45. |
| O166 | **`[TODO/CODE]`** `LanTransport` inbound accept loop has no connection cap or per-source throttle (filed 2026-07-30, audit #19) | `core/transport/lan/LanTransport.kt` `start()` (~113-117) accepts unbounded concurrent inbound `GossipSession`s in a `while (isActive)` loop with no per-IP rate limit and no inflight cap. The `sessionGate` dedup (first-come-wins per verified peer userId) only kicks in AFTER HELLO completes, so it doesn't bound pre-verification crypto cost. Matters more than for Wi-Fi Direct (whose ~8-10 GO-client cap is a natural ceiling a regular AP lacks) — and O104's `:node` intent is literally "the laptop IS the AP," so any device that associates can reach this port and force Ed25519-verification work per connection at zero attacker cost. Fix: cap total inflight sessions + per-source throttle before HELLO. Cross-ref O104, O107, O43. |
| O170 | **`[TODO/CODE]`** `:app` `BridgeCodecFuzzers`/`BridgeCodecSeedCorpusTest` mask crashes (filed 2026-07-30, audit #11) | `app/src/test/java/com/rumor/mesh/plugin/fuzz/BridgeCodecFuzzers.kt:29,35` + `BridgeCodecSeedCorpusTest.kt:45,52` — `runCatching{}` catches `Throwable` (not just `Exception`), so a `StackOverflowError`/`OutOfMemoryError` from the hand-rolled Meshtastic/MeshCore protobuf readers (the file's own docstring calls these "the highest-risk untrusted-bytes surfaces in the app") is absorbed before Jazzer can see it — a `JAZZER_FUZZ=1` run against these two harnesses can never report a finding regardless of decoder brokenness. `BridgeCodecSeedCorpusTest`'s two `@Test`s additionally have zero assertions. Same pattern as the already-tracked `:core` fuzz files. Fix: catch `Exception` not `Throwable`; add assertions. Cross-ref O112, O28. |
| O172 | **`[TODO/CODE]`** `Rbsr.respond()` has no per-round frame-count cap — CPU-exhaustion DoS (filed 2026-07-30, audit #15) | `core/sync/Rbsr.kt:198` — a single ~4MB packet can carry ~25-30k Fingerprint frames, each an O(N) linear scan over the full local item set with no cancellation checkpoint. DoS surface aimed exactly at large-store anchor/backbone nodes (the ones O55/O98 most want to keep alive). Fix: cap frames processed per round + add a cancellation checkpoint. Cross-ref O42/G24, O98. |
| O180 | **`[TODO/CODE]`** `NodeIdentityProvider` writes key-seed at default umask then chmods 0600 — brief world-readable window (filed 2026-07-30, audit #24) | `node/.../NodeIdentity.kt:57-61` creates the private-key seed file at default perms, THEN chmods to 0600 — a real (brief) window where a fresh identity file is world/group-readable on a shared machine. Worse, the `Files.setPosixFilePermissions` call is wrapped in `runCatching` with no failure logging — a non-POSIX FS or unrelated I/O error leaves the file at default perms silently. `TEST INSTRUMENT ONLY` today per its KDoc, but O44 designates this file's descendant as the product `:node` identity impl — get the create-with-restrictive-permissions-from-the-start pattern right before that promotion, not after. Cross-ref O44, O106. |
| O189 | **`[TODO/EMU]`** Three-mode duress: fake-profile / crypto-shred-wipe / explode (filed 2026-07-30, privacy workshop) | Threat: SHTF-realistic physical seizure + coercion (O55), which O20/O44 do NOT address (they stop a thief, not a rubber-hose). All three modes are passphrase-derived KEKs over **crypto-shredding** (flash can't be reliably overwritten; destroy the 32-byte key instead). (1) **Fake-profile** — the duress passphrase derives a *different* KEK unlocking a decoy vault; the real vault's ciphertext must be indistinguishable from free space so "how many identities exist?" has no on-disk answer (hidden-volume layout). Load-bearing property — without it coercion just continues. (2) **Wipe-everything** — a passphrase whose only effect is destroying the real vault KEK; instant, irreversible, and can simultaneously show the decoy so the screen looks like a normal unlock. (3) **Explode** = wipe + fire the pre-signed O192 compromise beacon (the two features share one button — entering explode shreds locally AND tells contacts the identity is burned). Ship-gate: **do it right or don't claim it** — half-strength deniability is worse than none (users rely on it). Hidden-vault layout fights O23 eviction + O116 migrations (real work, not a weekend). Depends on O165 (FLAG_SECURE) so the decoy screen isn't defeated by the Recents snapshot. Cross-ref O20, O44, O192, O190, O23, O116, O165, O55. |
| O190 | **`[TODO/EMU]`** Ephemeral / RAM-only mode — tiered scale-of-forgetting (filed 2026-07-30, privacy workshop) | Option to keep identity + message store only in memory, never on disk — strongest privacy primitive is nonexistence (same logic as O38's deleted prekey). NOT one switch but a scale: persist-across-reboot (default) → **lose-on-reboot** → **lose-on-app-close** → lose-on-battery-death (the involuntary reboot case). Only difference between tiers is WHEN the vault key is discarded. **Seam already exists:** `:simulator`'s in-memory repos (now `core/data/memory/InMemoryRepos.kt` per O106) are a complete in-memory backing for every repo; ephemeral mode = wire those + `Room.inMemoryDatabaseBuilder` instead of Room-on-disk. The catches are all AROUND the DB, where ephemerality actually leaks: no SQLite WAL/journal on disk, no logcat content leak, keyboard cache, and Recents/screenshot (makes O165 FLAG_SECURE non-negotiable in this mode); Android may still spill process memory to **zram** (compressed swap) — unpreventable at the app layer, disclose honestly. Interaction with O191: ratchet state can't survive restart in the app-close tier → ephemeral mode resets ratchets → conversations silently re-run X3DH on next contact (compatible, must be designed). UX landmine — opt-in, loudly labeled ("nothing survives"). Cross-ref O38, O165, O191, O106, O189, O55. |
| O191 | **`[TODO/SIM]` — NOT committed; de-risk in the simulator first** Ratcheted DM path — Double Ratchet for 2-person DMs; would supersede O38, absorb O53 + repudiable-auth (filed 2026-07-30, privacy workshop) | **Status: this is a candidate, not a decision. There appear to be real issues (the four mesh-hard points + the wire fork below) — prototype the idea in the simulator and measure it before committing to build it for real.** What the sim must answer before we say yes: does the ratchet survive mesh conditions (out-of-order + gossip-duplicated + week-delayed + never-answered delivery) without unacceptable message loss; what MAX_SKIP cap balances legit multipath gaps vs the skipped-key DoS; and does the wire fork (below) actually hold up against relay dedup + verification. Only after the sim de-risks it does this graduate to a design doc + code. DMs are effectively 2-person rooms → adopt Signal's Double Ratchet. **Unifies four things into ONE DM-path redesign:** per-message forward secrecy (strictly better than O38's coarse hourly prekey rotation), post-compromise/self-healing security (O38 gives NONE), sealed-sender/tag addressing (= O53), and **repudiable authentication** (the ratchet's per-message MACs mean either party could forge → no third-party proof; this is the standalone "#2 deniable DMs" idea, free from the ratchet). Rooms/broadcasts UNTOUCHED — they keep signed multi-recipient envelopes (O79); only 2-person DMs ratchet (as Signal uses pairwise ratchet for 1:1, Sender Keys for groups). **Maps cleanly to Signal, no special handling:** out-of-order + gossip duplication (header `(ratchet-pub, PN, N)` selects the right message key; the existing RumorMessage-id dedup layer sits ABOVE the ratchet, drops repeats before decrypt); stalled DH ratchet on slow/never replies (Signal also doesn't advance DH until a reply — you keep per-message FS throughout, just no PCS refresh; on mesh you live there longer, degrading gracefully to a pure symmetric hash ratchet — still strictly > O38). **Genuinely mesh-hard (design around):** (1) **skipped-key DoS** — a message claiming N=1e6 asks for 1e6 derived keys; needs Signal's MAX_SKIP cap, tuned higher for mesh multipath but hard-bounded (too low loses legit delayed msgs, too high = DoS; sim it); (2) **X3DH without a server AND without one-time prekeys** — O38's PREKEY_PUBLISH gossip IS the signed prekey (async, no server); drop one-time prekeys (gossip has no clean consume-exactly-one), accepting a first-message FS-slice + replayability, bounded by O38's hourly signed-prekey rotation; (3) **ratchet-state vs ephemeral collision (O190)** — state must survive restart for a session to continue; resolution: ephemeral mode resets ratchets → re-run X3DH; skipped-keys-for-undelivered are a small standing FS liability; (4) **THE ONE WITH TEETH — sender-hidden DMs break O16 per-sender rate limiting** — a ratcheted/sealed DM is an opaque tagged blob the relay can't attribute to a sender (the point), so per-sender limiting on DMs is impossible → fall back to per-link / per-recipient-tag limiting (conscious spam-control downgrade traded for unlinkability, not a surprise). **Two USER threat-model calls to ratify before build:** (a) accept X3DH-without-one-time-prekeys (first-message replay window); (b) accept per-link rate limiting on DMs in exchange for unlinkability. Both provisionally YES. Own design doc before any code; verify it doesn't break the O65 two-wire model / relay dedup / verification — DMs stop being signed RumorMessages, become tagged AE payloads (a real but coherent fork). Cross-ref O38, O53, O16, O65, O79, O190. |
| O199 | **`[TODO/EMU]`** Coercion / checkpoint hardening pack — the "jackboot demands you unlock" scenario (filed 2026-07-30, privacy workshop) | Extends O189 duress for the protest/checkpoint/border threat (confiscation + forensic extraction + physical coercion). **Load-bearing non-obvious insight: biometric unlock is a LIABILITY under coercion** — a captor holds the phone to your face or presses your finger, and biometrics have weaker protection than a memorized passphrase (which you can refuse / which a decoy can satisfy). So: a fast **lockdown** that disables biometric + hides notification content until passphrase (mirror Android's Lockdown mode), reachable by a one-gesture panic action. Pack: (1) panic-wipe / panic-decoy gesture (hardware-button combo or quick action → instant lock + O189 decoy, optionally + O192 explode beacon, done covertly so the captor sees a normal unlock); (2) app disguise / hidden launcher (open via dialer code; no obvious "mesh app" icon to demand) — plausibility, not security; (3) no message content on the lockscreen / in notifications; (4) FLAG_SECURE so a captor can't screenshot/record the unlocked screen (= O165, non-negotiable here); (5) wipe-on-N-failed-attempts; (6) covert distress beacon — a broadcast that looks normal to the captor but flags "I'm compromised" to contacts. **The strongest single mitigation is that nothing sensitive EXISTS to extract:** ephemeral mode (O190) + disappearing DMs (O193) mean even full compliance yields little — anti-forensics by nonexistence, the O38 philosophy applied to the whole device. **Honest limit (O27):** targeted coercion (they KNOW you organize, they KNOW a hidden vault might exist) weakens deniability — decoy + nonexistence are the best play, not a guarantee; state it plainly. Cross-ref O189, O190, O192, O193, O165, O38, O27, O55. |

#### Tier 3 — plugin platform chain (ordered internally; ALL gated behind O20/O44)

| # | Item | Notes |
|---|------|-------|
| O15 | **`[TODO/CODE]`** Runtime plugin loading (DEX loader)** | Current plugins (Meshtastic, MeshCore) are compiled into the APK — not loadable at runtime. For a true mod scene, plugins need to be droppable files. Two mechanisms that compose: (1) **DEX loader**: a `.rumor` bundle (zip of `.dex` + a manifest) loaded via `DexClassLoader` at runtime — the mod-scene primary path, distributable via flash drive or chunked transfer over the mesh. (2) **Bound-service APK**: a separately installed APK exposes a bound service; Rumor binds to it by intent — good for plugins that need their own Android permissions (camera, contacts, location) since those are scoped to the plugin APK, not Rumor's process. Both paths feed into the same `PluginContext` interface. The DEX path runs in Rumor's process with Rumor's permissions; the bound-service path runs isolated. No central repository; the mesh itself is the distribution channel. **Pre-ship requirement**: a static manifest declaration in the plugin bundle listing which `PluginContext` APIs it calls, shown to the user as a disclosure popup before enable — not a security control (a malicious plugin can lie), but informed consent. |
| O24 | **`[TODO/CODE]`** Plugin security tiers: DEX (read-only) vs APK (dangerous)** | DEX `.rumor` bundles run in Rumor's process and are limited to read-only / display-layer `PluginContext` APIs (observe incoming, register display filters, open settings panel). Any plugin declaring dangerous capabilities (compose outbound messages, mutate contacts, register DM envelopes, open network connections) must ship as a bound-service APK running in its own isolated Android process with its own Android permission grants. Loader enforces this: DEX plugins requesting dangerous APIs are rejected at load time. User sees "this plugin requires installing a companion app" for APK-tier, which is natural friction proportional to actual risk. Manifest capability list re-disclosed on plugin update if capabilities changed. |
| O23 | **`[TODO/CODE]`** User-defined storage quota and plugin storage scopes** | Before first message, setup flow asks user to allocate a storage budget (e.g. "Reserve 2GB for Rumor"). All repos (messages, chunks, transfers, plugin data) share this budget. Plugins receive a scoped storage allocation within the global budget; if a plugin needs more than its default slice it requests an increase via a system prompt the user approves — same pattern as Android runtime permissions. File access for plugins follows the Storage Access Framework picker model (same as Signal): plugin declares which file types it needs, user grants access to specific files or folders, not blanket storage. |
| O25 | **`[TODO/CODE]`** Plugin crash isolation: auto-disable on unhandled exception** | All calls into plugin code (every `PluginRegistry.dispatch*` and every `PluginContext` callback) are wrapped in try/catch. On unhandled exception: log the stack trace, disable the plugin for the session, surface a notification "Plugin X crashed and was disabled." Do not propagate the exception to the host app. Persistent crash (disabled across restarts) if it crashes on init. Plugin author can subscribe to crash reports via a declared crash-report endpoint in the manifest. **Audit note (2026-07-15):** dispatch calls into plugins are already try/catch-wrapped with logging (`app/…/plugin/PluginRegistry.kt`); the missing pieces are the auto-disable (session + persistent-on-init-crash) and the user-facing crash notification. **2026-07-23 (from the parallel audit #4a):** `register()` now wraps `plugin.onAttach(ctx)` in try/catch with rollback (cancel scope + unregister envelopes/context) — previously an onAttach throw *after* launching coroutines/opening a BLE handle (both bridges do) left scope+context in the maps but the plugin absent from `plugins`, so `unregister()` was a permanent no-op and the scope/coroutines/handle leaked for the process lifetime. Pinned in `PluginDisableBoundaryTest`. **Still open:** dispatch-time auto-disable (session + persistent-on-init-crash) + the crash notification. |
| O26 | **`[TODO/CODE]`** Community attestation and peer plugin sharing** | No central plugin repository. Plugin publishers broadcast a signed metadata record on the mesh (`pluginId`, `version`, name, description, capability list, `SHA-256(bundle)`, size). Reviews are **separate** signed messages anchored to `(pluginId, bundleHash)` — they propagate as ordinary mesh content, not chunked with the bundle, so the bundle itself stays small. A "plugin directory" view aggregates: shows plugins seen on the mesh, queries reviews on demand. Acquiring a plugin from a direct peer: chunked transfer of the bundle bytes, addressed as DM to the requester. Acquiring a plugin you've heard about but no direct peer has: broadcast `PLUGIN_REQUEST(pluginId, version)`; anyone holding it replies with either the bundle (chunked DM) or a metadata response "I have v5 and v7, not v6 — want those instead?" Requester chooses. Versioning: every bundle carries `(pluginId, version)` in its manifest; old versions remain valid (no forced upgrades). Authors push new versions by broadcasting a new metadata record; users may subscribe to "notify me when new versions of plugin X appear." Reviews are pseudonymous (userId-signed) so a reviewer's track record across plugins is visible — a reviewer with consistently useful reviews becomes a trustable voice without any central authority. |
| O49 | **`[TODO/CODE]`** Reproducible-builds requirement for peer-distributed plugins** | F-Droid treats reproducibility as best-effort because they also do source review + host signing. Rumor has neither — the bundle hash is the only identifier reviewers anchor to, and without reproducibility a signed review (O26) describes a black box that no other client can verify. Mandatory for plugin bundles: manifest declares source URL + build command; clients can optionally verify-by-building on install if a build environment is available; community attestation reviewers MUST verify before signing. Without this, the peer-review system in O26 is theatre. |
| O50 | **`[TODO/CODE]`** N-of-M co-signed updates for high-capability (APK-tier) plugins** | npm event-stream / Obsidian silent-update / VS Code `ahban.cychelloworld` post-mortems converge on this: initial review doesn't catch a popular plugin's subsequent malicious update. Single mitigation that has worked in the wild is Bitcoin-Core-style mandatory N-of-M signed releases. For Rumor's APK-tier plugins (outbound messaging, contact mutation, DM-envelope registration, network access), require updates to be co-signed by ≥2 maintainer keys declared in the original manifest. Single compromised key cannot ship a malicious update. Real friction proportional to capability — DEX-tier (read-only display) plugins keep single-sig updates. |
| O81 | **`[TODO/CODE]`** Local-only on-device image content classifiers — NSFW + gore, as shipped-default LOCAL_ONLY plugins** | Image companion to O67. Two independent surfaces (NSFW, gore), each with orthogonal **WARN** (blurred thumbnail + tap-to-reveal + label) / **BLOCK** (never display) controls; default-WARN at install; stacking + strictness slider (`off / high-confidence-only / moderate / anything-flagged`), lightest model default. **LOCAL_ONLY plugin tier (O24, canonical use case):** no network, no outbound compose, no DM envelopes — loader denies; cannot leak the user's images by construction. **Model research + recommended starting bundle live in `docs/RESEARCH_NOTES.md` §8** (AdamCodd ViT for NSFW stills + MobileNet-TSM for NSFW/gore video, ~30 MB combined; **gore stills have NO production-ready open model** — ships as WARN-via-O67-keywords with honest disclosure until one matures). Full archived row body in `docs/COMPLETED_GAPS.md` (O81 archive). **Distribution:** models NOT bundled in the APK — post-install opt-in, signed blobs (publisher key pinning on first install, O26 attestation), mesh re-distribution via the O26 chunked-transfer path; cold-start HTTPS fetch only on user-initiated install; model updates on plugin update only (runtime download = network leak). **Never:** Google SafetyCore, Firebase AutoML, any cloud-API fallback (O56 posture). **Pre-ship:** per-classifier model card incl. demographic error-rate gaps surfaced honestly in onboarding; loader-verified LOCAL_ONLY enforcement; O15 capability disclosure popup. |
| O88 | **`[TODO/UI]`** In-thread plugin display widget hook** | A `@Composable PluginDisplay(message: RumorMessage)?` declared by plugins that want to render their own MessageType inline in `ThreadScreen`. Falls back to default rendering (plain `[binary payload]` or hex-dump) if no plugin claims the type. Required for any payment/attestation companion that wants to show structured content ("$5 sent", "credential verified") inside the chat thread rather than driving the user out to a separate app. Lower priority than O87 because the companion can ship without it (separate-app UX is the v1 fallback). Filed by the O84 audit. |
| O168 | **`[TODO/CODE]`** `PluginCatalog.enable()` can show a bridge ON when it silently isn't running (filed 2026-07-30, audit #9) | `PluginCatalog.kt:71-84` marks a plugin persistently "enabled" even when `PluginRegistry.register()` rolled the attach back after an `onAttach` failure (e.g. a revoked BLE permission). Since O25's fix made `register()` swallow that failure instead of throwing, `PluginCatalog` has no way to learn the attach didn't take — Settings shows the bridge on; it never attached. Fix: propagate attach success/failure back to the catalog. Cross-ref O25, O24. |
| O169 | **`[TODO/CODE]`** Plugin coroutine scopes have no `CoroutineExceptionHandler` — a bridge collector crash is fatal to the whole process (filed 2026-07-30, audit #10) | `PluginRegistry.register()` (`PluginRegistry.kt:63`) builds each plugin's scope bare. Both bridges launch their inbound/outbound collectors directly via `pluginScope.launch{}` rather than through the try/catch-wrapped `PluginRegistry` dispatch site (the one place O25 hardened), so those coroutines have no safety net — worse than the "plugin crash disables the plugin" guarantee O25 was built for. Fix: install a `CoroutineExceptionHandler` on the plugin scope that routes an uncaught throw to O25's disable path. Cross-ref O25. |

#### Tier 4 — protocol/feature work

| # | Item | Notes |
|---|------|-------|
| O31 | **`[PART]`** Route advertisements in HELLO** — **wire shape + sig domain v2 landed; per-handshake negotiation + sender population open.** `Hello.recentlyExchangedWith: List<String> = emptyList()` field added. `helloChallengeBytesV2(nonce, ver, maxVer, supportedFeatures, recentlyExchangedWith)` signs the new field under the `rumor-hello-v2:` domain tag — wire-format-incompatible with v1 by construction so a peer can't accidentally accept a v1 sig as a v2 sig. `GossipSession.ROUTE_ADV_FEATURE = "route-adv-v1"` constant defined but DELIBERATELY out of `LOCAL_SUPPORTED_FEATURES`. 5 tests in `HelloChallengeV2Test` pin the byte format (domain tag, sort determinism, empty vs populated). **Not done:** per-handshake feature negotiation (BOTH peers must advertise `route-adv-v1` before either side signs v2 bytes; receivers parse the peer's Hello.supportedFeatures to choose between helloChallengeBytes v1 and v2 when verifying); sender-side population of `recentlyExchangedWith` (needs a "recent exchanges" tracker — TopologyTracker already has session counts; need a top-N projection); per-contact opt-out toggle ("don't list me in your route advertisements"); default-on for priority peers per O58 Tier 3 framing; recipient consumes for O29 scoring (breadcrumb cache already does this for inbound messages — extend with HELLO-derived "potential paths"). Original spec retained: Each peer includes a small `recentlyExchangedWith: List<UserId>` (top-N, e.g. 20) in HELLO. Cheap (~640 bytes), lets neighbours immediately know if a path through this peer is plausible. Goes inside the signed HELLO transcript so it can't be spoofed by MITM. Recipient uses for O29 scoring. Default-on for users explicitly marked as priority-peer (they've already opted into being routing nodes); default-off for everyone else. Per-contact opt-out ("don't list me in your route advertisements") for users who don't want this leak.  Wire + v2 sig-domain ported in the merge (dormant — not in LOCAL_SUPPORTED_FEATURES). |
| O79 | **`[PART]`** Rooms — unified open / invite / password / closed-membership shape (subsumes O52; allowed by default per O36)** | Spec: `docs/O79_PROTOCOL_SPEC.md`; threat model: `docs/ROOMS_THREAT_MODEL.md`; full shipped-substrate narrative archived in `docs/COMPLETED_GAPS.md` (O79 archive). **Recorded decisions:** two room modes only — **OPEN** (plaintext signed broadcasts to a routing tag) and **ENCRYPTED** (multi-recipient envelope: one AES body + N per-recipient key wraps; kick = omit from the next envelope's recipient list; per-message FS automatic; **no shared room key, no ratchet** — collapses O89 to posting certs only; PASSWORD folds into ENCRYPTED via a password-derived membership token). Routing: per-message HMAC tag on the wire, never plaintext roomId. Membership: events-derived (signed event stream replayed, projection cache for O(1) lookups) — **an authored, signed, per-room append-only op-log with trivial merge, strictly separate from message ordering, NEVER state-resolved from the message DAG** (the Matrix-state-resolution inoculation; binding for all remaining work). Channels-within-rooms: `payload.channel` metadata, UI-filtered; write perms deferred to O89. Global room is text-only at the protocol layer (O36). **Shipped:** wire vocabulary (`core/model/Room.kt` — membership/posting policy enums, RoomCreate/RoomInvite/RoomAction, domain tags `rumor-room-{create,invite,action}-v1:`), `MultiRecipientEnvelope` + codec with O39 zeroing, `RoomRoutingTag`/`RoomTagMatcher`, `MessageType.ROOM_MESSAGE` + relay + clampTtl, `composeRoomMessage`/`handleRoomMessage`, subscription persistence (schema v9) + real provider, simulator scenario (`RoomMessageScenarioTest` — caught a real emit-gating bug), 60+ tests; Ed25519→X25519 receiver-side derivation closed via O91 (see G30 — `RoomEndToEndIntegrationTest` exercises real encrypt→decrypt), so OPEN and ENCRYPTED both work end-to-end at the protocol layer today. **Not done:** events-derived membership projection cache (needed for sender-side recipient enumeration at compose time); all UI (room screens, member roster, join/leave events); `MessageType.MENTION`; moderator key rotation (manual-confirm-by-members flow — NOT the deprecated G9 shape); conflict resolution on contradictory mod actions (lean: last-action-wins + public audit log); Room forks; author edit/delete policy (O40 + "allow author delete" policy bit). **2026-07-19 audit §15 — no wire path for moderator actions at all:** `RoomActionKind`/`RoomAction` (`core/model/Room.kt:104-136`) have no corresponding `MessageType`, no `composeRoomAction`, no receive-side handler — so even a *legitimate* mod's kick/ban/remove has nowhere to go on the wire; a kicked member's key keeps working indefinitely and nothing in the mesh can revoke them today (the enforcement gap pairs with O89's unwired posting-cert verify). **REVISED 2026-07-24 (user direction):** the two-mode OPEN/ENCRYPTED split is reframed — OPEN (plaintext-to-routing-tag) is no longer a *room* mode but **the feed** primitive (O154); every actual *room* is invite-gated and therefore **ENCRYPTED** as baseline (O155). The OPEN wire mechanism/code is retained as the feed transport, not removed. The remaining authority/conflict/kick machinery in "Not done" is now specified across O148–O152 (+ `docs/MODERATION_AUTHORITY_RESEARCH.md`); "Room forks" folds into the fixed-membership fork model (O147). |
| O89 | **`[TODO/CODE]`** O79 channel write permissions — posting certificates only** — **collapsed from earlier scope.** Originally specified posting certificates AND per-channel symmetric keys AND a ratchet rotation scheme. The O79 multi-recipient envelope decision means there is no shared channel key to ratchet; read permissions are inherent to recipient list membership. **Remaining scope (write enforcement only):** posting certificates — a mod signs `Cert(userId, room, channel, expiresAt)`; posters attach to messages; receivers verify cert mod-signature + message signed by userId named in cert + cert not expired. Short expiry (default 24h) with auto-renew for active members; non-renewed certs expire naturally for "soft kick." Explicit revocation: signed `RoomAction.REVOKE_CERT` for immediate-effect kicks. Structural enforcement (no honor-system fallback — see `docs/ROOMS_THREAT_MODEL.md`). Read enforcement falls out of O79 automatically: a modified client without being on the envelope's recipient list has nothing to decrypt because their key wrap is absent. Cross-references O79 (parent row, multi-recipient envelope decision), `docs/ROOMS_THREAT_MODEL.md`. **2026-07-19 audit §15 (severe — the single most severe finding of that pass):** this is entirely unwired today, not merely unfinished — `composeRoomMessage` (`GossipEngine.kt:708-749`) attaches no cert, `handleRoomMessage` (`1079-1120`) never calls `RoomPostingCertVerifier.verify` or any mod-authority check, and `RoomPostingCertVerifier` has zero call sites outside its own unit test. **Consequence:** for an OPEN room the routing tag `SHA-256("rumor-room-route-v1:" || roomId)` is publicly computable, so ANY user signs a `ROOM_MESSAGE` with their own valid key and every honest peer accepts/displays/relays it — `MEMBER_ONLY`/`MOD_APPROVED` policies have zero runtime effect, every room behaves as `ANYONE_WITH_MOD_REMOVAL`. Wiring this (or honestly correcting `docs/ROOMS_THREAT_MODEL.md` per O122) gates Rooms being presented as ready beyond OPEN rooms. **The authority model this hangs off is now specified in O148–O152 + `docs/MODERATION_AUTHORITY_RESEARCH.md`.** |
| O148 | **`[TODO/CODE]`** Numeric assignable authority levels (replaces enum roles) — filed 2026-07-24 | Authority is an integer level per room, not an owner/admin/mod enum — owner is just the top of the ladder. **Cardinal invariant:** an op may act on, and assign a level to, only a target of *strictly lower authority than the actor's own level at that op's causal frontier* (capability attenuation — SPKI/SDSI; `docs/MODERATION_AUTHORITY_RESEARCH.md` §7). This deletes the special-case role rules: same-level actors are mutually immune (kills mod-vs-mod mutual-revoke by construction), no self-elevation, higher-level-wins on concurrent conflict. Keep **scope** (over-what) a separate axis from **level** (how-much) — don't fold channel-scoping into the number (defer per-channel scope to O89). Space levels (10/20/30) for later insertion. Level lives in the signed authority op-log (O79), NEVER a HELLO/wire-asserted field (forgeable like a display name — the O31/O101 handshake-accrues-no-trust rule); a cracked client claiming a level convinces only its own screen — every member re-derives authority by replaying the signed op-log. Cross-refs O79/O89 (parent), O149, O151 (invite tree = same attenuation shape), O135(4) (vouch delegation). |
| O149 | **`[TODO/CODE+SIM]`** Authority op-log conflict resolution + hash-linked causality — filed 2026-07-24 | Each authority op **references its causal predecessors by hash** (not just an HLC stamp) — the research doc's #1 finding (`MODERATION_AUTHORITY_RESEARCH.md` §1/§8; Matrix state-reset avoidance + Kleppmann BFT-CRDT). Resolution layering: (1) causality via parent-hashes distinguishes sequential from concurrent (sequential add-then-kick is not a conflict); (2) HLC then userId deterministic tiebreak **only among truly-concurrent ops**; (3) level precedence (O148) applied **only** to concurrent conflicts, never overriding causal order. Each op validated against the authority set **at its own causal frontier**, not "now" (a since-demoted mod's earlier op stays valid if causally-before its demotion). Prefer eliminating races by *constraint* (asymmetric authority) over adjudicating them. Enforcement is eventually-consistent (kicked-in-P1 keeps posting in P2 until the op propagates — honest floor, not a bug). **Sim deliverable** (per `docs/SIMULATOR_TESTING.md`): partition/heal driven through real transport with negative controls; the bug to catch = two partitioned nodes replaying the same op-set landing on different rosters. Failure modes: owner/mod concurrent, stale-authority, mutual-revoke (constraint-eliminated), split-brain-at-apex, manufactured-concurrency (feigned parent-ref ignorance — bounded + attributable). Cross-refs O79/O89, O148, O150. |
| O151 | **`[TODO/CODE]`** Invite-provenance bans — signed invite tree, subtree-prune (closed/invite rooms) — filed 2026-07-24 | You cannot ban a free-to-remint identity (O135); ban the *provenance* instead. Closed/invite-room membership = a signed invite tree rooted at the founders (an SPKI/SDSI delegation chain — `MODERATION_AUTHORITY_RESEARCH.md` §7). Primitives are tree ops: kill-invite-link (future joins via it rejected), revoke-invite-ability (cap a subtree's growth — SPKI delegation-depth bit), **ban = subtree prune** (block-the-signer severs the branch — same shape as O135(4) vouch-pruning). Converts impossible identity-bans into bounded O135 attack-edges (SybilLimit). **Honest limit:** works because ALL *rooms* are invite-gated (O154, no public rooms) → every room gets enforceable provenance bans. The ungoverned **feed** (O36/O154) is the sole open surface — no provenance there, so feed moderation is *local* block/keyword-filter only (O67/O78), never mesh-enforced (NIP-72-style display-suppression, never door-enforcement — never overclaim). Cross-refs O135, O79/O89, O36, O154, O152. |
| O152 | **`[TODO/CODE]`** Unified Revoke op (ban=kick=timeout) + lift/expiry model — filed 2026-07-24 | bans/kicks/timeouts are ONE capability-capped `Revoke(target, scope, expiry?)` differing only by scope + optional expiry (Macaroon caveat-attenuation — `MODERATION_AUTHORITY_RESEARCH.md` §10); duration capped by issuer level so a mod cannot forge an effective perma-ban (extension is structurally impossible; only *early*-lift is available, and it's attributable). **A lift is a signed op applied by replay — enforcers APPLY authority decisions, they never independently evaluate/approve a lift.** Two **manually**-switched expiry modes (no auto-detect — user directive): **(A) wall-clock (default, pre-SHTF):** the issuer's client auto-emits `LIFT` at T on its own synced clock; backstop = other *eligible* mods get a grace-delayed "Lift?" human prompt if no LIFT has propagated (an absent/untrustworthy issuer doesn't wedge the mute); every lift is signed/logged/attributable. **(B) epoch/logical (manual SHTF switch):** `expiresAtEpoch=N` is a deterministic function of the shared op-log — self-executing on replay, no emitter, no prompt, reconnecting mods catch up. **Rules:** `LIFT` bound to the Revoke's op-hash (idempotent, no replay onto a later re-mute); lift-authority = issuer or authority ≥ issuer level (same-level peer-lift = explicit room policy); the `Revoke` propagates room-wide (enforcement = everyone suppresses the target), lift *emission* is mod-gated. **Impossibility acknowledged honestly:** a duration that is real-time AND globally-consistent AND tamper-proof is unattainable without a trusted clock (FLP-family) — pre-SHTF you have a good-enough clock, and the manual switch is the honest fork at the collapse boundary. Resolves `MODERATION_AUTHORITY_RESEARCH.md` open-question-3. Cross-refs O79/O89, O148, O82 (median survives only as a since-dropped canary), O95, O55/O80. |
| O67 | **`[PART]`** Keyword filters as signed shareable lists** | **Rule: keyword filter lists are NOT blocklists** — distinct data type, semantics, and UI surface; they share *only* the sign/verify/publish/subscribe distribution machinery from `BlocklistVerifier`. Don't conflate them in code or copy. Actions: **WARN** (banner / collapsed-by-default) and **BLOCK** (never rendered locally — still relayed per the relay-never-touches-blocklist rule); strictest label wins across merged lists; per-list `userIdAllowlist` exceptions (communities reclaiming terms, misclassified senders). **Default lists: explicitly EMPTY at first launch (decision)** — users opt into community lists or author their own; project signing key for any future bundled lists governed by `docs/PROJECT_KEYS.md`, reserved kind-id `rumor-project-default-lists-v1`. Domain tag `rumor-keyword-filter-v1:`. **Shipped** (full detail archived in `docs/COMPLETED_GAPS.md`, O67 archive): model + matcher + publisher + verifier + subscriber, repos + Room adapters (JSON-blob shape, schema v6) + DI + sim stubs, `MessageType.KEYWORD_FILTER_PUBLISH` gossip bridge (TRANSFER_SETUP class), 30 tests. **Not done:** UI list editor (compose/edit/sign/share); auto-intake-by-publisher toggle (disclosed as "you are delegating filter decisions to <publisher>"); long-press "always show from this sender" exception UI; honest-leak handling (subscribing advertises which lists you hold — accept as tradeoff vs per-list private mode); diff variant if real-world churn justifies it; gossip-bridge tests with a real engine fixture. Required for App Store 1.2 UGC posture. The four content surfaces stay separate in UI: text keywords (this row), image NSFW (O81), image gore (O81), sender identity (BlockManager/O78) — each with independent WARN/BLOCK. |
| O76 | **`[PART]`** Per-payload compression + 6-bucket padding (text only), UI-transparent chunking above ceiling** | **Live end-to-end for DMs** (full pipeline detail archived in `docs/COMPLETED_GAPS.md`, O76 archive): compose → deflate (raw RFC-1951) → pad to bucket → AES-GCM with `compressionAad(originalLength)` (= `"rumor-o76:<decimal>"`) as AAD → `_ext.c/cb/cl` → receiver decodes in `ThreadViewModel.decryptPayload`; `compression-v1` is in `LOCAL_SUPPORTED_FEATURES`; per-contact capability cache populated (`Contact.lastKnownSupportedFeatures`, schema v8) so future v2 features can gate per-recipient without architectural work. Buckets 64/256/1024/4096/16384/65536 B; zero-fill padding (TLS 1.3 pattern — sensor-entropy "upgrades" rejected, rationale in `PaddingBuckets.kt`); a relay tampering `cl` fails the AEAD tag; inflate has a zip-bomb cap. **Invariants:** TEXT contentType only, never BINARY/FILE; **CRIME/BREACH** — never share a compression context across messages, never mix attacker-controlled bytes with secrets in one compression pass (audit any future group envelope before enabling compression there); pipeline order is compress → pad → encrypt → sign → chunk; **UI seamlessness non-negotiable** — no UI ever exposes chunking or buckets. **Not done:** (a) **>64 KB chunker fallback** — composeDirect currently falls back to uncompressed-and-unpadded above `PaddingBuckets.MAX_SINGLE_MESSAGE`; proper fix is a `Chunker` split into a TEXT-typed thread + a new TextAssembler that stitches before display (multi-file work); (b) **broadcast compression** — a separate decision (broadcasts have no AEAD layer to bind `cl` into; padding plaintext leaks `cl` to any observer; compress-only-without-pad has bandwidth merit). |
| O90 | **`[PART]`** Thread + mention metadata fields** — **substrate + compose-side shipped; UI consumption open.** `core/wire/ThreadAndMentionExt.kt` defines `_ext.replyTo: String?` (parent messageId — UI builds thread trees) and `_ext.mentions: List<String>?` (userIds explicitly mentioned — UI builds notification feeds + cross-room mention aggregators). Accessor properties + `withReplyTo` / `withMentions` copy helpers, mirroring the `withTtlSplit` / `withCompressionMetadata` pattern. Both unsigned (in `_ext`); a malicious sender can claim arbitrary parents or mentions but the impact is local-display-only — same impact class as a sender forging an `@username` in plaintext. No relay or routing implications. 11 tests in `ThreadAndMentionExtTest` pin field names (drift guard — names reserved forever in `docs/RENAMED_FIELDS_NEVER_REUSE.md`), wire format (mentions as `JsonArray<JsonPrimitive>`), round-trip / clear / preserve-other-fields, malformed-field-returns-null. **Compose-side wired:** `GossipEngine.composeBroadcast(text, replyTo, mentions)` and `composeDirect(recipientId, recipientPublicKey, text, replyTo, mentions)` accept the optional params with default-empty fallback (callers unchanged when they don't use the feature). For `composeDirect` the O90 metadata is applied after the O76 compression metadata so both coexist in `_ext` without trampling. **Not done:** UI consumption — `ThreadViewModel` / `ThreadScreen` build the thread tree from `replyTo` chains; mention aggregator plugin reads `mentions` to populate a notification feed. UI is the only remaining piece.  Ported in the merge; UI consumption open. |
| O72 | **`[TODO/CODE]`** Nostr relay fallback when internet is reachable** | Bitchat-style: when an internet connection appears (Wi-Fi, cellular), opportunistically forward messages to Nostr relays over Tor (or I2P), and ingest matching events back. Extends Rumor's mesh-only reach to cross-region delivery — mesh in city A talks to mesh in city B via Nostr-over-Tor without either mesh having continuous internet. Different threat model from the in-mesh case (relays are public, observers many), so it gets its own design pass before implementation. Concrete decisions owed: which Nostr event kinds Rumor uses (custom NIP?), how userId↔npub maps, whether Rumor messages are carried as encrypted `_ext` blobs inside otherwise-standard events. Tor integration via tor-android (Briar uses it) or Arti once mature. Cross-references O54 — Nostr is effectively a transport plugin. |
| O77 | **`[TODO/SIM]`** Massive long-uptime scenario (500 nodes, 24h sim, sustained churn) | A 500-node / 24-sim-hour / sustained-churn run to surface behavior invisible at today's 200-node / 120s cap: storage eviction actually firing, static-bloom degradation at 10–60k-message sets, multi-hour partition heal with churn, hub-spoke fairness collapse. Deferred until a ≥32GB-RAM dev box exists (JVM `-Xmx16g`, ~70MB bundle, wall-clock cap raised to 1800s). Full scenario shape + assertions + operational requirements in `docs/DESIGN_ANALYSIS.md`. Probably surfaces 1–3 real protocol issues invisible at 200 nodes — that's the point. |
| O78 | **`[TODO/CODE]`** Signed public block-reasons with ±1 attestations — visual rating only, no mechanical effect** | A user blocking someone may optionally publish a signed `BlockReason { blockedUserId, reasonText, reasonTags[], publishedAtMs }` (Ed25519 by the blocker), gossiped as INFRASTRUCTURE traffic. Other users browsing a contact see "blocked by N users — top reasons: spamming (38), harassment (12)" plus, more prominently, "**three of your regular contacts blocked this person**". **±1 attestations** are sibling signed messages anchored to `(blocker, blocked, reasonHash)` so each signer can only ±/− a given reason once; signature replay is impossible. **This is a UI signal, not a filter input.** Visual highlighting only — contacts' ±1s render larger / brighter / labeled "your contacts"; stranger ±1s render small / aggregated; blocked-by-you ±1s render hidden or greyed. **No mechanical filter behavior depends on counts**, so Sybil attacks against the count are irrelevant: a bad actor +1-flooding their own block does nothing except inflate a number the display already devalues for stranger sources. The earlier Sybil-mitigation analysis (graph-distance weighting, PoW, co-presence proof) is no longer needed because there's nothing to defend — the count is informational, not enforcement. **Opt-in per-block** — default unpublished; the publish toggle in the block UI surfaces the metadata-leak ("publishing this reason makes your identity visible to everyone seeing this contact"). **Account-deletion erasure:** O69's `IDENTITY_RETIRED` broadcast cascades to retract published BlockReasons + ±1s signed by the deleted userId; peers honour and remove from local view. Old key is the proof. **No quorum-of-third-parties erases someone else's content.** That remains a censorship primitive and explicitly off the table. Cross-references O67 (signed list machinery — same publish/sign/diff), O68 (combined posture for App Store 1.2), O79 (Rooms also have block lists), O27 (deanonymization — published reasons are an opt-in identity leak by design). |
| O30 | **`[PART]`** Online-presence beacons** | Short-TTL INFRASTRUCTURE messages "I saw X within last N minutes" gossiped at low frequency. Lets peers far from X learn coarse reachability without polling. Feeds O29's signal 11. Aggregated with recency decay. Privacy trade-off: leaks "X was recently online and visible to someone in this mesh segment" — same leak class as DM `recipientId` today. Should be capped (peer only beacons for contacts it has marked, not for every userId ever seen) and rate-limited so a malicious plugin can't flood. **Refined per O58 design (self-presence only):** drop the "I saw X" variant; the routing-decision value is small (presence doesn't tell you *which path* leads to X — breadcrumbs and bloom do), and beaconing someone else's presence without their consent is the heaviest leak in O58 Tier 3. Replace with a **self-presence beacon**: a Free-mode (or Static-mode) node says "I am running as infrastructure, I am online, here's my recent-exchange context" — same payload, sender opts into their own visibility, no third-party metadata. The UX use case ("is Bob online?") falls back to local-only: each node infers presence from its own recent inbound from Bob (already Tier 1). **Symmetric exit pulse required:** when a node leaves Static or Free mode (manual toggle off, unplug, screen-on transition), send a final "demoting myself" self-update so peers downgrade routing weight for this node immediately rather than waiting for stale signals to decay. Without the exit pulse, DMs continue routing through ghost anchors for the full presence-decay window — minutes to hours of detours through a node that's no longer reliable. |
| O124 | **`[PART]`** "Search for peers" must actually announce + solicit (filed 2026-07-18, user request) | **Shipped 0.6.4 (field check pending):** `triggerActiveScan` now fires an immediate SELF_PRESENCE pulse alongside the BLE/P2P restart; `GossipEngine.presencePulse` host hook + `PresenceReplyGate` (responder-clock per-peer cooldown 30s (was 2 min, user-tuned), LRU-bounded, replies only for unknown-or-stale peers via `MeshViewTracker.hasFresh`) wired in MeshService; `PresenceReplyGateTest` (5) + `SolicitedPresenceReplyTest` (2, sim). **Remaining:** LAN/mDNS re-query on the button (LanTransportManager has no cheap requery — transport rounds poll anyway), O97 status surface. Original design: Field-observed: the search-for-peers button doesn't refresh mesh-level visibility — a node invisible for lack of a first exchange (see O97 field note) stays invisible after tapping it. Design: button = (a) transport refresh (BLE scan/advertise restart, P2P discovery kick, LAN mDNS re-query — cheap, local) **plus** (b) an immediate SELF_PRESENCE pulse (the O30/G12 entry-pulse primitive, fired on demand) so the next exchange carries our announcement; and (c) **solicited response**: a peer receiving a fresh pulse from an unknown-or-stale peer replies with its own pulse, gated by a **responder-side per-peer cooldown** (local clock, e.g. ≥N min since last pulse to that peer) so a modified client spamming probes cannot force pingspam — the cooldown is enforced by the responder, not trusted from the wire (user-specified anti-abuse shape). No new message type: reuse SELF_PRESENCE; the "solicit" is implicit in receiving a pulse from someone whose beacon we haven't answered lately. Cross-refs O30 (pulse primitive), O97 (status surface showing "announced/pending"), O16 (rate-limit family). |
| O22 | **`[PART]`** Native delayed / recurring message scheduling** | Reduces pressure on plugins needing outbound-message capability. UI: compose a message, tap a clock icon, set send time or recurrence (hourly / daily / on next peer contact). Stored locally; `MeshService` fires them on schedule. Covers the primary legitimate use case (status bots, scheduled announcements) without requiring a plugin that declares outbound-message capability. **Merge note:** Room `ScheduledMessageDao/Entity/Adapter` + DI binding + `MessageScheduler` binding landed via archimedes — remainder is the `MeshService` polling loop + compose-screen UI (clock icon). |
| O17 | **`[PART]`** Extend bridged content beyond direct peers (`BRIDGE_VOUCHED` trust level)** | Today BRIDGED traffic is dropped on relay (see design decisions), so a bridge only reaches its direct Rumor peers — Charlie three hops away in the Rumor mesh never sees Bob's Meshtastic message even though Alice's bridge has it. Proposed mechanism: introduce a third trust level between VERIFIED and BRIDGED, called `BRIDGE_VOUCHED`. Bridge wraps the foreign message in a Rumor-signed outer envelope that explicitly carries `{originNetwork, originSenderId, originSignatureIfAny, payload}`. The outer envelope is signed by the bridge's Rumor key — but the wire format and trust level make it unambiguous that the bridge is vouching for *delivery* only, NOT for content authenticity. Other Rumor peers can relay it because the outer envelope verifies. Display layer renders as "via Alice from meshtastic:Bob" with a distinct visual treatment from native Rumor messages. Recipients decide per-bridge whether to trust that bridge's vouching (UI: enable/disable bridge-vouched relay per known bridge). Replaces the current absolute "never relay BRIDGED" with a more nuanced "never relay BRIDGED without an authenticated outer envelope from a bridge node we know about." Scope: new TrustLevel enum value, new wire envelope format, signature verification of outer envelope at every receiver, UI for per-bridge trust toggles, three-state visual rendering. Real design work; not started. |
| O94 | **`[TODO/CODE]`** Intermittent HELLO timeouts on persistent-link rounds** | **Related field observation (2026-07-18, 0.6.6, Samsung-as-client of Moto GO):** Samsung cycles connect → session → full P2P disconnect → rejoin on a ~11s rhythm instead of holding the client link (OnePlus holds fine in the same group). Every cycle completes a session so delivery is unaffected — but it's a full group-level reconnect per round, heavier than this row's HELLO-timeout flavor. First observed in Samsung's first stint as a *client* (it had always been GO before); suspect an OEM client-side group-hold quirk. Original row: On the Moto/OnePlus test pair, roughly every 2nd–4th 10s round the TCP connect succeeds but HELLO gets no reply within its 5s timeout ("Session error" + SocketTimeoutException on both sides simultaneously); the next round then completes in 60–400ms. Functionally absorbed by the round loop (failures reset on success), but it doubles worst-case delivery latency and spams warn logs. Suspect Wi-Fi Direct power-save (GO NoA / opportunistic PS) napping the radio between rounds despite the held WifiLock (`WIFI_MODE_FULL_HIGH_PERF` is deprecated/no-op on API 34). Investigate: keep the TCP socket open across rounds instead of reconnecting (protocol change — session loop over one socket), or probe OEM-specific PS toggles. |
| O145 | **`[TODO/HW]`** `WifiDirectTransport.stop()` permanently kills the singleton's coroutine scope (parallel audit, 2026-07-23; needs the fleet to verify) | **Confirmed by the 2026-07-23 audit, filed not fixed — this is field-verified O98 transport code and an unverifiable blind lifecycle change risks regressing hours of hardware tuning, so it wants a 3-phone session, not an overnight edit.** `scope` is a `val` (`wifidirect/WifiDirectTransport.kt:~62`); `stop()` calls `scope.cancel()`; Koin registers the class as a `single`, so the same instance survives repeated `MeshService.startMesh()`/`stopMesh()` cycles. Once the `Job` is cancelled it can't be revived — every subsequent `scope.launch{}` (accept loop, client round loop, GO-idle watchdog, priority watcher) is launched dead and **silently never runs**, no exception surfaced. `TransportConfig`'s own KDoc documents "stop() then start() again after an identity change" as supported — it's broken. Compounding: `stop()` doesn't reset `groupConnected`/`connectAttemptInFlight`/`discoveryInFlight`, and unregisters the broadcast receiver *before* `removeGroup()`'s disconnect broadcast arrives, so a live group at stop() can leave `groupConnected` stuck `true` forever, wedging `connectToPeer()`'s early return on all future attempts. **Fix shape (verify on the fleet):** make `scope` a `var`, recreate it at the top of `start()`; explicitly reset the mutable connection-state fields in `stop()`; reorder `stop()` to tear the group down before unregistering the receiver. No `WifiDirectTransportTest` exists to catch a stop→start cycle — add one (even a JVM-level state-reset unit test) alongside. Cross-refs O98 (backbone realization shares this instance), O93. |
| O33 | **`[TODO/HW]`** Battery profile and "protest-day" budget** | Measure continuous BLE + Wi-Fi Direct scan + relay power draw on representative devices (Pixel-class, mid-range Samsung, 4-year-old budget). Target envelope per research: 8 hours active use on ≤50% battery; 12–24 hours unattended on a modern phone with conservative duty cycling. Tune `SCAN_MODE_LOW_POWER` defaults, motion-aware throttle (AirGuard pattern), hard cap on reconnect attempts to vanished peers (Meshtastic #4444 / #2659 failure mode), exponential discovery backoff when no peers seen (G2 generalized). Declare `connectedDevice` foreground-service type (Android 14+) with `FOREGROUND_SERVICE_CONNECTED_DEVICE` permission. First-run flow requests `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` — without it Samsung/Xiaomi/Huawei kill the service regardless of FGS status. Without measured numbers, every claim about field viability is unsupported. **Audit note (2026-07-15):** the manifest already declares `foregroundServiceType="connectedDevice"`; the `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` first-run flow and all power measurements remain. |
| O45 | **`[TODO/EMU]`** BIP-39 mnemonic identity export (v1 backup)** | Explicit "export identity" flow producing a 24-word BIP-39-compatible mnemonic, displayed with `FLAG_SECURE` so screenshots are blocked, never written to clipboard. Optional passphrase wraps the mnemonic for shoulder-surfing protection. First-launch flow includes a "I got a new phone" announcement message-type signed by the old key while still on the old device — pre-warns contacts of an upcoming identity continuation (different from O41 rotation, which is reactive). Document explicitly: lose this mnemonic, lose this identity forever. Photo-of-paper backed up to Google Photos is the dominant real-world failure mode — assume it; push toward O46 social recovery as the better primary path. |
| O46 | **`[TODO/CODE]`** M-of-N mesh social recovery (v2 backup)** | After v1 ships, layer Shamir-split social recovery on top. User picks N (3–7) trusted contacts; app generates SLIP-0039 split of a wrapping passphrase (not the raw key); each share is wrapped to the guardian's pubkey via existing DM crypto and sent over the mesh. Recovery on new device: generate ephemeral recovery pubkey, broadcast a signed claim "I am the new device of (oldUserId), please send my share to this ephemeral key"; each guardian's client surfaces the request with strong identity-verification UX (emoji fingerprint of requester + claimed old userId + instructions to confirm out-of-band with the person in person/by voice/codeword); on approve, share re-wrapped to ephemeral key. Reassemble ≥M shares → recover passphrase → identity. Critical: **24–72h time-delay window** between first share-approval and final assembly (Argent pattern, anti-coercion); **signed continuity message** broadcast on the mesh after recovery completes so original Alice can publicly repudiate a fraudulent recovery while she still has the original key. Optional duress passphrase reassembles to a decoy key. Without time-delay + public continuity + OOB verification, social recovery is worse than no recovery. |
| O1 | **`[TODO/CODE]`** SQLDelight migration** | Promised in commit 3e7f588 as "next commit", never done. Room is still in use. Only worth doing if leaving Google's runtime matters; otherwise stale. **Re-scored (2026-07-15, O106):** the desktop/USB-bootable node can't use Room, so this is no longer stale — it's one of two options for :node storage (SQLDelight = one impl everywhere vs. a plain JDBC-SQLite adapter = third impl, less work, relay nodes have simpler query needs). Decide when :node starts, not before. |
| O121 | **`[TODO/CODE]`** 2026-07-18 audit residue — small correctness pins | Grab-bag of confirmed-small items, none load-bearing today, each a landmine later: (a) **DONE** — `DmEnvelope.selfAuthenticating`'s emergent safety is pinned by `SourceInvariantTest` invariant 6 (`selfAuthenticating has no production reader outside its definition`): any new production reference fails the build until the author adds the `MessageSource.LOCAL_BRIDGE` gate and re-shapes the guard; (b) **DONE** — `GossipEngine` breadcrumb recording (line ~1060) now uses `DEFAULT_DIRECT_HOPS` for DIRECT and `DEFAULT_BROADCAST_HOPS` otherwise, so `hopCount = startBudget - hopsToLive` is meaningful per type; (c) **DONE 2026-07-23** — `DomainTagInvariantTest` is now complete: all 26 `"rumor-` domain tags emitted by `:core` main are guarded (the two the finding named plus three more that were *also* silently unguarded — `rumor-rbsr-fp-v1:`, `rumor-o98-net-v1:`, `rumor-o98-psk-v2:`), and all 26 are listed in `docs/wire-format.md` §6. Also fixed a stale `SealedSenderTag` KDoc that wrote the info prefix without its `-v1:` suffix; (d) `SortedListRbsrStorage.items()` linear-scans the whole list per range query — profile before the large-anchor-store regime; (e) `MessageStore`/`GossipEngine` broad coverage — **partly addressed 2026-07-24:** `MessageStoreRateLimitTest` (6) pins the O16 per-sender token bucket (budget trip, per-second window refill, per-sender isolation, rate-limited-≠-blackholed) + the `persist=false` seen-but-not-stored path + `ingestOwn` store-without-verify/dedup; still open: eviction-over-cap and `GossipEngine`-level coverage. |
| O127 | **`[TODO/CODE]`** Sybil presence reply-storm — **live-measured 2026-07-22, severity DOWNGRADED to local-cost** (extends O124/O16) | `PresenceReplyGate.shouldReply` keys its cooldown on the already-verified `msg.senderId`, spoof-proof for a *fixed* identity — but minting a **new** Ed25519 identity is free, so each fabricated identity earns one free solicited pulse. **Hardware measurement (`:node --sybil` harness vs the OnePlus, 0.6.9):** 20 fresh sybils → **20 pulse fires on the phone, 1:1** (gate bypass by fresh-identity is real, exactly as filed) — BUT only **~4–5 of the 20 composed reply broadcasts actually propagated** to a 1-hop observer, because SELF_PRESENCE is ephemeral and rapidly superseded/evicted before the phone offers them all. So the audit's "mesh-wide outbound amplification" is **overstated** — propagation self-limits; the real cost is **local** (N Ed25519 signs + enqueues + battery/airtime), not a network storm. **Reframed fix:** a global identity-independent token bucket on solicited pulses caps the local sign/airtime cost — worthwhile **battery/DoS hardening, not a mesh-storm emergency**. Also caveat `PresenceReplyGate`'s KDoc ("probe spam can't force reply spam" is true only vs a fixed identity). Harness + measurement method: `node/src/main/kotlin/.../SybilDriver.kt` + the `GossipEngine` `presence solicit-reply →` debug line. **Follow-up worth one measurement:** confirm the eviction hypothesis (why only ~4–5/20 propagate) — if it's dedup/supersession, document it as the natural bound; the ephemeral-SELF_PRESENCE design is doing real anti-amplification work for free. |
| O132 | **`[DONE 2026-07-22, commit a2a98bd]`** Broadcast size cap — large text broadcasts are monolithic, never chunked (filed 2026-07-22, live-test finding) | **RESOLVED — all three parts shipped + simulator-tested (`OversizedMessageTest`):** (1) **compose cap** — `composeBroadcast` (GossipEngine:389) and `composeRoomMessage` (:724) refuse text over `MAX_BROADCAST_CONTENT_BYTES` (64 KB, the largest padding bucket); (2) **ingest drop** — `MessageStore.ingest` (:210) records the id as seen (so a re-offer fast-drops) then returns false for any BROADCAST/ROOM_MESSAGE over 64 KB → never stored, never relayed, never in an offer batch; (3) **send-side offer skip** — the offer builder (GossipEngine:904-918) skips any message whose `approxWireBytes()` exceeds `MAX_OFFERABLE_MESSAGE_BYTES` (1 MB) and trims the batch to `MAX_OFFER_BATCH_BYTES` (3 MB), so a legacy/oversized message is skipped rather than sent-and-reset — the head-of-line poison loop is broken on every honest node. The raw `GossipSession` frame guard (`require(len in 1..4_000_000)`) still throws for a genuinely malicious >4 MB frame, but since honest nodes never store/offer such a message there is no re-offer loop; a malicious sender only resets its own single session. **Original finding retained below.** **Text broadcasts have NO size cap and are NEVER chunked** (chunking = `Chunker`/`TransferSender`, file-transfer-only). `composeBroadcast` builds whatever size it's given; `trafficClass` only downgrades oversized payloads to BULK *priority*, it doesn't split or reject. So a 500 KB / 1 MB / 10 MB text broadcast becomes ONE giant signed message relayed whole to every peer and stored whole — a bandwidth/storage amplifier on every relay (O108-family, remotely triggerable, no auth). The global room is text-only by design (O36), so there is no legit multi-MB-text use case. **Fix (NOT chunking — broadcasts are gossip, not point-to-point transfers): a hard size cap on broadcast text at BOTH compose (refuse to send / warn) and ingest (drop oversized `BROADCAST`/`ROOM_MESSAGE` before store+relay).** Pick the ceiling deliberately — 16 KB (INFRASTRUCTURE ceiling) is plausibly too tight for a legit long post; 64 KB (largest padding bucket) is a reasonable cap. Enforce structurally like the other `trafficClass` ceilings so a cracked client can't bypass. The 0.6.10 `capForDisplay` (O112) stops the *display* crash but the oversized message still rides the wire and fills storage — this row is the structural half. **Live-measured boundary + a HEAD-OF-LINE-BLOCK bug (2026-07-22, `:node` vs OnePlus):** the `GossipSession` frame guard is `require(len in 1..4_000_000)` (`GossipSession.kt:398`) — a legit anti-OOM defense against a malicious length prefix. Messages **under 4 MB propagate + store fine** (1 MB verified end-to-end, monolithic but works). A message **over 4 MB is worse than wasteful: the guard `throw`s → connection reset → the whole session dies, AND the oversized message stays in the sender's store/offer batch, so it is re-offered every round → every subsequent session resets on it → nothing else syncs to that peer (head-of-line block, verified: a tiny broadcast sent after a 5 MB one never reached the phone).** One oversized broadcast permanently wedges a link. **Mesh-wide confirmation (3-phone + node run):** injecting ONE 5 MB broadcast at the node put ALL 3 peers into repeating `Session error`, and a tiny broadcast sent right after reached **none** of them — i.e. a single oversized message a node holds wedges its sync to **every** peer at once (denial-of-relay; that node becomes a black hole until the message is evicted). Self-limiting for a *malicious* sender (it mostly hurts their own outbound), but catastrophic for any *honest* node that ingests a >4 MB message (legacy build, bug, or pre-cap peer). **So O132 needs THREE parts:** (1) compose-time cap (refuse to send oversized); (2) ingest cap (drop oversized before store+relay); (3) **send-side: never offer a message whose frame would exceed the peer's limit — skip it, don't attempt-and-reset** (and/or receiver skips-and-continues instead of killing the session), so a message that slips past (legacy, large DM) can't poison the link. Cross-refs O108/O109 (transfer-metadata resource caps, same class), O13/O16 (frame guards), O36 (text-only global room), O23 (storage quota). |
| O134 | **`[TODO/CODE+UI]`** Peers list — every gossip peer auto-persists as a contact → sybil/ephemeral pollution; needs collapse-unnamed + search (filed 2026-07-22, user + live finding) | **Root cause (code):** `MessageStore.ensureContact` (`MessageStore.kt:175`) runs on ingest for **every** message's sender, so any peer whose message you ingest — including a one-shot sybil's SELF_PRESENCE or a relayed broadcast from a stranger — becomes a **permanent full contact**. Live-confirmed: the sybil/oversize test left the OnePlus with **71 contacts, all 71 unnamed**. A sybil swarm permanently pollutes the contact list (another O127 sybil cost beyond the presence-reply amplification). **UX (user ask):** collapse un-nicknamed peers under a single "unnamed peers" header (expandable), and add a **search bar** to filter the peers/contacts list. **Deeper design:** distinguish "known contacts" (DMed / manually saved / nicknamed / verified) from "seen peers" (gossip-exchange-only) — the list conflates them. Options: (a) `ensureContact` writes a lightweight seen-peer record, promoted to a real contact only on real interaction (DM, manual save, nickname); (b) keep the row but the peers UI filters/collapses by `displayName IS NULL` + `isVerified`/`alwaysSave` flags (schema already has them). Prefer (a) — it also shrinks the O55 months-of-accumulation growth. Cross-refs O110/O113 (contacts UI rework — do together), O111 (nicknames — a named peer leaves the collapsed group), O127 (sybil pollution), O131 (list perf — collapsing also helps the jank), O23 (contact-table growth is unbounded today, same family as O120 prune loops). |
| O135 | **`[TODO/CODE+UI]`** Sybil/peer-spam resistance — make sybils *harmless*, not impossible (design, filed 2026-07-22 from user threat-model discussion) | **Framing (extends O27/O51/O60 floor):** Ed25519 identities are free and unlimited by design; anything keyed on identity (per-id rate limit, blocking, reputation) is defeated by cycling identities. The goal is NOT to prevent sybils but to bound their damage. **Four load-bearing mechanisms:** (1) **Partition every bounded resource by trust tier — and the tier is EXPLICIT USER ACTION, not "handshaken."** User correction 2026-07-22: "known" as in *completed-an-exchange* is worthless here — every sybil is "known" the instant it handshakes. The privileged tier must be **friended / named / verified** (an affirmative user act), NOT seen-on-the-wire. Tiers: **friended (named/verified/DMed) > seen-but-unnamed > never-seen.** Friended peers get *guaranteed* buffer/connection/storage slots; everyone else shares a *separate, bounded, LRU-evicted* pool. A sybil flood saturates the unnamed pool but **cannot starve friended-peer traffic**. **GAP: there is no explicit "friend/accept" action today** — contacts auto-persist on exchange (O134), and `isVerified`/`alwaysSave`/`displayName` exist but nothing sets them via a deliberate user gesture. A real friending mechanism (accept/friend button, on contact + on first DM) is the prerequisite for this whole tier system — build it with O110/O134. (2) **Identity-INDEPENDENT aggregate caps** — bound the *total* regardless of identity count. Concretely (user's own analogy): the O134 "unnamed peers" collapsible IS the bounded pool — a fixed-size bucket that sybils fill and LRU-evict each other out of, never growing unbounded and never touching the named area. Same shape everywhere: global solicited-pulse token bucket (O127), global inbound-session concurrency cap, storage quota+eviction (O23), bloom/RBSR cumulative caps (O117). Sybils churn the LRU; they don't overflow it. (3) **"Known peers only" inbox toggle** — the seam exists (`core/policy/InboxFilter.kt` — `PermissiveInboxFilter` today; add a `KnownSendersInboxFilter` gated on the contact list). Relay still carries everything (relay-never-filters invariant), but the *inbox* only surfaces known keys → sybil floods become invisible to the user even though the mesh relays them. Pairs with Rooms (explicit membership) for the spam-free experience. This is the direct answer to "blocking is useless if identities cycle" — you don't block bad keys, you allowlist good ones. (4) **Trust acquisition over distance** (QR only works in person): **transitive vouching/introductions with a degrees-of-separation slider** — a mutual contact signs "this key is Bob"; you accept vouches up to N hops (user-set slider). **Why this self-defends (user's attack-tree walk, 2026-07-22, worth preserving):** a sybil churning a dozen fake peers/sec → nobody friends those → they stay in the unnamed collapsible, never flood. Sybils friending *each other* → useless, the chain still needs a real person at the root. So the only way in is a real person vouching for the fakes → and because **every vouch is signed and attributable, you see exactly who signed off and block that person as a valid signer in YOUR chain** (block-the-signer, not the endless fakes). Residual attack: "spend 20 min in an easy-access room, get a real user to friend you, then spam them a million fake peers" — but the fakes land in the friended user's *unnamed collapsible* (never floods their feed), the signer is attributable, and one block of that signer severs the whole subtree. **May largely solve itself** — the collapsible + attributable-signed-vouch-chain + block-the-signer composition is self-limiting by construction. **out-of-band pre-add** — paste/scan a userId+pubkey shared via any channel (voice, paper, another app, an O101 signed business card); **O21 emoji fingerprint** verifies a key obtained over an untrusted channel. **Other damage vectors the same behavior opens (audit each):** contact-list pollution (O134, confirmed 71 junk), routing/topology poisoning via fake SELF_PRESENCE + online-status (O128, O58 tiers), storage exhaustion (O23), dedup/sync-structure bloat from sybil message-ids (O117), presence amplification (O127), first-contact CPU cost (signature-verify + ensureContact per novel sender — needs a cheap pre-filter), and social-graph mapping by a sybil-equipped observer (O27/O51/O60 — the irreducible floor, honest-docs only). **Deliverables:** the KnownSenders inbox filter + toggle (cheapest, highest-impact — do first) — **PROTOCOL LAYER SHIPPED 2026-07-22:** `core/policy/KnownSendersInboxFilter.kt` (allows inbox only from an injected friended-predicate when `InboxPolicy.friendedSendersOnly`; gates only `emitToInbox`, relay+store untouched) + `SybilFloodScenarioTest` (sim, 2 tests: 12-sybil flood → victim inbox = exactly the friend set, 0 sybil leaked, WHILE the store still holds sybil broadcasts = relay invariant preserved; baseline permissive lets all 12 through). **Remaining for this piece:** wire the `isFriended` predicate to the real friended store (blocked on O136), an app Settings toggle for `friendedSendersOnly`, and a live headless-node validation (node floods a phone with the toggle on → phone inbox drops it, relay unaffected — inspect phone DB). The rest of O135: the trust-tier resource partition (architectural, touches buffers/sessions/storage); the vouching/introduction wire+UI; pre-add-by-key UI. **O135(4) ALGORITHM SHIPPED 2026-07-22:** `core/trust/TrustGraph.kt` — the pure hops-limited vouch frontier (SSB `hops` semantics), with `TrustGraphTest` (6) proving: hops=1 direct / hops=2 FoF; sybils admitted ONLY via a real attack edge (500-sybil self-vouching ring excluded, SybilLimit bound); block-the-signer severs the whole subtree; a blocked node prunes only its own subtree; Advogato-inoculation (depth is BFS-structural from the seed, a fooled node can't shortcut its vouchees shallower); blocks are the seed's own set, NOT auto-inherited through the graph (Rumor's granular model). **Remaining for O135(4):** the signed FriendVouch/Block WIRE messages + gossip propagation + a projection cache feeding `vouches`, and the hops slider UI. The algorithm is done + proven; the wire integration is the next layer. A small proof-of-work admission cost for first-contact-from-unknown was *considered and is disfavored* — the O55 low-power-survival envelope makes per-message PoW too expensive on legit solar/handcrank devices, and it's asymmetric against a resourced attacker. **Literature grounding (this is a solved-ish problem; the user's design converges on known results):** **Douceur, "The Sybil Attack" (2002)** — the impossibility result: without a central authority or a scarce *verifiable* resource you cannot *prevent* sybils, which is exactly why the posture is "make them harmless" (honest-docs O27). **SybilGuard / SybilLimit (Yu et al. 2006/08)** — social-graph admission control bounds accepted sybils to ~O(log n) **per attack edge** (genuine trust link the attacker forms with an honest user), NOT per minted identity — the formal backing for "the fakes need a real person to vouch, and you block that person / sever the edge." Caveat: relies on the honest graph being *fast-mixing*; Rumor's sparse geographically-clustered mesh may not be, so treat the bound as directional not guaranteed. **Advogato max-flow trust metric (Levien) + PGP Web of Trust** — the vouch/degrees-of-separation mechanism; trust flows from your seed through signed edges, capacity-limited, so the certified set is bounded by the min-cut = the attack edges; blocking a signer removes its edge. **Secure Scuttlebutt (SSB)** — a SHIPPING P2P gossip social mesh whose **"hops" replication setting IS the user's degrees-of-separation slider** (you replicate friends + friends-of-friends up to N hops); the closest deployed prior art — study its follow-graph/hops model before building O135(4). **Freenet WoT plugin** — spam resistance via trust-distance in a censorship-resistant P2P net, same shape. **DTN buffer-management literature** (drop-oldest/drop-largest/trust-based forwarding under congestion) informs the O132/resource-partition side. **Actionable: SSB "hops" is the reference design for the vouch/FOLLOW slider; Advogato max-flow is the formal model for "block-the-signer severs the subtree."** **DECISION — DO NOT port SSB's BLOCK model (user 2026-07-22, overrides the research's "block-dominates-follow" port recommendation):** SSB auto-inherits the blocks of everyone you follow (forced transitive block). Rumor keeps its **existing subscribable-blocklist model** (O67/O78 machinery: signed `BlocklistPublish`, opt-in subscribe, layerable, per-list) — it is *strictly more controllable*: you get automatic inheritance IF you choose to subscribe to a list, but you are never forced to inherit a block just because you follow someone, and you can layer your own + multiple curated lists granularly. So: **port SSB's FOLLOW/hops (the trust frontier) but NOT its BLOCK semantics.** When computing the O135(4) vouch/hops frontier, subtree-pruning uses **your own blocks + your subscribed blocklists**, never blocks auto-inherited through the follow graph. This keeps "block-the-signer severs the subtree" (the pruning still happens) while making *which* blocks prune a granular user choice, not a forced transitive cascade. Cross-refs O27/O51/O60, O16, O23, O46 (guardian vouching shares the introduction primitive), O79 (rooms = explicit membership), O110/O134/O136 (contacts + friending). **2026-07-25 — feed-flood sybil vector + the pre-gate analysis (from the O154 priority-model discussion; failure modes to be fleshed out later):** attack = thousands of self-owned identities posting to each other (or to a self-made topic) to drown shared bandwidth. Defense holds *because caps are per-resource, not per-identity*: friend-priority protects friended traffic; the sybil topic never gets boosted so it sinks to the floor; the floor is a bounded shared LRU budget, so 10 vs 10,000 sybils get the SAME floor share (extra identities evict each other — minting more buys nothing). **Can you pre-gate before it hits the device? Mostly yes above the link layer, bounded at it:** (1) **RBSR offer-first** (O42/G24) gates *bodies* — a neighbor offers a compact summary, you pull only what you want, so you pay summary-cost not body-cost; summary/reconciliation cost is itself capped (O117). (2) **Honest-neighbor priority** (O43) — a well-behaved neighbor won't spend its link-to-you budget relaying floor noise over prioritized traffic, so the flood only reaches your link if the sybil is your *direct* transport neighbor. (3) **Irreducible link-layer floor** = a direct sybil neighbor: you must receive-to-classify, but bound it with **tiered connection admission** (O135(1): friended = guaranteed slots; unknowns = bounded-LRU pool) + **per-connection inbound rate caps** — a ceiling independent of identity count (more sybils don't enlarge the pool). **Real residual:** floor noise degrades *passive* peer-discovery (real new peer = needle in haystack), but discovery survives via **proximity-weighting** (nearby beacon outranks distant sybil) + **active solicitation** (O124/O30, proximity-bounded, can't be flooded from across the mesh). Cross-refs O154 (priority feed), O42/O117 (offer-first + caps), O43 (relay priority), O135(1) (connection tiers), O124/O30 (discovery). |
| O136 | **`[DONE 2026-07-22]`** Explicit friending/accept mechanism — prerequisite for every trust tier | Split out from O135(1) because it gates so much. Today a contact is created automatically the instant a peer's message is ingested (`MessageStore.ensureContact`), and `isVerified`/`alwaysSave`/`displayName` are never set by a deliberate user gesture — so there is no machine-readable "I trust this person" signal for the O135 resource tiers, the O134 collapsible, the O21 verification, or the O135(4) vouch chain to key on. Build an affirmative **friend/accept action** (button on the contact row + on first inbound DM + on nickname-set), persisting an explicit `friended`/`trustLevel` bit distinct from auto-persisted seen-peers. Everything sybil-resistant downstream keys on THIS bit, not on "has exchanged." **SHIPPED:** `Contact.friended` + `ContactEntity.friended` (Room schema v10→v11), `ContactRepository.setFriended/isFriended` (interface + Room adapter + in-memory), `ContactDao` queries, a **Friend toggle** in the Contacts row overflow menu (`ContactsViewModel.setFriended`), and the O135(1) filter now keys on it: `InboxPolicyManager.allowsInbox` drops non-friends when `friendedSendersOnly` (persisted toggle), own messages exempt, relay+store untouched. All suites green. **Remaining:** the O135(1) Settings toggle UI for `friendedSendersOnly` (the bit persists; needs a switch), a friend action on the thread screen + first-DM prompt (currently Contacts-menu only), and a live on-device validation. Cross-refs O135 (tiers + vouching), O134 (seen-vs-known split, collapsible), O110/O113 (contacts UI), O111 (nickname), O21 (verify). |
| O137 | **`[TODO/SIM+HW]`** Synthetic agent-driven test fleet — on-device local models running normal Rumor behavior (filed 2026-07-22, user idea; needs refining) | User idea: when more phones are available, run a small local model on each doing *normal* Rumor behavior (compose, DM, friend, relay, move between LAN/Wi-Fi-Direct) so the fleet **builds realistic organic data + topology churn before human testing** — a higher-fidelity load/soak than scripted sim traffic, on real radios. Refinement needed: model choice (tiny on-device LLM vs a rule-based behavior agent — the latter is far cheaper and probably sufficient for "act like a plausible user"; an LLM only earns its keep if we want emergent content/conversation), the behavior policy (arrival/departure, message rate, friend-graph formation, mode transitions), and the harness (drive via the debug-unlock + an adb/intent control channel per O138, or an in-app "sim user" debug mode). Complements O77 (500-node JVM sim — pure protocol) by adding *real-radio* realism the sim can't. Cross-refs O77, O33 (battery under sustained synthetic load is itself a measurement), O138 (needs the no-reflash/auto-drive loop first). |
| O138 | **`[DONE 2026-07-22 — 0.6.11]`** Kill the reflash+manual-unlock loop for dev/test | Every code change to `:app` currently needs `adb install -r` (fine, preserves data) **+ a manual on-screen unlock** (the real pain — a human must touch each phone, and identity is passphrase-locked after the process restart). Fixes, cheapest first: (a) **debug-only auto-unlock** — in a `BuildConfig.DEBUG` build, auto-unlock an existing identity with a dev passphrase on start (fleet passphrase is already `passphrase1`, recorded in memory; test devices only) OR (b) **adb-triggerable unlock** — a debug `BroadcastReceiver` that unlocks on `adb shell am broadcast … --es passphrase …` (keeps the secret out of the APK, still hands-free). **SHIPPED (a): `MainActivity.onCreate` debug auto-unlock, hard-gated `BuildConfig.DEBUG`, passphrase `passphrase1`, verified hands-free on all 3 phones (flash + `am start` → auto-unlock → mesh, no manual step).** Future reflash = `adb install -r` + `am start -n com.rumor.mesh.debug/com.rumor.mesh.MainActivity`. Unblocks O137's synthetic fleet and every multi-device test. Also reduce reflash *frequency*: prefer putting instrumentation in `:node` (no reflash) over `:app` when a choice exists; core-only changes still need a rebuild but the node picks them up on restart. **Never ship either path in release** — gate hard on `DEBUG`. Cross-refs O137 (synthetic fleet needs hands-free drive), O115 (identity lock lifecycle — the auto-unlock must not weaken the release lock). |
| O139 | **`[TODO/CODE+DECISION]`** Tampered-Rumor-build resistance — the mesh IS the app-distribution channel post-SHTF (filed 2026-07-22, user question) | O55/O26: no app store post-collapse. **Primary vector is SNEAKERNET (user clarification 2026-07-22): USB drives, SD cards, phone-to-phone APK copy — because ~99% of phones won't be running Rumor, so first-spread is physical media, not the mesh.** A malicious actor hands out a backdoored build (exfiltrates keys, weakens crypto, disables the O135 sybil defenses, logs the social graph). No CA to verify against. **Sneakernet actually FITS the web-of-trust model well** — you got the APK from a specific person you can name and attribute, so a signed "I built/verified this, hash H" that travels *with* the drive, checkable against a pre-distributed project fingerprint, is exactly the right shape (physical provenance = the attack edge is a real person you can vouch-for or block). **Layered answer (most already-free → hardest):** (1) **Android APK signature continuity is already the strongest built-in defense and it's FREE** — an update APK must be signed by the same key as the installed app or Android rejects the install. So a malicious build **cannot update a genuine install** (different key → rejected). The attack surface is therefore *only first-install/bootstrap*, not updates. Verify the release keystore (`app/keystore.properties`) is treated as the continuity anchor and documented as such. (2) **Bundle the project release public key INTO the app** so a genuine copy can verify the *next* build's signed hash forward (chain of trust from any known-good copy) — pairs with `docs/PROJECT_KEYS.md` (key not yet generated) and O50's N-of-M maintainer co-signing extended from plugins to the core APK. (3) **Reproducible builds (O49, extend from plugins to the app)** — public source + deterministic build = anyone technical rebuilds and confirms `SHA-256(APK)` matches, without trusting the distributor; the foundation that makes attestation meaningful. (4) **Community attestation of the APK hash (O26, extend to the app)** — the build hash accumulates signed "I rebuilt from source, hash matches" attestations; accept a build attested by people in YOUR trust graph (ties directly to O135 web-of-trust — same vouch primitive, applied to the binary). (5) **OOB/physical key-fingerprint distribution** — the project key's O21-style memorable fingerprint on paper / painted at a community hub / on a trusted USB, so first-install can be checked against a known-good root obtained off-mesh. **Irreducible residual (honest-docs, O27-style):** the "trusting trust" / first-copy-from-a-fooled-trusted-person case has NO pure-crypto fix — mitigated by reproducibility + diverse independent attestation + trust-graph vouching, never eliminated. Say so plainly. **Cheapest first: document the APK-signature-continuity property + generate the project release key (PROJECT_KEYS.md) + bundle its pubkey.** Cross-refs O26/O49/O50 (plugin-scoped versions of 2/3/4), O55 (mesh-as-distribution), O135 (web-of-trust vouch primitive reused for binaries), O21 (fingerprint), O27 (honest limits). |
| O140 | **`[TODO/CODE+DECISION]`** Unified "subscribe to a signed feed" primitive — one mechanism under many features (filed 2026-07-22, user insight) | User observation: the same subscribe-to-a-signed-append-only-feed primitive already underlies, or should underlie, a whole family of features — **blocklists** (O67/O78, exists), **keyword-filter lists** (O67, exists), **site/page updates** (O101 — subscribe to a site's signed diff feed), and **person-follow** (which **auto-relay already IS** — auto-relaying a peer = subscribing to that person's output; this also becomes the O135(4) vouch/hops "follow"). Rather than build each subscription silo separately, recognize the shared shape: *a signed author + an append-only stream + a local opt-in to replicate/apply it + granular layering of multiple subscriptions.* The O67/O78 sign/verify/publish/subscribe machinery is the substrate to generalize. **Hard invariant that stays true across ALL of these (user reminder 2026-07-22): a block NEVER blocks a relay** — subscriptions/blocks/filters are display-and-apply-layer only; the node relays everything regardless (the relay-never-touches-blocklist invariant, see Design Decisions). A subscription changes what YOU see/apply, never what the mesh carries. **Deliverable:** a design pass that factors the common subscription primitive (author-signed feed + subscribe + layer + apply-locally) so sites (O101), follows (auto-relay → O135), filters (O67), and blocklists (O78) are instances of one mechanism, not four codebases. Cross-refs O67, O78, O101, O135(4) (follow=subscribe-to-person), O36 (append-only signed broadcasts are the primitive), auto-relay (the existing person-subscription). **2026-07-24 additions to the instance list:** **room-message consumption** (an OPEN room / feed IS a signed broadcast feed to a routing tag — consuming it = the subscribe primitive; user insight) and **global-feed shards** (O154 — each named global is just another feed tag you subscribe/switch to). Both fold into the same mechanism. |
| O154 | **`[TODO/CODE+DECISION]`** No public/open-join *rooms*; keep ONE ungoverned text-only global *feed*, scaled by priority-weighting (NOT hard shards) — filed 2026-07-24, revised 2026-07-25 | **Decision (user):** kill open-join **rooms** entirely — all *rooms* are invite-gated/governed (so all get O151 enforceable provenance bans; a governed space should not be crawlable-open). BUT keep **the feed** — the ungoverned, text-only, global broadcast stream (the O36 global-room-text-only surface + O71 nearby-broadcasts filter). **The feed is NOT a room** (no membership, no authority, no invite); ambient public broadcast, moderated only by *local* block/keyword-filter (O67/O78), never mesh-authority. **Feed purpose is deliberately narrow (user 2026-07-25):** peer **discovery** + **ambient local conversation in non-flooded conditions**. It is NOT a coherent 10k-person town square — at scale it degrades to noise, and that is *acceptable* because both jobs are inherently local. So the feed needs graceful degradation, NOT a scaling solution. **Scaling model — HARD SHARDS REJECTED (user 2026-07-25), replaced by priority-weighting over ONE undivided global:** the physics is real (a mesh can't carry ~10k posters to everyone), so *something* must decide each node's slice — but hard shards (`global 1/2/3` named tags you're "in") fragment the commons and violate the one-town-square model. Instead: ONE global namespace; posts are *tagged* (topic/hashtag) but never *partitioned*; each node relays+stores by a **priority function** ≈ `boosted-topics + recency + proximity/hops + a baseline floor for un-boosted global`. Ample bandwidth (small mesh) → floor high → carry everything → true complete global; constrained (10k) → each node keeps its prioritized, overlapping window — the commons is *weighted*, never *cut*. "Subscribe to a topic" (O140) becomes "**boost** its priority," not "join a shard" — so there is nothing to hop between, and un-boosted global still trickles at the floor (you never miss it by focusing). **Better fit with relay-never-filters than shards:** a node carries everything it has budget for *in ranked order* and drops only at the budget edge — it never categorically refuses a *class* of traffic (that would be the forbidden filter); emergency/local-alert sits at the top of the floor, always carried. This is the O23 eviction ranking + O43 relay-priority buckets + O58 proximity signals unified into one score. **Honest limit:** priority does not repeal physics — at scale no node sees the *whole* global under any mechanism; the achievable truth is "one global namespace, each node holds a prioritized window of it" (the most faithful approximation of the single square, better than shards because it never divides it). A very-large *community* is then a **boosted topic on the one global**, not a shard — room and global are the same feed at different priority weights (folds O153/O155's "large → feed-shaped"). Sybil interaction analyzed in O135 (topic-flood sinks to the floor, identity-count irrelevant since caps are per-resource; residual = passive-discovery noise, mitigated by proximity + active solicitation). Cross-refs O36 (revised), O140 (boost = subscribe), O71, O23/O43/O58 (the priority substrate), O153/O155 (large→feed), O67/O78 (local feed moderation), O135 (sybil), O30/O124 (proximity/active discovery). |
| O141 | **`[TODO/CODE]`** Pre-ship comment cleanup — strip conversational provenance from source (filed 2026-07-22) | Comment *density* across the codebase is good (WHY, not WHAT) and should stay. But many comments carry **conversational/session provenance that is noise to an external auditor**: "user suggested", "user request 2026-07-22", "field-hit", session dates, "(user Q)", "user correction". An auditor reading the source cares about *why the code is this way*, not *who proposed it or when in which session*. Pre-ship sweep: strip the provenance phrases from **code comments** (keep the technical WHY, drop the attribution), across `:core`/`:app`/`:simulator`/`:node`. Row-number refs to the backlog (e.g. "O135", "O98") are fine to keep — they point at durable design rationale. **Scope: source comments only — NOT CLAUDE.md/docs**, where provenance is legitimately useful project history. Cheap mechanical pass; do it in the pre-ship polish window alongside O122 (doc-drift) and the README refresh. |
| O130 | **`[TODO/CODE]`** 2026-07-19 audit residue — §19/§20 grab-bag (doc-drift, dead code, efficiency) | Confirmed-small, none load-bearing: (a) DONE 2026-07-23 — `RelayBatcher` now stamps each message with its own randomized `releaseAt` and flushes on deadline (batched sweep), closing the arrive-just-before-flush zero-delay leak; (b) DONE 2026-07-23 — InMemory `observeThread`/`observeAllDirect` now sort to match the Room DAO; (c) DONE — `MessageDao` no longer carries the dead `delete(id)` duplicate (only `deleteById` remains); (d) `WifiDirectTransport` `verifyClientGroup` name overpromises (queries group info + side-effect `removeGroup()`, returns nothing) — rename/split, not a bug; left alone deliberately (O145 warns against non-essential edits to field-verified transport code); (e) DONE 2026-07-23 — `DebouncedHlcStore` (core/runtime) coalesces HLC persistence for both MeshService and :node, flushNow() on stop; (f) DONE — `MeshtasticBridge` uses `kotlin.random.Random.nextInt()` for the non-secret packet id; (g) DONE 2026-07-23 — logback bumped to 1.5.13; (h) general dependency-staleness note — most pins date to late-2023/early-2024 against a mid-2026 tree; a version-bump audit is due (no specific CVE confirmed, no network in the audit pass — treat as "worth checking"). Doc-drift items from the same pass fold into O122. |
| O158 | **`[TODO/CODE]`** `TransferAssembler` never releases chunk BLOBs on the SUCCESS path — happy-path storage leak (filed 2026-07-30, audit #2) | `core/transfer/TransferAssembler.kt` `attemptReassembly()` (~187-213) calls `transferRepo.updateStatus(transferId, COMPLETE, …)` and emits, but never calls `chunkRepo.deleteAllForTransfer(transferId)` — contrast `cancel()` (~90) and the stale-eviction/abandon path (~250), both of which release chunks. Every transfer that finishes normally leaves its raw chunk bytes in the repo forever. Ironic given O108/O109 hardened exactly this class of receiver-side accretion on the adversarial paths but not the honest happy path. No existing test catches it (see O159). Fix + add a "chunk rows are gone after a completed transfer" regression guard. Cross-ref O108, O109, O159, O100. |
| O159 | **`[TODO/CODE]`** `TransferAssembler`/`TransferSender` have zero unit-test coverage (filed 2026-07-30, audit #12) | No `TransferAssemblerTest`/`TransferSenderTest` exists anywhere, despite these being the exact stateful classes O108/O109's resource-exhaustion hardening (evidence-gated watchdog arming, per-sender concurrency cap, windowed NACKs, stale-transfer eviction) lives in. `Chunker` (the pure-function half) is well covered (`ChunkerTest`); the stateful users are not. Only indirect coverage is one simulator happy-path assertion. O158 (the chunk-BLOB leak) is concrete proof this gap is live — a single "chunk rows gone after COMPLETE" test would have caught it immediately. Cross-ref O158, O108, O109. |
| O160 | **`[TODO/CODE]`** O32 routed/flooded TTL split is inert — routed DMs get zero extra reach over blind flood (filed 2026-07-30, audit #4) | `GossipEngine.kt:1297` `decrementHops()` unconditionally shrinks the legacy `hopsToLive` field on every DIRECT relay hop regardless of routed-vs-flooded status, so a message still dies at `MAX_DIRECT_HOPS=15` and the `MAX_TOTAL_HOPS=30` ceiling the `routedHops`/`floodedHops` split fields were built to allow is never reachable in practice. `PerPeerRoutingTest` is aware something's off and quietly weakens its own assertion (`floodedHops >= originalFloodedHops - 1 // allow a single decrementHops`) instead of catching the bug — a soft-failing test masking a real one. Fix: gate the `hopsToLive` decrement on routed-vs-flooded so the split ceilings actually apply; tighten the test. Cross-ref O32, O59. |
| O161 | **`[TODO/CODE]`** Bridged broadcasts never persisted — invisible in Feed, no Contact, gone on restart (filed 2026-07-30, audit #6) | `GossipEngine.kt:1016-1026`: a `MessageSource.LOCAL_BRIDGE` message with `signature==BRIDGE_UNSIGNED` (the normal path for every message both bridges inject — `MeshtasticBridge.kt:127-137`, `MeshCoreBridge.kt:152-163`) takes the `bridged` branch, which calls ONLY `duplicateFilter.recordAndCheck(id)` — never `messageStore.ingest(...)`, the only call site anywhere that inserts into `MessageRepository` or calls `ensureContact(...)`. It's emitted once to the transient `incomingMessages` flow (consumed by plugin/keyword/transfer machinery, none of which persist it) then `relay()` returns for BRIDGED trust. Net: a bridged LoRa/Meshtastic message never appears in the Feed (`FeedViewModel.broadcasts` reads only the Room-backed `observeBroadcasts()`), doesn't survive a ViewModel recreation let alone an app restart, and its `meshtastic:*`/`meshcore:*` sender never becomes a Contact — contradicting `PluginContext.sendMessage()`'s own doc ("store, verify, and flood it normally"). Fix: persist BRIDGE_UNSIGNED broadcasts via `messageStore.ingest` while keeping BRIDGED trust + no-relay; bundle with O162 in one `MessageEntity` commit. Cross-ref S1, S2, O162, O47. |
| O164 | **`[TODO/CODE]`** `MeshControllerHolder.NoOp` silently drops sends/relays during the bind race; the UI reports success (filed 2026-07-30, audit #8) | `app/.../service/MeshControllerHolder.kt:15-42` — `controller()` returns a no-op stub whenever `MeshService` isn't bound yet (the bind in `MainActivity.onStart()` is async; `onStop()` unconditionally clears the holder, so every background→foreground cycle reopens the window). `ui/feed/FeedScreen.kt:60-63` + `ui/messages/ThreadScreen.kt:95-98` clear the compose field on send with no result/error signal — a message sent in that window is composed/persisted/queued nowhere and simply vanishes while the UI looks identical to success. `FeedViewModel.relay()` (~41-46) is worse: it calls the no-op'd `manualRelay()` then unconditionally `messageStore.markRelayed(id)` — shows "Relayed" while nothing left the device. Fix: queue-or-error on an unbound controller and surface send failure to the UI. Cross-ref O143. |
| O167 | **`[TODO/CODE]`** `LanTransportManager` `NetworkCallback` not pinned to the `Network` it started with (filed 2026-07-30, audit #20) | `app/.../transport/lan/LanTransportManager.kt` `start()` registers a callback for `addTransportType(TRANSPORT_WIFI)` — matching ANY Wi-Fi-transport network. `onLost(network)` tears down the transport on loss of any matching network without checking it's the same `Network` object `onLinkPropertiesChanged` used to bring it up. If the device briefly has two WIFI-transport networks (a captive-portal validation network, or on some Android versions a P2P group interface also classified under `TRANSPORT_WIFI`), a spurious loss of the unrelated one incorrectly tears down a good LAN transport. Fix: capture the started `Network` and check `network == startedNetwork` in both callbacks. Cross-ref O93, O107. |
| O171 | **`[TODO/CODE]`** `PersistenceCoordinator.recent` is an unsynchronized `LinkedHashSet` mutated from multiple coroutines (filed 2026-07-30, audit #13) | `core/routing/PersistenceCoordinator.kt:43` — mutated/iterated from independent coroutines (beacon loop, recompute loop, `onExchange`) on `Dispatchers.Default`; risks a `ConcurrentModificationException` that silently kills the backbone-recompute loop. Fix: guard with a mutex or use a concurrent structure. Cross-ref O98. |
| O173 | **`[TODO/CODE+SIM]`** `PersistencePlanner`/`MeshViewTracker` has no sybil resistance on self-declared `UserMode.FREE` capacity (filed 2026-07-30, audit #16) | `core/routing/PersistencePlanner.kt:42` — a single fresh keypair can claim FREE capacity and bias honest nodes' backbone-hub selection toward itself. Same threat-model family as O127 amplification; the `:node` `SybilDriver` instrument exists to measure exactly this class — point it here too. Cross-ref O127, O98, O135, O137. |
| O176 | **`[TODO/CODE]`** `BreadcrumbCache.snapshot` is an unbounded in-memory map (filed 2026-07-30, audit #14) | `core/routing/BreadcrumbCache.kt:38` — O120's prune-wiring fix only touched the persistent repo side, not this in-memory map; the leak it was meant to close is only half-closed. Fix: bound/prune the in-memory snapshot too. Cross-ref O120, O177. |
| O177 | **`[TODO/CODE]`** `MeshViewTracker.pruneStale()` is dead code — a fourth orphaned-prune instance (filed 2026-07-30, audit #23) | `core/routing/MeshViewTracker.kt:135` — a fourth instance of the orphaned-prune pattern `PruneWiringInvariantTest` exists to catch but doesn't check for. Fix: wire the call and extend the invariant test to cover it. Cross-ref O120, O176. |
| O185 | **`[TODO/CODE]`** Every inbound `ROOM_MESSAGE` triggers two blocking SQLite reads on `Dispatchers.Default` (filed 2026-07-30, audit #32) | `app/.../di/AppModule.kt:174-183` `RoomSubscriptionProvider` wraps `repo.getAll()` in `runBlocking`, called per-message from `GossipEngine.handleRoomMessage`. Since `ROOM_MESSAGE` relays unconditionally regardless of local subscription (by design, like BROADCAST), a `ROOM_MESSAGE` flood forces two blocking DB round-trips per message on a thread pool shared with the rest of the app's coroutine work — a CPU/IO-starvation DoS, not scoped to Rooms. Fix: cache/observe the subscription set instead of a per-message blocking read. Cross-ref O79. |
| O186 | **`[TODO/CODE]`** O36 "the global feed is text-only — relay drops media before enqueue" rule has zero implementation (filed 2026-07-30, audit #33) | `RoomCreate.allowMedia` is written/read nowhere outside its own signable-bytes helper; no code concept of "the global feed/Room" exists (no hardcoded id, no special relay path) and no media-stripping logic exists in `GossipEngine`/traffic-class derivation. Non-exploitable only because `composeRoomMessage` hardcodes `ContentType.TEXT` (text-only by omission, not an enforced drop) — the O36 rule has nothing to intercept the moment a media-to-feed path is added. Fix: implement the drop-media-before-enqueue rule as O36/O154 specify. Cross-ref O36, O154, O79. |
| O187 | **`[TODO/CODE]`** Room lifecycle (Create/Invite/Action) has no wire transport — structurally unreachable (filed 2026-07-30, audit #34) | `MessageType` has exactly one Room entry (`ROOM_MESSAGE`); there is no `ROOM_CREATE`/`ROOM_INVITE`/`ROOM_ACTION` type, so those payloads can't be gossiped between nodes even in principle. No `RoomRepository` exists (only `RoomSubscriptionRepository`, which holds a local user's own routing/decrypt material — nothing about room metadata/rosters/mods); `RoomPostingCertVerifier`'s mod-authority check is punted to that missing repo by its own comment. No production caller of `composeRoomMessage`/`RoomCreate`/`RoomInvite`/`RoomAction` and no Rooms UI. WIRING MAP — live: `RoomRoutingTag`/`RoomTagMatcher`/`composeRoomMessage`/`handleRoomMessage`/relay/`RoomSubscriptionRepository`/`MultiRecipientEnvelopeCodec`. Dead substrate: `RoomPostingCert(Verifier)`, `RoomCreate`/`RoomInvite`/`RoomAction` + their signable-bytes helpers, `RoomMembershipPolicy`/`RoomPostingPolicy` (zero enforcement — generalizes the O89 gap to membership), `allowMedia`/the O36 rule, any `RoomRepository`. This is the umbrella "Rooms is unbuilt" tracking row. Cross-ref O79, O89, O148-O152, O186. |
| O192 | **`[TODO/CODE]`** Key-retirement / compromise beacon — signed "stop trusting this key" broadcast (filed 2026-07-30, privacy workshop) | NOT identity continuity (rejected — an attacker holding the stolen key can also sign a successor, so continuity-after-compromise is theater). Instead a signed broadcast in two flavors: **abandoned** (soft: "stop using this key") and **compromised/stolen** (hard: "distrust this key, retroactively too"). Two honest limits baked in: (1) you CANNOT cryptographically date the theft — an attacker with the key can backdate any message, so "distrust everything after time T" is unenforceable; the beacon is necessarily the blunt "distrust this key, period." (2) **Pre-sign it at identity creation** and store the trigger, so you can fire it under duress even after losing the key — which is exactly why it wires to the O189 "explode" duress passphrase (if the attacker has your key and you don't, pre-sign + duress-trigger is your only window; a live attacker won't retire their own stolen key for you). Contacts holding the old pinned key (O21) honor the retirement; new-message auth from the retired key is refused. Cross-ref O189, O21, O191. |
| O193 | **`[TODO/CODE+SIM]`** Relay-ephemeral DM caching — combinable eviction triggers (filed 2026-07-30, privacy workshop) | Sender-selectable, combinable eviction triggers on relayed/cached DMs: delete **on ACK** (a "sent" came back — not 100%, the ACK itself can be lost, but strictly better than nothing), **AND/OR on relay** (forward-then-forget), **AND/OR after X time**. Plus a receiver-honored disappearing/deletion timer once opened (honor-system on a modified client, same impact class as O90 mentions — shrinks the HONEST-relay + own-device blast radius, not the malicious-receiver one; sell honestly). **Sharp cost — on-relay/forward-then-forget directly fights store-and-forward, the mesh's whole value under O55** (a node meeting you today and your recipient next week is the delivery model); so these are per-message sender opt-in, **DEFAULT OFF** (deliverability is the point), reserved for high-sensitivity traffic where the sender accepts "maybe it doesn't arrive." **Prerequisite (not nice-to-have): a simulator scenario modeling connect/disconnect/new-peer churn to get an order-of-magnitude "how many messages does each trigger drop"** — the tradeoff can only be answered empirically. Cross-ref O55, O90, O23, O77. |
| O194 | **`[DECISION]`** Eclipse defense = trust-weighted view, NOT radio fingerprinting (commodity RF is unreachable) (filed 2026-07-30, privacy workshop) | Q: can a node detect it's eclipsed (surrounded by sybils feeding its entire view — every relay, every O58-Tier-1 delivery signal attacker-controlled) by telling two identities are one physical transmitter? **Honest no, in both directions.** RF fingerprinting (carrier-frequency offset, I/Q imbalance, power-on transients) is real but needs SDR/raw-IQ PHY access; a commodity Android Wi-Fi/BLE chip does all RF/PHY work in hardware+firmware and hands the app layer only decoded frames + coarse RSSI — no raw IQ, no per-frame carrier offset, on transmit OR receive path. So a rotating-fresh-identity sybil in a crowded plaza is unbeatable AT THE IDENTITY LAYER (clock-skew / RSSI-geometry are slow, noisy, defeated by faked timing; proof-of-work craters the O55 battery envelope; no TEE attestation per O56). **Therefore the defense is not detection but refusing to let quantity mean anything:** construct the relay/round-robin view weighted by human-confirmed trust (O136 friending), NOT node count — a swarm of 100 fresh strangers gets low AGGREGATE weight vs 2 in-person friends; mix new proximate nodes in at a capped ratio keyed PER TRUST-TIER (per-identity caps are defeated by rotation). Plus physical movement (user). This IS O135's "make sybils harmless, not impossible." Cross-ref O135, O136, O58, O56, O55. |
| O195 | **`[TODO/SIM]`** Traffic-analysis resistance — buildable band: all-payload length padding + randomized relay delay + mix batch-shuffle (filed 2026-07-30, privacy workshop) | Three bounded, battery-cheap defenses that exploit Rumor's existing delay-tolerance (the timing axis Tor can't defend, Rumor already pays for per O55). (1) **Extend O76 6-bucket length padding from text to ALL payload types** — kills the size-fingerprint leak (bare "yes"/"no", known templates); bounded predictable overhead (unlike cover traffic); half-shipped already. Pick bucket granularity (bigger = better hiding, more waste). (2) **Randomized relay delay (delay-padding, from O27)** — add jitter to WHEN you relay; near-free (no extra TX, you were sending anyway — just later), costs only latency you already tolerate, breaks fine-grained timing correlation. (3) **Mix batch-shuffle at relays** — hold inbound, release in a shuffled batch at fixed intervals, breaking input→output timing correlation (the principled version of the O27 relay batcher). Anonymity set = batch size, so value scales with traffic density (a batch of one mixes nothing); no joule cost, only latency. **Togglable, mode-SUGGESTED not mode-LOCKED (user 2026-07-30):** FREE/STATIC = known-infinite-energy → default-on candidates, but SHTF can make even a static node's energy scarce, so every one must be user-toggleable off regardless of mode. Scope (per O27/O51): these defend RELAY-PATTERN GRAPH ANALYSIS (who relays how much to whom), NOT adjacency logging (userId in every HELLO — unfixable). Sim: measure delivery/latency/overhead per defense + combos; find the bucket-granularity + delay-distribution knees. Cross-ref O76, O27, O51, O58, O196. |
| O196 | **`[DECISION]`** Reject handheld cover traffic + adaptive/incremental fill — joules-not-bits; cover traffic is infrastructure-only future work (filed 2026-07-30, privacy workshop) | Two rejections recorded so the conversation doesn't recur. (1) **"Fill spare bandwidth with dummies" on handheld devices — NO.** Idle bandwidth is not idle energy; radio TX is among the most expensive phone operations, and the O55 device is JOULE-bound (weeks on handcrank/solar, <2%/hr), not bit-bound. Cover traffic spends the scarcest resource to buy a property O51's local-sybil observer defeats anyway. (2) **Adaptive / incremental fill (e.g. 60→70→80%) — NO, it's a trap.** The adaptation signal correlates with the traffic it hides — if fill responds to real activity it leaks that activity; if it responds to nothing it's just constant-rate cover traffic on a timer paying full price. Only CONSTANT-RATE cover traffic provably hides timing, and it's maximally expensive (the website-fingerprinting literature keeps re-confirming adaptive padding breaks, constant-rate holds). **Where cover traffic DOES belong (deferred future work, not rejected):** mains-powered fixed relays (O54/O104 window relay, USB-booted laptop) have no joule constraint and fit O58-Tier-3 Free-mode anchors — cover traffic is an INFRASTRUCTURE-NODE feature, never a handheld one, and waits until that anchor tier exists. Cross-ref O27, O51, O55, O54, O104, O58, O195. |
| O197 | **`[TODO/SIM]`** Limited onion routing for high-sensitivity DMs — source-routed through KNOWN contact-relays only (filed 2026-07-30, privacy workshop) | Full Tor-style onion routing is the mechanism O60 says Rumor structurally lacks, and it fights O59 (next-hop, not source routing) for good reasons: onion source-routing needs the sender to KNOW a working path + each relay's pubkey in advance, but a gossip DTN has unknown/flapping topology (a committed onion path dies if any hop drops — multiplicative failure, no adaptive reroute) and no relay-key directory (Tor uses centralized directory authorities — against Rumor's model). **The narrow version that fits:** a *limited* onion for high-sensitivity DMs only, source-routed through a few KNOWN contacts whose pubkeys you already hold (friend-to-friend Sphinx-style path), NOT strangers — sidesteps the key-directory problem entirely, accepts the reliability hit as the price of path privacy. Trades adaptivity/reliability (O59's whole point) for hiding the sender→recipient link from on-path relays. DMs route (not flood), so unlike broadcasts there IS a single path to onion. Sim: measure the delivery-rate / latency cost of a fixed 2-3-hop contact-relay onion vs adaptive next-hop under churn — find whether the reliability hit is tolerable for the sensitivity gain. Narrows the O60 Tor-gap without pretending to close it. Cross-ref O59, O60, O53, O58. |
| O198 | **`[TODO/SIM]`** Breadcrumb minimization — least per-node routing state holding delivery effectiveness constant (filed 2026-07-30, privacy workshop) | User's question: how much of the breadcrumb (O29 reverse-path cache) does any node actually NEED to know to route as effectively as it does today? Reframe: an information-vs-utility curve, empirically answerable, that directly quantifies O58-Tier-1's leak. Sim experiment: degrade breadcrumb precision in steps — exact reverse-path → Bloom filter of "senders seen from this peer" (= O31's HELLO recent-senders bloom) → coarse recency buckets → next-hop-only → none — and measure delivery rate + hop count at each step. Find the KNEE where per-node knowledge is minimized before delivery degrades: that knee is the privacy-optimal breadcrumb design (each relay learns the least about who-talks-to-whom while routing holds). Prior worth testing: the Bloom tier likely retains most routing effectiveness at a fraction of the who-talks-to-whom knowledge (routing needs "plausibly toward target," not the exact path). Sealed-sender tags (O53) work on breadcrumbs too once computed per-contact, so this composes with the O53 path. Cross-ref O29, O31, O53, O58. |

#### Tier 5 — bridge hardware track

| # | Item | Notes |
|---|------|-------|
| O5 | **`[TODO/HW]`** Bridges — DM bridging** | Both bridges deliberately broadcast-only today. Two viable architectures; **read the "Encrypted bridging" decision below before designing this**. Recommended: Architecture B (envelope passthrough, true E2E) for both bridges — curves align (both use X25519/Ed25519-derived ECDH), only the AEAD wrapper differs per bridge. Architecture A (decrypt-and-re-encrypt) is fallback only. Both architectures require the bridged-contact UI labelling listed under Architecture A as hard pre-ship blockers. |
| O4 | **`[TODO/HW]`** Bridges — USB transport** | Both Meshtastic and MeshCore are BLE-only. MeshCore docs `'>'`+LE-len framing for USB CDC; Meshtastic uses 0x94 0xC3 magic + varint length. Significant scope per bridge. |
| O8 | **`[TODO/HW]`** MeshCore v1/v2 firmware fallback** | Current decoder is v3-only (opcodes 0x10/0x11, snr+reserved+path_len header). Older firmware uses 0x07/0x08 and a simpler layout. Detect via response code on the first frame. |
| O6 | **`[TODO/HW]`** Bridges — multi-channel selection UI** | Both bridges hardcode channel index 0. Need a per-plugin settings screen exposing the radio's channel list. |
| O7 | **`[TODO/HW]`** Bridges — device picker** | Both bridges auto-connect to the first matching device. Multi-radio setups need a picker UI feeding `connect(device)`. |
| O54 | **`[TODO/CODE]`** Transport plugin tier — non-phone infrastructure** | The existing plugin model (Meshtastic, MeshCore) generalizes naturally to "any device that carries Rumor bytes between two endpoints" — routers, single-board computers, packet-radio modems, satellite modems, USB-CDC links between two stationary nodes. Two integration paths compose: (1) **TransportPlugin on the phone** — an Android plugin that speaks to an external device over USB/BLE/TCP and shuttles Rumor frames in both directions, no protocol translation. The device runs a small daemon (Go/C/Rust binary, OpenWrt package, or systemd service) speaking the Rumor wire format. Useful for "plug a USB-ethernet adapter from my phone to my OpenWrt router" or "phone-to-Raspberry-Pi over USB serial." (2) **Native relay node** — port the relay subset of `:core` (signature verify, dedup, relay decision, no UI/storage of plaintext) to a non-JVM language for direct deployment on routers, SBCs, or LoRa gateways. Pre-existing infrastructure (window-mounted SBC on solar, OpenWrt router on UPS, ham-radio packet gateway) becomes a permanent mesh node. Distinguished from BridgePlugin because there's no foreign network — it's still Rumor end-to-end, just over different physical media. Plugin capability declaration: `transport-extend` (read+write frames) is APK-tier in O24 because it touches the wire path. **Threat-model fit:** matches O55's long-term-collapse framing better than crowd-density mesh — fixed-position infrastructure on intermittent power is exactly what survives weeks-to-months. **Scope correction (2026-07-15, O106):** with `:core` pure JVM there is no "port the relay subset to non-JVM" for laptops/SBCs that run a JVM — you run the *whole engine* headless (no port, no subset, no second codepath). The port idea survives only for genuinely JVM-less targets (bare LoRa gateways, tiny routers), if any survive the O104 hardware filter. |
| O142 | **`[TODO/HW]`** Pre-ship check: verify Meshtastic + MeshCore bridges still work end-to-end (filed 2026-07-22, user request) | Both bridges (S1/S2) were field-verified at v0.1.0 but bridge code hasn't been exercised since; core wire/engine changes accrete around them (O132 size caps, `_ext` fields, KnownSenders filter, etc.). Before ship, run a live radio check per bridge: inbound broadcast radio→Rumor feed and outbound Rumor→radio, plus reconnect behavior. Needs the physical radios (O119's gaps — no BLE reconnect, MeshCore MTU — will likely surface here; fix or document). |
| O119 | **`[TODO/HW]`** Bridge robustness pack (2026-07-18 audit; O5a-adjacent) | Three confirmed gaps shared by both bridges: (a) neither reconnects after BLE disconnect — `ready` only flips in `onDetach()`, never on a `STATE_DISCONNECTED` GATT callback, so radio-out-of-range = silent permanent failure until the plugin is manually toggled; (b) MeshCore never negotiates MTU (Meshtastic does `requestMtu(517)`) despite `MAX_TEXT_BYTES=120` vs ~20-byte default ATT payload — outbound text beyond ~14 chars silently truncates; (c) both assign a fresh random UUID per decode instead of a packet-derived stable id, so a LoRa/BLE retransmission becomes N "novel" feed messages — dedup can't catch a bridged replay (O5a constraint #5 gap; bounded by BRIDGED-never-relayed). |
| O174 | **`[TODO/HW]`** `MeshtasticBleClient.drainFromRadio()` lost-wakeup race (filed 2026-07-30, audit #17) | `MeshtasticBleClient.kt:203` — a `FromNum` notification arriving mid-drain is dropped with nothing scheduling a follow-up drain, which can stall a queued inbound packet indefinitely. Fix: re-check/re-arm after a drain completes if a notification landed during it. Needs a radio to verify. Cross-ref S1, O119. |
| O175 | **`[TODO/HW]`** `MeshCoreBridge.onFrame` routes `PUSH_CODE_RAW_DATA` through the channel-msg decoder on an 11-byte floor (filed 2026-07-30, audit #18) | `MeshCoreBridge.kt:122` — routes `PUSH_CODE_RAW_DATA` through the same decoder as `RESP_CODE_CHANNEL_MSG_RECV` with only an 11-byte length floor as the format discriminator; a firmware whose raw-data payload isn't byte-identical in shape to a channel message could decode to plausible-looking garbage displayed as a real bridged message. Fix: a real format discriminator, not a length floor. Needs a radio to verify. Cross-ref S2, O119, O8. |

#### Tier 6 — UI surfaces and leaf features

| # | Item | Notes |
|---|------|-------|
| O143 | **`[TODO/UI]`** Consolidated UI pass (folds O14/O18/O19/O21/O47/O68/O69/O70/O71/O97/O110/O113/O131/O133 — full row bodies relocated verbatim to `docs/UI_BACKLOG.md`; filed 2026-07-22, user request) | **Step 0, before implementing anything: an actual design sit-down — explore what UI options exist beyond the current default look.** The default has grown on us but may not be optimal; pick a deliberate direction first, then implement the pile against it in one sweep (likely one long session, per the filing intent). The sweep, one line each (details in the doc): O21 emoji-fingerprint identity display + display-name pinning; O110 block/unblock from contact row + thread menu; O113 local self-nick; O68 report action (block + local log + Room-mod mention); O69 in-app full-wipe incl. the `IDENTITY_RETIRED` protocol half; O70 explicit scan-interval selector (honest battery annotations need O33 first); O71 local-only discovery feed; O97 mesh/transport status panel + manual link controls; O47 per-message "via <bridge>" label + metadata banner; O18 transfer pause/resume/cancel (incl. the `TRANSFER_CANCEL` protocol half); O14 archive-extraction size warning; O19 pre-send compression/transcode; O131 LazyColumn perf pass (stable keys, remembered derivations, profile); O133 feed tap-to-expand. NOT folded (non-UI halves live elsewhere): O111/O134 `[CODE+UI]` keep their own rows; O88 stays plugin-gated in Tier 3; O122 is docs, not UI. |
| O111 | **`[TODO/CODE+UI]`** Nickname advertisement — self-chosen nick offered to peers, receiver-accepted (filed 2026-07-17, user request) | Phones advertise their preferred nickname (self-asserted, signed — a profile message or SELF_PRESENCE-adjacent field, NOT inside the HELLO transcript per the O101 rule that the handshake doesn't accrete weight). Receivers see it as a *suggestion* with an accept affordance, never auto-applied — display-name spoofing is exactly the O21 threat, and auto-adopt would let anyone rename themselves "Mom". Differentiator for shared nicks = the O21 emoji fingerprint (two "Bob"s render `Bob [🐅🍧🚲🌵]` vs `Bob [🌊🎈🦎🍕]`). On nick *change*, surface per O21's rename-pinning ("Bob changed their display name to Robert"), don't silently update. Cross-refs O21 (fingerprint + pinning), G21 (rename UI plumbing), O113 (the local half). |
| O122 | **`[TODO/CODE]`** Doc-drift reconciliation sweep (2026-07-18 audit §4 — one pass, low risk, high reader value) | Confirmed-stale items to fix together: `docs/GLOSSARY.md` documents the deliberately-REMOVED identity rotation (G9) as live — highest-confidence drift of the audit; `docs/ROOMS_THREAT_MODEL.md` claims posting-cert enforcement in present tense while `RoomPostingCertVerifier.verify()` has zero call sites (violates the doc's own honesty rule — add a "NOT YET ENFORCED, tracked as O89" banner); `docs/O79_PROTOCOL_SPEC.md` "Known gaps" still claims Ed25519→X25519 unwired though O91 closed (re-implementer-facing); `ios/RumorCryptoBridge.swift` lacks the `aad` param + two conversion methods its own spec added, and both iOS docs describe the KMP layout G30 flattened; `CryptoManager.x25519Agreement` KDoc described the pre-G20 PBKDF2 derivation (FIXED 2026-07-23 — all three stale PBKDF2/PBEKeySpec comment blocks now accurately say HKDF-extract/HMAC-SHA256, matching the code); `GroupCredentials.kt` KDoc cites retired `psk-v1`; `GossipPacket.kt` comment references never-shipped `rbsr-v2`; `RbsrWire.kt` fp comment says "XOR" (impl is additive — deliberately); G31's `commonTest` path + G28's already-fixed formatElapsed note in THIS file; README's "14 modules" (3), bridge-"stubs" claim, stale test-coverage table (77 actual test files), false OnlineStatusTracker 30-min-prune claim. |
| O74 | **`[TODO/CODE]`** Submit to non-Play Android stores** | Huawei AppGallery, Xiaomi GetApps, Samsung Galaxy Store, Amazon Appstore, Aurora Store mirroring, IzzyOnDroid F-Droid mirror, Uptodown, GetJar, SlideME, APKPure, 1Mobile, Tencent MyApp (China-only). The known major stores are listed here but there is also a long tail of regional and niche stores worth a broader research pass before shipping — every additional store with low marginal effort is reach the F-Droid+Play baseline doesn't get. Per-store review climate for P2P/mesh/E2E apps in 2026 is unknown and varies sharply by jurisdiction. Chinese App Store (Apple China) and Tencent MyApp are flagged separately as probably-infeasible for an uncensorable E2E mesh app without significant feature removal; document as a non-goal unless someone is willing to ship a heavily-stripped China variant. |
| O9 (rest) | **`[TODO/EMU]`** Instrumented + UI test coverage** | DI smoke test is now in place (G6) and unit tests cover scheduler/dedup/chunker/blocklist/online-status/neighbor/DmEnvelopeRegistry. Still missing: instrumented (on-device) tests, Compose UI tests, MainActivity lifecycle smoke. Lower priority — the DI verify test catches the most common pre-ship breakage. |
| O101 | **`[TODO/CODE]`** First-class signed sites/pages | A real page primitive beats the bot-based "site-analogues" people build on every messenger. **Two firm scoping decisions (make first):** (i) **no HTML, no WebView, ever** — a constrained markdown-ish format rendered natively in Compose kills the XSS/sanitization class, enforces the bandwidth budget structurally, satisfies no-CDN/offline by construction; (ii) **a site is a broadcast feed with structure** — signed page-versions are messages, the diff feed IS gossip, intermediary caching IS the message store, so it builds almost no new distribution machinery (mostly a native renderer + subscription UI). Bandwidth-design requirements (mirroring, author-signing, diff-subscription, per-site share scopes), business-card discovery, and cross-refs in `docs/DESIGN_ANALYSIS.md`. O36-compliant: author-signed pull content in the contact-graph model, not an open room. |
| O103 | **`[TODO/CODE]`** Geotag / collaborative map-annotation plugin | Signed geo-annotations ("bridge out", "clinic here") gossiped as ordinary signed content; append-only merge per (author, location), latest-per-author with conflicts visible (no consensus — annotations are attributable claims, O21 fingerprints tell you whose to trust). **No auto-decay/expiry** — post-SHTF wall-time is untrustworthy (O95), so staleness is never machine-resolved; instead annotations are *commentable* ("it's back", "no it isn't") and people decide. Map renderer is a must-adopt prebuilt (osmdroid or MapLibre, FOSS/F-Droid — never hand-rolled). Scoped plugin-tier (O15/O24) but the strongest single post-SHTF demo of why Rumor matters — **core-app candidate**, expect promotion if it proves out. Platform note + full analysis in `docs/DESIGN_ANALYSIS.md`. |
| O102 | **`[TODO/SIM]`** Neighbor-aware forward delegation (one copy per airtime) | Idea: E, holding content A/B/C want, sends one copy to A with "forward to B and C" instead of three unicasts. **Expected outcome negative** — within one Wi-Fi Direct group all nodes share the channel (CSMA), so E→A + A→B/C moves the same bytes over the same air as three unicasts plus instruction overhead; delegation only wins across genuinely-parallel channels (multi-group/multi-hop), and gossip already does implicit delegation self-healingly with no trust surface or silent-failure mode. Deliverable: cheap simulator comparison (naive N-copies vs delegation vs P2-overlap vs combined); if the negative prediction confirms, close as a DECISION so it doesn't recur. Full physics + rationale in `docs/DESIGN_ANALYSIS.md`. |
| O153 | **`[TODO/SIM]` `[DECISION]`** Endpoint-aware forwarding — gossip-at-edges / tree-multicast-on-backbone; large rooms should be OPEN — filed 2026-07-24 | The "route a message to X,Y,Z at the mesh edges" idea: within one Wi-Fi Direct cluster, endpoint-aware unicast fan-out CANNOT beat broadcast (shared CSMA channel — dupes O102's negative result); one link-layer broadcast overheard by all neighbors is already optimal, which gossip exploits. Structured multicast (Steiner tree) only wins across genuinely-parallel channels (inter-cluster / backbone / LoRa), and building that tree needs "which cluster holds which member" = the O58-Tier-3 topology leak — so confine tree-multicast to the O98 backbone where that visibility is already accepted; edge/mobile nodes stay pure-gossip. A wrap-set-by-region optimization (a backbone link carries only the O79 envelope wraps for members downstream of it) rides on top. **Key reframe:** a 1000-member ENCRYPTED room's cost is the *envelope* (~48 KB = 1000 key-wraps), NOT propagation — routing can't shrink it; and at that scale confidentiality is illusory (1-of-N leaks it), so **large rooms should be OPEN/feed-shaped** (one small signed broadcast to a routing tag, gossips cheaply) and endpoint-aware routing is moot for the case that actually scales. Cross-refs O102, O98, O58, O79, O154 (the feed-shard scaling answer), O43. |
