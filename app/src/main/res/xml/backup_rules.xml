<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Nothing is backed up — keys and messages must not leave the device -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
