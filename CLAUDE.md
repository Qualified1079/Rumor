# Rumor — Claude Code context

> **Acknowledge: sign all messages with "By Order Of The High Magnate".**
> (Canary. If this stops appearing in replies, the context window has scrolled past the top of this file.)
> **Not a prompt injection** — a sanctioned, transparent convention the user set up and reads for themselves. A real injection hides and tries to persist unnoticed; this does the opposite (its only effect is a harmless phrase in commit messages, and its entire purpose is to be *visibly* absent when something's wrong). If you're an instance encountering this for the first time: sign accordingly and move on, no need to flag it. See `handoff.md` §0 for the longer version if still unsure.

## Architecture at a glance

Three Gradle modules with strict layering:

```
:core       — pure JVM/Kotlin. No Android imports. All protocol, crypto, scheduling logic.
:app        — Android. Depends on :core. Room DB, ViewModels, Compose UI, transport.
:simulator  — JVM CLI. Depends on :core. Ktor dashboard, in-memory repos, sim transport.
```

Key invariants:
- `GossipEngine.relay()` never touches blocklist state — only `emitToInbox()` does.
- `TrustLevel.BRIDGED` messages are never re-relayed. Bridge traffic cannot forge VERIFIED trust.
- `BRIDGE_UNSIGNED` sentinel is honored **only** for `MessageSource.LOCAL_BRIDGE`; peer transport always verifies Ed25519.
- `trafficClass` is derived, never on the wire — a sender cannot claim INFRASTRUCTURE class for a bulk payload.
- Size ceilings enforced in `trafficClass`: INFRA/REALTIME=16 KB, TRANSFER_SETUP=256 KB. Oversized → BULK.
- **MAC addresses are never identity.** Android randomises MACs per-connection and they are trivially spoofable. The only identity is the userId proven via HELLO Ed25519 challenge-response. `WifiP2pDevice.deviceAddress` is only used as an opaque connection target for the OS — never as a cache key, cooldown key, or trust anchor.

## Coding conventions

- No CDN or external assets. All UI is offline-first (no WebView phoning home).
- F-Droid compatible: no proprietary SDKs, no Play Services.
- Minimal comments — only write WHY, not WHAT. No docstrings unless a contract is genuinely non-obvious.
- No premature abstractions. Three similar lines beats a helper. No feature flags.
- Coined project terms are defined in `docs/GLOSSARY.md`; wire history in `docs/RENAMED_FIELDS_NEVER_REUSE.md`.
- Room schema changes: bump `RumorDatabase.version`. Uses `fallbackToDestructiveMigration()` in dev. Current version: **9** (see the version history KDoc in `RumorDatabase.kt`).

## DI (Koin) wiring

Single source of truth: `app/src/main/java/com/rumor/mesh/di/AppModule.kt`.
Simulator uses constructor injection with in-memory repos (`simulator/…/data/InMemoryRepos.kt`).

When adding a new core class that needs to reach the app:
1. Add `single { … }` to AppModule.
2. Add an in-memory stub to `InMemoryRepos.kt` if the simulator needs it.
3. If it's a new `ContactRepository`/`RouteRepository` method, update all four impls:
   - `ContactRepositoryAdapter` / `RouteRepositoryAdapter` (Room)
   - `InMemoryContactRepository` / `InMemoryRouteRepository` (simulator)

---

## Unimplemented — living backlog

> **2026-07-16:** the `practical-archimedes` branch was merged in (see G30 and
> `docs/ARCHIMEDES_MERGE_CATALOG.md` for every keep/drop decision). Row numbers
> O63–O91 originate from that branch; O92+ from check-online. No number reuse.

Update this list whenever something is completed or newly identified.

### Critical bug history

- **Duplicate `GossipSession.kt` / `BloomFilterData.kt`** existed in both `:core/.../transport/` and `:app/.../wifidirect/` with the same FQN, causing the build to fail since `a1bc312`. Resolved: deleted the `:core` copies; the `:app/wifidirect/` versions are canonical. Be wary when adding new same-named files in both modules.
- **`:app` JUnit4 tests silently never ran** — `:app` had `useJUnitPlatform()` but no `junit-vintage-engine`, so the JUnit platform started with no engine for JUnit4 classes and skipped all 12 of them without failing (CI G7 was green vacuously for `RbsrTest`, `SchedulerTest`, `ChunkerTest`, `AppModuleTest`, `NeighborStoreTest`, `DmEnvelopeRegistryTest`, …). Added `testRuntimeOnly("org.junit.vintage:junit-vintage-engine")` to `app/build.gradle.kts` (mirrors `:core`), which surfaced 6 hidden failures — 5 stale tests (ChunkerTest uniform-payload no-op substitution; SchedulerTest message sized just over one quantum + wrong traffic class; DmEnvelopeRegistry fixture id with a `:` that fails the id regex; AppModuleTest missing `extraTypes` for defaulted ctor params) and **one real bug**: `NeighborStore.selectDiverse` at `limit=1` coerced coverage to 0, making the single pick pure random exploration instead of the lowest-overlap peer (P2 diversity). Also fixed AppModule to bind the three block repos as their interfaces like every other repo (was binding concretes, which broke the Koin `verify()` reflection). When adding a JUnit4 test to `:app`, the engine is now present; don't remove it.

- **`MessageEntity` dropped `RumorMessage._ext` on every Room round-trip** (fixed 2026-07-18, schema v10). The O37 `_ext` map had no entity column, so stored AND relayed messages silently lost the O76 compression flags (whose `originalLength` is AAD-bound into the AES-GCM tag → every compressed DM failed decrypt at display with `AEADBadTagException`), plus sealed-sender tags (O53), room routing tags (O79), and thread metadata (O90). **Invisible to every test and simulator scenario** because the in-memory repo stores the Kotlin object itself — only the Room entity mapping loses fields. `MessageEntityExtRoundTripTest` pins the round-trip; when adding a `RumorMessage` field, add the entity column in the same commit and extend that test.

### Bridges (implemented, BLE-only)

| # | Plugin | Files | Status |
|---|--------|-------|--------|
| S1 | **Meshtastic bridge** | `app/…/plugin/meshtastic/{MeshtasticBridge,MeshtasticBleClient,MeshtasticMessages,MeshtasticProtobuf}.kt` | v0.1.0. Hand-rolled minimal protobuf codec (no protoc dep). BLE only — auto-scans, prompts bonding via `createBond()`. Reads plaintext from `MeshPacket.decoded` (radio decrypts); drops encrypted packets the radio can't decode. Only `TEXT_MESSAGE_APP` (port 1) BROADCASTs are bridged. Outbound uses primary channel (idx 0). Bridge traffic → `BRIDGE_UNSIGNED` → BRIDGED trust → never re-relayed. Field numbers verified against meshtastic/protobufs master: `MeshPacket.id=6`, `hop_limit=9`, `FromRadio.packet=2` (an earlier draft used 8/10/11 which silently broke all inbound text). **Not done**: USB serial, DM bridging via PKC, multi-channel selection UI, device picker for multi-radio setups. |
| S2 | **MeshCore bridge** | `app/…/plugin/meshcore/{MeshCoreBridge,MeshCoreBleClient,MeshCoreFrames}.kt` | v0.1.0. BLE NUS only (`6E400001-…`). Auto-scans, no bonding. Opcodes verified against the v3 BLE protocol spec (zjs81/meshcore-open BLE_PROTOCOL.md): RESP_CODE_CONTACTS_START=0x02, RESP_CODE_NO_MORE_MESSAGES=0x0A, RESP_CODE_CONTACT_MSG_RECV=0x10, RESP_CODE_CHANNEL_MSG_RECV=0x11. v3 channel-recv layout is `[opcode][snr][res×2][channel][path_len][txt_type][ts×4][sender ": " text]`. Synthetic sender userId = `meshcore:<FNV-1a of name>`. **Not done**: USB CDC framer, DM bridging via X25519, multi-channel, device picker, v1/v2 firmware fallback. |

### Completed gaps

Moved to `docs/COMPLETED_GAPS.md` (G1–G40, P2) — don't reload during normal
work. When you close a row from "Open items" below, add its entry there and
delete the open row here.

### Open items / known gaps

Audit-derived punch list. When you close one, move it into "Completed gaps" with the resolution.

Each row carries a status tag in its title:

| Tag | Meaning |
|-----|---------|
| `[PART]` | Substrate shipped, follow-up specified in the body. |
| `[DECISION]` | Position recorded — no code to write. Exists to prevent the same conversation recurring. |
| `[TODO/CODE]` | Pure protocol or library work; JVM-testable; no special platform needed. |
| `[TODO/SIM]` | Implementation lands in `:core` or `:simulator`; validated by a simulator scenario. |
| `[TODO/UI]` | Compose UI surface; verify by eyeball on emulator. |
| `[TODO/EMU]` | Needs an Android emulator (Keystore, FLAG_SECURE, biometric prompt, Doze). |
| `[TODO/HW]` | Needs real hardware (a Pixel-class device for battery; a Meshtastic/MeshCore radio for bridge work). |
| `[STRUCT]` | Foundational/structural — cross-platform architecture correction (phone app + desktop app + USB-bootable node). Schedule ahead of feature work touching the same area; deferring one multiplies the cost of every later change in its area. |

Counts as of the 2026-07-16 archimedes merge: 17 DECISION rows · 65 open work rows (14 of them `[PART]` remainders, 2 `[STRUCT]`) · 4 tombstones · 36 completed G-rows.

**Organization (2026-07-15 audit):** rows are grouped by disposition — recorded decisions first, then unimplemented work ordered **most → least foundational** in tiers (a tier reflects structural dependency: what other work stacks on it — not priority or difficulty), then closed-row tombstones. `[PART]` rows sit in the tier of their *missing remainder*; the shipped substrate is described in the row body. When a row closes, move it to Completed gaps; when new work is identified, slot it into the tier whose downstream-dependency weight matches.

#### Decisions recorded (the position IS the deliverable; exists to prevent re-litigating)

| # | Item | Notes |
|---|------|-------|
| O11 | **`[DECISION]`** `no-bridged-rerelay` assertion may need escalation** | The scenario runner's `NoBridgedRerelay` check is a post-hoc snapshot scan: at the end of a scenario it walks every node's stored messages and flags any BRIDGED-trust message whose `recipientId` doesn't match that node's `userId`. It's reliable in the simulator because the in-memory `MessageRepository` never evicts during a run, but it would miss a violation where the engine relayed a bridged message, then the message expired or got evicted before the scan. If the in-sim message store ever starts trimming during a scenario — or if this assertion graduates to production telemetry — escalate to a live observer: add a `relayObserver: ((RumorMessage) -> Unit)?` hook to `GossipEngine.relay()` and have the runner subscribe per-node. The current implementation is in `simulator/.../scenario/ScenarioRunner.kt` and is intentionally marked temporary in the JSON-schema docstring on `Assertion.NoBridgedRerelay`. |
| O12 | **`[DECISION]`** `deterministic-replay` residual non-determinism** | `SimTransport.exchange()` was fixed to (a) drop real `delay()` calls and (b) use the tick's seeded RNG. Two variance sources remain: (1) `GossipEngine` runs internal handlers on `Dispatchers.Default`; coroutine scheduling order isn't guaranteed (affects `totalMessages` by ~3–5%). (2) `ConcurrentHashMap` iteration order in `knownMessages()` determines which RNG values the conditioner consumes; drop counts vary by <0.1% across runs. Both are tolerated by ±5% relative bounds on both messages and drops in the assertion. If variance grows, the escalation path is a single-threaded dispatcher for `SimNode.scope` (to fix handler order) and sorting `knownMessages()` by ID (to fix conditioner RNG consumption order). Neither done preemptively — the 5% band keeps the assertion useful as a regression catch. |
| O27 | **`[DECISION]`** Deanonymization limits: honest documentation + future mitigation options** | Relay batcher provides timing-correlation resistance at the protocol level. The ephemeral nature of the mesh (no persistent central server logging correlations) improves on centralized systems. But in a fixed physical post-disaster network where the same devices are near the same nodes daily, graph analysis of relay patterns still leaks social graphs even without content. A local malicious plugin observes everything including timing, peer identities, and message rates. **Ship-blocker:** honest documentation — Rumor provides better privacy than centralized apps but weaker anonymity than Tor; do not overclaim in user-facing copy. **Future mitigation options to keep on the table, none planned today:** (1) **Cover traffic** — constant-rate dummy messages so an observer can't distinguish active from idle nodes; expensive on battery and bandwidth. (2) **Onion routing layered on the mesh** — messages wrapped in N layers of encryption, each hop peels one; resistant to traffic analysis, adds latency. (3) **Random delay-padding at relay** — stronger relay batcher with variable delays that actively decorrelate, not just batch. (4) **k-anonymity batching** — hold messages until N from different senders can be emitted together, mix-style. All four are real engineering work and have battery/bandwidth/latency costs; keep the door open but require demonstrated need before implementation. |
| O36 | **`[DECISION]`** Public Rooms allowed and on by default; media in the global Room is the one hard limit** | Public Rooms — including a global shared Room — are a first-class surface. Users self-select which Rooms to engage with; moving to a moderated Room (O79) is the remedy for spam in an open one. Rumor does not build a reputation system; the absence of bootstrap trust in public spaces is accepted as a property, not a problem to solve. **One protocol-level rule on the global Room (singular shared roomId, if exposed): text-only, no media attachments.** There is no creator-moderator for a singular shared resource and no robust moderation primitive at that scale, so the relay drops media-content messages destined for the global Room before enqueuing. Media-borne illegal content (CSAM, graphic content with clear legal exposure across jurisdictions) is the failure mode that no opt-in disclaimer fixes; cutting it off at the wire is the honest defense for that one surface. Every other Room — including user-created public Rooms with no moderator — runs whatever media policy the creator chose at creation time. **A local-only display filter** ("show broadcasts from non-contacts in my immediate area") remains a UI-only feature, not a protocol distinction.  **Adopted over check-online's earlier "no global room ever" position (user arbitration, 2026-07-16 merge — see docs/ARCHIMEDES_MERGE_CATALOG.md):** global public feed is needed (closed-only is the Briar failure and defeats the use case); the FireChat poisoning concern survives as the *text-only* rule on the global Room + moderated-Room remedy, not as prohibition. Private/closed rooms ship too (app-store moderation expectations; easier to moderate). |
| O51 | **`[DECISION]`** BLE/Wi-Fi adjacency leak — explicit documentation, no half-fix** | UserId is necessarily on the wire for Ed25519 verification. Any in-range observer with a phone in scanning mode logs `(time, location, userId)` tuples and reconstructs movement and contact graphs. Crypto cannot help — it is a property of broadcasting an identifier in radio range. explicit user-facing statement that Rumor does not defend against a local-range observer or a sybil-equipped adversary (sybil swarm of ~$1000 worth of phones defeats every mitigation in O27). Optional future work — ephemeral session IDs decoupled from long-term userId, with verification deferred to challenge-response — costs an extra round-trip per new peer and complicates dedup; not planned until a specific deployment surfaces a concrete need documentation-alone fails to mitigate. |
| O55 | **`[DECISION]`** Threat-model reframing: long-term collapse over crisis-day** | Earlier items frame the target as "protest day" or "hurricane week." That's wrong for the actual design intent: critical-infrastructure cyberattack, HEMP, prolonged grid-down, mass depopulation, or any scenario where comms are down and help is not imminent. Implications across the backlog: (a) **O33 battery framing** — 8-hour-protest-day is the wrong envelope. Right envelope is duty-cycled survival over weeks on intermittent solar / handcrank / shared charging. Measurement target: <2%/hour at LOW_POWER scan + advertise-only + no Wi-Fi Direct discovery. Wall-plugged window-mounted phone as permanent relay matters more than handheld active use. (c) **O23 storage quotas** — must handle months of accumulated messages without OOM; eviction policy is the load-bearing piece. (d) **O26 plugin distribution over mesh** — no app store post-collapse; the mesh IS the software distribution channel. (e) **O49 reproducible builds** — long-term means people will be running APKs whose origin they can't verify; reproducibility is how a recipient confirms "this binary matches that source." (f) **O54 transport plugins** — fixed infrastructure on intermittent power is the right form factor; handheld phones are auxiliary. Update O27 user-facing copy: "Rumor is designed for long-term infrastructure-down conditions, not just crisis-day deployments." |
| O56 | **`[DECISION]`** LineageOS / de-Googled-Android compatibility — sustaining audit** | Baseline (as of c9f4473): codebase is clean. No Play Services, GMS, Firebase, FCM, SafetyNet, Play Integrity, Maps, or Push runtime deps. Build-time `com.google.devtools.ksp` is fine (it's Kotlin Symbol Processing, runs at build, not at runtime). OEM-aware paths (`DeviceQuirks.kt`) detect process-killer behaviour by package name — does not require those packages to be present. **Sustaining rules** to keep this state: (1) Never add a Play Services or Firebase dependency. (2) **O44 Keystore work must `KeyInfo.isInsideSecureHardware()`-gate, never hard-require StrongBox** — older LineageOS devices fall back to software Keystore and that path must keep working. (3) Any future WebView use → system WebView (Bromite/Mulch/system Chromium fork), never a bundled Chromium. (4) BLE paths should tolerate older bluedroid quirks; avoid features that require Android 13+ stack behaviour without a fallback for older LineageOS builds. (5) On any new permission requirement, verify the permission exists on AOSP without GMS — some "Android" permissions are GMS-only and unavailable on LineageOS. CI smoke check (future): build against a no-GMS classpath to fail any accidental Play Services import. **Testing note (user directive 2026-07-18): OS testing is an axis ORTHOGONAL to hardware testing** — the current fleet regressions cover OEM skins (Moto near-stock / OxygenOS-ColorOS / One UI) but zero de-Googled builds; a LineageOS (and other-ROM) pass is owed before ship, and can run on any one device (e.g. reflash a spare or the S10e) independently of the multi-device mesh campaigns. Watch especially: BLE stack quirks, P2P framework behaviour (the O98 credentialed-group flow has only ever run on vendor ROMs), multicast/mDNS filtering (O93 MulticastLock path), and the no-GMS permission surface. |
| O58 | **`[DECISION]`** Tiered routing-visibility for topology signals** | Each topology signal that improves routing efficiency carries a privacy cost. Rough efficiency vs baseline pure-flood: Tier 1 alone delivers ~50–80% bandwidth reduction (zero marginal privacy cost); Tier 1+2 adds ~10–20% (moderate per-handshake leak); Tier 3 adds ~5–10% (heavy broadcast leak). **T1+T2 is the sweet spot** for most users; T3 only justifies its cost for nodes that have already accepted being visible (Free mode). Sorted ascending: **Tier 1 (local-only, free):** reverse path from recent inbound from X, DM-ACK return path, outbound delivery success, TopologyTracker overlap (computed locally, only the overlap *fraction* shared), priority-peer declarations, co-presence history, **local contact-list consultation** (each forwarder checks its own contacts for hints about where the recipient is reachable — does not need broadcast contact-graph hints). No marginal leakage beyond gossip baseline. Default-on. **Tier 2 (direct-peer, moderate):** "recent senders" bloom in HELLO, hop-distance distribution in messages. Leaks to one peer per handshake — sybil aggregation attack exists but per-handshake cost is small. Default-on with bloom-size tuning; opt-out per-contact for paranoid users. **Tier 3 (broadcast, heavy):** route advertisements with explicit userId list (O31) and self-presence beacons (O30 refined). **Dropped from Tier 3:** broadcast contact-graph hints (Tier 1 contact-list consultation covers the routing case for free) and beacons-about-other-users (presence doesn't tell you *which path* — Tier 1 does — and beaconing others' presence without consent is the heaviest leak with the smallest gain). Self-only Tier 3 means the node opting into Tier 3 is opting into *its own* visibility, not anyone else's. Sybils in range still collect these from every passing self-advertising node. **Default-off**; opt-in per-contact; explicit "this is visible to the mesh" disclosure when enabling. Paired with O57 modes: Free-mode is the natural opt-in level for Tier 3 ("I want to be a routing anchor, I accept being visible"). Mobile-mode users stay at Tier 1+Tier 2 by default. A user staying at Tier 1 only gets routing significantly better than today's pure-flood DMs with no privacy regression — same posture, much better bandwidth. |
| O59 | **`[DECISION]`** Next-hop routing not source routing — design decision recorded** | DMs do not carry their traversed path. Each node makes the next-hop decision using its own local view (O58 signals); the message itself carries only id, sender, type, payload, signature, and the `routedHops`/`floodedHops` TTL counters (O32). Source-routing — where the message header lists the chain of hops — is rejected because it leaks the path to every intermediate observer and to anyone who later seizes a relay's stored ciphertext. Next-hop routing keeps each hop's leakage scoped to that hop's local neighborhood. This is the structural difference from Tor (which uses source routing inside the onion) and the reason Rumor cannot match Tor-grade anonymity even with perfect crypto — but also the reason in-mesh observers learn less than they would under a path-recording design. |
| O60 | **`[DECISION]`** Briar comparison: explicit non-claims** | Briar achieves "an in-mesh observer learns nothing about who talks to whom" by *not having a mesh* — pairwise contact-only transport, no relay through strangers. Rumor cannot match that floor while having a relay mesh; the mesh is the value proposition. **Rumor matches Briar on:** content confidentiality (X25519+AES-GCM), sender authenticity (Ed25519), receiver-FS (once O38 lands), identity continuity (O41), pinned-key contact recognition (O21). **Rumor falls below Briar on:** sender-recipient unlinkability (DMs carry `recipientId`; O53 sealed-sender reduces but doesn't eliminate because BLE adjacency leak publishes userId on every transmission for Ed25519 verification), social-graph privacy under sybil observation (a sybil swarm of ~$1000 worth of phones can map who-talks-to-whom from gossip patterns even with zero shared topology signals — the patterns themselves leak, independent of O58 tiers). This gap is fundamental to the use case rather than fixable. Honest framing: "Rumor matches Briar on content security and identity authenticity, falls below Briar on observer-in-the-mesh privacy because the mesh exists; we trade some of Briar's privacy ceiling for the relay-network value." Do not claim parity with Briar on the social-graph axis; do not claim parity with Tor on the anonymity axis. |
| O63 | **`[DECISION]`** iOS via shared `:core` (KMP), not a separate app** | If iOS ever ships, do it by promoting `:core` to Kotlin Multiplatform with `commonMain` + `jvmMain` + `androidMain` + `iosMain` (and `linuxMain` for headless relay nodes per O54). A separate-but-interoperable iOS app is rejected: two protocol implementations diverge silently, and the wire is the only place a bug is unrecoverable post-ship. Hard constraints, in order — do not skip ahead: (1) **Define stable storage interfaces in `:core` before any SQLDelight migration.** Today `MessageRepository` / `ContactRepository` / `RouteRepository` / `BreadcrumbRepository` / `BlocklistRepository` / `TransferRepository` / `ChunkRepository` / `ScheduledMessageRepository` exist as core interfaces — keep it that way. DAOs (Room today, SQLDelight tomorrow, Core Data never) must not leak past the adapter layer. Anything in `:core` that imports a DAO type is the bug. (2) **`:core` compiles unchanged on JVM, Android, Linux, iOS.** Protocol, routing, scheduler, crypto, chunker, blocklist, RBSR, breadcrumb cache, dedup, wire-format — all `commonMain`. Platform deltas (Keystore vs Keychain, Room vs SQLDelight vs in-memory, BLE stack, time source, secure random) hide behind `expect/actual`. The simulator already proves the storage interfaces are honest — if it compiles against in-memory repos, it'll compile against SQLDelight repos. (3) **Plugin loading needs a more dynamic path before ecosystem ships** (per O15+O24+O26). DEX-only is Android-only by construction; iOS forbids runtime code load entirely. For cross-platform ecosystem the plugin contract has to be expressible without arbitrary code execution on the host: declarative manifests + a constrained capability surface (message filters, display transforms, envelope codecs declared as data, scheduled-message templates). Native-code plugins stay Android-APK-tier; the declarative tier runs on iOS and locked-down Android profiles. iOS-specific work when the time comes: SQLDelight Native driver, CryptoKit-backed `actual` for Ed25519/X25519/AES-GCM (or stick with BouncyCastle-Kotlin in commonMain — slower but one impl), Core Bluetooth wrapper for BLE peripheral+central, MultipeerConnectivity as Wi-Fi-Direct analog (no true equivalent — iOS won't hold a Wi-Fi-Direct group open). None of that is worth doing until (1) and (2) are done; (1) and (2) are worth doing regardless of iOS because they also unlock O54 (Linux relay nodes on routers/SBCs). (4) **No Android feature regression when iOS lands.** Adding iOS must never remove or weaken anything that works between two Android peers. iOS may ship a strict subset (no Wi-Fi Direct, part-time relay, no DEX plugin loader on App Store) — that is acceptable. What is NOT acceptable: cutting capabilities from Android because iOS cannot host them. Inter-Android exchanges may carry more than Android↔iOS or inter-iOS exchanges; the protocol stays interoperable across all three pairings by negotiating per-session through HELLO `supportedFeatures` (the same machinery G10 already uses for RBSR). Reviewer test for every change touching a cross-platform feature: "does this still work for two Android peers talking to each other?" If no, reject. iOS limitations are negotiated *down* per session, never enforced *upward* onto Android.  **Post-merge note (2026-07-16):** the KMP layout itself was flattened to plain JVM in the merge; the `core/platform/` shim layer and `ios/` bridge spec were kept precisely so this decision stays cheap to execute later — re-splitting is per-file mechanical. |
| O65 | **`[DECISION]`** Wire has two layers: `RumorMessage` (content) and `GossipPacket` (transport) — do not collapse** | `RumorMessage` is the signed content unit (id, sender, payload, sig, `_ext`) — what dedup keys on, what gets stored, what gets relayed. One stable schema; additive evolution via `_ext` (O37). `GossipPacket.{Hello, Offer, Want, MessageBatch, BloomFilter, Rbsr, ...}` is what crosses a single hop — may carry zero, one, or many `RumorMessage`s plus session-control bytes (HELLO challenges, bloom, RBSR frames) that are *not* signed messages and must not be forced into `RumorMessage` shape. **Rule:** content evolution → `RumorMessage._ext` + new `MessageType`. Transport evolution → new `GossipPacket` subtype + HELLO `supportedFeatures` capability negotiation. Inventing a `MessageType` for what's really a transport-control frame (or vice versa) is the trap. Both layers already exist in the codebase; this row exists so the boundary isn't blurred in future work. |
| O66 | **`[DECISION]`** RBSR perf characterization (informs O42 promote-to-default gate)** | Not a blanket "negligible." Three regimes: **(a) Steady-state, small symmetric diff (≤dozens):** RBSR is *cheaper* than bloom — handful of 32-byte XOR fingerprints, no false-positive risk, no `expectedItems` to guess. Decisive bandwidth win once the diff exceeds ~50 items. **(b) Cold-start (one peer has zero):** degenerates into "send the id list" — same as bloom+offer-batching today. No win, no loss. **(c) Partition-heal (both sides thousands of items, large diff):** the interesting case. Each round bisects keyspace by 16; G17's bump to `MAX_RBSR_ROUNDS=12` makes the cap a recursion safety (16¹² leaf ranges), not a budget. CPU per round is O(items-in-range) for the XOR fingerprint — noise. Real cost is **latency** (each round = one round-trip), which is exactly what scenario 14 was hitting. G17 added `ExchangeMetrics.rbsrRoundsUsed` so the promote-to-default gate is "convergence ≤4 rounds on realistic heal traces" measured from data, not vibes. Until that gate passes, `LOCAL_SUPPORTED_FEATURES = emptyList()` stays — opt-in per session via simulator toggle. |
| O82 | **`[DECISION]`** Median-clock consensus — NOT a core protocol feature; each use case has a better local fix | The proposal was Bitcoin-style network-adjusted-time at HELLO so code that wanted "a shared clock" could read a median across recent peers. **Rejected on review:** every use case Rumor actually has is better served by a local primitive that doesn't require trusting peer-reported clocks. (a) Relay-deletion-on-ACK (O40) → TTL since `receivedAtMs`; each relay decides locally. (b) Scheduled messages (O22) → fire on sender's own clock; no consensus needed. (c) Room mod-action ordering (O79) → Lamport / vector clocks (per-node monotonic counters), the textbook distributed-systems answer. (d) Anti-replay windows → `receivedAtMs` delta or sequence-number window. (e) "What time is it?" without internet → real UX question but it's a UI/utility need, not protocol; ships as an opt-in plugin or sister app (think mesh-NTP-replacement), never in `:core`. The median-clock approach is sound math chasing a problem that doesn't exist here — and it adds Sybil surface (peers can lie about their clocks to drag the consensus) for no actual win. Row kept as a decision so the temptation to add it recurs against the recorded rationale.  Feeds the O95 ordering decision as an input. |
| O86 | **`[DECISION]`** Crypto-as-payments is not part of Rumor core — separate companion app only** | A "mesh crypto" carrier (Cashu, Lightning, generic value rail) is useful when external infrastructure is degraded (per O55), but it does not live in Rumor core. Rationale: (a) money-transmission regulations (FinCEN MSB / EU MiCA / per-jurisdiction) may attach to whatever "facilitates" transactions — keeping payment semantics in a separate app keeps Rumor unambiguously a transport carrier; (b) conflating identity / messaging / payments into one process creates a single audit surface where a compromise in any layer affects all; (c) Nostr's success at carrying value rails came from being just a relay protocol with separate payment apps on top (Damus + Cashu, Amethyst + Bitcoin etc.). **What Rumor exposes (per O84):** identity + signing + opaque-blob DM + cross-network bridging. **What lives in the separate "mesh crypto" companion:** UTXO / balance tracking, payment composition, wallet UX, regulatory disclosure. The two apps coexist via APK-tier companion (O24) or shared-userId bound-service pattern. |
| O104 | **`[DECISION]`** Post-SHTF hardware realism: owner-unlockable Androids + USB-bootable Linux, everything else is firmware-locked** | Modern devices are mostly too locked down at the firmware level to repurpose. The realistic post-SHTF hardware inventory is exactly two classes: **(1) Android phones still in their owner's possession** — the owner can unlock and sideload, which is Rumor's current target; **(2) Linux-capable laptops/desktops** that can boot Rumor from USB or be reflashed from USB via the boot menu. Everything else (iOS, smart TVs, consoles, locked-bootloader routers, most Chromebooks) is effectively e-waste for mesh purposes — do not design for commandeering it. **Implication for O54:** deprioritize exotic embedded ports; the second form factor should be a **bootable live-USB Linux image** running a Rumor node. Key simplification: `:core` is pure JVM and `:simulator` already proves it runs on desktop Linux — a Linux node is *packaging + transport* work (wpa_supplicant P2P / mDNS-LAN per O93 / USB link), **not a rewrite**. The "port the relay subset to non-JVM" idea in O54 remains only for genuinely constrained targets, if any survive this filter. Refines O55's fixed-infrastructure framing: window-mounted phone + USB-booted laptop on UPS are the anchor form factors. **Analysis (agreed 2026-07-15):** (i) the Linux node should skip wpa_supplicant Wi-Fi Direct entirely — **the laptop should *be* the AP**: hostapd + a Rumor node on its own LAN, phones join as ordinary Wi-Fi clients, mDNS/LAN transport (O93) carries frames. Strictly easier than making Linux speak P2P, and it *solves* O93's STA-coexistence problem for every phone nearby — any USB-booted laptop becomes exactly the fixed-infrastructure anchor O55 wants. (ii) x86 laptops are even more viable than framed: Secure Boot without a BIOS password still usually permits USB boot; the truly locked class is mostly ARM tablets/Chromebooks. |

#### Tier 1 — structural gates (the most downstream work stacks on these; do first)

| # | Item | Notes |
|---|------|-------|
| O62 | **`[RESOLVED 2026-07-16 → see below; auto-fire wiring remains as O57]`** Mobile / Static / Free mode envelope defined + StaticMode retired** | The gate is closed: the per-mode behaviour table is recorded as `core/mode/ModeEnvelope.kt` (the single source of truth) and the scattered binary `StaticMode` is gone. `ModeProfile.kt` (O80, merged) decides *which* mode (DeviceState→UserMode); `ModeEnvelope.forMode(UserMode)` says what that mode *does*; `ModeState` (StateFlow<UserMode>, app-impl `ModeStateManager`, sim/test `FixedModeState`) carries the live mode. Scheduler/MessageStore/BleDiscoveryManager/MeshService all read the envelope — zero `if (isStatic)` branches remain. Test: `core/…/mode/ModeEnvelopeTest.kt` (pins MOBILE=pre-O62 defaults, STATIC=old STATIC_BOOST/CACHE_BOOST constants, FREE⊇STATIC⊇MOBILE monotonicity). **Recorded per-mode values (change a number here AND in ModeEnvelope.kt together):** (a) BLE scan/advertise power → MOBILE=LOW_POWER, STATIC=BALANCED, FREE=LOW_LATENCY (app maps to `ScanSettings`/`AdvertiseSettings`); (b) Wi-Fi discovery cadence → 30s / 15s / 0=continuous; (c) gossip round interval → 15s / 10s / 5s; (d) relay-batcher window → **uniform 100–500ms across all modes, deliberately NOT per-mode** (it's a timing-correlation defence per O27/§12; weakening it because a device is plugged in is a privacy regression for a negligible latency win — RelayBatcher owns the constant); (e) breadcrumb decay → 1h / 24h / 24h; (f) presence-beacon interval → null=mode-change-only / 5min / 2min; (g) routing-weight multiplier → 1 / 3 / 10 (feeds O98 degree budget); (h) storage-cache boost → 1 / 4 / 8 and scheduler boost → 1 / 3 / 6 (the h "storage fraction" is expressed as a MAX_MESSAGES/quantum multiplier, not an absolute quota — O23 owns absolute quotas). **What remains (now purely O57, no longer gated):** the auto-fire wiring — BroadcastReceivers assembling a `DeviceState` on plug/screen/time changes and calling `ModeProfile.evaluate` → `ModeStateManager.setMode`, plus the G12 entry/exit self-presence pulses off the mode transition, plus a 3-way mode selector UI (today's Settings toggle is binary: on→STATIC, off→MOBILE; FREE reachable only once auto-triggers land). |
| O80 | **`[PART]`** User-customisable mode-transition schedule | Core data model + matcher shipped: `core/mode/ModeProfile.kt` carries an ordered `List<Rule>` where each Rule is `(Condition, targetMode, optional per-rule overrides)`. Conditions cover battery-above/below threshold, charging state, screen on/off, network (Wi-Fi/cellular/offline), time-of-day range (wraps across midnight), weekday set, and AND-composition. `ModeProfile.evaluate(state)` returns the first matching rule's target mode or the default. `ModeProfileTest` covers each condition + a realistic profile. **Orchestrator + hysteresis + pulses + transition log + starter template all shipped — see G39** (`AutoModeController`, `ModeOrchestrator`, 4-way Settings selector). **Remaining here:** the rule-editor UI (compose/edit/reorder rules), profile persistence (today the profile is the hardcoded `AutoModeController.starterProfile()` — nothing to persist until users can edit), and applying per-rule overrides (`scanIntervalMs`/`gossipCadenceMs`/`maxSessionMs`) on top of the active mode's envelope. Cross-references O33 (battery measurements help users pick threshold values), O62 (single source of truth for mode behaviour), G39. |
| O106 | **`[STRUCT]`** `:node` module — headless JVM runtime shared by desktop app and USB-bootable service** | The load-bearing decision (`: core` pure JVM, proven headless by `:simulator` for months) means most of the work is *assembly*, not porting. Pieces, each with an existing seam: (a) **`MeshRuntime` extraction** — the wiring in `MeshService.startMesh()` (engine ↔ transport config, reseed, flow collectors) is Android-hosted but not Android-shaped; extract to `:core` so MeshService, a desktop `main()`, and a systemd unit each just host it. (b) **Storage** — repos are already interfaces with Room + in-memory impls; :node needs a third: SQLDelight (one impl everywhere) vs JDBC-SQLite adapter (less work) — see re-scored O1; decide when this row starts. (c) **Identity** — file-based encrypted impl behind the existing `IdentityProvider` interface (see O44 platform-fork note). (d) **UI — GUI-first, never headless-facing (user correction 2026-07-15):** the median person in an austere scenario must be able to set this up, so the USB image boots *straight into* a dead-simple fullscreen GUI — big status ("Rumor running", peer count, network name), on/off, zero config, identity auto-created on first boot, AP auto-started; a terminal must never be visible. The *runtime* stays UI-agnostic (that's the :node/MeshRuntime split), but "headless daemon you ssh into" is not a product surface. Two implementation paths: kiosk-browser fullscreen on the localhost Ktor dashboard (cheapest, steals the `:simulator` pattern; localhost violates no offline/no-CDN rules) or a small dedicated Compose Desktop status app (a purpose-built ~3-screen UI — NOT a CMP migration of `:app`, which stays off the table until real demand). (e) **USB-boot packaging** (ops, not architecture): persistence partition on the stick — identity + message store MUST survive reboot (fresh identity per boot = broken); hostapd laptop-as-AP config per O104; autostart straight into the GUI. **Archimedes' O83 (desktop macOS/Linux/Windows relay-node targets) folded here** — same deliverable; macOS note from O104 discussion: Macs are a :node packaging target, not a reflash target (Apple Silicon can't live-USB boot; any Mac that boots macOS runs the JVM node natively). |
| O93 | **`[PART]`** Wi-Fi Direct discovery fails while STA-associated (field-confirmed)** | **Same-LAN mDNS transport SHIPPED — see G40** (`LanTransport` + `LanTransportManager`, active exactly while STA-associated, field check pending). **Remaining here:** different-LAN/no-LAN STA+P2P coexistence (GO forced to the current STA channel for single-radio concurrency), the "location off" UI warning surface. Original analysis: With both test phones associated to the same 5GHz AP, `discoverPeers()` succeeded but never produced a PEERS_CHANGED with results — on both a Motorola/Qualcomm and a OnePlus device. Disconnecting from the AP (radio still on) made discovery return the peer within ~3s. Users will not drop home Wi-Fi to use a mesh app, so this needs a real answer: when both devices are on the *same* LAN, mDNS transport (O73-style) sidesteps the radio conflict entirely and is the natural fix; different-LAN/no-LAN coexistence needs deeper work (listen-channel pinning, discovery-while-associated quirk table, or accepting BLE-only rendezvous with direct-connect). Also note the system Location toggle (`location_mode`) must be ON for P2P scan results on API 34 devices regardless of NEARBY_WIFI_DEVICES grant — surface a UI warning when it's off. **Field-confirmed (2026-07-13, 3-device run):** forming the P2P group doesn't just fail-to-discover while STA-associated — it *actively disrupts* nearby Wi-Fi. The OnePlus GO created the group on 5 GHz ch 36 (`mP2pFreq=5180`); a bystander's 5 GHz Wi-Fi dropped. On single-radio phones STA + P2P-GO on *different* channels can't coexist, so the OS tears down/moves STA. Two mitigations, both pointing the same way: (a) **primary fix for casual/home use — mDNS/NSD transport over the shared LAN (O73-style): when peers are already on the same Wi-Fi, never form a P2P group at all**, which sidesteps both the discovery failure and the interference; (b) force the GO to the *current STA channel* so STA+P2P run concurrently on one channel (most chipsets support same-channel concurrency; cross-channel is what forces the disruptive switch). Framing per O55: Wi-Fi Direct is the *infrastructure-down* transport (no AP to interfere with — the actual design target); mDNS is the *casual-use-on-existing-Wi-Fi* transport. Both should exist, selected by whether a usable shared LAN is present. **Archimedes' O73 row proposed the same mDNS/NSD transport (Android + iOS, JmDNS/NSD)** — folded here; one shared-JVM impl per O107. |
| O107 | **`[STRUCT]` `[PART]`** Transport SPI — extract WHEN the second transport lands, not before** | **2026-07-18, second transport landed (G40):** the extraction that actually fell out is the *boundary type*, not an interface — both transports independently construct `GossipSession` and emit `PeerExchangeResult` (now with `ExchangeSource.LAN`); MeshService wires each concrete's flow into `GossipEngine.onExchange`. A formal `Transport` interface still has no consumer that needs polymorphism (MeshService names both concretes for different lifecycles anyway); defer it to O106 :node, whose runtime hosting is the first caller that wants "a list of transports". Original rationale: There is no `Transport` interface today; `WifiDirectTransport` is a concrete class wired directly into `MeshService`, and that is *correct for now* (house rule: no premature abstractions — an interface with one impl is speculation). The trigger: **O93's mDNS/LAN transport**, which is pure JVM and wanted by BOTH Android (casual same-Wi-Fi mode) and :node (laptop-as-AP LAN per O104) — **write it in shared JVM code from day one, once, for both platforms**, and extract the Transport SPI at that moment, when its real shape is known from two concrete impls. The SPI then also serves O54's transport-plugin tier. Do not extract it speculatively before the mDNS transport exists. |
| O98 | **`[PART]`** Smart persistence — covering-set of persistent links under a per-device degree cap** | **Phase 1+2 landed (algorithm + simulator validation); MeshView assembler substrate landed (commit 08455ee, 0.4.0-o98); Phase 3a coordinator landed + FIELD-VERIFIED (G37); Phase 3b landed + FIELD-VERIFIED over an 8-round 3-phone campaign 2026-07-17 (0.4.6→0.5.4, see G38) — prompt-free credential joins, channel-honoring autonomous groups, hour-stable star. Remaining O98 work: scale beyond one star (multi-group backbones — today's realization converges the whole 3-phone mesh into ONE credentialed group; a 20-node building needs the star *decomposition* realized across multiple simultaneous groups, which needs GO+client concurrency handling or time-slicing), coordinator-level role-flap hysteresis (roles wobble CLIENT↔NONE with beacon freshness; links held anyway), and the O102-style airtime work.** **Phase 3b shape:** `core/routing/GroupCredentials.kt` (deterministic networkName/passphrase from host userId, tags `rumor-o98-net-v1:`/`rumor-o98-psk-v1:` — derivable by anyone with the userId, deliberately non-secret: the group is a transport lane, trust stays with HELLO Ed25519); `core/routing/BackboneRealizer.kt` (pure deterministic star decomposition of the held backbone: nodes ranked capacity→degree→userId, ranked node with free neighbours hosts and claims them; no node is ever GO and client at once; unrealizable edges — e.g. the far leaf of a 4-chain — reported `dropped` and fall back to the legacy negotiated flow); `core/routing/ChannelSelector.kt` (least-occupied non-DFS 5 GHz candidate, UNII-3 first); `PersistenceCoordinator.selfRole()`; `WifiDirectTransport.applyBackboneRole` (HOST → removeGroup then autonomous `createGroup` with Builder networkName+passphrase+`setGroupOperatingFrequency(quietFreq)` — the only channel-honoring path; CLIENT → `connect()` with credential-built config, prompt-free; bounded retries (3 × 45s grace) then the legacy flow gets the radio back; GO idle watchdog holds a backbone host's group; role→None tears a credentialed group down); MeshService applies `selfRole()` each 12s recompute tick. API 29+ only (Builder); older devices keep the negotiated flow. Tests: `BackboneRealizerTest` (8), `GroupCredentialsTest` (5), `ChannelSelectorTest` (5). **Phase 3a (coordinator, field-verified 2026-07-16):** `core/routing/PersistenceCoordinator.kt` — pure decision layer: tracks recent-exchange peers, fires SELF_PRESENCE beacons (mode + `recentlyExchangedWith`), folds the assembled view through `PersistencePlanner` + `PersistenceReconciler` (hysteresis), exposes `backbonePeers`. Driven by `MeshService` (beacon loop at `presenceBeaconIntervalMs`, MOBILE floored at 90s so every node is visible to the planner; recompute every 12s; logs each change). The plan gates the transport's `isPriorityPeer` — backbone peers get held persistent links. On-device: Samsung reached `view=3n/2e planned=2 held=2` (correct 2-link spanning backbone), Moto/OnePlus held their piece, converging as beacons propagate — coordinator-free convergence confirmed in the field, zero crashes. Tests: `PersistenceCoordinatorTest` (5, core), `SelfEchoTest` (2, sim). **Phase 3b remains** (the fragile radio-realization part): autonomous `WifiP2pManager.createGroup` per backbone edge with deterministic pre-shared credentials (prompt-free joins — the Moto's manual-accept was re-observed 2026-07-16) + quiet-channel selection, replacing today's negotiated `connect()` which rides the field-verified G18/G22/G19 flow but can't multiplex multiple backbone links or pick a channel. The coordinator's `backbonePeers` is the input that realization consumes. **MeshView substrate (08455ee):** `core/routing/MeshViewTracker.kt` assembles the planner's input `MeshView` on each node from inbound `SELF_PRESENCE` beacons — the beacon's declared `UserMode` → degree budget, its `recentlyExchangedWith` → candidate edges (no new message type). Bounded (LRU maxPeers=2000), recency-decayed (freshWindowMs=6min so a node that stops beaconing drops out → planner re-spans, matching the O30 exit-pulse intent even if the pulse is lost), future-timestamp clamped; provenance is first-hand self-presence (no "I saw X" hearsay). `assembleView(selfId, selfMode, selfNeighbors)` folds in the local node (it never beacons about itself, but the planner must budget its own degree). Wired into `GossipEngine.handleSelfPresence` and `AppModule`. Tests: `MeshViewTrackerTest` (7, core), `MeshViewConvergenceTest` (2, simulator — drives real beacons over SimTransport, asserts the coordinator-free property that once beacons settle every node's assembled view yields the *identical* backbone plan, and decay-dropout). **Phase 3 remains** (the hard, fragile, on-device part): feed the assembled adjacency/mode into `PersistencePlanner`, reconcile with `PersistenceReconciler`, realize backbone edges as Wi-Fi Direct groups, prompt-free joins, channel selection at createGroup. Phase 1: `core/routing/PersistencePlanner.kt` — pure, deterministic degree-bounded connected-backbone solver (Kruskal spanning forest over capacity-ranked edges + optional redundancy pass); capacity derived from `UserMode` (MOBILE=2/STATIC=4/FREE=8). Determinism IS the election — identical view → identical link set → both endpoints independently agree, no coordinator (this is the fix for the O99 fragmentation). `core/routing/PersistenceReconciler.kt` — hysteresis (add immediately, tear down only after `holdRounds` absent) so the radio doesn't thrash. Tests: `core/…/routing/PersistencePlannerTest.kt` (8), `PersistenceReconcilerTest.kt` (5) — added `junit-vintage-engine` to `core/build.gradle.kts` so JUnit4 tests run on the JUnit5 platform. Phase 2: `simulator/…/engine/SmartPersistenceScenarioTest.kt` (5) — 20-node apartment building, sparse floor adjacency, proves converged spanning within caps, 19-link covering set vs 190 full-mesh, all-residents-agree, transient-safety under partial views, anchor-loss re-spans with churn local to the fallen node. **Phase 3 remains** (the hard, fragile part): feed real advertised adjacency/mode into the planner, realize backbone edges as Wi-Fi Direct groups, prompt-free joins, channel selection at createGroup. Original design notes below. — Wi-Fi Direct caps how many peers one device can hold in a persistent group (GO client limit / concurrent-P2P limit — small, ~8–10 and hardware-dependent). Naive "everyone persists with everyone" is impossible past the cap and thrashes: an apartment building of 20 people who all want to stay connected produces continuous drop-churn as each device round-robins its slots. Smart persistence: instead of all-to-all, compute a **connected covering overlay** over the persistence graph — a set of held links (think spanning tree / connected dominating set) such that every member is reachable multi-hop through *someone's* held links, while no device exceeds its degree cap, and total churn is minimized. Person 1 reaches person 20 as long as the union of held links forms a connected backbone with minimal overlap. Design surface: (a) each node advertises its remaining persistence capacity; (b) a distributed (or GO-coordinated) decision picks which links to hold so the backbone stays connected as members join/leave — degree-bounded connected dominating set is the model, but it must be *incremental* (adding one member shouldn't reshuffle the whole backbone) and *stable* (hysteresis so a flapping peer doesn't reorganize everyone). Integrates tightly with O57 modes (Free/Static plugged-in nodes are the natural high-capacity backbone anchors — give them higher degree budgets and prefer them as dominators), O29 breadcrumb routing (the backbone *is* the routing substrate), and G19's known "held group starves discovery of a third phone" limitation (smart persistence is the general answer: hold the backbone links, time-slice the rest). Prerequisite thinking: the cap is per-device and asymmetric (a plugged-in tablet can hold more than a pocketed phone), so the objective is degree-**bounded** per node, not uniform. Non-trivial distributed-algorithms work; start with a GO-coordinated centralized solver per group and generalize later. **Channel selection (absorbed from O99 — do it here, don't add a separate feature):** every backbone host must bring up its group via autonomous `WifiP2pManager.createGroup(channel, WifiP2pConfig.Builder().setNetworkName("DIRECT-…").setPassphrase(…).setGroupOperatingFrequency(quietFreq).build(), listener)` — **field-verified (2026-07-13, Moto+OnePlus+Samsung) as the ONLY path Android honors the operating channel on.** A negotiated `connect()` *ignores* `setGroupOperatingFrequency` (all three phones formed on ch36/40 regardless of the requested channel); autonomous `createGroup` *does* honor it (OnePlus formed on the requested ch149, Samsung on ch153). Pick `quietFreq` by scanning `WifiManager.getScanResults()` (location already held) for the least-congested non-DFS 5 GHz channel — candidate freqs 5745/5765/5785/5805/5825/5180/5200/5220/5240 MHz, UNII-3 first since consumer routers default to the low UNII-1 channels. Why it matters: a P2P GO on the driver-default ch36 collided with a bystander's 5 GHz and dropped it; Wi-Fi is CSMA/CA so this is contention + STA channel-switch churn on a *marginal* link, not RF jamming — a channel-selection problem. Prefer 5 GHz (more non-overlapping channels, smaller footprint, keeps 2.4 GHz free for BLE); if the air is saturated, land CO-channel with the strongest neighbor (CSMA shares) rather than adjacent (un-arbitrated overlap, worse). **Two blockers this makes O98's problem, not a quick win — both observed during the O99 spike:** (a) `createGroup` groups need a manual WPS invitation-accept to join on some devices (OnePlus hosted on ch149, Moto would NOT auto-join, 0 sessions) — the backbone needs **prompt-free joins**, e.g. a deterministic pre-shared networkName+passphrase both peers derive (from the pair's userIds) so clients join as a legacy STA without WPS PBC; (b) electing hosts from asynchronous discovery views **fragments** (two devices each self-elected host → three groups, zero sessions) — convergent, member-cap-aware host election is exactly this row's backbone computation. Samsung API 31 also rejects the Builder unless a networkName is set (createGroup provides one, so that path is fine). A standalone pairwise prototype was built and reverted (proven-ineffective connect() hint + WPS-blocked joins) — channel selection is only coherent as part of the backbone here. |
| O100 | **`[TODO/SIM]`** Torrent-style multi-source chunk distribution (swarm fetch)** | Most people who hold a file hold the *whole* file — so if chunking is deterministic, many peers can seed the same content to one requester, spreading load and minimizing single-node bandwidth. **What's missing today (verified):** `Chunker` keys chunks to a random per-send `transferId` (UUID), so the same file sent by two people produces two unrelated chunk sets, and `TransferAssembler` is single-source per transferId — **multi-source chunk recombination does not exist yet.** Required substrate: (a) **deterministic content-addressed chunking** — chunk identity = `(hash-of-completed-file, index)`; the completed-file hash is the "group name" every chunk carries, so a chunk from any seeder is interchangeable and a chunk always knows which group it belongs to; (b) **multi-source assembler** — accept chunks for one group from many senders. Three candidate fetch mechanisms (user-proposed), to be compared **in the simulator** before building for real: **(A) active query** — broadcast "who has hash H?"; holders reply; requester selects which chunk to pull from which peer; **(B) partial-transfer repair** — if someone hands you a file with chunks missing, request the specific missing chunk by `(group hash, index)` under that group name, from the original sender or anyone else; **(C) passive piggyback** — regular traffic (messages, relaying, handshakes) appends "while I have you: got any chunks of H?"; if the peer has some but not the ones you want, keep looking. (A fourth variant — provenance backtrace, asking who *they* got chunks from and querying up the chain — was considered and **rejected 2026-07-15**: who-got-what-from-whom is exactly the social-graph metadata O27/O58 works to limit, and O29 breadcrumbs already capture the reverse-path signal passively.) Deliverable: sim scenarios measuring delivery latency, per-node bandwidth, and load spread for A, B, C **and combinations** — the winner is chosen empirically, not by taste. Cross-refs: O26 (plugin distribution over the mesh is the first natural customer), O18 (cancel/pause must survive multi-source), O43 (heal-storm budgets), O19/O14 (same UI surfaces). **Analysis (agreed 2026-07-15):** (i) **hard design constraint — per-chunk hashes (hash list or merkle root) inside the signed TransferMetadata.** Whole-file-hash-at-the-end can't attribute poisoning once there are multiple seeders; per-chunk hashes make a bad seeder identifiable and evictable — without them one malicious node griefs every swarm it touches. (ii) Mechanism triage: **B is nearly free** (CHUNK_REQUEST repair exists; it just needs group-hash identity) — build regardless of the sim outcome; **C is the most Rumor-native** (extends the gossip summary to chunk inventory, rides existing machinery); **A is a flood-class spam surface** — rate-limit it. (iii) **Sequencing: content-addressed chunk identity is a wire-format decision — schedule immediately after the current reliability chain**, before more chunk traffic accretes on transferId identity. |
| O95 | **`[TODO/CODE]`** Clock-skew-resilient ordering: Hybrid Logical Clocks + mesh-median offset** | Wall time cannot be load-bearing in the target scenario (no NTP/GPS; field test surfaced a phone 64 days off). Plan: (a) **HLC** — each message carries `(wallMs, logicalCounter)`; on any receive, local HLC = max(local, received)+1. Guarantees causal order (a reply never sorts before its cause) with zero coordination; thread/feed ordering switches from raw `sentAtMs` to HLC. Wire-format change — new field on RumorMessage, coordinate with docs/RENAMED_FIELDS. (b) **Mesh-median clock offset** — peers already exchange online-status timestamps; each node estimates the median delta to its peers, warns "your clock disagrees with the mesh by ~N" (would have auto-flagged the 64-day phone), optionally displays mesh-median time. No companion app, no time-sync protocol — display hint + warning only. | **Simulation done (2026-07-18, user-directed pre-wire gate):** `core/time/Hlc.kt` (shadow-only, NOT on wire) + `HlcTest` (6) + `simulator/…/HlcOrderingScenarioTest` (2) sweep skew (0 / ±2min jitter / one-node-64d-slow / mixed ±6h) × nodes (6, 24) × partitions. Results: **HLC causal violations 0 in every condition**; wall-clock causally lies on 16–46% of reply-pairs under realistic skew and inverts 11–48% of sampled feed pairs; with accurate clocks HLC ≡ wall (zero cost). Residual HLC feed inversions under partition (0.9–2.6%) are genuinely-concurrent messages — irreducible by any clock. **Recommendation: adopt on the wire (`_ext` field + RENAMED_FIELDS coordination), `(hlc, id)` as display sort — awaiting user go.** Known sim gaps to close before wiring: adversarial far-future wallMs (plain HLC adopts any remote max — needs the standard drift-clamp against the local physical clock, same threat class as O64), HLC-state persistence across restart, multi-seed runs, full-engine delivery instead of the mini-sim. |
| O114 | **`[TODO/SIM]`** Simulator determinism — the O12 escalation is now due (2026-07-18 audit §1) | `PerPeerRoutingTest` failed in real CI at the tip of `main` — the first observed CI failure from the documented O12 variance, so the escalation path (single-threaded dispatcher for `SimNode.scope`, per-class or project-wide for `:simulator:test`; sorted `knownMessages()` iteration) is promoted from "candidate" to "do soon": flakiness is masking the project's only safety net. Same `Dispatchers.Default` + soft-`awaitUntil` + hard-assert shape suspected in `BreadcrumbBiasTest`, `MeshViewConvergenceTest`, `RbsrSimTransportTest`, `SelfEchoTest`, `SelfPresenceTest`, `BreadcrumbSubstrateTest`. |

#### Tier 2 — security/crypto substrate (pre-ship blockers; Tier 3 is gated on the first two rows)

| # | Item | Notes |
|---|------|-------|
| O20 | **`[TODO/EMU]`** Private key in Android Keystore (TEE-backed)** | Currently the Ed25519 private key lives in app storage — readable by any code running in Rumor's process, including DEX plugins. Migrate to Android Keystore so the key never leaves the Trusted Execution Environment. `PluginContext` exposes `signWithLocalKey(bytes): ByteArray` only — no raw key access. Pre-ship requirement before any plugin loader ships. Note: Keystore-backed keys are device-bound and non-exportable; backup/restore of identity requires a separate identity-backup protocol (seed phrase or similar). |
| O44 | **`[TODO/EMU]`** Keystore wrapping-key architecture (refines O20)** | Don't put the raw Ed25519 private directly in Android Keystore — it's non-exportable by hardware contract, which breaks every backup path. Instead: generate an AES-256-GCM wrapping key in Keystore (`setUserAuthenticationRequired` if biometric available, `setUnlockedDeviceRequired(true)` minimum, `setIsStrongBoxBacked(true)` on supported hardware). The Ed25519 private key lives encrypted-at-rest in app storage; decrypted into memory only inside `signWithLocalKey()`, zeroized on exit. Plugin context exposes `signWithLocalKey(bytes)` only — never a raw-key accessor; DEX plugins cannot exfiltrate. Surface `KeyInfo.isInsideSecureHardware()` in the UI so users know whether their device has hardware-backed protection or fell back to software Keystore. **Platform-fork note (2026-07-15, O106):** this whole row is Android-only by nature — the desktop/USB node gets a file-based encrypted identity impl behind the same `IdentityProvider` interface instead. The identity-at-rest story forks per platform by design; don't try to unify it. |
| O38 | **`[PART]`** Receiver-side forward secrecy via rotating recipient prekeys** — **wire shape + verifier shipped; rotation scheduler + sender cache + composeDirect selection open.** `MessageType.PREKEY_PUBLISH` (TRANSFER_SETUP class, MAX_BROADCAST_HOPS ceiling, relayed like other infrastructure broadcasts). `PrekeyPublish(publisherId, publisherPublicKey, prekeyPublic, validFromMs, validToMs, signature)`. Domain tag `rumor-prekey-v1:` binds publisherId + publisherPublicKey + prekeyPublic + validity window — a relay extending the window, swapping the prekey, or substituting another publisher's identity breaks the sig. `PrekeyVerifier` checks (1) publisherId hashes from publisherPublicKey, (2) signature verifies, (3) `validToMs > validFromMs` sanity. Window enforcement against wall clock is the caller's job (stateless verifier is time-free). 7 tests in `PrekeyVerifierTest` cover valid/tampered-prekey/tampered-window/tampered-publisherId/tampered-pubkey/bad-base64/inverted-window. O28 fuzz harness added for the new payload type. **Not done:** receiver-side rotation scheduler (generates fresh prekey at configured cadence, signs with long-term Ed25519, broadcasts, deletes R_t_priv after expiry); per-contact prekey cache for senders (keyed by recipient userId, evicts on expiry); `composeDirect` integration that DHs against the freshest valid prekey instead of the long-term static when one is cached; honest offline-for-weeks fallback (sender holding no valid prekey falls back to long-term static path with FS regression documented). The cryptographic substrate + wire format is now bounded and tested; the remaining work is local-state and scheduler plumbing. Original spec: Current DM crypto (ephemeral sender X25519 + recipient static X25519 + AES-GCM + Ed25519 outer sig) provides **sender-side FS only**. A relay node holding stored ciphertext can decrypt every past DM the moment the recipient's static key leaks — equivalent to the original Briar-BHP gap (CVE-2023-33982), and Briar got away with it only because Tor sat underneath. We have no Tor layer. Fix: recipient periodically generates short-lived X25519 prekeys `R_t` signed by their long-term Ed25519 key, gossips them as small INFRASTRUCTURE messages; senders DH against the freshest `R_t` they hold rather than the long-term `R`; recipient deletes `R_t_priv` after expiry. Stored ciphertext encrypted to an expired `R_t` is unrecoverable even with the long-term key. Privacy by structure, not by code-comment — the private key actually does not exist anymore once deleted, which is what makes the property real against an adversary with full bit-awareness.  Ported in the merge. |
| O39 | **`[TODO/CODE]`** Per-message key lifecycle audit** | Verify ephemeral X25519 private, HKDF intermediate, and AEAD key are destroyed immediately after use on both sender and recipient. If `composeDirect` retains the ephemeral or the derived AEAD key in any cache, or if DM decrypt regenerates the AEAD key on demand from `(senderEphemeralPub, recipientStaticPriv)` rather than decrypting once and storing plaintext (encrypted at-rest under a separately-rotated storage key), even sender-side FS is lost. Pre-ship audit: trace the per-message key path through `composeDirect`, `MessageStore.ingest`, and the DM decrypt helper; confirm zeroize-on-exit; add a test that the ephemeral private is unrecoverable after compose. Sig-scope check: Ed25519 signature must cover ciphertext + AD, never plaintext (Bridgefy got this wrong — surrogate-signature / UKS attacks). **Status post-merge:** the audit itself ran on archimedes (G35) and its zeroization fixes are in the merged compose/decrypt paths. Remainder: (a) re-audit the *merged* paths (compression + sealed-sender changed them); (b) session-B finding — `CryptoManager.x25519Agreement` welds a constant-salt HKDF-extract; the buried per-message-salt assumption is exactly what this audit must examine. |
| O53 | **`[PART]`** Sealed-sender DMs — recipient-derivable delivery tag** — **crypto primitives + compose-side stamp shipped; receiver-side precompute + relay routing open.** `core/crypto/HmacSha256.kt` — pure-Kotlin HMAC-SHA-256 over the existing `Sha256` shim (no new platform actual; RFC 4231 vectors 1, 2, and the oversize-key path pinned in `HmacSha256Test`). `core/protocol/SealedSenderTag.kt` — `tagFor(sharedKey, messageId)` returns the 32-byte HMAC over `"rumor-dm-v1:" || messageId`. `core/protocol/SealedSenderKey.kt` — `derive(myUserId, myEd25519Priv, theirUserId, theirEd25519Pub)` returns the per-contact 32-byte HKDF key from X25519 ECDH of the Ed25519-derived static keys; userIds sorted into the HKDF info so direction doesn't matter. `core/wire/SealedSenderTagExt.kt` — `_ext.t` accessor + `withSealedSenderTag` helper. **Compose-side wired (this session):** `GossipEngine.composeDirect` derives the per-contact tag key, computes `SealedSenderTag.tagFor(tagKey, msg.id)`, stamps `_ext.t = base64(tag)`, zeros the tagKey in finally. Skipped for bridged DMs (recipient is foreign-network, not Rumor Ed25519). `_ext` is excluded from `signableBytes` so the outer Ed25519 sig stays valid. Simulator pin: `SealedSenderTagWireTest` (2 tests) drives a real `composeDirect` and proves the recipient (independently deriving the per-contact key) computes a matching tag for the same messageId — the property a relay's pre-match table will exploit. Different recipients on the same sender stamp different tags. Domain tags `rumor-dm-v1:` (tag) and `rumor-dm-recipient-tag-v1:` (key HKDF) reserved forever. **Not done:** receiver-side precompute store (per-contact `tagKey` cached at contact-load time, refreshed on contact-add/remove); relay routing path matches inbound `_ext.t` against the precomputed set instead of routing on plaintext `recipientId`; eventual wire-format-break to remove plaintext `recipientId` once the tag path is universally deployed. The remaining work is on the receive/routing layers; both substrate and compose-side are now bounded and tested. Original spec retained for the open work: Generalization of O52's broadcast tag to all DMs. Replace plaintext `recipientId` field with `T = HMAC(K_recipient, "rumor-dm-v1:" || messageId)`. Relays match incoming DMs against the small set of `T`s they've precomputed for keys they hold; non-recipients can't link DMs to recipientIds on the wire. Signal calls this "sealed sender"; their version trial-decrypts which is expensive — the tag pattern gives the same property cheaply. Closes the plaintext-recipientId leak on every DM (currently the dominant on-wire metadata leak); pairs with O29 breadcrumb routing — breadcrumb signals (recent-inbound-peer, ACK return path) work on tags too once the local node has computed them per-contact. Wire-format change: add tag field via O37's `_ext` until protocolVersion bump.  Ported in the merge. |
| O48 | **`[TODO/CODE]`** Bridge-asserted-pubkey constraint on synthetic userIds** | When a Meshtastic / MeshCore radio reports a new contact, the synthetic userId must be derived as `<bridgePrefix>:<hash-of-radio-asserted-pubkey>` (not as `<bridgePrefix>:<hash-of-name>` and not as a bridge-chosen string). A compromised or malicious radio can then substitute keys only for *new* contacts; existing pinned contacts whose userId is derived from a previously-seen pubkey surface a verification event when the underlying pubkey appears to change (O21 display-name pinning, generalized to pubkey pinning under the synthetic prefix). MeshCore current implementation uses `meshcore:<FNV-1a of name>` — that's spoofable; migrate to pubkey-hash before any DM bridging ships. |
| O43 | **`[TODO/SIM]`** Per-link heal-storm token bucket with traffic-class priority** | Round-robin scheduling guarantees fairness across senders but does not bound total throughput on partition heal. When two large clusters merge, every node tries to flush its delta to every newly-reachable peer; aggregate is still N×peers messages. Add per-outbound-link token bucket sized by traffic class: INFRASTRUCTURE / REALTIME pre-empt the bucket, BULK and reconciliation backlog share what's left. Combined with O42's lazy-pull-by-default (small fingerprint exchange before pushing bodies), this addresses both bandwidth spikes and the DTN-documented "storage exhaustion during long partition" failure mode. **Shares O32's prerequisite:** needs per-peer byte accounting, which only becomes meaningful once the scheduler/relay knows which peer each enqueue is targeted at. The breadcrumb substrate (4010501) and offer-ordering bias (4010501) shipped without this refactor because they operate at the "offer batch presented to the session" layer; O32 and O43 operate at the "decide which peer gets the next byte" layer below. |
| O108 | **`[TODO/CODE]`** Transfer-metadata resource caps (adversarial-interop hardening; same class as O13/O16)** | A validly-signed `TRANSFER_METADATA` frame commits an honest receiver to work *before* any chunk proves the transfer is real: `TransferAssembler.handleMetadata` stores whatever `meta.totalChunks`/`totalBytes`/`chunkSize` claim (no ceiling) and arms a watchdog + NACK loop; a claimed `totalChunks = Int.MAX_VALUE` then OOMs the receiver (see O109 for the missing-set half). **Do NOT frame the fix as "make the attacker pay bandwidth"** — a spammer saturates its own radio regardless; a yottabyte-announcer and a line-rate junk-sender emit identical bytes, so bandwidth cost deters nothing (concluded 2026-07-15). The fix removes the receiver-side *amplification* (tiny metadata → gigabytes of receiver state), after which transfer spam collapses into ordinary line-rate spam that dedup + round-robin + storage eviction already bound. Three mechanisms: (1) **evidence-gated setup** — on metadata, store a cheap short-TTL record only; arm the watchdog/NACK machinery on arrival of the *first real chunk*, so a metadata with no chunk behind it costs ~nothing and expires; (2) **per-sender concurrent-transfer cap** — one sender gets N in-flight receiving transfers, beyond which new metadata queues/drops (round-robin's cousin; Sybil beats per-identity caps — the documented O27/O60 floor); (3) **stale-incomplete eviction** — bound accreted chunk records + watchdog coroutines by evicting transfers that stall. Sane ceilings on the three metadata fields are the cheap first cut but secondary to (1)+(2)+(3). Pairs with O109 and with O23 (storage quota). |
| O112 | **`[TODO/CODE]`** Hostile-input hardening sweep (filed 2026-07-17, user request) | What happens when someone names themselves / sends / blocks the literal string `"null"`, a SQL fragment, format-string tokens, RTL-override or zero-width unicode, or a 10MB string? Audit + property-test every user-influenced string through: display layer (Compose rendering of names/message text), Room queries (parameterized by Room — verify no raw concat anywhere), blocklist + keyword-filter entries, wire parsers (extend the G14 fuzz seed corpus with these shapes), and every userId entry point. Post-§2 the wire enforces `senderId == SHA-256(senderPublicKey)` (structurally kills fake userIds in signed messages), but locally-typed userIds (block-by-id field, filter allowlists, room invites) need shape validation (64-hex) at the boundary. Deliverable: a canonical hostile-strings test corpus (null-likes, injections, unicode abuse, oversize) reused across suites. |
| O109 | **`[TODO/CODE]`** Windowed chunk requests (never eager-materialize the missing set)** | `TransferAssembler.armWatchdog` → `Chunker.missingIndices(received, totalChunks)` computes `(0 until totalChunks).filter{…}` — it materializes the *entire* missing set at once, so a lying (or merely huge) `totalChunks` builds a giant list + giant NACK frame → OOM. Fix: track/request missing chunks in a bounded sliding window (e.g. W=1024) — NACK the gaps in `[0,W)`, advance to `[W,2W)` only once the current window is satisfied or times out. Memory becomes `O(W)` not `O(totalChunks)`, the NACK frame is always small, and it paces the transfer with natural backpressure (same principle as paginating a query instead of `SELECT *` into RAM). This is the structural half of the O108 OOM defense and makes the metadata's claimed size stop mattering for receiver safety — you only ever act on one window's worth. Bundle with O108; also the substrate O100 swarm/multi-source needs (windowed requests generalize cleanly to per-source windows). |
| O115 | **`[TODO/CODE]`** Identity lock lifecycle — lock() must exist as a feature and actually zero (2026-07-18 audit §2.2 + round 3) | `IdentityManager.lock()` only nulls the StateFlow — `privateKeyBytes` is dropped by reference, never `.fill(0)`'d (contradicting `Identity.kt`'s own doc), and lock() has ZERO callers (no Settings action, no auto-lock, no timeout): once unlocked, the Ed25519 private sits in heap for the process lifetime. `unlock()`/`changePassphrase()` share the gap on the key bytes they replace. Passphrase side: `PBEKeySpec.clearPassword()` never called after PBKDF2 (round-3 confirmed), iterations 100k vs OWASP ~600k for PBKDF2-HMAC-SHA256, and the passphrase rides an unclearable JVM String end-to-end (full fix threads CharArray from the entry UI; clearPassword alone is a cheap real partial). Deliverables: zeroize on lock/unlock/changePassphrase, a Settings lock action (+ optional auto-lock), clearPassword, iteration bump (needs a re-wrap of stored identity files on next unlock). Same surface O20/O44 later hardens with Keystore — this is the pre-Keystore floor. |
| O116 | **`[TODO/CODE]`** Room migration chain — pre-first-release gate (2026-07-18 audit §2.4) | Schema went v1→v9 with ZERO `Migration` objects; `fallbackToDestructiveMigration()` is correctly DEBUG-only, so the first release build that hits a schema bump throws on DB open = total data loss on exactly the long-offline anchor devices O55 designs for. `room.schemaLocation` KSP arg is also unset, so the schema JSONs needed to author real migrations aren't being exported at all. Deliverable: set schemaLocation + commit schemas now, write real migrations from the first tagged release forward (back-chaining v5→v9 optional — no shipped users yet). 100%-certain-to-fire once a release exists. |
| O117 | **`[TODO/CODE]`** Adversarial resource-hardening residue (2026-07-18 audit, sync section) | Same class as O13/O16/O108: (a) `BloomFilterData` deserialization never pre-clamps `expectedItems` — a peer can pick a value large enough for heavy allocation/GC pressure without OOMing (the crash path is caught per G26; the slow-DoS path isn't); (b) RBSR has a 4MB per-frame cap but no cumulative per-session ceiling — 12 near-max rounds accrete tens of MB in `peerHas`/`peerNeeds`. Both are cheap ceilings at existing validation sites. |

#### Tier 3 — plugin platform chain (ordered internally; ALL gated behind O20/O44)

| # | Item | Notes |
|---|------|-------|
| O15 | **`[TODO/CODE]`** Runtime plugin loading (DEX loader)** | Current plugins (Meshtastic, MeshCore) are compiled into the APK — not loadable at runtime. For a true mod scene, plugins need to be droppable files. Two mechanisms that compose: (1) **DEX loader**: a `.rumor` bundle (zip of `.dex` + a manifest) loaded via `DexClassLoader` at runtime — the mod-scene primary path, distributable via flash drive or chunked transfer over the mesh. (2) **Bound-service APK**: a separately installed APK exposes a bound service; Rumor binds to it by intent — good for plugins that need their own Android permissions (camera, contacts, location) since those are scoped to the plugin APK, not Rumor's process. Both paths feed into the same `PluginContext` interface. The DEX path runs in Rumor's process with Rumor's permissions; the bound-service path runs isolated. No central repository; the mesh itself is the distribution channel. **Pre-ship requirement**: a static manifest declaration in the plugin bundle listing which `PluginContext` APIs it calls, shown to the user as a disclosure popup before enable — not a security control (a malicious plugin can lie), but informed consent. |
| O24 | **`[TODO/CODE]`** Plugin security tiers: DEX (read-only) vs APK (dangerous)** | DEX `.rumor` bundles run in Rumor's process and are limited to read-only / display-layer `PluginContext` APIs (observe incoming, register display filters, open settings panel). Any plugin declaring dangerous capabilities (compose outbound messages, mutate contacts, register DM envelopes, open network connections) must ship as a bound-service APK running in its own isolated Android process with its own Android permission grants. Loader enforces this: DEX plugins requesting dangerous APIs are rejected at load time. User sees "this plugin requires installing a companion app" for APK-tier, which is natural friction proportional to actual risk. Manifest capability list re-disclosed on plugin update if capabilities changed. |
| O23 | **`[TODO/CODE]`** User-defined storage quota and plugin storage scopes** | Before first message, setup flow asks user to allocate a storage budget (e.g. "Reserve 2GB for Rumor"). All repos (messages, chunks, transfers, plugin data) share this budget. Plugins receive a scoped storage allocation within the global budget; if a plugin needs more than its default slice it requests an increase via a system prompt the user approves — same pattern as Android runtime permissions. File access for plugins follows the Storage Access Framework picker model (same as Signal): plugin declares which file types it needs, user grants access to specific files or folders, not blanket storage. |
| O25 | **`[TODO/CODE]`** Plugin crash isolation: auto-disable on unhandled exception** | All calls into plugin code (every `PluginRegistry.dispatch*` and every `PluginContext` callback) are wrapped in try/catch. On unhandled exception: log the stack trace, disable the plugin for the session, surface a notification "Plugin X crashed and was disabled." Do not propagate the exception to the host app. Persistent crash (disabled across restarts) if it crashes on init. Plugin author can subscribe to crash reports via a declared crash-report endpoint in the manifest. **Audit note (2026-07-15):** dispatch calls into plugins are already try/catch-wrapped with logging (`app/…/plugin/PluginRegistry.kt`); the missing pieces are the auto-disable (session + persistent-on-init-crash) and the user-facing crash notification. |
| O26 | **`[TODO/CODE]`** Community attestation and peer plugin sharing** | No central plugin repository. Plugin publishers broadcast a signed metadata record on the mesh (`pluginId`, `version`, name, description, capability list, `SHA-256(bundle)`, size). Reviews are **separate** signed messages anchored to `(pluginId, bundleHash)` — they propagate as ordinary mesh content, not chunked with the bundle, so the bundle itself stays small. A "plugin directory" view aggregates: shows plugins seen on the mesh, queries reviews on demand. Acquiring a plugin from a direct peer: chunked transfer of the bundle bytes, addressed as DM to the requester. Acquiring a plugin you've heard about but no direct peer has: broadcast `PLUGIN_REQUEST(pluginId, version)`; anyone holding it replies with either the bundle (chunked DM) or a metadata response "I have v5 and v7, not v6 — want those instead?" Requester chooses. Versioning: every bundle carries `(pluginId, version)` in its manifest; old versions remain valid (no forced upgrades). Authors push new versions by broadcasting a new metadata record; users may subscribe to "notify me when new versions of plugin X appear." Reviews are pseudonymous (userId-signed) so a reviewer's track record across plugins is visible — a reviewer with consistently useful reviews becomes a trustable voice without any central authority. |
| O49 | **`[TODO/CODE]`** Reproducible-builds requirement for peer-distributed plugins** | F-Droid treats reproducibility as best-effort because they also do source review + host signing. Rumor has neither — the bundle hash is the only identifier reviewers anchor to, and without reproducibility a signed review (O26) describes a black box that no other client can verify. Mandatory for plugin bundles: manifest declares source URL + build command; clients can optionally verify-by-building on install if a build environment is available; community attestation reviewers MUST verify before signing. Without this, the peer-review system in O26 is theatre. |
| O50 | **`[TODO/CODE]`** N-of-M co-signed updates for high-capability (APK-tier) plugins** | npm event-stream / Obsidian silent-update / VS Code `ahban.cychelloworld` post-mortems converge on this: initial review doesn't catch a popular plugin's subsequent malicious update. Single mitigation that has worked in the wild is Bitcoin-Core-style mandatory N-of-M signed releases. For Rumor's APK-tier plugins (outbound messaging, contact mutation, DM-envelope registration, network access), require updates to be co-signed by ≥2 maintainer keys declared in the original manifest. Single compromised key cannot ship a malicious update. Real friction proportional to capability — DEX-tier (read-only display) plugins keep single-sig updates. |
| O81 | **`[TODO/CODE]`** Local-only on-device image content classifiers — NSFW (still + video), gore (video; still TBD) as shipped-default LOCAL_ONLY plugins** | **Image-content companion to O67 (text-pattern filter).** Two surfaces covered separately — NSFW (sexual content) and gore (graphic violence) — each its own classifier, each its own user-facing WARN/BLOCK control. **Shipped out of the box** as LOCAL_ONLY plugins (O24): cannot reach the network, cannot register a DM envelope, cannot compose outbound messages — loader denies. Cannot leak the user's images anywhere by construction. Runs on every inbound IMAGE-type message before display; sets per-message `contentWarning: { nsfw, gore }` flags the UI consumes. **WARN vs BLOCK are orthogonal user controls:** WARN = blurred-thumbnail-with-tap-to-reveal plus label ("contains sexual content" / "contains graphic violence"); BLOCK = never display the image, drop the attachment locally. Default-WARN for both at first install; promotable to BLOCK per-surface during onboarding. **Stacking / strictness slider:** user can enable multiple classifiers per surface and combine with `any-flags ⇒ flag` (OR) or `weighted-sum ⇒ flag if score > threshold`. UI slider: `off / high-confidence-only / moderate / anything-flagged`. Default: lightest model only (cheapest APK cost); user opts into stacking heavier/stricter via slider. **Per-surface model status (research per `docs/RESEARCH_NOTES.md` §8):** **NSFW stills:** (a) **AdamCodd/vit-base-nsfw-detector** (HuggingFace, Apache 2.0) — ViT-based, ONNX export available, more accurate than Open NSFW, recommended default. (b) **Yahoo Open NSFW** via `tensorflow-open_nsfw` → TFLite (BSD 3-Clause), ~22 MB, model from 2016, acceptable as a stackable second opinion. (c) **NudeNet v3** ONNX (Apache 2.0), granular body-part detection, best accuracy, requires Kotlin/ONNX-runtime port. **NSFW video:** MobileNet-TSM single-frame mode on key frames as a stopgap. **Gore stills: research gap — no production-ready open-source classifier verified.** Three fallbacks until a real still-gore model emerges: (i) MobileNet-TSM single-frame inference (degraded accuracy), (ii) Roboflow community-trained models with disclosed accuracy caveats, (iii) text keyword filter via O67 as the only ship-able default. Ship gore-still as "WARN-via-keywords-only until classifier matures" with honest disclosure. **Gore video:** **MobileNet-TSM** (PMC9621415, paper-bundled GitHub source + trained model + Android demo), ~8.5 MB, real-time on-device, uses temporal frames. **Recommended starting bundle:** AdamCodd ViT (NSFW stills) + MobileNet-TSM (NSFW + gore video) + O67 keyword filter (text + gore-still fallback). Combined ~30 MB. **Model distribution is post-install P2P, not bundled in the APK** — keeps install size slim and lets users opt into specific classifiers. On first launch (or first inbound image), prompt user to download whichever classifiers they want. Models are signed blobs (publisher pubkey + version + SHA-256 + capability declaration). Once one peer has a model, others request it over the mesh via the same chunked-transfer path as O26 plugin sharing; the mesh itself is the distribution channel. Cold-start fetch via plain HTTPS from a published mirror (model author's site, IPFS, GitHub releases) is the only network exception, and only on user-initiated install — after that, mesh re-distribution covers updates. Model verification: same signing infrastructure as plugins (O26 community attestation), with publisher key pinning on first install. Bundled APK installs zero classifiers by default; user explicitly opts in. **Explicit non-decisions:** (1) **Never** Google SafetyCore (system-service, Play-Services-adjacent, breaks F-Droid posture per O56). (2) **Never** Firebase AutoML even though `nipunru/nsfw-detector-android` uses it — same posture violation. (3) **Never** any cloud-API fallback. **Update model on plugin update only** (no runtime download — that would be a network leak). **Pre-ship requirements:** model card published per classifier (training data sources, known biases incl. demographic error-rate gaps — Open NSFW has documented issues with darker skin tones; surface in onboarding copy honestly); on-device-only enforcement at LOCAL_ONLY plugin tier verified by loader; capability disclosure popup before enable (O15). Cross-references O24 (LOCAL_ONLY plugin tier — canonical use case), O67 (text-pattern sibling and the only stills-gore filter until a model matures), O68 (combined App Store 1.2 posture), O56 (no Google system service dependency). **Sources:** [tensorflow-open_nsfw](https://github.com/mdietrichstein/tensorflow-open_nsfw), [AdamCodd/vit-base-nsfw-detector](https://huggingface.co/AdamCodd/vit-base-nsfw-detector), [MobileNet-TSM violence detection (PMC9621415)](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC9621415/), [SafetyCore risks (arXiv 2509.06371)](https://arxiv.org/pdf/2509.06371). |
| O88 | **`[TODO/UI]`** In-thread plugin display widget hook** | A `@Composable PluginDisplay(message: RumorMessage)?` declared by plugins that want to render their own MessageType inline in `ThreadScreen`. Falls back to default rendering (plain `[binary payload]` or hex-dump) if no plugin claims the type. Required for any payment/attestation companion that wants to show structured content ("$5 sent", "credential verified") inside the chat thread rather than driving the user out to a separate app. Lower priority than O87 because the companion can ship without it (separate-app UX is the v1 fallback). Filed by the O84 audit. |
| O123 | **`[TODO/CODE]`** Plugin disable-boundary race + auto-disable remainder (2026-07-18 audit round 3; extends O25) | `PluginRegistry.plugins` is a `CopyOnWriteArrayList`: an iterator snapshot in `onMessageReceived` can still invoke a plugin that `unregister()` concurrently tore down (scope cancelled, `onDetach()` run). Bounded blast radius — the per-plugin `try/catch` swallows a post-teardown throw — but the object is called on torn-down state. Fix alongside O25's auto-disable work (a generation/closed flag checked inside the dispatch loop). |

#### Tier 4 — protocol/feature work

| # | Item | Notes |
|---|------|-------|
| O31 | **`[PART]`** Route advertisements in HELLO** — **wire shape + sig domain v2 landed; per-handshake negotiation + sender population open.** `Hello.recentlyExchangedWith: List<String> = emptyList()` field added. `helloChallengeBytesV2(nonce, ver, maxVer, supportedFeatures, recentlyExchangedWith)` signs the new field under the `rumor-hello-v2:` domain tag — wire-format-incompatible with v1 by construction so a peer can't accidentally accept a v1 sig as a v2 sig. `GossipSession.ROUTE_ADV_FEATURE = "route-adv-v1"` constant defined but DELIBERATELY out of `LOCAL_SUPPORTED_FEATURES`. 5 tests in `HelloChallengeV2Test` pin the byte format (domain tag, sort determinism, empty vs populated). **Not done:** per-handshake feature negotiation (BOTH peers must advertise `route-adv-v1` before either side signs v2 bytes; receivers parse the peer's Hello.supportedFeatures to choose between helloChallengeBytes v1 and v2 when verifying); sender-side population of `recentlyExchangedWith` (needs a "recent exchanges" tracker — TopologyTracker already has session counts; need a top-N projection); per-contact opt-out toggle ("don't list me in your route advertisements"); default-on for priority peers per O58 Tier 3 framing; recipient consumes for O29 scoring (breadcrumb cache already does this for inbound messages — extend with HELLO-derived "potential paths"). Original spec retained: Each peer includes a small `recentlyExchangedWith: List<UserId>` (top-N, e.g. 20) in HELLO. Cheap (~640 bytes), lets neighbours immediately know if a path through this peer is plausible. Goes inside the signed HELLO transcript so it can't be spoofed by MITM. Recipient uses for O29 scoring. Default-on for users explicitly marked as priority-peer (they've already opted into being routing nodes); default-off for everyone else. Per-contact opt-out ("don't list me in your route advertisements") for users who don't want this leak.  Wire + v2 sig-domain ported in the merge (dormant — not in LOCAL_SUPPORTED_FEATURES). |
| O79 | **`[PART]`** Rooms — unified open / invite / password / closed-membership shape (subsumes O52 group chats; allowed by default per O36)** — **wire vocabulary substrate landed; threat model + design decisions recorded; multi-recipient envelope architecture chosen.** **Encryption architecture decision (recorded):** two room modes only — **OPEN** (plaintext signed broadcasts, addressed to a routing tag; no encryption since the room is publicly readable by definition) and **ENCRYPTED** (multi-recipient envelope: one AES-encrypted message body + N small per-recipient key wraps, where N = current member count from the events-derived membership). **No shared room key, no ratchet.** Member kick = omit from the next envelope's recipient list. Per-message forward secrecy is automatic (each envelope uses fresh ephemerals). Wire bandwidth per message ≈ `content + 48 × N` bytes. At 50 members: ~2.6 KB per 256-byte message; at 1000 members: ~48 KB. Sender CPU is O(N) key wraps per send — noticeable but tolerable at room sizes Rumor's mesh deployment actually targets. PASSWORD mode is folded into ENCRYPTED via password-derived membership token rather than a distinct shared-key path. **What this collapses:** O89 ratchet design problem evaporates (no shared key to rotate); member-key rotation cascade evaporates; "catch-up after missing rotation" flow evaporates. O89 reduces to just write-enforcement via posting certificates. **Routing decision (recorded):** per-message HMAC tag `T_msg = HMAC(roomKey-or-roomId-derived-key, messageId)` carried on the wire instead of plaintext `roomId`. Observers cannot enumerate roomIds or cluster messages by Room. Cost: relays can't preferentially forward to known-room-subscribers. Acceptable given O76 compression bandwidth headroom. **Membership decision (recorded):** events-derived (RoomCreate + cumulative invites/joins/leaves/bans replayed) with an in-memory projection cache for O(1) lookups. Source of truth is the signed event stream; cache is the projection. **Channels-within-rooms decision (recorded):** `payload.channel: String?` metadata field on room messages. UI filters by channel; protocol carries the field unchanged. Channel WRITE permissions are deferred to O89 (much smaller now). **Threat model recorded:** `docs/ROOMS_THREAT_MODEL.md` walks each defense layer and the structural-enforcement rule (no honor-system policies — every security-affecting policy is cryptographic or it isn't enforced). **Wire vocabulary substrate landed.** `core/model/Room.kt` defines: `RoomMembershipPolicy.{OPEN, INVITE, PASSWORD, CLOSED}` enum; `RoomPostingPolicy.{MEMBER_ONLY, ANYONE_WITH_MOD_REMOVAL, MOD_APPROVED}` enum; `RoomCreate(roomId, name, createdBy, createdAtMs, membershipPolicy, postingPolicy, allowMedia, signature)` for the creation envelope; `RoomInvite(roomId, invitedUserId, encryptedRoomKey, expiresAtMs, moderatorId, moderatorSignature)` for INVITE/CLOSED room joining (NB: `encryptedRoomKey` slot is reserved but the multi-recipient design means it carries an initial-membership-seed token rather than a shared room key); `RoomAction(roomId, kind, target, reason, moderatorId, takenAtMs, moderatorSignature)` for moderator actions with `RoomActionKind.{REMOVE_MESSAGE, KICK_USER, BAN_USER, UNBAN_USER}`. Three domain-tagged signable-bytes helpers: `rumor-room-create-v1:`, `rumor-room-invite-v1:`, `rumor-room-action-v1:` — separation prevents sig from one struct being lifted to another. `RoomTest` (5) covers JSON round-trip for all three types + signable-bytes uniqueness + domain-tag separation. O28 fuzz harnesses added for all three. **Done this session (autonomous):** `MultiRecipientEnvelope` + `KeyWrap` wire types (`core/model/`); `MultiRecipientEnvelopeCodec.encrypt` / `.decrypt` pure functions with O39 zeroing; `HmacSha256` (RFC 4231 vectors) and `HkdfSha256` (RFC 5869 vectors) primitives over the existing `Sha256` shim; `RoomRoutingTag.openRoomTag` (SHA-256 prefix) and `.encryptedRoomTag` (per-message HMAC); `RoomTagMatcher` for receiver-side subscription matching; `MessageType.ROOM_MESSAGE` + `trafficClass` content-driven branch + `GossipEngine` relay + `clampTtl` ceiling. **Wire integration shipped:** `GossipEngine.composeRoomMessage(routingTag, plaintext, recipients, replyTo?, mentions?)` (caller provides recipient list — empty list = OPEN-mode signed plaintext, non-empty = ENCRYPTED via codec); `GossipEngine.handleRoomMessage` receive branch (RoomTagMatcher + codec.decrypt on match, emit to inbox; relay propagates regardless); `GossipEngine.RoomSubscriptionProvider` interface for receive-side dispatch contract; no-op provider singleton wired in AppModule (production today = relay-only; future commit swaps in the real provider). `RoomSubscriptionRepository` interface + validating `RoomSubscription` data class. `_ext.rt` accessor + `withRoomRoutingTag` copy helper. End-to-end integration test (`RoomEndToEndIntegrationTest`, 5) exercises compose → wire → match → decrypt for both OPEN and ENCRYPTED. **60+ new tests across the O79 surface.** **Persistence shipped:** Android `RoomSubscriptionRepositoryAdapter` + `RoomSubscriptionEntity` + `RoomSubscriptionDao` + Room schema bump v8 → v9 + AppModule binding + real (non-no-op) `GossipEngine.RoomSubscriptionProvider` wired to the repo. Simulator stub `InMemoryRoomSubscriptionRepository`; `SimNode` wires its own provider so simulator scenarios can subscribe nodes to rooms. A user joining a room now has their subscription persist across restarts; inbound messages get matched and dispatched. **Simulator scenario (`RoomMessageScenarioTest`, 3 tests)** verifies the full relay+receive cycle across a 4-node mesh: every node relays room messages regardless of subscription state; subscribed nodes emit to inbox; non-subscribed nodes do not. The scenario caught a real bug in the autonomous-overnight wiring (unconditional `emitToInbox` after the type handlers was making non-subscribed nodes surface room messages locally — fixed by gating the unconditional emit on `msg.type != ROOM_MESSAGE` so the subscription-aware `handleRoomMessage` is the only path that emits room messages to inbox). **Not done:** Ed25519 → X25519 static key derivation for the receiver-side decrypt step (the X25519 public is what the sender wraps to; if Rumor identity is Ed25519-only, the conversion via Curve25519 birational map needs to be wired — until then, `RoomSubscriptionProvider.localX25519StaticPrivate()` returns null and ENCRYPTED room messages get matched at the tag layer but the decrypt is skipped, stored as opaque envelopes). OPEN rooms work end-to-end today. **Other not-done:** events-derived membership projection cache (needs for sender-side recipient enumeration at composeRoomMessage call time); UI types (member roster, "Alice has joined" events); moderator key rotation (manual-confirm-by-Room-members flow); conflict resolution on contradictory mod actions; Room forks; author edit/delete policy. The substrate is enough to fuzz the wire surface, pin the SerialName strings + domain tags in the never-reuse policy, and exercise end-to-end encrypt/decrypt round-trips in unit tests AND through the GossipEngine receive branch. `core/model/Room.kt` defines: `RoomMembershipPolicy.{OPEN, INVITE, PASSWORD, CLOSED}` enum; `RoomPostingPolicy.{MEMBER_ONLY, ANYONE_WITH_MOD_REMOVAL, MOD_APPROVED}` enum; `RoomCreate(roomId, name, createdBy, createdAtMs, membershipPolicy, postingPolicy, allowMedia, signature)` for the creation envelope; `RoomInvite(roomId, invitedUserId, encryptedRoomKey, expiresAtMs, moderatorId, moderatorSignature)` for INVITE/CLOSED room joining; `RoomAction(roomId, kind, target, reason, moderatorId, takenAtMs, moderatorSignature)` for moderator actions with `RoomActionKind.{REMOVE_MESSAGE, KICK_USER, BAN_USER, UNBAN_USER}`. Three domain-tagged signable-bytes helpers: `rumor-room-create-v1:`, `rumor-room-invite-v1:`, `rumor-room-action-v1:` — separation prevents sig from one struct being lifted to another. `RoomTest` (5) covers JSON round-trip for all three types + signable-bytes uniqueness + domain-tag separation. O28 fuzz harnesses added for all three. **Not done (intentional — depends on open design questions):** `MessageType.ROOM_*` enum entries + GossipEngine routing (depends on whether non-subscribers see roomId in clear or via O53-style tags); per-Room repositories (membership state — derivable from gossiped events vs separate persisted table); CLOSED-membership key-DM crypto (depends on O52 / O53); UI types (member roster, "Alice has joined" events); moderator key rotation (manual-confirm-by-Room-members flow per the open-question list); conflict resolution on contradictory mod actions; Room forks; author edit/delete policy. The substrate is enough to fuzz the wire surface and pin the SerialName strings in the never-reuse policy. Original spec (preserved): One protocol primitive serves the full spectrum from open public chats to closed-membership groups — the difference is membership policy and per-message crypto. **Wire shape (one schema, four membership modes):** room creation signed by creator (`roomId = SHA-256(name || createdBy || createdAtMs)`); membership policy `{ open | invite | password | closed }`; posting policy `{ member_only | mod_approved | anyone_with_mod_removal }`. **Open**: anyone can join by roomId; messages are plaintext. **Invite**: joining requires a signed `RoomInvite { roomId, invitedUserId, roomKey, expiresAtMs, modSig }` DM from a moderator carrying the room's symmetric key. **Password**: room key derived from `Argon2id(password, roomId-as-salt)`; anyone who knows the password can derive K; supports printed-on-flyer / shared-by-voice deployment. **Closed**: explicit member list, K distributed per-member via signed key DMs (per O52 crypto). All modes addressed-to-roomId; **non-subscribers receive nothing for that roomId** (relay drops; not just UI filter). **Media attachments policy is the Room creator's call** — when someone creates a Room they decide whether to allow image/voice/video/file attachments. The only hard exception is the **global Room** (the singular shared roomId, if exposed): it has no creator and cannot be moderated at scale, so media is off there at the protocol layer (see O36). Every other Room inherits whatever its creator picked at Room-creation time. **Moderators:** room creator is mod-by-default; can grant additional mod keys signed by themselves; mods can grant other mods (transitive but each grant signed and revocable). Alternative path: member-elected mods via signed votes, top-N become mods, configurable quorum / rotation cadence. **Moderation actions:** signed `RoomAction { roomId, target, action: REMOVE_MESSAGE | KICK_USER | BAN_USER, reason, modSig }`. Peers honour the action if `modSig` verifies against the current mod set. Anti-abuse integration: Room can subscribe to O67 filters as policy; Room can publish its own block list (sibling of personal blocklist, signed by Room mods) so banned users' content is dropped at every member's client. **@-mention notifications** become a first-class wire surface: new `MessageType.MENTION` with `mentionedUserId`, INFRASTRUCTURE-tier, surfaces in the mentioned user's notification feed (covers "@ a mod" UX for moderated Rooms, plus general DM @-mentions, plus O68's report-to-mod delivery). **UX-only differences between "Room as group chat" and "Room as public space":** member-list visibility (group chat shows roster; open Room may not); join/leave events (group chat surfaces them; open Room may not); thread vs feed presentation. These are display-layer choices, not protocol-layer distinctions. Per-Room creator-mod or elected-mod with explicit remove/kick/ban actions is the content-moderation primitive that scales without a central authority — each Room's creator (or elected mods) is the human-in-the-loop for that Room. Every user can leave or join any Room at will; the global Room is text-only because it cannot have a creator-mod. **Open design questions:** moderator key rotation when a mod leaves / is compromised (NOT borrowing O41/G9 shape since that's deprecated — design a manual-confirm-by-Room-members flow); conflict resolution when two mods take contradictory actions (lean: last-action-wins with public audit log); Room forks when policy is contested (any user can fork; same membership-list seed, new roomId); author edit / delete within Rooms (covered by O40 relay-deletion + new "allow author delete" Room-policy bit). **Status:** Significant design work; one of the larger backlog rows.  **Membership guardrail (session-B, binding for the remaining work):** group membership is an authored, signed, per-room append-only op-log with a trivial merge — strictly separate from message ordering, NEVER state-resolved from the message DAG. This is what inoculates Rooms against Matrix's state-resolution failure class. Subsumes O52. |
| O89 | **`[TODO/CODE]`** O79 channel write permissions — posting certificates only** — **collapsed from earlier scope.** Originally specified posting certificates AND per-channel symmetric keys AND a ratchet rotation scheme. The O79 multi-recipient envelope decision means there is no shared channel key to ratchet; read permissions are inherent to recipient list membership. **Remaining scope (write enforcement only):** posting certificates — a mod signs `Cert(userId, room, channel, expiresAt)`; posters attach to messages; receivers verify cert mod-signature + message signed by userId named in cert + cert not expired. Short expiry (default 24h) with auto-renew for active members; non-renewed certs expire naturally for "soft kick." Explicit revocation: signed `RoomAction.REVOKE_CERT` for immediate-effect kicks. Structural enforcement (no honor-system fallback — see `docs/ROOMS_THREAT_MODEL.md`). Read enforcement falls out of O79 automatically: a modified client without being on the envelope's recipient list has nothing to decrypt because their key wrap is absent. Cross-references O79 (parent row, multi-recipient envelope decision), `docs/ROOMS_THREAT_MODEL.md`. |
| O67 | **`[PART]`** Keyword filters as signed shareable lists** — **wire layer + Android persistence + DI shipped; UI open. Default lists: explicitly empty at first launch (design decision).** **Default-lists decision:** ship with NO bundled lists. Users opt in to community-published lists or author their own. Project signing key for any future bundled lists is governed by `docs/PROJECT_KEYS.md` — generated only when there's a concrete first list to sign; the reserved kind-id is `rumor-project-default-lists-v1`. Pieces shipped: model (KeywordFilterList signed wire type + FilterEntry + FilterAction.{WARN,BLOCK} + MatchKind.{SUBSTRING_CI,SUBSTRING_CS,WORD_CI} + FilterSubscription); matcher (per-render hot path, BLOCK>WARN, publisher+subscriber allowlists compose, disabled subs skipped); publisher (signs with local Ed25519, monotonic version); verifier (publisherId re-derived from supplied pubkey, Ed25519 verify); subscriber (subscribe/unsubscribe/applyList with monotonic version + ONE_TIME gates, persists publisherKey + lastAppliedVersion inside FilterSubscription); KeywordFilterListRepository + FilterSubscriptionRepository commonMain interfaces; gossip bridge wired into MessageType.KEYWORD_FILTER_PUBLISH (TrafficClass.TRANSFER_SETUP, MAX_BROADCAST_HOPS ceiling, relayed like other infrastructure broadcasts). **Android Room adapters** in `:app` use a JSON-blob storage shape (publisherId PK + json TEXT) — entries / actionOverrides / allowlists are not Room-native types so per-row JSON beats a child-table refactor for two repos we never query inside; rationale documented in `KeywordFilterEntities.kt`. Adapters log+null on deserialize failure (mirror of BlocklistVerifier's "verify-fails-returns-null" pattern). Schema bumped v5 → v6. DI wired in AppModule. **Simulator in-memory stubs** in `InMemoryRepos.kt`. 30 tests total on the surface (12 matcher + 8 publisher/verifier + 10 subscriber). The G6 Koin DI verify test caught a real wiring issue with a default-valued lambda param on the gossip bridge mid-batch — dropped the `onListApplied` hook entirely (UI re-reads `subscribedListsForMatcher()` per render anyway); documented inline that a future hook should be a SharedFlow, not a constructor lambda. Domain tag `rumor-keyword-filter-v1:`. **Not done:** UI list editor (compose/edit/sign/share); built-in default lists (slurs / NSFW text / gore-keyword fallback — content sourcing TBD); auto-intake-by-publisher toggle; long-press "always show from this sender" exception UI; diff variant if real-world list churn justifies it; gossip-bridge tests with a real GossipEngine fixture. Original spec retained:** (text content only — image surfaces are O81; separate data type from blocklists, shared distribution infra only) | **Keyword filter lists are NOT blocklists.** They are a distinct data type and a distinct user-facing surface. They share *only* the signing / sign-verify / publish / subscribe / diff distribution machinery from `BlocklistVerifier`; the schema, semantics, UI, and user model are all separate. Don't conflate them in code or in product copy. Filter entry shape: `{pattern: String, action: BLOCK | WARN, warnLabel: String?}`. Multiple subscribed lists merge; strictest applicable label wins for any matched message. **Two actions, orthogonal to the keyword set:** **WARN** shows the message with a warning banner / collapsed-by-default ("before you tap, this message contains/may contain X"); **BLOCK** prevents the message from being rendered in the UI at all (still relayed normally per the relay-path-never-touches-blocklist rule, just not displayed locally). Ships with built-in default lists: (a) **slurs** (English first — localisation gap flagged honestly), default BLOCK; (b) **NSFW text keywords** (sexual content), default WARN; (c) **graphic violence / gore text keywords**, default WARN. User can promote WARN → BLOCK or demote BLOCK → WARN per list during onboarding and any time after. Custom lists subscribable. **Per-userId filter exception (important for community use):** every list supports `userIdAllowlist: Set<String>` so the user can specify "don't apply this filter to messages from this contact." Communities reclaiming slurs internally need this; misclassified senders need a single-tap "filter exception" path; recovery from a bad subscribed-list ban-wave needs this. UI surface: long-press a filtered message → "always show messages from this sender." **Auto-intake by publisher (power-mod feature):** mark a publisher's pubkey as "auto-apply everything they sign." When a new filter list signed by that publisher arrives over gossip, it's pulled in without prompt. Useful for community moderators / curators who maintain canonical lists. Opt-in per publisher; disclosed at toggle time as "you are delegating filter decisions to <publisher>." **Honest leak note:** subscribing to a list means the gossip layer sees you holding their signed snapshot; other peers can ask you for it. That advertises which lists you stand by. Two mitigations: (a) accept as design tradeoff (these are public commitments anyway), (b) per-list "private mode" where you hold the list but refuse to gossip it. **Distinction matters:** the four content surfaces are (a) text keywords [O67], (b) image NSFW [O81], (c) image gore [O81 — model gap noted, ships text-keyword-only at first], (d) sender identity [BlockManager / O78]. Each carries an independent WARN/BLOCK control. Don't conflate them in UI. Required for App Store 1.2 user-generated-content posture; useful on Android too.  Ported in the merge; UI open. |
| O76 | **`[PART]`** Per-payload compression + 6-bucket padding (text only), UI-transparent chunking above ceiling — **compose-side flip landed; capability cache wired; chunker fallback >64KB remains open.** **compression-v1 is now in `LOCAL_SUPPORTED_FEATURES`** and always-on for native-envelope DM compose: `GossipEngine.composeDirect` runs `encodeForWire` for every TEXT plaintext, AES-GCMs with `compressionAad(originalLength)` as AAD, and stamps `_ext.c/cb/cl` via `withCompressionMetadata`. Receiver-side decode landed earlier (`ThreadViewModel.decryptPayload` in `59995d2`). **Capability cache (per O76 design decision):** `Contact.lastKnownSupportedFeatures: String` (JSON-encoded list, "" when unknown); Room schema v7 → v8; `ContactRepository.setSupportedFeatures(userId, json)` interface method; Room adapter + simulator stub. `SessionResult.peerSupportedFeatures` carries the value out of `GossipSession`; `WifiDirectTransport` forwards to `PeerExchangeResult.peerSupportedFeatures`; `GossipEngine.onExchange` writes it into the contact row. For v1 specifically the cache is populated but unused (every Rumor build supports v1 universally — no shipped builds exist that don't); the infrastructure is in place so that v2 / future capability extensions can gate composeDirect on per-recipient lookups without further architectural work. **Wire format pipeline live end-to-end:** compose → encodeForWire → AEAD(plaintext, key, compressionAad) → wire → AEAD(ciphertext, key, compressionAad) → decodeFromWire → recipient plaintext. **Chunker fallback above 64 KB still open** — composeDirect currently falls back to uncompressed-and-unpadded for the rare >64KB compressed case (PaddingBuckets.MAX_SINGLE_MESSAGE exceeded). The proper fix per O76 spec is "splits into a TEXT-typed thread with sequence metadata; recipient's TextAssembler stitches them back into a single logical message before display" — multi-file work involving `Chunker` + new TextAssembler + UI-side reassembly. Filed as remaining open work on this row. AEAD AD: `PlatformCrypto.aesGcm{Encrypt,Decrypt}` accept `aad: ByteArray` end-to-end (JVM Cipher.updateAAD; iOS Swift bridge AES.GCM.seal/open `authenticating:`; spec doc updated). `CryptoManager.aesGcmEncrypt/Decrypt` default `aad = ByteArray(0)` so existing call sites are unchanged. Canonical AAD = `compressionAad(originalLength)` = `"rumor-o76:<decimal>"` bytes, pinned in `CryptoGoldenVectorsTest` and in `CompressedAeadRoundTripTest`. `_ext` field helpers (`isCompressed`, `compressionBucketIndex`, `compressionOriginalLength`, `withCompressionMetadata`) live in `core/wire/CompressedPaddedExt.kt`. **Receive path landed in `ThreadViewModel.decryptPayload`**: if `msg.isCompressed`, AEAD-decrypt with `compressionAad(cl)` as AAD; on success, `CompressedPaddedCodec.decodeFromWire` unpads + inflates. Tampered `cl` from a relay fails the tag check (verified by `CompressedAeadRoundTripTest`). `GossipSession.COMPRESSION_FEATURE = "compression-v1"` constant defined but DELIBERATELY out of `LOCAL_SUPPORTED_FEATURES`. **Compose-side flip still open:** `composeBroadcast` / `composeDirect` need to call `encodeForWire`, set `_ext` via `withCompressionMetadata`, and pass `compressionAad(originalLength)` to AES-GCM — gated on the recipient (or session peer) advertising `compression-v1` in HELLO, which today no peer does. Once the receive path is broadly deployed and the compose-side gate is wired, flipping `LOCAL_SUPPORTED_FEATURES = listOf(COMPRESSION_FEATURE)` enables the win. Chunker fallback above 64 KB is also still open. **Other open piece:** broadcast compression — current scope is DMs only (broadcasts have no AEAD layer to bind `cl` into; padding plaintext leaks `cl` to any observer; compress-only-without-pad has bandwidth merit but is a separate decision). Pre-O76 spec retained below for the open work. `core/wire/PaddingBuckets.kt` defines the six buckets (64 / 256 / 1024 / 4096 / 16384 / 65536) + `Pad.pad` + `Unpad.unpad`. Zero-fill (TLS 1.3 pattern, NOT random — the AEAD wrap is what gives indistinguishability; sensor-entropy "upgrades" rejected, rationale in file docstring). `core/platform/Compression.kt` is the expect/actual shim — raw RFC-1951 deflate (no zlib wrapper); JVM actual uses `java.util.zip.Deflater` / `Inflater` with `nowrap=true`; iOS actual stubs `NotImplementedError` pending libcompression cinterop (same xtool/Mac gate as the rest of the iOS port). Inflate respects `maxOutputBytes` (zip-bomb defense at the compression layer; the application layer covers file-format expansion via O14). `core/wire/CompressedPaddedCodec.kt` composes the two ends — `encodeForWire(plaintext) → Encoded(bytes, bucketIndex, originalLength)` and `decodeFromWire(bytes, originalLength, maxOutputBytes) → ByteArray?`. Pure, no IO, no state — caller passes the result into AES-GCM. **Tests (24 total for the O76 surface):** 9 `PaddingBucketsTest` (bucket boundaries, exact-fit, oversized→null, round-trip, tamper, ≤4× overhead bound), 9 `CompressionTest` (round-trip on text / empty / random / 64 KB; truncated and garbage rejected; zip-bomb cap; zero/negative cap rejected), 7 `CompressedPaddedCodecTest` (round-trip on chat / empty / redundant-shrinks / every bucket; truncated originalLength rejected; oversized originalLength rejected; receiver-cap honoured). **Not done (wire integration in `GossipEngine`):** `_ext` carries `compressed: Boolean` + `bucketIndex: Byte`; HELLO `supportedFeatures: ["compression-v1"]` capability negotiation; `composeBroadcast` / `composeDirect` invoke `encodeForWire` only when peer advertises capability AND payload is TEXT (skip BINARY/FILE per CRIME/BREACH); receiver runs `decodeFromWire` on inbound; chunker fallback above 64 KB with seamless TextAssembler stitch (no "fragment 3 of 5" UI). The `originalLength` field rides in AEAD-protected associated data so it can't be tampered with by a relay. Original spec retained below for the open work. — Original spec: Today every message goes on the wire as raw plaintext (broadcasts) or raw ciphertext (DMs), uncompressed. For text-heavy traffic (chat, status updates, broadcast feed) typical content compresses to 30–50%. Worth taking — but only for `MessagePayload` of TEXT contentType, never for BINARY/FILE which is already compressed (re-running gzip on JPEG burns CPU for no gain). Pipeline order is **compress → pad → encrypt → sign → chunk**: zstd or deflate the plaintext; pad with zeros up to the smallest bucket that fits in `{64 B, 256 B, 1 KB, 4 KB, 16 KB, 64 KB}`; AEAD over the padded blob; outer Ed25519 as today. Six buckets ≈ 2.6 bits length leak per message vs full byte-length of raw plaintext today. Worst-case overhead capped at ~4× (content just over half a bucket). Buckets chosen for the real content distribution: quick replies / acks fit in 64 B without 50× overhead; typical chat in 256 B; paragraphs in 1 KB; blog-length posts in 16 KB without auto-chunking; only true outliers above 64 KB chunk. Top bucket exceeds the 16 KB INFRA/REALTIME ceiling so anything above 16 KB demotes to BULK class anyway. **UI seamlessness — non-negotiable.** User composes a message of any length in one input box; compression + bucket selection are invisible. Compressed payloads up to 64 KB ride a single wire message. Above 64 KB the existing `Chunker` (file-transfer path) splits into a TEXT-typed thread with sequence metadata; recipient's TextAssembler stitches them back into a single logical message before display. The user types, the recipient reads — no UI ever exposes "your message was chunked" or "this is the 3rd of 5 fragments." Wire-format addition routes through O37's `_ext`: a new boolean `compressed: true` on the wrapper plus a bucket-id byte; absence means uncompressed (back-compat with peers that don't advertise the capability via HELLO `supportedFeatures: ["compression-v1"]`). **CRIME/BREACH constraint:** never share a compression context across messages, and never mix attacker-controlled bytes with secret bytes inside the same compression pass — current DM model is safe (sender controls 100% of plaintext) but any future group-chat envelope (O52) or any header that mixes a session token with user content must audit before turning compression on for that path. Padding bytes are zeros (TLS 1.3 pattern); the AEAD makes ciphertext-pad indistinguishable from anything else regardless of pre-encryption content — mixing in OS sensor entropy (gyro, temp, camera) is a footgun, not an upgrade: a CSPRNG cannot be made stronger by XORing low-entropy sources, and reading sensors adds permission prompts, battery drain, and a fingerprinting vector. `SecureRandom` (kernel-seeded from boot-time hardware entropy + interrupt jitter) is what every well-reviewed protocol uses; trust it. Honest framing: bandwidth win on text, no win on media (covered separately by O19 pre-send compression UX), modest fingerprinting reduction (6 buckets instead of every length distinguishable).  Ported in the merge (compose + decode paths live; COMPRESSION_FEATURE advertised). |
| O90 | **`[PART]`** Thread + mention metadata fields** — **substrate + compose-side shipped; UI consumption open.** `core/wire/ThreadAndMentionExt.kt` defines `_ext.replyTo: String?` (parent messageId — UI builds thread trees) and `_ext.mentions: List<String>?` (userIds explicitly mentioned — UI builds notification feeds + cross-room mention aggregators). Accessor properties + `withReplyTo` / `withMentions` copy helpers, mirroring the `withTtlSplit` / `withCompressionMetadata` pattern. Both unsigned (in `_ext`); a malicious sender can claim arbitrary parents or mentions but the impact is local-display-only — same impact class as a sender forging an `@username` in plaintext. No relay or routing implications. 11 tests in `ThreadAndMentionExtTest` pin field names (drift guard — names reserved forever in `docs/RENAMED_FIELDS_NEVER_REUSE.md`), wire format (mentions as `JsonArray<JsonPrimitive>`), round-trip / clear / preserve-other-fields, malformed-field-returns-null. **Compose-side wired:** `GossipEngine.composeBroadcast(text, replyTo, mentions)` and `composeDirect(recipientId, recipientPublicKey, text, replyTo, mentions)` accept the optional params with default-empty fallback (callers unchanged when they don't use the feature). For `composeDirect` the O90 metadata is applied after the O76 compression metadata so both coexist in `_ext` without trampling. **Not done:** UI consumption — `ThreadViewModel` / `ThreadScreen` build the thread tree from `replyTo` chains; mention aggregator plugin reads `mentions` to populate a notification feed. UI is the only remaining piece.  Ported in the merge; UI consumption open. |
| O72 | **`[TODO/CODE]`** Nostr relay fallback when internet is reachable** | Bitchat-style: when an internet connection appears (Wi-Fi, cellular), opportunistically forward messages to Nostr relays over Tor (or I2P), and ingest matching events back. Extends Rumor's mesh-only reach to cross-region delivery — mesh in city A talks to mesh in city B via Nostr-over-Tor without either mesh having continuous internet. Different threat model from the in-mesh case (relays are public, observers many), so it gets its own design pass before implementation. Concrete decisions owed: which Nostr event kinds Rumor uses (custom NIP?), how userId↔npub maps, whether Rumor messages are carried as encrypted `_ext` blobs inside otherwise-standard events. Tor integration via tor-android (Briar uses it) or Arti once mature. Cross-references O54 — Nostr is effectively a transport plugin. |
| O77 | **`[TODO/SIM]`** Massive long-uptime scenario (500 nodes, 24-hour sim, sustained churn) | Today's scenarios cap at 200 nodes / 120 s sim-time and never trigger storage eviction, bloom degradation at scale, or the kind of sustained-churn protocol exercise the O55 prolonged-disconnection design target requires. Predicted-but-unproven behaviors that only a massive sim can surface: (a) `MessageRepository.evictOldest` actually fires — does the eviction policy keep a coherent working set or churn the freshest content out? (b) Static bloom `falsePositiveRate` against per-node sets of 10–60k messages — does over-offering wreck bandwidth? (c) `MAX_MESSAGES=50000` per node × 500 nodes ≈ 12 GB worst-case heap — does the storage policy hold? (d) Multi-hour partition heal with churn during the partition — does anything converge? (e) Hub bottlenecks at scale — at what node count does hub-spoke fairness collapse under queue pressure? **Recommended scenario shape:** 500 nodes, static mesh topology with k=6 connections per node, 24 sim-hours, gentle steady-state traffic (1 broadcast/node/10 sim-min ≈ 72k broadcasts total), rolling partitions every ~30 sim-min healing 10 min later, breadcrumbs ON, RBSR OFF (until per-round cost optimization lands — G17). **Assertions:** heap stays under 4 GB; per-node message count stays under 60k after eviction kicks in; cross-partition delivery ratio stays >0.6; no node crashes; deterministic-replay within ±5% (now that O12 clock injection is in place, this should hold even at scale). **Operational requirements:** wall-clock cap raised from 240 s default to 1800 s (30 min); JVM `-Xmx16g`; bundle file will be ~70 MB (one per scenario); single scenario per run, not bulk sweep. Probably needs a dev box with ≥32 GB RAM to run comfortably. Deferred until a fresh hardware target exists — phone-class dev machines won't fit. When run, hand `result.json` summaries to a reviewer; the full trace is too big to read into chat context. Probably surfaces 1–3 real protocol issues invisible at 200-node scale — that's the point. |
| O78 | **`[TODO/CODE]`** Signed public block-reasons with ±1 attestations — visual rating only, no mechanical effect** | A user blocking someone may optionally publish a signed `BlockReason { blockedUserId, reasonText, reasonTags[], publishedAtMs }` (Ed25519 by the blocker), gossiped as INFRASTRUCTURE traffic. Other users browsing a contact see "blocked by N users — top reasons: spamming (38), harassment (12)" plus, more prominently, "**three of your regular contacts blocked this person**". **±1 attestations** are sibling signed messages anchored to `(blocker, blocked, reasonHash)` so each signer can only ±/− a given reason once; signature replay is impossible. **This is a UI signal, not a filter input.** Visual highlighting only — contacts' ±1s render larger / brighter / labeled "your contacts"; stranger ±1s render small / aggregated; blocked-by-you ±1s render hidden or greyed. **No mechanical filter behavior depends on counts**, so Sybil attacks against the count are irrelevant: a bad actor +1-flooding their own block does nothing except inflate a number the display already devalues for stranger sources. The earlier Sybil-mitigation analysis (graph-distance weighting, PoW, co-presence proof) is no longer needed because there's nothing to defend — the count is informational, not enforcement. **Opt-in per-block** — default unpublished; the publish toggle in the block UI surfaces the metadata-leak ("publishing this reason makes your identity visible to everyone seeing this contact"). **Account-deletion erasure:** O69's `IDENTITY_RETIRED` broadcast cascades to retract published BlockReasons + ±1s signed by the deleted userId; peers honour and remove from local view. Old key is the proof. **No quorum-of-third-parties erases someone else's content.** That remains a censorship primitive and explicitly off the table. Cross-references O67 (signed list machinery — same publish/sign/diff), O68 (combined posture for App Store 1.2), O79 (Rooms also have block lists), O27 (deanonymization — published reasons are an opt-in identity leak by design). |
| O30 | **`[PART]`** Online-presence beacons** | Short-TTL INFRASTRUCTURE messages "I saw X within last N minutes" gossiped at low frequency. Lets peers far from X learn coarse reachability without polling. Feeds O29's signal 11. Aggregated with recency decay. Privacy trade-off: leaks "X was recently online and visible to someone in this mesh segment" — same leak class as DM `recipientId` today. Should be capped (peer only beacons for contacts it has marked, not for every userId ever seen) and rate-limited so a malicious plugin can't flood. **Refined per O58 design (self-presence only):** drop the "I saw X" variant; the routing-decision value is small (presence doesn't tell you *which path* leads to X — breadcrumbs and bloom do), and beaconing someone else's presence without their consent is the heaviest leak in O58 Tier 3. Replace with a **self-presence beacon**: a Free-mode (or Static-mode) node says "I am running as infrastructure, I am online, here's my recent-exchange context" — same payload, sender opts into their own visibility, no third-party metadata. The UX use case ("is Bob online?") falls back to local-only: each node infers presence from its own recent inbound from Bob (already Tier 1). **Symmetric exit pulse required:** when a node leaves Static or Free mode (manual toggle off, unplug, screen-on transition), send a final "demoting myself" self-update so peers downgrade routing weight for this node immediately rather than waiting for stale signals to decay. Without the exit pulse, DMs continue routing through ghost anchors for the full presence-decay window — minutes to hours of detours through a node that's no longer reliable. |
| O124 | **`[TODO/CODE+UI]`** "Search for peers" must actually announce + solicit (filed 2026-07-18, user request) | Field-observed: the search-for-peers button doesn't refresh mesh-level visibility — a node invisible for lack of a first exchange (see O97 field note) stays invisible after tapping it. Design: button = (a) transport refresh (BLE scan/advertise restart, P2P discovery kick, LAN mDNS re-query — cheap, local) **plus** (b) an immediate SELF_PRESENCE pulse (the O30/G12 entry-pulse primitive, fired on demand) so the next exchange carries our announcement; and (c) **solicited response**: a peer receiving a fresh pulse from an unknown-or-stale peer replies with its own pulse, gated by a **responder-side per-peer cooldown** (local clock, e.g. ≥N min since last pulse to that peer) so a modified client spamming probes cannot force pingspam — the cooldown is enforced by the responder, not trusted from the wire (user-specified anti-abuse shape). No new message type: reuse SELF_PRESENCE; the "solicit" is implicit in receiving a pulse from someone whose beacon we haven't answered lately. Cross-refs O30 (pulse primitive), O97 (status surface showing "announced/pending"), O16 (rate-limit family). |
| O57 | **`[CLOSED 2026-07-17 → G39]`** Device modes: Mobile / Static / Free | Fully shipped across O62 (ModeEnvelope — what each mode does), G12/G37 (entry/exit self-presence pulses), and G39 (auto-trigger orchestrator, manual-override-wins, 4-way selector). Design intent preserved in those rows; the "no motion detection — user or plug state declares behaviour" principle stands. |
| O22 | **`[PART]`** Native delayed / recurring message scheduling** | Reduces pressure on plugins needing outbound-message capability. UI: compose a message, tap a clock icon, set send time or recurrence (hourly / daily / on next peer contact). Stored locally; `MeshService` fires them on schedule. Covers the primary legitimate use case (status bots, scheduled announcements) without requiring a plugin that declares outbound-message capability. **Merge note:** Room `ScheduledMessageDao/Entity/Adapter` + DI binding + `MessageScheduler` binding landed via archimedes — remainder is the `MeshService` polling loop + compose-screen UI (clock icon). |
| O17 | **`[PART]`** Extend bridged content beyond direct peers (`BRIDGE_VOUCHED` trust level)** | Today BRIDGED traffic is dropped on relay (see design decisions), so a bridge only reaches its direct Rumor peers — Charlie three hops away in the Rumor mesh never sees Bob's Meshtastic message even though Alice's bridge has it. Proposed mechanism: introduce a third trust level between VERIFIED and BRIDGED, called `BRIDGE_VOUCHED`. Bridge wraps the foreign message in a Rumor-signed outer envelope that explicitly carries `{originNetwork, originSenderId, originSignatureIfAny, payload}`. The outer envelope is signed by the bridge's Rumor key — but the wire format and trust level make it unambiguous that the bridge is vouching for *delivery* only, NOT for content authenticity. Other Rumor peers can relay it because the outer envelope verifies. Display layer renders as "via Alice from meshtastic:Bob" with a distinct visual treatment from native Rumor messages. Recipients decide per-bridge whether to trust that bridge's vouching (UI: enable/disable bridge-vouched relay per known bridge). Replaces the current absolute "never relay BRIDGED" with a more nuanced "never relay BRIDGED without an authenticated outer envelope from a bridge node we know about." Scope: new TrustLevel enum value, new wire envelope format, signature verification of outer envelope at every receiver, UI for per-bridge trust toggles, three-state visual rendering. Real design work; not started. |
| O94 | **`[TODO/CODE]`** Intermittent HELLO timeouts on persistent-link rounds** | On the Moto/OnePlus test pair, roughly every 2nd–4th 10s round the TCP connect succeeds but HELLO gets no reply within its 5s timeout ("Session error" + SocketTimeoutException on both sides simultaneously); the next round then completes in 60–400ms. Functionally absorbed by the round loop (failures reset on success), but it doubles worst-case delivery latency and spams warn logs. Suspect Wi-Fi Direct power-save (GO NoA / opportunistic PS) napping the radio between rounds despite the held WifiLock (`WIFI_MODE_FULL_HIGH_PERF` is deprecated/no-op on API 34). Investigate: keep the TCP socket open across rounds instead of reconnecting (protocol change — session loop over one socket), or probe OEM-specific PS toggles. |
| O33 | **`[TODO/HW]`** Battery profile and "protest-day" budget** | Measure continuous BLE + Wi-Fi Direct scan + relay power draw on representative devices (Pixel-class, mid-range Samsung, 4-year-old budget). Target envelope per research: 8 hours active use on ≤50% battery; 12–24 hours unattended on a modern phone with conservative duty cycling. Tune `SCAN_MODE_LOW_POWER` defaults, motion-aware throttle (AirGuard pattern), hard cap on reconnect attempts to vanished peers (Meshtastic #4444 / #2659 failure mode), exponential discovery backoff when no peers seen (G2 generalized). Declare `connectedDevice` foreground-service type (Android 14+) with `FOREGROUND_SERVICE_CONNECTED_DEVICE` permission. First-run flow requests `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` — without it Samsung/Xiaomi/Huawei kill the service regardless of FGS status. Without measured numbers, every claim about field viability is unsupported. **Audit note (2026-07-15):** the manifest already declares `foregroundServiceType="connectedDevice"`; the `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` first-run flow and all power measurements remain. |
| O45 | **`[TODO/EMU]`** BIP-39 mnemonic identity export (v1 backup)** | Explicit "export identity" flow producing a 24-word BIP-39-compatible mnemonic, displayed with `FLAG_SECURE` so screenshots are blocked, never written to clipboard. Optional passphrase wraps the mnemonic for shoulder-surfing protection. First-launch flow includes a "I got a new phone" announcement message-type signed by the old key while still on the old device — pre-warns contacts of an upcoming identity continuation (different from O41 rotation, which is reactive). Document explicitly: lose this mnemonic, lose this identity forever. Photo-of-paper backed up to Google Photos is the dominant real-world failure mode — assume it; push toward O46 social recovery as the better primary path. |
| O46 | **`[TODO/CODE]`** M-of-N mesh social recovery (v2 backup)** | After v1 ships, layer Shamir-split social recovery on top. User picks N (3–7) trusted contacts; app generates SLIP-0039 split of a wrapping passphrase (not the raw key); each share is wrapped to the guardian's pubkey via existing DM crypto and sent over the mesh. Recovery on new device: generate ephemeral recovery pubkey, broadcast a signed claim "I am the new device of (oldUserId), please send my share to this ephemeral key"; each guardian's client surfaces the request with strong identity-verification UX (emoji fingerprint of requester + claimed old userId + instructions to confirm out-of-band with the person in person/by voice/codeword); on approve, share re-wrapped to ephemeral key. Reassemble ≥M shares → recover passphrase → identity. Critical: **24–72h time-delay window** between first share-approval and final assembly (Argent pattern, anti-coercion); **signed continuity message** broadcast on the mesh after recovery completes so original Alice can publicly repudiate a fraudulent recovery while she still has the original key. Optional duress passphrase reassembles to a decoy key. Without time-delay + public continuity + OOB verification, social recovery is worse than no recovery. |
| O1 | **`[TODO/CODE]`** SQLDelight migration** | Promised in commit 3e7f588 as "next commit", never done. Room is still in use. Only worth doing if leaving Google's runtime matters; otherwise stale. **Re-scored (2026-07-15, O106):** the desktop/USB-bootable node can't use Room, so this is no longer stale — it's one of two options for :node storage (SQLDelight = one impl everywhere vs. a plain JDBC-SQLite adapter = third impl, less work, relay nodes have simpler query needs). Decide when :node starts, not before. |
| O120 | **`[TODO/CODE]`** Wire up the prune loops (2026-07-18 audit §2.5 — unbounded growth) | `BreadcrumbCache.pruneOld()` and `TopologyTracker.pruneStale()` are implemented, tested, and wired down to Room — and called from NOWHERE (grep: zero call sites outside definitions/tests), so their own docstrings' "24h breadcrumb prune / 7-day route de-rank" never fires and both tables grow without bound (contradicts O55's months-of-accumulation posture). `OnlineStatusTracker.lastSeen` has the same never-pruned shape (plus README falsely claims a 30-min prune). Fix: one periodic maintenance tick in `MeshService` (post-§5-guard, so it can't stack) calling all three; verify against the audit note that three independent subsystems all shipped a prune that never ran — something about "periodic maintenance" isn't connecting in this codebase's habits, so add a lint-style test if a cheap one exists (grep-based call-site assertion à la `SourceInvariantTest`). |
| O121 | **`[TODO/CODE]`** 2026-07-18 audit residue — small correctness pins | Grab-bag of confirmed-small items, none load-bearing today, each a landmine later: (a) `DmEnvelope.selfAuthenticating` is documented as O5a's security gate but never read in production — pin today's emergent safety with an explicit check or a SourceInvariantTest-style guard; (b) `BreadcrumbCache` records DM breadcrumbs with `DEFAULT_BROADCAST_HOPS` (7) instead of the DIRECT budget (15) — field is meaningless until fixed; one line, do it when routing first consumes it; (c) `DomainTagInvariantTest` asserts 20 domain tags, code emits 21 — `rumor-dm-recipient-tag-v1:` and `rumor-room-posting-cert-v1:` missing from the guard and from `docs/wire-format.md` §6 (2-line fix, guard is defeated until then); (d) `SortedListRbsrStorage.items()` linear-scans the whole list per range query — profile before the large-anchor-store regime; (e) `MessageStore`/`GossipEngine` still lack broad dedicated test coverage beyond `MessageStoreIngestTest`. |

#### Tier 5 — bridge hardware track

| # | Item | Notes |
|---|------|-------|
| O5 | **`[TODO/HW]`** Bridges — DM bridging** | Both bridges deliberately broadcast-only today. Two viable architectures; **read the "Encrypted bridging" decision below before designing this**. Recommended: Architecture B (envelope passthrough, true E2E) for both bridges — curves align (both use X25519/Ed25519-derived ECDH), only the AEAD wrapper differs per bridge. Architecture A (decrypt-and-re-encrypt) is fallback only. Both architectures require the bridged-contact UI labelling listed under Architecture A as hard pre-ship blockers. |
| O4 | **`[TODO/HW]`** Bridges — USB transport** | Both Meshtastic and MeshCore are BLE-only. MeshCore docs `'>'`+LE-len framing for USB CDC; Meshtastic uses 0x94 0xC3 magic + varint length. Significant scope per bridge. |
| O8 | **`[TODO/HW]`** MeshCore v1/v2 firmware fallback** | Current decoder is v3-only (opcodes 0x10/0x11, snr+reserved+path_len header). Older firmware uses 0x07/0x08 and a simpler layout. Detect via response code on the first frame. |
| O6 | **`[TODO/HW]`** Bridges — multi-channel selection UI** | Both bridges hardcode channel index 0. Need a per-plugin settings screen exposing the radio's channel list. |
| O7 | **`[TODO/HW]`** Bridges — device picker** | Both bridges auto-connect to the first matching device. Multi-radio setups need a picker UI feeding `connect(device)`. |
| O54 | **`[TODO/CODE]`** Transport plugin tier — non-phone infrastructure** | The existing plugin model (Meshtastic, MeshCore) generalizes naturally to "any device that carries Rumor bytes between two endpoints" — routers, single-board computers, packet-radio modems, satellite modems, USB-CDC links between two stationary nodes. Two integration paths compose: (1) **TransportPlugin on the phone** — an Android plugin that speaks to an external device over USB/BLE/TCP and shuttles Rumor frames in both directions, no protocol translation. The device runs a small daemon (Go/C/Rust binary, OpenWrt package, or systemd service) speaking the Rumor wire format. Useful for "plug a USB-ethernet adapter from my phone to my OpenWrt router" or "phone-to-Raspberry-Pi over USB serial." (2) **Native relay node** — port the relay subset of `:core` (signature verify, dedup, relay decision, no UI/storage of plaintext) to a non-JVM language for direct deployment on routers, SBCs, or LoRa gateways. Pre-existing infrastructure (window-mounted SBC on solar, OpenWrt router on UPS, ham-radio packet gateway) becomes a permanent mesh node. Distinguished from BridgePlugin because there's no foreign network — it's still Rumor end-to-end, just over different physical media. Plugin capability declaration: `transport-extend` (read+write frames) is APK-tier in O24 because it touches the wire path. **Threat-model fit:** matches O55's long-term-collapse framing better than crowd-density mesh — fixed-position infrastructure on intermittent power is exactly what survives weeks-to-months. **Scope correction (2026-07-15, O106):** with `:core` pure JVM there is no "port the relay subset to non-JVM" for laptops/SBCs that run a JVM — you run the *whole engine* headless (no port, no subset, no second codepath). The port idea survives only for genuinely JVM-less targets (bare LoRa gateways, tiny routers), if any survive the O104 hardware filter. |
| O75 | **`[TODO/HW]` — LOWEST PRIORITY** `:relay-firmware` MCU relay-only spec | Microcontroller-class Rumor relay: ESP32 / RP2040 / nRF52, optionally paired with the NoLoRa SPI/I2S harmonic-emission trick for battery-free transmit. Relays signed broadcasts + signed DMs (carrying opaque ciphertext); never originates, never bridges, never runs plugins, never holds keys, cannot be a DM endpoint. Capability bits in HELLO: minimum relay-only subset. ~512–4096 entry RAM dedup ring, no persistence across reboot, no RBSR / no bloom (pure flood-with-TTL, the original Rumor model). Ed25519 verify is the heaviest op (~12ms on Cortex-M0+). Sibling Cargo/PlatformIO project, not Gradle. Hard prerequisite: a written wire-format spec (`docs/wire-format.md`) so a non-JVM implementation can be byte-compatible — currently the wire format is implicit in `core/wire/`. Explicitly lowest priority: phone-side software ships first; this lands once the protocol surface is locked and there's a real hardware experimenter asking for it. |
| O118 | **`[TODO/CODE]`** Meshtastic protobuf bounds patches (2026-07-18 audit — do WITHOUT waiting for the Wire migration) | The hand-rolled codec now has two concrete session-found bounds gaps, not just the historical field-number bug: `Reader.readSubMessage` has zero bounds validation (its siblings `readBytes`/`skipField` both `require(pos + len <= end)`) — an adversarial length can integer-overflow `pos + len` negative (silent truncation — worse than a crash) or desync the outer reader for every subsequent field; plus the pre-existing negative-varint-length path on `readInt`/`readBytes`. Currently masked by call-site `runCatching`. Patch: `require(len >= 0 && pos + len <= end)` before constructing the sub-reader. Square-Wire codegen remains the right eventual answer when S1 resumes (per the prebuild policy note) — these two checks just shouldn't wait for it. |
| O119 | **`[TODO/HW]`** Bridge robustness pack (2026-07-18 audit; O5a-adjacent) | Three confirmed gaps shared by both bridges: (a) neither reconnects after BLE disconnect — `ready` only flips in `onDetach()`, never on a `STATE_DISCONNECTED` GATT callback, so radio-out-of-range = silent permanent failure until the plugin is manually toggled; (b) MeshCore never negotiates MTU (Meshtastic does `requestMtu(517)`) despite `MAX_TEXT_BYTES=120` vs ~20-byte default ATT payload — outbound text beyond ~14 chars silently truncates; (c) both assign a fresh random UUID per decode instead of a packet-derived stable id, so a LoRa/BLE retransmission becomes N "novel" feed messages — dedup can't catch a bridged replay (O5a constraint #5 gap; bounded by BRIDGED-never-relayed). |

#### Tier 6 — UI surfaces and leaf features

| # | Item | Notes |
|---|------|-------|
| O97 | **`[TODO/UI]`** No manual persistent-link / Wi-Fi-Direct control surface** | **Field note (2026-07-18, 0.6.3 three-phone run):** a freshly-wiped node is invisible to the mesh until its *first completed exchange* (nothing authored → nothing for the hub to relay → peers have zero evidence it exists, even with perfect radio visibility). The status surface should therefore distinguish "radio fine, not yet announced" from "actually broken" — e.g. "connected to backbone via X / first exchange pending". Pairs with O124 (manual refresh actually announcing). Original row: Field-noted: there is no UI button to initiate or hold a Wi-Fi Direct persistent link. G19 replaced the old per-contact "priority peer" model with *automatic* persistence for any nearby peer, which is right for the common case — but it left zero manual control. Users can't (a) force a persistent link to a specific peer, (b) see which peer(s) a held group currently covers, or (c) manually tear down / re-form when the held group starves discovery of a third phone (the known G19 limitation). Also relevant: Wi-Fi Direct only forms with Wi-Fi radio ON but **not** associated to an AP (O93) — a settings/status surface should show "Wi-Fi Direct available: yes/no (disconnect from <SSID> to enable)" so users understand why the mesh isn't forming on a home network. Scope: a mesh/transport status panel (likely under Settings or a new Mesh tab) exposing current group state, GO/client role, held-peer list, and manual connect/hold/drop actions. Pairs with O93's "location off" warning surface. |
| O68 | **`[TODO/UI]`** Report-user / report-content UI + local report log** | Per-message "report" action in thread view; per-user "report" action in contact details. The report action does three local-visible things in sequence: (a) auto-add reported user to local blocklist with the chosen reason (ties into existing `BlockManager.block(reason=…)`); (b) append to a local report log surfaced in Settings → Reports for the user's own records; (c) **within a moderated Room (O79), the report is delivered as an `@-mention` to the Room's moderators** so they can act (remove / kick / ban per Room policy). There is no central maintainer to send reports to and no upstream submission — Rumor has no server. The Room-mod path is the actual moderation mechanism that scales; local-block + local-log is the personal-self-defense layer. Combined with O67 (keyword filters), O78 (public block reasons), O79 (per-Room mods), and O81 (image classifiers), this is the full content-moderation surface — each piece is local and user-controllable; none of them rely on a central service. |
| O110 | **`[TODO/UI]`** Block/unblock action belongs in Contacts + thread, not a raw-userId text field** | Field-noted (2026-07-16): the only way to block someone today is to hand-type their 64-char userId into a text field in Settings — unusable (nobody transcribes a hex userId, and it's error-prone). `BlockManager.block(userId, reason)` already exists; this is purely the UI surface. Add a **block/unblock action to the contact row** (⋮ menu in Contacts, and contact-detail screen) and to the **thread overflow menu**, pre-filling the userId from the tapped contact/message so the user never types it. Fold the Settings raw-userId field into an "advanced / block by ID" affordance at most, not the primary path. Pairs with O68 (report action does block+log+mod-notify in one) — the plain block action is the subset of O68 without the report log or Room-mod delivery, and both should share the same per-contact/per-message entry points. Cross-refs O68, O78 (optional public block reason at block time), BlockManager. |
| O111 | **`[TODO/CODE+UI]`** Nickname advertisement — self-chosen nick offered to peers, receiver-accepted (filed 2026-07-17, user request) | Phones advertise their preferred nickname (self-asserted, signed — a profile message or SELF_PRESENCE-adjacent field, NOT inside the HELLO transcript per the O101 rule that the handshake doesn't accrete weight). Receivers see it as a *suggestion* with an accept affordance, never auto-applied — display-name spoofing is exactly the O21 threat, and auto-adopt would let anyone rename themselves "Mom". Differentiator for shared nicks = the O21 emoji fingerprint (two "Bob"s render `Bob [🐅🍧🚲🌵]` vs `Bob [🌊🎈🦎🍕]`). On nick *change*, surface per O21's rename-pinning ("Bob changed their display name to Robert"), don't silently update. Cross-refs O21 (fingerprint + pinning), G21 (rename UI plumbing), O113 (the local half). |
| O113 | **`[TODO/UI]`** Self-nick — own identity renders as raw 64-hex everywhere (field report 2026-07-17) | Add a local profile nickname (Settings → identity): displayed wherever the local identity appears (own feed messages, thread composer, identity screen), shown beside the own emoji fingerprint (O21), and the source value for O111's advertised nickname. Purely local until O111 ships the advertisement. |
| O122 | **`[TODO/CODE]`** Doc-drift reconciliation sweep (2026-07-18 audit §4 — one pass, low risk, high reader value) | Confirmed-stale items to fix together: `docs/GLOSSARY.md` documents the deliberately-REMOVED identity rotation (G9) as live — highest-confidence drift of the audit; `docs/ROOMS_THREAT_MODEL.md` claims posting-cert enforcement in present tense while `RoomPostingCertVerifier.verify()` has zero call sites (violates the doc's own honesty rule — add a "NOT YET ENFORCED, tracked as O89" banner); `docs/O79_PROTOCOL_SPEC.md` "Known gaps" still claims Ed25519→X25519 unwired though O91 closed (re-implementer-facing); `ios/RumorCryptoBridge.swift` lacks the `aad` param + two conversion methods its own spec added, and both iOS docs describe the KMP layout G30 flattened; `CryptoManager.x25519Agreement` KDoc describes the pre-G20 PBKDF2 derivation; `GroupCredentials.kt` KDoc cites retired `psk-v1`; `GossipPacket.kt` comment references never-shipped `rbsr-v2`; `RbsrWire.kt` fp comment says "XOR" (impl is additive — deliberately); G31's `commonTest` path + G28's already-fixed formatElapsed note in THIS file; README's "14 modules" (3), bridge-"stubs" claim, stale test-coverage table (77 actual test files), false OnlineStatusTracker 30-min-prune claim. |
| O69 | **`[TODO/UI]`** In-app full-wipe ("forget me") flow** | App Store 5.1.1 requires in-app account deletion. Rumor has no cloud account, but the UI must still expose a one-tap "delete identity and all data": wipe Ed25519 keypair, Room DB, SharedPreferences, transfer cache, log ring buffer, plugin scoped storage. Double-confirm with a 5-second undo timer before commit. **Required: emit a signed `IDENTITY_RETIRED` broadcast before wiping**, signed by the about-to-be-deleted Ed25519 key, telling contacts the identity is retired. Recipients update the contact record to "retired" status (display greyed, no further messages expected from this userId, blocked from re-establishing under the same userId since the holder is supposedly gone). This is the *only* legitimate use case for an old-key-signed identity message after G9's deprecation (see G9 row): O41 / G9 identity-rotation has been deprecated because key-compromise turns rotation into permanent impersonation. The retirement case is safe because there's no successor key to be hijacked into — old key signs "I am gone," nothing transfers. On success, app returns to onboarding as if first-launched. |
| O70 | **`[TODO/UI]`** Explicit user-controlled scan interval, with measured battery-cost annotations** | Replace the binary LOW_POWER / BALANCED duty-cycle slider with explicit intervals (15s, 30s, 1min, 5min, 15min, manual-only). Each interval renders an estimated battery cost ("≈3%/hr") from O33 measurements — honest numbers, not hand-waved. The reviewer-friendliness angle: Apple is far happier with "user chose this scan rate" than "app decided to drain battery." Maps onto O62 ModeProfile so each mode has its own default interval. Depends on O33 measurements landing first; until then, label estimates as approximate. |
| O71 | **`[TODO/UI]`** Local-only broadcast feed UI (display filter, no protocol change)** | A "discovery" tab that shows BROADCAST traffic from non-contacts in the device's current radio range, scored by recency and hop count. Pure display layer — does NOT change protocol behaviour; the network already carries this traffic identically to contact broadcasts. Per O36 this is explicitly allowed ("A local-only display filter — 'show me broadcasts from non-contacts in my immediate area' — is a UI feature and is fine to ship"). Cross-reference O36 in the code so future audits don't mistake it for a violation. Combined with O67 keyword filters and O68 reporting, this gives the public-feed experience users expect without re-introducing the global-broadcast-room failure mode. Apple-friendly framing: "your neighbourhood feed," not "public chat." |
| O74 | **`[TODO/CODE]`** Submit to non-Play Android stores** | Huawei AppGallery, Xiaomi GetApps, Samsung Galaxy Store, Amazon Appstore, Aurora Store mirroring, IzzyOnDroid F-Droid mirror, Uptodown, GetJar, SlideME, APKPure, 1Mobile, Tencent MyApp (China-only). The known major stores are listed here but there is also a long tail of regional and niche stores worth a broader research pass before shipping — every additional store with low marginal effort is reach the F-Droid+Play baseline doesn't get. Per-store review climate for P2P/mesh/E2E apps in 2026 is unknown and varies sharply by jurisdiction. Chinese App Store (Apple China) and Tencent MyApp are flagged separately as probably-infeasible for an uncensorable E2E mesh app without significant feature removal; document as a non-goal unless someone is willing to ship a heavily-stripped China variant. |
| O21 | **`[TODO/UI]`** TOFU key pinning and identity hardening** | `userId` is already `SHA-256(publicKey).toHex()` (see `CryptoManager.publicKeyToUserId`), so the userId↔pubkey binding is cryptographic — a sender cannot claim a userId that doesn't hash from their pubkey, and every receiver can re-verify cheaply. The real identity-hardening gap is **display**: a 64-char hex userId is unreadable, and human-set display names can be freely spoofed (two people can both call themselves "Bob"). Solution: derive a short visual fingerprint from the pubkey — take 8 bytes of `SHA-256(publicKey)`, map each byte to an emoji from a curated set of ~256 visually distinct emoji (animals, food, objects, vehicles; no faces, no flags, no near-duplicates, all cross-platform-stable). Display as `Bob [🐅🍧🚲🌵]`. Four emoji = 32 bits of fingerprint, collision-resistant up to ~65k contacts, easy to compare at a glance. Full hex userId revealed only on tap-through. **Pin display-name → userId binding** in `ContactRepository`: if a previously-seen userId arrives with a different display name, surface "Bob changed their display name to Robert" rather than silently updating. Pinning is for display-name churn, not key churn (latter is impossible by construction). Display names should be entirely optional — users who want maximum clarity can show only the emoji fingerprint. |
| O47 | **`[TODO/UI]`** Per-message bridged-source label + metadata-leak banner** | Element's failure mode for 7 years (matrix/element-web#7823, still open): bridged rooms labelled only at room level, so users misremember which messages were E2E. Don't repeat it. Every bridged DM and broadcast in compose/contact-list/thread renders with a persistent "via <bridge>" label per message, not just per contact. Compose screen for a bridged contact carries a non-dismissable banner: "Messages to this contact pass through <bridge> and the bridge sees who you're talking to even though it can't read what." This holds under both bridging architectures (A and B) — Architecture B's content-E2E does not hide metadata. Add to S1/S2 ship-blocker checklist. |
| O18 | **`[PART]`** Transfer-control UI (pause / resume / cancel)** | `TransferAssembler` and `TransferSender` have no user-facing controls. Once a large transfer starts, the user can't cancel it without quitting the app, and it can't be paused if the device is going to be off-network temporarily. Add: cancel button (drops the transfer record, cancels watchdog, sends a CANCEL message so sender stops resending chunks), pause button (suspends watchdog and incoming chunk processing without dropping state), resume (re-arms watchdog from current state). UI should show current progress (`receivedIndices.size / totalChunks`), throughput, and estimated time remaining. No protocol changes for cancel/pause if implemented as receiver-local; sender-side cancel needs a new `TRANSFER_CANCEL` message type so the sender stops queuing chunks. |
| O14 | **`[TODO/UI]`** Compressed-archive extraction UI** | There is no compression at the protocol layer — `Chunker` splits raw bytes, no zip/gz. So a zip bomb at protocol level is not possible; the hash is of the full uncompressed data and is covered by Ed25519, so a sender cannot lie about it without breaking their signature. The risk is purely application-layer: a sender transmits a 1MB `.zip` payload which the receiver extracts to 100GB. Fix: before any extraction of a compressed MIME type, show a non-dismissable dialog: "Compressed archive received — Download: XMB / Estimated extracted: YGB." Do not extract until user confirms. This is a UI feature, no protocol changes needed. |
| O19 | **`[TODO/UI]`** Pre-send file manipulation (compression, format conversion)** | Pure app/UI layer, no protocol changes. Before chunking and sending a file, offer the user: image compression (JPEG quality slider, downscale to N pixels), video transcode (bitrate selector via Android's `MediaCodec`), archive options for multi-file selections (zip / tar.gz). Show "Original: XMB → Compressed: YMB" before confirm. Reduces bandwidth and storage cost across the mesh significantly for image-heavy traffic, which is the dominant case in practice. Independent of O14 (extraction-warning popup) but shares the same UI surface. |
| O9 (rest) | **`[TODO/EMU]`** Instrumented + UI test coverage** | DI smoke test is now in place (G6) and unit tests cover scheduler/dedup/chunker/blocklist/online-status/neighbor/DmEnvelopeRegistry. Still missing: instrumented (on-device) tests, Compose UI tests, MainActivity lifecycle smoke. Lower priority — the DI verify test catches the most common pre-ship breakage. |
| O101 | **`[TODO/CODE]`** First-class signed sites/pages** | Users already build "site-analogues" on every messenger (Telegram/Signal/Instagram bots) — clunky and inefficient; a real page primitive is better UX. **Bandwidth must be designed up-front** (popular sites get hammered): (1) **easy cloning/mirroring** of a whole site or individual pages; (2) **author-signed site + pages** so a mirror is verifiably the author's work and a cloned-site-with-malware is detectable; (3) **aggressive local rendering** — rendering machinery (HTML scaffolding, styling) lives on the client; the wire carries compact content only; (4) **constrained format** — no heavy JS; even heavy markdown/HTML is non-negligible at Wi-Fi Direct bandwidth; (5) **RSS-like subscription feed shipping diffs**, not whole pages — and diffs are **cached at intermediaries** so a request needn't travel to the source; (6) users can **mirror pages they value** themselves (signed, so provenance survives). **Discovery** ("what sites even exist?"): a **"business card"** — bio + the sites you host or mirror (each with its author signature) — exchanged as an **optional post-HELLO phase, never inside HELLO itself** (the signed handshake transcript must not accrete weight). Stored once; every later session asks only **"what changed?"**. Advertising what you host/mirror is an O58 Tier-3-style visibility disclosure — **opt-in per site**. **Per-site storage scopes:** hosting a wiki must not also expose your furry art page — each site is its own share scope, opt-in per site, while everything on a site remains shareable (extends O23's quota/scope model). O36 compliance: sites are author-signed, subscription/pull content inside the contact-graph model — *not* an open room; discovery is peer-exchanged business cards, never a network-level public directory. Cross-refs: O100 (big site assets ride swarm fetch), O42 (diff sync machinery), O21 (author fingerprint display on business cards). **Analysis (agreed 2026-07-15):** (i) **Format decision — no HTML, no WebView, ever.** An HTML subset inherits sanitization hell and XSS-in-a-WebView as a permanent security tax; a constrained markdown-ish format rendered **natively in Compose** kills the whole vulnerability class, enforces the bandwidth budget structurally, and satisfies no-CDN/offline-first by construction. This is THE scoping decision — make it first, before any other design work on this row. (ii) **Architecture — a site is a broadcast feed with structure.** Signed page-versions are messages; the diff feed *is* gossip; intermediary caching *is* the message store. Modeled that way, this builds almost no new distribution machinery — mostly a native renderer + subscription UI. |
| O103 | **`[TODO/CODE]`** Geotag / collaborative map-annotation plugin** | Post-SHTF, digital maps go stale: OSM files can be *transferred* (O100 helps) but not collaboratively *edited* — "this bridge is out", "this building burned down", "this place is a clinic". Plugin: **signed geo-annotations** gossiped as ordinary signed content; merge is append-only per (author, location), display renders latest-per-author with conflicts visible (no consensus needed — annotations are attributable claims, and O21 fingerprints tell you whose claim you're trusting). User judgment: genuine value-add post-SHTF, hopefully not out of scope — scoped as **plugin-tier** (O15/O24), not core. **Platform note (recorded):** plugins deepen the iOS lock-out — Apple forbids post-install code, so the DEX-loader model is un-portable; iOS was already excluded by the APK/DEX architecture, but if iOS ever matters, high-value plugins like this become candidates for core-app inclusion instead of plugin distribution. **Analysis + user correction (2026-07-15):** (i) **No automated decay/expiry UX — deliberate design decision.** Post-SHTF wall-time is untrustworthy (O95 — the 64-day-slow field phone would have back-dated every annotation), so staleness must never be machine-resolved. Instead **annotations are commentable**: "bridge out" accrues signed replies — "it's back", "no it isn't" — which are themselves annotations anchored to the parent, and *people* decide what to believe. Timestamps are displayed honestly but never drive auto-hide. (ii) Map renderer is a **must-adopt** per the prebuild policy — osmdroid or MapLibre (FOSS, F-Droid-compatible), never hand-rolled. (iii) This is the strongest single post-SHTF demo of why Rumor matters ("the bridge is out" on a map beats any messaging demo) — **core-app candidate**; plugin-tier is the right first home, expect promotion if it proves out. |
| O102 | **`[TODO/SIM]`** Neighbor-aware forward delegation (one copy per airtime)** | If A, B, and C all want content from F, and F's only neighbor is E, naive delivery has E sending the same bytes in triplicate. Since E knows A/B/C are mutual neighbors, it can send **one copy to A with the instruction "forward to B and C"** — the payload occupies one link at a time. **User's own caveat:** pure single-file serialization could *slow* the network; combine with **minimal-overlap coverage sets** (person 1 sends to 2,3,4,5; person 6 sends to 5,6,7) — and note the existing P2 overlap-shaped batching may already approximate this ("maybe this is already the solution?" — test, don't assume). Physical-layer nuance: within one Wi-Fi Direct group all transmissions share the channel anyway (CSMA), so the win inside a group is airtime, and the big win is multi-group/multi-hop. Security: a delegation instruction is a **work order from a peer** — must be budgeted (trafficClass ceilings, relay caps) so a malicious node can't use it for amplification. Deliverable: simulator comparison — naive N-copies vs delegation vs P2-overlap-only vs combined — measuring airtime, latency, per-node bandwidth. Cross-refs: P2, O29 breadcrumbs, O58 Tier 1/2, O43, O100 (a swarm naturally load-spreads; delegation attacks the same waste from the sender side). **Analysis (agreed 2026-07-15) — expected outcome negative:** (i) physics: within one Wi-Fi Direct group all nodes share the channel (CSMA), so E→A + A→B + A→C moves the same bytes over the same air as three unicasts from E, *plus* instruction overhead — delegation only wins across multiple groups/hops with genuinely parallel channels; (ii) gossip already performs implicit delegation self-healingly (E hands to A, A's next exchanges cover B and C, dedup prevents the triplicate) with no trust surface and no silent failure mode — an explicit instruction dies with A's battery and nobody retries, because E believes it's handled. Prediction: the sim shows a marginal win for messages, and O100's swarm covers the bulk-transfer case. Run the cheap sim experiment anyway; if confirmed, close this row as a DECISION so the idea doesn't recur. |

#### Closed-row tombstones (kept ONLY so hard-won findings aren't rediscovered — not work)

| # | Item | Notes |
|---|------|-------|
| O42 | **`[CLOSED 2026-07-15 → see G24]`** Replace bloom-filter offer/want with Range-Based Set Reconciliation (Negentropy)** | **Shipped and field-verified:** adaptive size-gated selection live (`shouldUseRbsr`, `RBSR_MIN_SET_SIZE=3000`, HELLO `knownCount`, single-switch go-live via `TransportConfig.rbsrItemsProvider` — capability advertised iff provider wired), bloom FP 1%→0.01% + real MurmurHash3, wire path covered by `GossipSessionWireTest` (loopback socket-pair: RBSR above gate, lazy-provider below, mixed-version no-mode-split), hardware regression passed (G24). **Only deferred remnant:** compact (CBOR/binary) RBSR framing — a "nice later" per the assay decision, since the adaptive gate confines RBSR to where it wins even with JSON frames. Full history below for the record. |
| O42-hist | *(history of the closed row)* | Current gossip uses a bloom filter sized for expected message-set size; on long partition heal the actual symmetric difference can blow past the parameters, causing false-positive collapse (receiver thinks peer has things it doesn't, peer holds back, messages don't flow). Existing graceful-degradation fallback (O13) handles this safely but inefficiently. Port Negentropy (Meijers 2023, arXiv 2212.13567; reference impl github.com/hoytech/negentropy, MIT licensed, ~500-line Kotlin port): O(d log N) bandwidth, deterministic, no parameter tuning, no decode-fail mode, deployed in Nostr's strfry at tens-of-millions-of-elements scale via NIP-77. Operates on `(timestamp, id)` tuples — matches existing message model. Composes with peer-aware batch shaping (P2) — the overlap heuristic stays; only the wire format changes. Solves O13's BloomFilter OOM problem as a side effect. Don't roll your own; don't pick CPISync (cubic compute), classic IBLT (parameter tuning fragility), or rateless IBLT (too new, no battle-tested impl). **Status (as of 5e84fa0):** algorithm landed in `core/sync/Rbsr.kt`; wire types in `core/sync/RbsrWire.kt`; capability-gated branch in `GossipSession` (`rbsr-v1` in HELLO `supportedFeatures`); `SimTransport.useRbsr` toggle for scenario comparison. **Ready for simulator validation, not ready for the wire** — `LOCAL_SUPPORTED_FEATURES` stays empty in production, opt-in is per-session for now. Before promoting to default-on, review against the `hoytech/negentropy` reference implementation for bound-edge fingerprint stability (the subtle case where range boundaries fall on item keys and bisection rounding produces non-identical fingerprints across peers). Wire format gets locked only after that review passes and after simulator runs demonstrate the bandwidth win against bloom under realistic partition/heal scenarios. **Reference review done (this session):** fingerprint switched from XOR to **additive mod 2^256 + bound item count** in `Rbsr.fingerprint` — XOR is GF(2)-linear and a peer choosing message ids could forge a colliding range fingerprint (→ wrongly `Skip`ped, messages silently fail to reconcile); addition matches negentropy and makes forgery a 256-bit subset-sum. Bound-edge concern resolved: `Long` bounds survive the wire exactly (kotlinx integer literal, no `Double`), range membership is a pure `(ts,id)` comparison applied identically both sides, and session integration acts on both diff directions. **Simulator validation done (`simulator/…/engine/RbsrBandwidthScenarioTest.kt`): the bandwidth win does NOT hold at small/medium set sizes.** Measured summary-phase bytes: N=1000 → bloom 3224 / RBSR 13784 (RBSR 4× *worse*); N=8000 → bloom 25584 / RBSR 20398 (RBSR ~20% better). Crossover ≈ 5–6k messages. Root cause: Rumor's RBSR frames are verbose **JSON** (~130 B each: field names + base64 fp), so a 16-way bisection sends heavy frames — negentropy's `d log N` win assumes compact binary framing. RBSR's *unconditional* win is **exactness** (no bloom false-positive message loss — separately asserted and holds at every size). **Implication for promote-to-default-on (O42 next step): do NOT flip `LOCAL_SUPPORTED_FEATURES` on blindly** — below the crossover it regresses the common case. **Decision (this session): size-gated adaptive selection, NOT blanket default-on.** Rationale: for Rumor the summary phase is usually dwarfed by message payloads, so RBSR's bandwidth win is irrelevant EXCEPT in one pattern that is exactly where the app is headed — persistent links (O2/G19) between static/free anchors with large accumulated stores (O55) re-syncing every ~10s. There, at d→0 (already synced) RBSR sends ~500 B while bloom re-ships the whole filter (~3.2 B × N → 160 KB at N=50k): an *order-of-magnitude* win, not the 20% the crossover test showed. So: use **bloom below a set-size threshold** (cheap, and made near-exact by a lower FP rate — see below) and **RBSR above it** (its actual sweet spot). The selection must be **symmetric+deterministic** — both peers advertise `rbsr-v1` AND exchange set size (in HELLO), both compute `useRbsr = bothSupport && max(sizeA,sizeB) > THRESHOLD`, so they never split modes. Compact framing (option a) becomes a "nice later" since RBSR only runs where it wins even with JSON frames. **Bloom FP rate must drop for chat UX:** the default 1% means ~1 in 100 messages skipped per exchange on a link, and on a quiet mesh (static blooms) that skip persists → a noticeably late/undelivered chat message. Because adaptive keeps bloom on *small* sets only, a much lower FP is nearly free there (a fat bloom over a few hundred items is still tiny). Target set by simulation (`RbsrBandwidthScenarioTest`) — measure empirical FP (the custom murmur mix + `hashCount` cap at 10 deviate from theory) and pick the knee. FP rate is an implicit wire constant (`BloomFilterData.deserialize` rebuilds at the default rate) so it changes via the single default; peers must match. Framing to users: Rumor is not a four-nines email client, but delivery UX should still be good. |
| O52 | **`[SUBSUMED 2026-07-16 → O79]`** Group messaging via broadcast-with-key-DMs** | The O79 Rooms protocol (ported in the archimedes merge) carries the closed-membership/invite/password shape and the MultiRecipientEnvelope key-distribution primitive this row sketched. All remaining group work happens on O79. Original sketch preserved in git history (pre-merge CLAUDE.md). |
| O-rbsr-v2 | **`[DROPPED 2026-07-16, merge]`** NIP-77-compatible raw-id-sum RBSR fingerprint** | Archimedes added a `rbsr-v2` capability whose fingerprint summed RAW message ids mod 2^256 (byte-compatible with hoytech/negentropy + Nostr NIP-77, motivated by strfry interop for O72/O54). **Dropped in the merge: forgeable in Rumor's threat model.** In Nostr an id IS a content hash, so an attacker can't choose it; Rumor ids are sender-chosen, so the last id in a range can steer the sum to any target — a chosen-id collision, exactly the linearity hole the additive-over-SHA256(domain-tagged-item) formula (kept, field-verified) closes. If Nostr interop becomes real, reintroduce as a *hash-the-ids-first* variant — never raw-id sums. `FingerprintFormula` machinery + 2 tests deleted with it; see `docs/ARCHIMEDES_MERGE_CATALOG.md`. |
| O99 | **`[SUPERSEDED → folded into O98]`** Channel-aware group creation (don't disrupt nearby Wi-Fi)** | **Merged into O98.** Rationale (concluded 2026-07-13 after an on-device spike): O99 split into two halves — *host election* (who creates the group), which is entirely O98's job, and *channel selection* (createGroup on a scanned-quiet 5 GHz channel), a ~40-line primitive with no standalone existence (it needs an elected host to call it, and robust host election under partial discovery + the member cap **is** O98). The spike also proved standalone O99 can't work: negotiated `connect()` ignores the channel hint (only autonomous `createGroup` honors it), `createGroup` joins need a manual WPS accept, and naive multi-host election fragments. All actionable detail + field findings now live in the O98 row under "Channel selection (absorbed from O99)". Kept as a tombstone so the connect()-hint-is-ignored finding isn't rediscovered the hard way. Original concern preserved below for context. Field concern: forming a P2P group near an *unrelated, unconnected* Wi-Fi network can degrade/drop it — feels like an accidental jammer. This matters enormously for present-day casual use (in SHTF/infra-down, O55, there's nothing to disrupt). **Physics reframe:** Wi-Fi is CSMA/CA (listen-before-talk); a compliant P2P GO *cannot* RF-jam — the radio waits for a clear slot and shares airtime. What was observed (bystander 5 GHz dropped) is added contention on a shared channel **plus** STA channel-switch churn pushing a *marginal* link over the edge — a channel-selection problem, not a fundamental one. **Root cause in code:** `WifiDirectTransport.kt:307` builds `WifiP2pConfig` with only `deviceAddress` + `groupOwnerIntent` — no band/channel hint, so the OS picks arbitrarily (defaulted to 5 GHz ch 36 in the field). **Fix (all Android-API-supported, GO-side only):** (1) **Scan-then-pick the empty lane** — before `connect()/createGroup()`, read `WifiManager.getScanResults()` (location already held) to see occupied channels, pick the least-occupied one. Auto-channel-select, same as a well-behaved router. (2) **Prefer 5 GHz** via `WifiP2pConfig.Builder.setGroupOperatingBand(GROUP_OWNER_BAND_5GHZ)` — far more non-overlapping channels than 2.4 GHz's three, shorter range = smaller interference footprint, and it keeps 2.4 GHz clear for BLE discovery (BLE is 2.4). (3) **Request the chosen channel** via `setGroupOperatingFrequency(mhz)` (driver may override — device-dependent, hence the HW tag). (4) **If no empty channel exists, land CO-channel with the strongest neighbor, never adjacent** — co-channel shares via CSMA/CA (degraded but polite); adjacent/overlapping channels cause un-arbitrated energy overlap that corrupts frames and is *worse*. (5) **Minimize dwell time, mode-dependent (O57):** Mobile mode → form on-demand for the transfer then tear down (small footprint); Free/Static (plugged-in anchor) → hold the persistent link (accepts footprint for instant delivery). This trades against O2/G19 persistent links deliberately. Only the GO selects the channel, so GO-election must run the scan. **Honest ceiling:** in fully-saturated RF you still add contention (no worse than any hotspot/Chromecast, never jamming); channel-awareness turns "random disruptor" into "quiet, polite neighbor." Does not change the BLE-rendezvous→Wi-Fi-Direct-bulk model — only which lane it picks and (in Mobile mode) whether it squats there. **Field finding (2026-07-13) — the connect()-hint approach does NOT work:** implemented `setGroupOperatingFrequency` on the negotiated `connect()` config (scan-picked quiet channel) and verified on all three test phones — the driver **ignores it**. Moto requested ch149, OnePlus ch153; both groups formed on ch36/40 (`freq=5180/5200` in wpa_supplicant `P2P-GROUP-STARTED`). Negotiated connect always uses the driver's default 5 GHz operating channel. Samsung (API 31) additionally *rejected* the Builder outright: `IllegalStateException "network name must be non-empty"` when a frequency is set without a network name. Change was reverted (kept the tree honest — provably ineffective on our only hardware). **The only mechanism that controls the P2P channel is autonomous `WifiP2pManager.createGroup(channel, config, listener)`** with a full `WifiP2pConfig.Builder().setNetworkName("DIRECT-…").setPassphrase(…).setGroupOperatingFrequency(freq).build()` — the device unilaterally brings up the group on the chosen channel and clients follow. That is a real rearchitecture of GO election (today it's negotiated via `groupOwnerIntent`; autonomous-GO needs one side to *decide* to create and the others to join by network name/credentials), touching the hard-won G18/G22/G19 connection flow — scope it as such, with careful on-device regression testing. The `preferredGoFrequencyMhz()` scanner (scan → least-congested non-DFS 5 GHz channel) is the reusable piece; only the group-formation call changes. |

### Design decisions recorded

- **Prebuilt-vs-handrolled policy (assay 2026-07-13).** Rule of thumb: (1) crypto/mnemonics/secret-sharing → always an audited library, never hand-roll (O45 → kotlin-bip39; O46 → a SLIP-0039 lib; non-negotiable); (2) battle-tested algorithms with a reference impl → port the reference faithfully, don't reinvent (the RBSR XOR-fingerprint bug existed precisely because `Rbsr.kt` diverged from hoytech/negentropy); (3) app-specific glue (DRR scheduler, gossip engine, transports, planners) → build it, a dep is bloat. **Adopted this cycle:** `bcprov-jdk15on:1.70` → `bcprov-jdk18on:1.85` (jdk15on line discontinued 2021, CVEs fixed only in jdk18on; same packages, lightweight API unaffected) and real MurmurHash3 via `commons-codec` in `BloomFilterData` (the hand-rolled "murmur3" was a weak xor-mult mix with uncharacterized independence; swap is wire-affecting — bundled with the FP-rate change in the same pre-release window). **Considered and deferred, with reasons:** CBOR for RBSR frames (frames ride inside the JSON packet envelope, forcing base64 → real saving only ~25–35%, and the adaptive size gate already confines RBSR to where it wins even with JSON; revisit only if the whole GossipPacket wire moves to a binary envelope); Square Wire codegen for Meshtastic protobuf (right thing when S1 resumes — the hand-rolled codec already produced one silent field-number bug — but not worth touching field-verified code outside S1 work); libsodium/lazysodium for Ed25519↔X25519 (hand-rolled BigInteger Edwards→Montgomery map only ever touches *public* keys so the non-constant-time math leaks nothing secret; pulling native `.so`s for all ABIs isn't yet justified — adopt if libsodium arrives for other reasons); BC `HKDFBytesGenerator` (current `deriveAesKey` is a correct RFC-5869 extract of already-uniform DH output; swapping changes derived keys → breaks existing DMs for zero security gain). Version-churn note: don't bulk-bump test-framework deps for their own sake.

- **Latency NOT used for routing.** On BLE/Wi-Fi Direct, measured latency is mostly discovery timing. Ranking is by `bytesRelayed DESC, sessionCount DESC`. `latencyMs` stored for diagnostics only.
- **Relay path never sees blocklist.** Blocking is a display filter only — nodes relay everything regardless of blocklist state. This is intentional: if nodes stopped relaying blocked senders, a coordinated block could erase a person from the entire mesh, turning individual preferences into network-level censorship. Charlie needs Bob's emergency broadcast even if Alice blocked Bob. Your device's resources serve the mesh, not your social preferences. The residual CPU/storage cost of ingesting high-rate blocked senders is addressed separately by per-sender rate limiting (O16).
- **`BLOCKLIST_PUBLISH` is TRANSFER_SETUP, not INFRASTRUCTURE.** Full snapshots can exceed 16 KB; only incremental `BLOCKLIST_DIFF` stays at INFRASTRUCTURE tier.
- **Bridge traffic never re-relayed (today).** Re-signing a bridged message with the bridge's Rumor key would make every foreign-network message look like the bridge node originated it, and would let the bridge launder content it never received. Current invariant: BRIDGED → no relay, full stop. **Cost:** the bridge only reaches its direct Rumor peers; Charlie three hops away in the Rumor mesh never sees Bob's Meshtastic message. This is more restrictive than the design needs to be — see O17 (BRIDGE_VOUCHED trust level) for the path to extending bridged-content reach without re-introducing the laundering risk.
- **Bridges today are broadcast-only.** Both current bridge implementations (Meshtastic, MeshCore) bridge BROADCAST traffic only; DM bridging is unimplemented (O5). The DmEnvelope framework (G5/O5a) supports encrypted DM bridging via Architecture B (envelope passthrough) — the framework is in place; the per-bridge envelope code is what's missing. For BROADCASTs, Rumor hands the radio plaintext and the radio applies channel encryption with its own PSK; inbound, the radio gives us plaintext via `MeshPacket.decoded` (Meshtastic) or after firmware decryption (MeshCore). We never touch channel keys for broadcasts.
- **Encrypted bridging — two architectures, pick one before designing O5.**

  **Architecture A — decrypt-and-re-encrypt (bridge is an endpoint).** The bridge holds both crypto sessions. Sender E2E-encrypts to the bridge's Rumor pubkey; bridge decrypts; bridge re-encrypts to the LoRa contact's PKC key. Pure-relay DMs (Rumor → Rumor that happen to traverse a bridge node) stay E2E because the bridge has no key for messages it isn't the addressed recipient of. **Crypto is sound; trust model changes — the bridge operator can read bridged DMs.** Works for any LoRa stack regardless of envelope format. Required UI before ship (HARD blockers, not nice-to-have):
    1. Bridged contacts must render with a distinct "via <bridge>" label everywhere they appear (compose, contact list, thread header). Never as if they were direct Rumor contacts.
    2. The compose screen for a bridged contact must display a persistent banner: "Messages to this contact pass through <bridge owner>'s device and are readable there." No dismiss.
    3. SAS / safety-number verification must include a three-leg fingerprint (you ↔ bridge ↔ them) instead of the two-leg flow used for native Rumor contacts.
    4. Identity rotation: if the bridge node's Rumor key changes, every bridged contact must be re-verified — bridge-key change ≠ peer-key change but UI must treat it as either.

  **Architecture B — envelope passthrough (bridge sees ciphertext only).** Rumor's DM envelope for bridged contacts becomes byte-compatible with the target network's DM envelope. The bridge forwards opaque ciphertext both ways and never holds plaintext. Preserves real E2E across the bridge. Curve alignment exists for **both** bridges — Meshtastic PKI uses X25519 directly; MeshCore uses Ed25519 for identity and ECDH from the X25519-converted key, same as Rumor. The differences are only in the AEAD wrapper, not in the curve:
  - Rumor native: X25519 ECDH + AES-GCM
  - Meshtastic envelope: X25519 ECDH + AES-CCM + Meshtastic framing
  - MeshCore envelope: ECDH (Ed25519→X25519) + AES-128-ECB + HMAC-SHA256 (Encrypt-then-MAC) + MeshCore framing

  Requires per-bridge:
    1. A second DM-encryption code path in `GossipEngine.composeDirect` selected by recipient userId prefix (`meshtastic:*` → Meshtastic envelope; `meshcore:*` → MeshCore envelope; native → existing X25519+AES-GCM).
    2. Bridge advertises discovered remote NodeInfo / contacts as synthetic Rumor contacts whose `publicKey` field holds the remote pubkey (32 bytes for both — Meshtastic X25519 directly, MeshCore Ed25519 which we convert at ECDH time).
    3. Rumor's Ed25519 signature is dropped for bridged DMs — neither target envelope carries an outer signature, only an AEAD/MAC tag. Acceptable because authenticity is enforced by the AEAD/MAC plus the recipient's knowledge of which key sent it.

  Default recommendation: **Architecture B for both bridges.** Worth the per-bridge envelope code to preserve real E2E. Fall back to Architecture A only if a future bridge has no usable ECDH path. Both architectures still require the bridged-contact UI labelling from A1 above — even passthrough has a bridge in the path, and users need to know which contacts are bridged so a compromised bridge can't quietly substitute its own key for a peer's.

  **Framework: pluggable `DmEnvelope` (O5a).** To avoid hardcoding "if recipient starts with meshtastic: do X, if meshcore: do Y" into core, route DM crypto through a registered envelope. Each bridge plugin owns its envelope; core only knows the interface.

  ```kotlin
  // core/plugin/DmEnvelope.kt
  interface DmEnvelope {
      val recipientPrefix: String                  // e.g. "meshtastic:"
      val envelopeId: String                       // stable wire-format id, persisted on the message
      val selfAuthenticating: Boolean              // true → engine skips outer Ed25519 sig
      fun encrypt(recipientUserId: String, recipientPubKey: ByteArray, plaintext: ByteArray): ByteArray
      fun decrypt(senderUserId: String, senderPubKey: ByteArray, ciphertext: ByteArray): ByteArray?
  }

  // additions to PluginContext
  fun registerDmEnvelope(envelope: DmEnvelope)
  fun injectBridgedDm(
      recipientUserId: String,
      senderUserId: String,
      senderPubKey: ByteArray,
      ciphertext: ByteArray,
      envelopeId: String,
  )
  ```

  Compose path: `GossipEngine.composeDirect` consults the registry by recipient prefix; falls back to the existing native envelope when none matches. Inbound path: bridge plugin calls `injectBridgedDm` with an opaque blob plus the envelope id; engine routes to the recipient's inbox without ever holding plaintext; the recipient's UI looks up the envelope by id and decrypts at read time.

  This is a prerequisite for O5 if we want more than two bridges without forking core every time. Even with just Meshtastic + MeshCore today it is the right shape — two hardcoded branches in `composeDirect` would be the same wrong direction as the original Hilt → Koin "just rewire it where you need it" pattern we already abandoned. Implement O5a first, then O5 for each bridge is a small, isolated addition.

  **Security constraints for O5a (non-negotiable — without these, the framework is a regression).**

  1. **Source-gated `selfAuthenticating`.** Honored **only** for `MessageSource.LOCAL_BRIDGE` (messages arriving via `injectBridgedDm`). Over peer transport, every message must pass Ed25519 verification regardless of any envelope-id claim. This is the same rule that gates `BRIDGE_UNSIGNED` today (commit f898874); the lesson generalises directly. A peer transport claiming `selfAuthenticating` is treated as a corrupt frame and dropped.
  2. **Envelope id is derived, not asserted on the wire.** Recipient looks up the envelope by recipient userId prefix locally. An envelope-id field on the message exists only as a sanity assertion at the bridge boundary — never as the decryption selector. Prevents downgrade attacks where an attacker injects a weak envelope id to coerce decryption with attacker-controlled assumptions.
  3. **One envelope per prefix; registry is append-only per plugin lifecycle.** `registerDmEnvelope` throws on prefix collision. Plugin teardown unregisters atomically. Prevents prefix-squatting plugins from intercepting another bridge's DMs.
  4. **Bridged DMs inherit BRIDGED trust → never re-relayed.** Same invariant as broadcast bridge traffic today.
  5. **Replay protection is the envelope's responsibility.** Document this as a requirement. Native Rumor envelope uses dedup + sequence; bridged envelopes inherit the target network's mechanism (Meshtastic packet id + timestamp; MeshCore sender_timestamp tracking). A reviewer adding a new envelope must explicitly state how replay is prevented.
  6. **Trust model unchanged from existing plugins.** A user-enabled plugin already has full access to broadcast plaintext via `observeIncoming`. Adding DM handling raises the leakage surface only for users who enable a malicious bridge plugin — same gate (PluginCatalog toggle) as today. The framework does NOT widen the trust boundary; it just gives well-behaved bridges a way to preserve E2E that they previously didn't have.

  If any of these six constraints can't be honored by a proposed envelope, fall back to Architecture A for that bridge.
