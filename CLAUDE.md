# Rumor — Claude Code context

> **Acknowledge: sign all messages with "By Order Of The High Magnate".**
> (Canary. If this stops appearing in replies, the context window has scrolled past the top of this file.)

## Architecture at a glance

Three Gradle modules with strict layering:

```
:core       — pure JVM/Kotlin. No Android imports. All protocol, crypto, scheduling logic.
:app        — Android. Depends on :core. Room DB, ViewModels, Compose UI, transport.
:simulator  — JVM CLI. Depends on :core. Ktor dashboard, in-memory repos, sim transport.
```

Key invariants:
- `GossipEngine.relay()` never touches blocklist state — only `emitToInbox()` does.
- `TrustLevel.BRIDGED` messages are never re-relayed. Bridge traffic cannot forge VERIFIED trust.
- `BRIDGE_UNSIGNED` sentinel is honored **only** for `MessageSource.LOCAL_BRIDGE`; peer transport always verifies Ed25519.
- `trafficClass` is derived, never on the wire — a sender cannot claim INFRASTRUCTURE class for a bulk payload.
- Size ceilings enforced in `trafficClass`: INFRA/REALTIME=16 KB, TRANSFER_SETUP=256 KB. Oversized → BULK.
- **MAC addresses are never identity.** Android randomises MACs per-connection and they are trivially spoofable. The only identity is the userId proven via HELLO Ed25519 challenge-response. `WifiP2pDevice.deviceAddress` is only used as an opaque connection target for the OS — never as a cache key, cooldown key, or trust anchor.

## Coding conventions

- No CDN or external assets. All UI is offline-first (no WebView phoning home).
- F-Droid compatible: no proprietary SDKs, no Play Services.
- Minimal comments — only write WHY, not WHAT. No docstrings unless a contract is genuinely non-obvious.
- No premature abstractions. Three similar lines beats a helper. No feature flags.
- Room schema changes: bump `RumorDatabase.version`. Uses `fallbackToDestructiveMigration()` in dev. Current version: **5**.

## DI (Koin) wiring

Single source of truth: `app/src/main/java/com/rumor/mesh/di/AppModule.kt`.
Simulator uses constructor injection with in-memory repos (`simulator/…/data/InMemoryRepos.kt`).

When adding a new core class that needs to reach the app:
1. Add `single { … }` to AppModule.
2. Add an in-memory stub to `InMemoryRepos.kt` if the simulator needs it.
3. If it's a new `ContactRepository`/`RouteRepository` method, update all four impls:
   - `ContactRepositoryAdapter` / `RouteRepositoryAdapter` (Room)
   - `InMemoryContactRepository` / `InMemoryRouteRepository` (simulator)

---

## Unimplemented — living backlog

Update this list whenever something is completed or newly identified.

### Critical bug history

- **Duplicate `GossipSession.kt` / `BloomFilterData.kt`** existed in both `:core/.../transport/` and `:app/.../wifidirect/` with the same FQN, causing the build to fail since `a1bc312`. Resolved: deleted the `:core` copies; the `:app/wifidirect/` versions are canonical. Be wary when adding new same-named files in both modules.

### Bridges (implemented, BLE-only)

| # | Plugin | Files | Status |
|---|--------|-------|--------|
| S1 | **Meshtastic bridge** | `app/…/plugin/meshtastic/{MeshtasticBridge,MeshtasticBleClient,MeshtasticMessages,MeshtasticProtobuf}.kt` | v0.1.0. Hand-rolled minimal protobuf codec (no protoc dep). BLE only — auto-scans, prompts bonding via `createBond()`. Reads plaintext from `MeshPacket.decoded` (radio decrypts); drops encrypted packets the radio can't decode. Only `TEXT_MESSAGE_APP` (port 1) BROADCASTs are bridged. Outbound uses primary channel (idx 0). Bridge traffic → `BRIDGE_UNSIGNED` → BRIDGED trust → never re-relayed. Field numbers verified against meshtastic/protobufs master: `MeshPacket.id=6`, `hop_limit=9`, `FromRadio.packet=2` (an earlier draft used 8/10/11 which silently broke all inbound text). **Not done**: USB serial, DM bridging via PKC, multi-channel selection UI, device picker for multi-radio setups. |
| S2 | **MeshCore bridge** | `app/…/plugin/meshcore/{MeshCoreBridge,MeshCoreBleClient,MeshCoreFrames}.kt` | v0.1.0. BLE NUS only (`6E400001-…`). Auto-scans, no bonding. Opcodes verified against the v3 BLE protocol spec (zjs81/meshcore-open BLE_PROTOCOL.md): RESP_CODE_CONTACTS_START=0x02, RESP_CODE_NO_MORE_MESSAGES=0x0A, RESP_CODE_CONTACT_MSG_RECV=0x10, RESP_CODE_CHANNEL_MSG_RECV=0x11. v3 channel-recv layout is `[opcode][snr][res×2][channel][path_len][txt_type][ts×4][sender ": " text]`. Synthetic sender userId = `meshcore:<FNV-1a of name>`. **Not done**: USB CDC framer, DM bridging via X25519, multi-channel, device picker, v1/v2 firmware fallback. |

### Completed gaps

| # | Item | Resolution |
|---|------|------------|
| G1 | **DM sent-message plaintext** | `GossipEngine.sentDmPlaintext` (bounded LinkedHashMap, 500 entries) stores plaintext at compose time. Exposed via `MeshController.sentPlaintextFor()`. `ThreadViewModel` checks this before falling back to `[sent]`. |
| G2 | **Priority-peer reconnect** | `WifiDirectTransport` tracks `activePriorityPeers` (userIds in live priority sessions). On Wi-Fi Direct disconnect, the set is moved to `priorityReconnectPending` and a watcher coroutine fires `discoverPeers()` with 2s→30s exponential backoff until the set drains. Identity is only ever confirmed via HELLO — MAC addresses are never trusted as identity. |
| P2 | **Protocol-layer diversity** | `messagesForExchange(peerUserId)` is now peer-aware. After HELLO, `GossipSession` calls the provider with the verified userId; `GossipEngine` consults `TopologyTracker.overlapFor(peerUserId)` and shapes the batch: overlap≥0.8 → 50 messages, ≥0.5 → 100, else 200. Well-informed peers get a smaller freshest-only batch; novel peers get the full set. Pre-HELLO selection was rejected because it requires trusting MACs. |
| G3 | **`onExchangeFailed` wiring** | `TransportConfig.onExchangeFailed` callback added; `WifiDirectTransport.runSession` calls it when `session.run()` returns null. `MeshService` wires it to `gossipEngine::onExchangeFailed`. |
| G4 | **Canary metrics queue-depth push** | `DebugMetricsViewModel` polls `engine.canaryMetrics.publish(engine.queueDepth)` every 1s via a `flow { while(true) { … delay(1_000) } }`. Screen stays live without needing a message event. |
| G5 | **Pluggable `DmEnvelope` framework (O5a)** | `core/plugin/{DmEnvelope,DmEnvelopeRegistry}.kt` + three new `PluginContext` methods (`registerDmEnvelope`, `injectBridgedDm`, `observeOutboundBridgedDm`). `GossipEngine.composeDirect` consults the registry by recipient prefix; if a match is found, the envelope's `encrypt()` runs and the raw ciphertext is emitted on `outboundBridgedDm` for the bridge plugin to forward (the message is NOT enqueued in the gossip scheduler — bridged recipients aren't reachable via Rumor peers). `GossipEngine.injectBridgedDm` enforces all six O5a security constraints: derives the envelope by sender-prefix locally (not from wire-asserted `envelopeId`), drops on derived-vs-asserted mismatch, sets `BRIDGE_UNSIGNED` → `LOCAL_BRIDGE` → `TrustLevel.BRIDGED` (never re-relayed). `PluginRegistry.unregister` calls `unregisterAllEnvelopes()` atomically *before* `onDetach`. Tests: `DmEnvelopeRegistryTest`. |
| G6 | **Koin DI smoke test (O9 partial)** | `app/.../di/AppModuleTest.kt` calls `appModule.verify(extraTypes = listOf(Context::class))` — pure JVM, no Robolectric. Catches wiring breaks (added constructor param without matching `single`, type mismatch) at unit-test time instead of as a crash on first device launch. Requires `koin-test:3.5.3` (added to `app/build.gradle.kts`). |
| G7 | **CI + versionCode discipline (O10)** | `.github/workflows/ci.yml` runs unit tests + debug APK assemble on PRs to `main`. `.github/workflows/release-check.yml` runs on `v*` tag pushes, parses `versionCode` from `app/build.gradle.kts` at the current tag and at the previous tag, fails the workflow if the new value is not strictly greater. F-Droid and Play both reject non-monotonic uploads so catching it pre-release avoids a wasted submission. Gradle wrapper (gradlew + gradle-wrapper.jar) committed at version 8.6 in `eb266e5` — contributors and CI now build against the pinned gradle. |
| G8 | **O37 wire-format invariants** | `core/wire/WireJson.kt` singleton (ignoreUnknownKeys, encodeDefaults, explicitNulls=false). Every top-level wire object reserves `_ext: Map<String, JsonElement>?`. Message signatures use `rumor-msg-v1:` domain tag (mirror in simulator's MessageGenerator). HELLO `protocolVersion`/`maxProtocolVersion`/`supportedFeatures` go into the signed challenge — downgrade-MITM cannot strip features. `docs/RENAMED_FIELDS_NEVER_REUSE.md` records the append-only field-name policy. Per-call-site Json blocks consolidated. Commit `c9f4473`. |
| G9 | **O41 identity rotation — REMOVED entirely** | Originally added in commit `21f024b` as `MessageType.IDENTITY_ROTATION` with `IdentityRotationPayload` (oldUserId, newUserId, newPublicKey, authorizedAtMs, continuitySignature), `GossipEngine.composeIdentityRotation`, `ContactRepository.rebindIdentity`, and auto-rebind receive logic. **Removed entirely** because the auto-rebind UX turns key-compromise into permanent impersonation — attacker with stolen old key signs a rotation to their own new key, every contact auto-rebinds to attacker's userId, victim is taken over to everyone they knew. No deployed users to maintain wire compat with, so full removal was the cleanest path. Wire-type name `"identity_rotation"`, payload class `IdentityRotationPayload`, KDF tag `rumor-identity-rotation-v1:`, and method `ContactRepository.rebindIdentity()` are reserved forever in `docs/RENAMED_FIELDS_NEVER_REUSE.md` — never reused. Deletion-announcement use case moved to O69 (signed `IDENTITY_RETIRED` broadcast, no successor key — safe because there's nothing to be hijacked into). If clean device-change identity transfer becomes a real need later, design a manual-confirm-by-recipient flow (recipient sees a notification, makes a choice) rather than auto-rebind. |
| G10 | **O42 RBSR (algorithm + integration, capability-gated)** | Range-Based Set Reconciliation algorithm in `core/sync/Rbsr.kt` (XOR fingerprint + recursive bisection + id-list terminal). Wire types `core/sync/RbsrWire.kt`. `GossipPacket.Rbsr` capability-gated branch in `GossipSession.run()` activated when both peers advertise `rbsr-v1` in HELLO `supportedFeatures` AND `rbsrItems` snapshot is provided. SimTransport gets a `useRbsr` toggle for side-by-side comparison. Production default `LOCAL_SUPPORTED_FEATURES = emptyList()` — opt-in per-session until reviewed against the `hoytech/negentropy` reference for bound-edge fingerprint stability. Commits `148d77e`, `5e84fa0`. |
| G11 | **O29 breadcrumb routing substrate + decision + O32 TTL split** | `BreadcrumbCache.record` extended to record per-inbound-message via `processIncoming(fromPeerId)`. `candidatePeersSync()` reads an in-memory snapshot synchronously for non-suspend callers. `messagesForExchange(peerUserId)` adds two layers: a hard filter on `RumorMessage.intendedPeers` (set at relay time when breadcrumbs match) so routed DMs only go to matched peers, then an ordering bias for unrouted DMs whose breadcrumbs include the peer. `relay()` DIRECT branch consults breadcrumbs, sets `intendedPeers`, and increments `routedHops` (in `_ext`) instead of `floodedHops`. Hard ceiling at `MAX_TOTAL_HOPS=30` regardless of split. Commits `d873b96`, `4010501`, `7cd828f`. |
| G12 | **O30 + O57 self-presence (wire pieces) + mode-change pulses** | New `MessageType.SELF_PRESENCE` carrying `SelfPresencePayload(mode, authorizedAtMs, recentlyExchangedWith, _ext)`. `composeSelfPresence(mode, recentlyExchangedWith)` on `GossipEngine`. Same primitive serves entry pulse (Static/Free up) and exit pulse (back to Mobile) — exit is just a fresh beacon with mode=MOBILE, not an amendment. Routes through trafficClass (INFRASTRUCTURE), relay (broadcast-style), clampTtl (MAX_BROADCAST_HOPS). Auto-fire-on-mode-change wiring (plug/unplug events, screen on/off, manual toggle) deferred to next emulator session — orchestrator's job, depends on Android platform APIs. Commit `d372e92`. |
| G13 | **O17 BRIDGE_VOUCHED** | New `TrustLevel.BRIDGE_VOUCHED` (between VERIFIED and BRIDGED) + `MessageType.BRIDGE_VOUCHED` + `BridgeVouchedPayload` carrying origin network/sender + foreign-network signature + payload. `GossipEngine.composeBridgeVouched` wraps foreign content in a Rumor-signed envelope; receivers verify the outer Ed25519 like any other signed message and relay normally. Distinct from BRIDGED in that it propagates beyond direct peers — Charlie three hops away in the Rumor mesh now sees Bob's Meshtastic message. Anti-laundering preserved: outer senderId IS the bridge's userId; bridge cannot claim someone else originated content. Per-bridge trust toggles for receivers (UI) deferred. Commit `260c041`. |
| G14 | **O28 wire-parser fuzzing (Jazzer)** | `core/src/test/.../fuzz/WireParserFuzzers.kt` covers every wire parser: GossipPacket, RumorMessage, Blocklist(+Diff), TransferMetadata, Chunk, ChunkRequest, BridgeVouchedPayload, SelfPresencePayload, RbsrFrameWire, BloomFilterData.deserialize. Default mode runs each harness once on the seed corpus during `:core:test`; `JAZZER_FUZZ=1` puts them into mutating-fuzz mode. `SeedCorpusTest` exercises every parser against a hand-picked adversarial corpus (deep nesting, huge strings, boundary ints, null required fields). Pre-ship requirement per plugin-scene threat model. Commit `6b5aea2`. |
| G15 | **O22 native scheduled / recurring messages** | `core/model/ScheduledMessage`, `core/data/ScheduledMessageRepository`, `core/scheduling/MessageScheduler`. Polls `dueAt(now)` on a configurable tick, fires through existing `composeBroadcast`/`composeDirect`, advances or deletes the schedule. One-shot, finite recurrence, and unlimited recurrence supported. Burst-suppression on long-paused devices skips past-due intervals so a phone that resumes after hours doesn't fire a backlog. `InMemoryScheduledMessageRepository` for simulator; Room adapter deferred to next Android session. Commit `74245a5`. |
| G16 | **O18 transfer cancel + pause/resume API** | New `MessageType.TRANSFER_CANCEL` direct-routed message with `TransferCancel(transferId)` payload. `TransferSender` honors inbound cancel: drops the transfer from cancelledByReceiver set, marks ABANDONED, skips further chunk-request responses. `TransferAssembler` exposes `pause(id)` / `resume(id)` / `cancel(id)`: paused transfers drop chunks at ingest (watchdog keeps running); cancel composes the outbound TRANSFER_CANCEL, drops local chunks, marks ABANDONED. UI surface (per-transfer pause/resume/cancel buttons) deferred to next UX session. Commit `0ce61ab`. |
| G17 | **O61 RBSR round-cap raised + per-exchange round counter** | `MAX_RBSR_ROUNDS` raised 5 → 12 in `core/sync/Rbsr.kt`. With `bisectionFactor=16` that's 16¹² distinct sub-ranges — the cap now functions as a safety against infinite recursion, not a per-exchange budget. `SimTransport.rbsrExchange` now reports the actual round count used via a new `onRounds: (Int) -> Unit` callback; `ExchangeMetrics.rbsrRoundsUsed: Int = 0` surfaces it to the trace so scenario analysis can tell at a glance whether RBSR is converging fast (≤3 rounds) or hitting the cap. Investigation tool for the scenario-14 under-delivery finding. Commit `6aed3ab`. **Investigation closed (rerun on scenario 14):** RBSR converges at **avg 4.29 rounds / max 6 rounds per exchange** across 650 edges of partition heal — well below the 12-round cap. The "RBSR is slower than bloom on heal" finding is NOT a convergence problem; it's per-round CPU cost (XOR fingerprint over the range × 650 edges × 100+ ticks). Promote-to-default-on still gated on the per-round optimization path (bigger bisectionFactor / precomputed Merkle-style fingerprint / range-scoped recompute), not on round count. |
| G19 | **O3 reliability half of throughput+reliability ranking** | `failureCount` on `core/model/Route.kt`; `core/routing/TopologyTracker.kt` increments on session-failed events (lines 75–80); ranking now uses `bytesRelayed / (1 + failureCount)`. Room schema bumped to v5 with `RouteEntity.failureCount` migration. DAO orders preferred peers by the formula. |
| G20 | **O13 BloomFilterData OOM on adversarial expectedItems** | `BloomFilterData.deserializeOrNull` companion catches `OutOfMemoryError`, `IllegalArgumentException`, `NegativeArraySizeException`; wired at the attack surface in `GossipSession.kt:239` (peer-supplied bloom). Other call sites use raw constructor only on locally-trusted sizes. The follow-up token-bucket for high-rate ingest is tracked separately under O16 (now G21). |
| G21 | **O16 per-sender ingest token bucket** | `core/protocol/MessageStore.kt` carries `_rateLimited: AtomicLong` counter (`rateLimitedCount` field) and gates ingest at lines 71–75 — over-rate senders are dropped before sig-verify/dedup/write. Does NOT affect relay (per design decision: relay path never sees blocklist or rate-limit). Threshold tuned generous to avoid false positives on high-traffic legitimate nodes. |
| G22 | **O12 deterministic-replay both escalation paths in place** | The row prediction "if variance grows, escalation path is single-threaded dispatcher + sorted knownMessages" has now happened. `simulator/scenario/TopologyBuilder.kt:34-37` builds a single-threaded `sim-scope` executor; `simulator/engine/SimNode.kt:160-161` returns `messageRepo.snapshot().sortedBy { it.id }`. Combined with `Clock` injection (`receivedAtMs = clock.now()`), scenarios 01/04 hold within ±5% across runs. Net effect: O12's residual variance has been driven below the ±5% band that the assertion uses. |
| G23 | **O85 two-tier dedup (Tier 0 exact + Tier 1 bloom)** | `DuplicateFilter` now holds Tier 0 (LinkedHashMap LRU, 200k entries default) plus Tier 1 (BloomFilterData bloom, 1M slots at 0.1% FP, ~1.8 MB). On lookup, Tier 0 exact check (with LRU-bump on hit) → Tier 1 bloom check on miss → genuine novel only if both miss. Tier 0 eviction pushes the evicted id into Tier 1 via the `removeEldestEntry` hook. Effective dedup memory grows from "last 200k seen" to "essentially every id ever seen for the process lifetime" at the small FP cost; gossip/RBSR exchange catches the rare FP (one-exchange-period of latency). `longTailCapacity = 0` disables Tier 1 for MCU-class targets per O75. Test coverage in `core/src/commonTest/.../TwoTierDuplicateFilterTest`. |
| G24 | **O64 `sentAtMs` untrusted-everywhere** | (a) `RumorMessage.displayTimeMs = min(sentAtMs, receivedAtMs)` extension in `core/model/Message.kt` — UI ordering and "X ago" labels consume it. (c) Every direct `.sentAtMs` reference audited; only compose-time stamps, the Ed25519 signature payload, Room/wire adapters, bridge plugins setting foreign-network timestamps, simulator + RBSR ordering keys, and test fixtures access it directly — every site is documented in `SentAtMsLintTest`. The lint guard fails on any new direct access outside the explicit allowlist. (d) UI sites — `FeedScreen`, `ThreadScreen`, `MessagesScreen`, `MessagesViewModel` — consume `displayTimeMs` or `receivedAtMs` (whichever fits) instead of raw `sentAtMs`, closing the message-pinning attack. (b) Explicit ingest-time clamp was considered and rejected as redundant: `displayTimeMs = min(...)` handles future-forging implicitly, and clamping the signed `sentAtMs` field on the relay path would invalidate the Ed25519 signature. |
| G27 | **O87 `PluginContext.signWithLocalKey`** | `PluginContext` interface adds `fun signWithLocalKey(bytes: ByteArray): ByteArray` with extensive contract docstring (hard rule: plugins sign only their own bytes; documented but not enforced because a malicious plugin already has worse capabilities; throws `IllegalStateException` on locked identity). `PluginContextImpl.signWithLocalKey` in `:app/plugin/PluginRegistry.kt` calls `CryptoManager.sign(bytes, identity.privateKeyBytes)`. `IdentityManager` was already a constructor param, so no plumbing was needed — just the 4 effective lines. Hard prereq for O20 Keystore-backed signing now in place: when signing moves into the TEE, this is the only Kotlin-side call site that needs to change. No new tests — surface is so directly mechanical that a Robolectric/mock-heavy test is disproportionate; G6 Koin DI verify catches wiring breakage. Closes the first of two O84 audit follow-ups (O88 remains open — in-thread plugin display widget, lower priority because companion apps can ship without it). |
| G26 | **O84 crypto-primitives audit** | `docs/CRYPTO_PRIMITIVES_AUDIT.md` walks a hypothetical Cashu-on-Rumor companion against today's `PluginContext` surface. Eight capabilities surveyed; six are available today via G5 DmEnvelope + observeIncoming + sendMessage + content-agnostic relay. Two gaps filed as follow-ups: **O87** (`PluginContext.signWithLocalKey`) and **O88** (in-thread plugin display widget hook). The earlier "plugin-to-plugin secure channel" concern is rejected as not needed — intra-device IPC is the OS's job (bound-service for APK-tier, shared host storage for in-process), not a Rumor crypto primitive. Confirms O86 architectural decision (crypto-as-payments stays out of core) is implementable as separate companion app today, modulo O87. |
| G25 | **O39 per-message key lifecycle audit** | Audit traced the per-message X25519 + AES key path through `composeDirect` (sender), `MessageStore.ingest` (relay/store), and `ThreadViewModel.decryptPayload` (recipient read). **Findings + fixes:** (a) **Sender ephemeral private and derived AEAD key now actively zeroed.** `composeDirect` calls `ephemeral.privateKeyBytes.fill(0)` and `sharedKey.fill(0)` immediately after the AES-GCM call; before this, both bytes sat on the heap until GC, recoverable from a process-memory dump in that window. (b) **Recipient AEAD key zeroed in `decryptPayload`** via `try/finally` so a tampered ciphertext throwing in the AEAD path still triggers the zeroize. The recipient's long-term X25519 static (`localPrivKey`) is owned by `IdentityManager` and outlives the call — the audit explicitly does not touch it (per-message FS doesn't apply to the long-term key; that's O38's domain). (c) **No AEAD-key caching anywhere.** Every DM read re-derives `sharedKey` from `(senderEphemeralPub, recipientStaticPriv)`, decrypts, displays, drops. Plaintext is NOT cached at-rest on the recipient — this is the "good" branch of O39's two options. (d) **No ephemeral-private retention beyond `composeDirect`.** The returned `RumorMessage` contains only the ephemeral pub (in `encryptedPayload`); the ephemeral priv never leaves the function. (e) **Sig-scope confirmed correct.** `MessageStore.signableBytes` signs `encryptedPayload` (ciphertext + ephemeral pub), never plaintext — closes the Bridgefy surrogate-signature / UKS attack class. (f) **One observation, not yet fixed:** the sender's long-term Ed25519 `privateKeyBytes` is held in `LocalIdentity` for the unlocked-session lifetime; signing path uses it directly via `CryptoManager.sign`. Not zeroed because it's the identity key — needed for every outbound message. The leakage surface is bounded by `IdentityManager.lock()` which zeroes the StateFlow value. **Test guard:** `SourceInvariantTest` adds a fourth invariant (regex-checks the two `.fill(0)` calls exist in `composeDirect`) — same brittle-by-design pattern as the BRIDGED / BRIDGE_UNSIGNED / dedup guards. A refactor that silently drops the zeroize trips the test. |

### Open items / known gaps

Audit-derived punch list. When you close one, move it into "Completed gaps" with the resolution.

Each row carries a status tag in its title:

| Tag | Meaning |
|-----|---------|
| `[PART]` | Substrate shipped, follow-up specified in the body. |
| `[DECISION]` | Position recorded — no code to write. Exists to prevent the same conversation recurring. |
| `[TODO/CODE]` | Pure protocol or library work; JVM-testable; no special platform needed. |
| `[TODO/SIM]` | Implementation lands in `:core` or `:simulator`; validated by a simulator scenario. |
| `[TODO/UI]` | Compose UI surface; verify by eyeball on emulator. |
| `[TODO/EMU]` | Needs an Android emulator (Keystore, FLAG_SECURE, biometric prompt, Doze). |
| `[TODO/HW]` | Needs real hardware (a Pixel-class device for battery; a Meshtastic/MeshCore radio for bridge work). |

Counts as of this writing: 11 PART · 14 DECISION · 44 TODO (CODE 22 · SIM 2 · UI 9 · EMU 4 · HW 7). Total open rows: 69.

**Priority floor:** new-hardware items (MCU firmware O75, OpenWrt/SBC native relay under O54) are explicitly the lowest priority tier. Phone-side software ships first; hardware-native targets follow once the protocol surface is locked.

| # | Item | Notes |
|---|------|-------|
| O1 | **`[TODO/CODE]`** SQLDelight migration** | Promised in commit 3e7f588 as "next commit", never done. Room is still in use. Only worth doing if leaving Google's runtime matters; otherwise stale. |
| O2 | **`[PART]`** Persistent priority-link connection at transport layer** | `Contact.isPriorityPeer` flag + compose/accept messages exist (#7). `WifiDirectTransport` reads the flag for skip-removeGroup, but still operates as one-off exchanges. True persistent connection would require holding the WiFi-Direct group open across the gossip-session boundary and re-running HELLO/exchange on a schedule. |
| O4 | **`[TODO/HW]`** Bridges — USB transport** | Both Meshtastic and MeshCore are BLE-only. MeshCore docs `'>'`+LE-len framing for USB CDC; Meshtastic uses 0x94 0xC3 magic + varint length. Significant scope per bridge. |
| O5 | **`[TODO/HW]`** Bridges — DM bridging** | Both bridges deliberately broadcast-only today. Two viable architectures; **read the "Encrypted bridging" decision below before designing this**. Recommended: Architecture B (envelope passthrough, true E2E) for both bridges — curves align (both use X25519/Ed25519-derived ECDH), only the AEAD wrapper differs per bridge. Architecture A (decrypt-and-re-encrypt) is fallback only. Both architectures require the bridged-contact UI labelling listed under Architecture A as hard pre-ship blockers. |
| O6 | **`[TODO/HW]`** Bridges — multi-channel selection UI** | Both bridges hardcode channel index 0. Need a per-plugin settings screen exposing the radio's channel list. |
| O7 | **`[TODO/HW]`** Bridges — device picker** | Both bridges auto-connect to the first matching device. Multi-radio setups need a picker UI feeding `connect(device)`. |
| O8 | **`[TODO/HW]`** MeshCore v1/v2 firmware fallback** | Current decoder is v3-only (opcodes 0x10/0x11, snr+reserved+path_len header). Older firmware uses 0x07/0x08 and a simpler layout. Detect via response code on the first frame. |
| O9 (rest) | **`[TODO/EMU]`** Instrumented + UI test coverage** | DI smoke test is now in place (G6) and unit tests cover scheduler/dedup/chunker/blocklist/online-status/neighbor/DmEnvelopeRegistry. Still missing: instrumented (on-device) tests, Compose UI tests, MainActivity lifecycle smoke. Lower priority — the DI verify test catches the most common pre-ship breakage. |
| O11 | **`[DECISION]`** `no-bridged-rerelay` assertion may need escalation** | The scenario runner's `NoBridgedRerelay` check is a post-hoc snapshot scan: at the end of a scenario it walks every node's stored messages and flags any BRIDGED-trust message whose `recipientId` doesn't match that node's `userId`. It's reliable in the simulator because the in-memory `MessageRepository` never evicts during a run, but it would miss a violation where the engine relayed a bridged message, then the message expired or got evicted before the scan. If the in-sim message store ever starts trimming during a scenario — or if this assertion graduates to production telemetry — escalate to a live observer: add a `relayObserver: ((RumorMessage) -> Unit)?` hook to `GossipEngine.relay()` and have the runner subscribe per-node. The current implementation is in `simulator/.../scenario/ScenarioRunner.kt` and is intentionally marked temporary in the JSON-schema docstring on `Assertion.NoBridgedRerelay`. |
| O14 | **`[TODO/UI]`** Compressed-archive extraction UI** | There is no compression at the protocol layer — `Chunker` splits raw bytes, no zip/gz. So a zip bomb at protocol level is not possible; the hash is of the full uncompressed data and is covered by Ed25519, so a sender cannot lie about it without breaking their signature. The risk is purely application-layer: a sender transmits a 1MB `.zip` payload which the receiver extracts to 100GB. Fix: before any extraction of a compressed MIME type, show a non-dismissable dialog: "Compressed archive received — Download: XMB / Estimated extracted: YGB." Do not extract until user confirms. This is a UI feature, no protocol changes needed. |
| O15 | **`[TODO/CODE]`** Runtime plugin loading (DEX loader)** | Current plugins (Meshtastic, MeshCore) are compiled into the APK — not loadable at runtime. For a true mod scene, plugins need to be droppable files. Two mechanisms that compose: (1) **DEX loader**: a `.rumor` bundle (zip of `.dex` + a manifest) loaded via `DexClassLoader` at runtime — the mod-scene primary path, distributable via flash drive or chunked transfer over the mesh. (2) **Bound-service APK**: a separately installed APK exposes a bound service; Rumor binds to it by intent — good for plugins that need their own Android permissions (camera, contacts, location) since those are scoped to the plugin APK, not Rumor's process. Both paths feed into the same `PluginContext` interface. The DEX path runs in Rumor's process with Rumor's permissions; the bound-service path runs isolated. No central repository; the mesh itself is the distribution channel. **Pre-ship requirement**: a static manifest declaration in the plugin bundle listing which `PluginContext` APIs it calls, shown to the user as a disclosure popup before enable — not a security control (a malicious plugin can lie), but informed consent. |
| O17 | **`[PART]`** Extend bridged content beyond direct peers (`BRIDGE_VOUCHED` trust level)** | Today BRIDGED traffic is dropped on relay (see design decisions), so a bridge only reaches its direct Rumor peers — Charlie three hops away in the Rumor mesh never sees Bob's Meshtastic message even though Alice's bridge has it. Proposed mechanism: introduce a third trust level between VERIFIED and BRIDGED, called `BRIDGE_VOUCHED`. Bridge wraps the foreign message in a Rumor-signed outer envelope that explicitly carries `{originNetwork, originSenderId, originSignatureIfAny, payload}`. The outer envelope is signed by the bridge's Rumor key — but the wire format and trust level make it unambiguous that the bridge is vouching for *delivery* only, NOT for content authenticity. Other Rumor peers can relay it because the outer envelope verifies. Display layer renders as "via Alice from meshtastic:Bob" with a distinct visual treatment from native Rumor messages. Recipients decide per-bridge whether to trust that bridge's vouching (UI: enable/disable bridge-vouched relay per known bridge). Replaces the current absolute "never relay BRIDGED" with a more nuanced "never relay BRIDGED without an authenticated outer envelope from a bridge node we know about." Scope: new TrustLevel enum value, new wire envelope format, signature verification of outer envelope at every receiver, UI for per-bridge trust toggles, three-state visual rendering. Real design work; not started. |
| O18 | **`[PART]`** Transfer-control UI (pause / resume / cancel)** | `TransferAssembler` and `TransferSender` have no user-facing controls. Once a large transfer starts, the user can't cancel it without quitting the app, and it can't be paused if the device is going to be off-network temporarily. Add: cancel button (drops the transfer record, cancels watchdog, sends a CANCEL message so sender stops resending chunks), pause button (suspends watchdog and incoming chunk processing without dropping state), resume (re-arms watchdog from current state). UI should show current progress (`receivedIndices.size / totalChunks`), throughput, and estimated time remaining. No protocol changes for cancel/pause if implemented as receiver-local; sender-side cancel needs a new `TRANSFER_CANCEL` message type so the sender stops queuing chunks. |
| O19 | **`[TODO/UI]`** Pre-send file manipulation (compression, format conversion)** | Pure app/UI layer, no protocol changes. Before chunking and sending a file, offer the user: image compression (JPEG quality slider, downscale to N pixels), video transcode (bitrate selector via Android's `MediaCodec`), archive options for multi-file selections (zip / tar.gz). Show "Original: XMB → Compressed: YMB" before confirm. Reduces bandwidth and storage cost across the mesh significantly for image-heavy traffic, which is the dominant case in practice. Independent of O14 (extraction-warning popup) but shares the same UI surface. |
| O20 | **`[TODO/EMU]`** Private key in Android Keystore (TEE-backed)** | Currently the Ed25519 private key lives in app storage — readable by any code running in Rumor's process, including DEX plugins. Migrate to Android Keystore so the key never leaves the Trusted Execution Environment. `PluginContext` exposes `signWithLocalKey(bytes): ByteArray` only — no raw key access. Pre-ship requirement before any plugin loader ships. Note: Keystore-backed keys are device-bound and non-exportable; backup/restore of identity requires a separate identity-backup protocol (seed phrase or similar). |
| O21 | **`[TODO/UI]`** TOFU key pinning and identity hardening** | `userId` is already `SHA-256(publicKey).toHex()` (see `CryptoManager.publicKeyToUserId`), so the userId↔pubkey binding is cryptographic — a sender cannot claim a userId that doesn't hash from their pubkey, and every receiver can re-verify cheaply. The real identity-hardening gap is **display**: a 64-char hex userId is unreadable, and human-set display names can be freely spoofed (two people can both call themselves "Bob"). **Three display surfaces, user-selectable (one default, two opt-in):** (1) **Diceware fingerprint (DEFAULT):** derive 4 words from the first 6 bytes of `SHA-256(publicKey)`, each from a curated ~4096-word list (EFF short list or equivalent — common-vocab English first, localisation gap flagged). 4 × 12 bits = 48 bits, collision-resistant to ~280 trillion contacts. Display as `Bob [tiger-river-cake-stone]`. Accessible to screen readers, copy-pasteable, comparable verbally. (2) **Emoji fingerprint (option):** same 8-byte slice mapped to 4 emoji from a curated cross-platform-stable set (animals, food, objects, vehicles; no faces, no flags). Display as `Bob [🐅🍧🚲🌵]`. Most compact in space-constrained UI. (3) **On-device nickname (always available):** user-set local label that overrides the contact's broadcast displayName globally on this device only. Lets the user disambiguate two Bobs without depending on either of them changing their name. **Pin display-name → userId binding** in `ContactRepository`: if a previously-seen userId arrives with a different display name, surface "Bob changed their display name to Robert" rather than silently updating. Pinning is for display-name churn, not key churn (latter is impossible by construction). Display names should be entirely optional — users who want maximum clarity can show only the fingerprint or only their nickname for that user. |
| O22 | **`[PART]`** Native delayed / recurring message scheduling** | Reduces pressure on plugins needing outbound-message capability. UI: compose a message, tap a clock icon, set send time or recurrence (hourly / daily / on next peer contact). Stored locally; `MeshService` fires them on schedule. Covers the primary legitimate use case (status bots, scheduled announcements) without requiring a plugin that declares outbound-message capability. |
| O23 | **`[TODO/CODE]`** User-defined storage quota and plugin storage scopes** | Before first message, setup flow asks user to allocate a storage budget (e.g. "Reserve 2GB for Rumor"). All repos (messages, chunks, transfers, plugin data) share this budget. Plugins receive a scoped storage allocation within the global budget; if a plugin needs more than its default slice it requests an increase via a system prompt the user approves — same pattern as Android runtime permissions. File access for plugins follows the Storage Access Framework picker model (same as Signal): plugin declares which file types it needs, user grants access to specific files or folders, not blanket storage. |
| O24 | **`[TODO/CODE]`** Plugin security tiers: LOCAL_ONLY / DEX (read-only) / APK (dangerous)** | Three tiers, ascending in risk. **LOCAL_ONLY:** runs in Rumor's process, no network access at all, no `PluginContext.sendMessage`, no DM-envelope registration — the loader actively denies those APIs. Suitable for on-device image classifiers (NSFW screener), display-layer translators, local stat dashboards. The point is to express "this plugin definitionally cannot leak my data anywhere" as an enforced capability gate, not just a marketing claim. **DEX `.rumor`:** read-only / display-layer `PluginContext` APIs (observe incoming, register display filters, open settings panel). **APK (bound-service):** any plugin declaring dangerous capabilities (compose outbound messages, mutate contacts, register DM envelopes, open network connections) must ship as a bound-service APK running in its own isolated Android process with its own Android permission grants. Loader enforces tiering: a plugin claiming LOCAL_ONLY but calling `sendMessage` is rejected at load time; a DEX plugin requesting dangerous APIs is rejected. User sees "this plugin requires installing a companion app" for APK-tier, which is natural friction proportional to actual risk. Manifest capability list re-disclosed on plugin update if capabilities changed. |
| O25 | **`[PART]`** Plugin crash isolation: auto-disable on unhandled exception** | **Done:** per-call try/catch in `PluginRegistry.onMessageReceived` (lines 88–94) with `RumorLog.w` on exception; `runCatching { plugin.onDetach() }` on teardown. A single crashing plugin does NOT propagate the exception to the host app. **Not done:** auto-disable plugin for the session on crash (continues to receive callbacks until host restart); persistent disable across restarts on init-crash; user-visible notification "Plugin X crashed and was disabled"; per-plugin crash-report endpoint declared in manifest. The hard part (host doesn't crash from plugin code) is shipped; the management UX is what's left. |
| O26 | **`[TODO/CODE]`** Community attestation and peer plugin sharing** | No central plugin repository. Plugin publishers broadcast a signed metadata record on the mesh (`pluginId`, `version`, name, description, capability list, `SHA-256(bundle)`, size). Reviews are **separate** signed messages anchored to `(pluginId, bundleHash)` — they propagate as ordinary mesh content, not chunked with the bundle, so the bundle itself stays small. A "plugin directory" view aggregates: shows plugins seen on the mesh, queries reviews on demand. Acquiring a plugin from a direct peer: chunked transfer of the bundle bytes, addressed as DM to the requester. Acquiring a plugin you've heard about but no direct peer has: broadcast `PLUGIN_REQUEST(pluginId, version)`; anyone holding it replies with either the bundle (chunked DM) or a metadata response "I have v5 and v7, not v6 — want those instead?" Requester chooses. Versioning: every bundle carries `(pluginId, version)` in its manifest; old versions remain valid (no forced upgrades). Authors push new versions by broadcasting a new metadata record; users may subscribe to "notify me when new versions of plugin X appear." Reviews are pseudonymous (userId-signed) so a reviewer's track record across plugins is visible — a reviewer with consistently useful reviews becomes a trustable voice without any central authority. |
| O27 | **`[DECISION]`** Deanonymization limits: honest documentation + future mitigation options** | Relay batcher provides timing-correlation resistance at the protocol level. The ephemeral nature of the mesh (no persistent central server logging correlations) improves on centralized systems. But in a fixed-position network where the same devices are near the same nodes daily, graph analysis of relay patterns still leaks social graphs even without content. A local malicious plugin observes everything including timing, peer identities, and message rates. **Ship-blocker:** honest documentation — Rumor provides better privacy than centralized apps but weaker anonymity than Tor; do not overclaim in user-facing copy. **Future mitigation options to keep on the table, none planned today:** (1) **Cover traffic** — constant-rate dummy messages so an observer can't distinguish active from idle nodes; expensive on battery and bandwidth. (2) **Onion routing layered on the mesh** — messages wrapped in N layers of encryption, each hop peels one; resistant to traffic analysis, adds latency. (3) **Random delay-padding at relay** — stronger relay batcher with variable delays that actively decorrelate, not just batch. (4) **k-anonymity batching** — hold messages until N from different senders can be emitted together, mix-style. All four are real engineering work and have battery/bandwidth/latency costs; keep the door open but require demonstrated need before implementation. |
| O30 | **`[PART]`** Online-presence beacons** | Short-TTL INFRASTRUCTURE messages "I saw X within last N minutes" gossiped at low frequency. Lets peers far from X learn coarse reachability without polling. Feeds O29's signal 11. Aggregated with recency decay. Privacy trade-off: leaks "X was recently online and visible to someone in this mesh segment" — same leak class as DM `recipientId` today. Should be capped (peer only beacons for contacts it has marked, not for every userId ever seen) and rate-limited so a malicious plugin can't flood. **Refined per O58 design (self-presence only):** drop the "I saw X" variant; the routing-decision value is small (presence doesn't tell you *which path* leads to X — breadcrumbs and bloom do), and beaconing someone else's presence without their consent is the heaviest leak in O58 Tier 3. Replace with a **self-presence beacon**: a Free-mode (or Static-mode) node says "I am running as infrastructure, I am online, here's my recent-exchange context" — same payload, sender opts into their own visibility, no third-party metadata. The UX use case ("is Bob online?") falls back to local-only: each node infers presence from its own recent inbound from Bob (already Tier 1). **Symmetric exit pulse required:** when a node leaves Static or Free mode (manual toggle off, unplug, screen-on transition), send a final "demoting myself" self-update so peers downgrade routing weight for this node immediately rather than waiting for stale signals to decay. Without the exit pulse, DMs continue routing through ghost anchors for the full presence-decay window — minutes to hours of detours through a node that's no longer reliable. |
| O31 | **`[TODO/CODE]`** Route advertisements in HELLO** | Each peer includes a small `recentlyExchangedWith: List<UserId>` (top-N, e.g. 20) in HELLO. Cheap (~640 bytes), lets neighbours immediately know if a path through this peer is plausible. Goes inside the signed HELLO transcript so it can't be spoofed by MITM. Recipient uses for O29 scoring. Default-on for users explicitly marked as priority-peer (they've already opted into being routing nodes); default-off for everyone else. Per-contact opt-out ("don't list me in your route advertisements") for users who don't want this leak. |
| O33 | **`[TODO/HW]`** Battery profile measurement** | Measure continuous BLE + Wi-Fi Direct scan + relay power draw on representative devices (Pixel-class, mid-range Samsung, 4-year-old budget). Target envelopes: a few hours of active foreground use without rapid drain; many hours of unattended duty-cycled background relay on a modern phone; sub-2%/hour at LOW_POWER scan + advertise-only + no Wi-Fi Direct discovery for plugged or solar-supplemented operation. Tune `SCAN_MODE_LOW_POWER` defaults, motion-aware throttle (AirGuard pattern), hard cap on reconnect attempts to vanished peers (Meshtastic #4444 / #2659 failure mode), exponential discovery backoff when no peers seen (G2 generalized). Declare `connectedDevice` foreground-service type (Android 14+) with `FOREGROUND_SERVICE_CONNECTED_DEVICE` permission. First-run flow requests `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` — without it Samsung/Xiaomi/Huawei kill the service regardless of FGS status. Without measured numbers every claim about field viability is unsupported. |
| O36 | **`[DECISION]`** Public Rooms allowed and on by default; media in the global Room is the one hard limit** | Public Rooms — including a global shared Room — are a first-class surface. Users self-select which Rooms to engage with; moving to a moderated Room (O79) is the remedy for spam in an open one. Rumor does not build a reputation system; the absence of bootstrap trust in public spaces is accepted as a property, not a problem to solve. **One protocol-level rule on the global Room (singular shared roomId, if exposed): text-only, no media attachments.** There is no creator-moderator for a singular shared resource and no robust moderation primitive at that scale, so the relay drops media-content messages destined for the global Room before enqueuing. Media-borne illegal content (CSAM, graphic content with clear legal exposure across jurisdictions) is the failure mode that no opt-in disclaimer fixes; cutting it off at the wire is the honest defense for that one surface. Every other Room — including user-created public Rooms with no moderator — runs whatever media policy the creator chose at creation time. **A local-only display filter** ("show broadcasts from non-contacts in my immediate area") remains a UI-only feature, not a protocol distinction. |
| O38 | **`[TODO/CODE]`** Receiver-side forward secrecy via rotating recipient prekeys** | Current DM crypto (ephemeral sender X25519 + recipient static X25519 + AES-GCM + Ed25519 outer sig) provides **sender-side FS only**. A relay node holding stored ciphertext can decrypt every past DM the moment the recipient's static key leaks — equivalent to the original Briar-BHP gap (CVE-2023-33982), and Briar got away with it only because Tor sat underneath. We have no Tor layer. Fix: recipient periodically generates short-lived X25519 prekeys `R_t` signed by their long-term Ed25519 key, gossips them as small INFRASTRUCTURE messages; senders DH against the freshest `R_t` they hold rather than the long-term `R`; recipient deletes `R_t_priv` after expiry. Stored ciphertext encrypted to an expired `R_t` is unrecoverable even with the long-term key. Document offline-for-weeks fallback honestly: a recipient who's been offline longer than any cached prekey is reachable only via the long-term key with documented FS regression for that case. Privacy by structure, not by code-comment — the private key actually does not exist anymore once deleted, which is what makes the property real against an adversary with full bit-awareness. |
| O40 | **`[TODO/CODE]`** Relay-deletion-on-ACK** | Even with O38/O39 the FS-exposure window for stored ciphertext is "until the message expires off all relays." Tighten: on recipient ACK, sender (or recipient) gossips a signed `DELETE(messageId, recipientUserId)`; relays purge from their MessageRepository on receipt. Doesn't recover FS against an attacker who already exfiltrated the ciphertext, but bounds the storage window for *future* compromise — a relay seized 30 days later only holds messages no recipient has ACK'd yet. Compatible with existing dedup machinery (the deleted messageId stays in the seen-set to prevent re-ingestion). Cost: one extra INFRASTRUCTURE message per ACK'd DM; affects BROADCAST messages too if we extend the pattern. |
| O42 | **`[PART]`** Replace bloom-filter offer/want with Range-Based Set Reconciliation (Negentropy)** | Current gossip uses a bloom filter sized for expected message-set size; on long partition heal the actual symmetric difference can blow past the parameters, causing false-positive collapse (receiver thinks peer has things it doesn't, peer holds back, messages don't flow). Existing graceful-degradation fallback (O13/G20) handles this safely but inefficiently. Port Negentropy (Meijers 2023, arXiv 2212.13567; reference impl github.com/hoytech/negentropy, MIT licensed, ~500-line Kotlin port): O(d log N) bandwidth, deterministic, no parameter tuning, no decode-fail mode, deployed in Nostr's strfry at tens-of-millions-of-elements scale via NIP-77. Operates on `(timestamp, id)` tuples — matches existing message model. Composes with peer-aware batch shaping (P2) — the overlap heuristic stays; only the wire format changes. Don't roll your own; don't pick CPISync (cubic compute), classic IBLT (parameter tuning fragility), or rateless IBLT (too new, no battle-tested impl). **Status (as of 5e84fa0):** algorithm landed in `core/sync/Rbsr.kt` (now in `:core/commonMain` post-Phase-1c); wire types in `core/sync/RbsrWire.kt`; capability-gated branch in `GossipSession` (`rbsr-v1` in HELLO `supportedFeatures`); `SimTransport.useRbsr` toggle for scenario comparison. **Ready for simulator validation, not ready for the wire** — `LOCAL_SUPPORTED_FEATURES` stays empty in production, opt-in is per-session for now. **Fingerprint-formula divergence from NIP-77 confirmed (audit by `RbsrFormulaComparisonTest`):** ours uses `XOR-reduce(SHA-256("rumor-rbsr-v1:" || ts || ":" || id))`; NIP-77 uses `SHA-256(sum_mod_2^256(IDs) || varint(count))`. Both commutative; outputs never match on the same input set. **Recommended migration before promote-to-default:** align with NIP-77 in a `rumor-rbsr-v2` domain-tag bump, capability-gated via `rbsr-v2` feature tag in HELLO `supportedFeatures`. Costs effectively nothing (rbsr-v1 is opt-in only in production today), wins free byte-compatibility with strfry / any NIP-77 relay — which unblocks O54 transport plugins and O72 Nostr fallback reusing the same RBSR machinery. Their formula also binds the element count (Sybil-resistance) and uses one SHA-256 call instead of N. The domain-tag advantage of ours is minor and redundant in their context (their IDs are already domain-tagged SHA-256s). Wire format gets locked only after the v2 alignment lands and after simulator runs demonstrate the bandwidth win against bloom under realistic partition/heal scenarios. |
| O43 | **`[TODO/SIM]`** Per-link heal-storm token bucket with traffic-class priority** | Round-robin scheduling guarantees fairness across senders but does not bound total throughput on partition heal. When two large clusters merge, every node tries to flush its delta to every newly-reachable peer; aggregate is still N×peers messages. Add per-outbound-link token bucket sized by traffic class: INFRASTRUCTURE / REALTIME pre-empt the bucket, BULK and reconciliation backlog share what's left. Combined with O42's lazy-pull-by-default (small fingerprint exchange before pushing bodies), this addresses both bandwidth spikes and the DTN-documented "storage exhaustion during long partition" failure mode. **Shares O32's prerequisite:** needs per-peer byte accounting, which only becomes meaningful once the scheduler/relay knows which peer each enqueue is targeted at. The breadcrumb substrate (4010501) and offer-ordering bias (4010501) shipped without this refactor because they operate at the "offer batch presented to the session" layer; O32 and O43 operate at the "decide which peer gets the next byte" layer below. |
| O44 | **`[TODO/EMU]`** Keystore wrapping-key architecture (refines O20)** | Don't put the raw Ed25519 private directly in Android Keystore — it's non-exportable by hardware contract, which breaks every backup path. Instead: generate an AES-256-GCM wrapping key in Keystore (`setUserAuthenticationRequired` if biometric available, `setUnlockedDeviceRequired(true)` minimum, `setIsStrongBoxBacked(true)` on supported hardware). The Ed25519 private key lives encrypted-at-rest in app storage; decrypted into memory only inside `signWithLocalKey()`, zeroized on exit. Plugin context exposes `signWithLocalKey(bytes)` only — never a raw-key accessor; DEX plugins cannot exfiltrate. Surface `KeyInfo.isInsideSecureHardware()` in the UI so users know whether their device has hardware-backed protection or fell back to software Keystore. |
| O45 | **`[TODO/EMU]`** BIP-39 mnemonic identity export (v1 backup)** | Explicit "export identity" flow producing a 24-word BIP-39-compatible mnemonic, displayed with `FLAG_SECURE` so screenshots are blocked, never written to clipboard. Optional passphrase wraps the mnemonic for shoulder-surfing protection. First-launch flow includes a "I got a new phone" announcement message-type signed by the old key while still on the old device — pre-warns contacts of an upcoming identity continuation (different from O41 rotation, which is reactive). Document explicitly: lose this mnemonic, lose this identity forever. Photo-of-paper backed up to Google Photos is the dominant real-world failure mode — assume it; push toward O46 social recovery as the better primary path. |
| O46 | **`[TODO/CODE]`** M-of-N mesh social recovery (v2 backup)** | After v1 ships, layer Shamir-split social recovery on top. User picks N (3–7) trusted contacts; app generates SLIP-0039 split of a wrapping passphrase (not the raw key); each share is wrapped to the guardian's pubkey via existing DM crypto and sent over the mesh. Recovery on new device: generate ephemeral recovery pubkey, broadcast a signed claim "I am the new device of (oldUserId), please send my share to this ephemeral key"; each guardian's client surfaces the request with strong identity-verification UX (emoji fingerprint of requester + claimed old userId + instructions to confirm out-of-band with the person in person/by voice/codeword); on approve, share re-wrapped to ephemeral key. Reassemble ≥M shares → recover passphrase → identity. Critical: **24–72h time-delay window** between first share-approval and final assembly (Argent pattern, anti-coercion); **signed continuity message** broadcast on the mesh after recovery completes so original Alice can publicly repudiate a fraudulent recovery while she still has the original key. Optional duress passphrase reassembles to a decoy key. Without time-delay + public continuity + OOB verification, social recovery is worse than no recovery. |
| O47 | **`[TODO/UI]`** Per-message bridged-source label + metadata-leak banner** | Element's failure mode for 7 years (matrix/element-web#7823, still open): bridged rooms labelled only at room level, so users misremember which messages were E2E. Don't repeat it. Every bridged DM and broadcast in compose/contact-list/thread renders with a persistent "via <bridge>" label per message, not just per contact. Compose screen for a bridged contact carries a non-dismissable banner: "Messages to this contact pass through <bridge> and the bridge sees who you're talking to even though it can't read what." This holds under both bridging architectures (A and B) — Architecture B's content-E2E does not hide metadata. Add to S1/S2 ship-blocker checklist. |
| O48 | **`[TODO/CODE]`** Bridge-asserted-pubkey constraint on synthetic userIds** | When a Meshtastic / MeshCore radio reports a new contact, the synthetic userId must be derived as `<bridgePrefix>:<hash-of-radio-asserted-pubkey>` (not as `<bridgePrefix>:<hash-of-name>` and not as a bridge-chosen string). A compromised or malicious radio can then substitute keys only for *new* contacts; existing pinned contacts whose userId is derived from a previously-seen pubkey surface a verification event when the underlying pubkey appears to change (O21 display-name pinning, generalized to pubkey pinning under the synthetic prefix). MeshCore current implementation uses `meshcore:<FNV-1a of name>` — that's spoofable; migrate to pubkey-hash before any DM bridging ships. |
| O49 | **`[TODO/CODE]`** Reproducible-builds requirement for peer-distributed plugins** | F-Droid treats reproducibility as best-effort because they also do source review + host signing. Rumor has neither — the bundle hash is the only identifier reviewers anchor to, and without reproducibility a signed review (O26) describes a black box that no other client can verify. Mandatory for plugin bundles: manifest declares source URL + build command; clients can optionally verify-by-building on install if a build environment is available; community attestation reviewers MUST verify before signing. Without this, the peer-review system in O26 is theatre. |
| O50 | **`[TODO/CODE]`** N-of-M co-signed updates for high-capability (APK-tier) plugins** | npm event-stream / Obsidian silent-update / VS Code `ahban.cychelloworld` post-mortems converge on this: initial review doesn't catch a popular plugin's subsequent malicious update. Single mitigation that has worked in the wild is Bitcoin-Core-style mandatory N-of-M signed releases. For Rumor's APK-tier plugins (outbound messaging, contact mutation, DM-envelope registration, network access), require updates to be co-signed by ≥2 maintainer keys declared in the original manifest. Single compromised key cannot ship a malicious update. Real friction proportional to capability — DEX-tier (read-only display) plugins keep single-sig updates. |
| O51 | **`[DECISION]`** BLE/Wi-Fi adjacency leak — explicit documentation, no half-fix** | UserId is necessarily on the wire for Ed25519 verification. Any in-range observer with a phone in scanning mode logs `(time, location, userId)` tuples and reconstructs movement and contact graphs. Crypto cannot help — it is a property of broadcasting an identifier in radio range. explicit user-facing statement that Rumor does not defend against a local-range observer or a sybil-equipped adversary (sybil swarm of ~$1000 worth of phones defeats every mitigation in O27). Optional future work — ephemeral session IDs decoupled from long-term userId, with verification deferred to challenge-response — costs an extra round-trip per new peer and complicates dedup; not planned until a specific deployment surfaces a concrete need documentation-alone fails to mitigate. |
| O52 | **`[TODO/CODE]`** Group messaging via broadcast-with-key-DMs (closed-membership path of O79)** | **Protocol primitives subsumed by O79 — rooms and group chats are the same wire shape at different membership policies.** Closed-membership group chat = O79 room with explicit member list. Public room = O79 room with open membership. UX differs (member-list visibility, "Alice has joined" events), but the relay layer is identical: addressed-to-roomId, signed-by-sender, optionally encrypted-to-N-member-keys. This row captures the *closed-membership crypto piece*: sender generates symmetric key K, broadcasts `AEAD(K, payload)` tagged with `T = HMAC(K, "rumor-bcast-v1:" || messageId)`; per-recipient DMs carry K encrypted under recipient pubkey. Recipients precompute T from their K and match incoming broadcasts; non-recipients can't link DMs to broadcasts. Bandwidth wins decisively for any group ≥3 over N-DM baseline. Lazy-key variant: broadcast first, distribute keys later for offline-at-broadcast-time recipients. Metadata leak equivalent to N-DM baseline (key DMs expose recipientId — solvable separately via O53 sealed-sender). Implement together with O79 — keep one set of wire types, two membership policies. Depends on O38 (receiver-FS), O42 (Negentropy for efficient distribution). |
| O53 | **`[TODO/CODE]`** Sealed-sender DMs — recipient-derivable delivery tag** | Generalization of O52's broadcast tag to all DMs. Replace plaintext `recipientId` field with `T = HMAC(K_recipient, "rumor-dm-v1:" || messageId)`. Relays match incoming DMs against the small set of `T`s they've precomputed for keys they hold; non-recipients can't link DMs to recipientIds on the wire. Signal calls this "sealed sender"; their version trial-decrypts which is expensive — the tag pattern gives the same property cheaply. Closes the plaintext-recipientId leak on every DM (currently the dominant on-wire metadata leak); pairs with O29 breadcrumb routing — breadcrumb signals (recent-inbound-peer, ACK return path) work on tags too once the local node has computed them per-contact. Wire-format change: add tag field via O37's `_ext` until protocolVersion bump. |
| O54 | **`[TODO/CODE]`** Transport plugin tier — non-phone infrastructure** | The existing plugin model (Meshtastic, MeshCore) generalizes naturally to "any device that carries Rumor bytes between two endpoints" — routers, single-board computers, packet-radio modems, satellite modems, USB-CDC links between two stationary nodes. Two integration paths compose: (1) **TransportPlugin on the phone** — an Android plugin that speaks to an external device over USB/BLE/TCP and shuttles Rumor frames in both directions, no protocol translation. The device runs a small daemon (Go/C/Rust binary, OpenWrt package, or systemd service) speaking the Rumor wire format. Useful for "plug a USB-ethernet adapter from my phone to my OpenWrt router" or "phone-to-Raspberry-Pi over USB serial." (2) **Native relay node** — port the relay subset of `:core` (signature verify, dedup, relay decision, no UI/storage of plaintext) to a non-JVM language for direct deployment on routers, SBCs, or LoRa gateways. Pre-existing infrastructure (window-mounted SBC on solar, OpenWrt router on UPS, ham-radio packet gateway) becomes a permanent mesh node. Distinguished from BridgePlugin because there's no foreign network — it's still Rumor end-to-end, just over different physical media. Plugin capability declaration: `transport-extend` (read+write frames) is APK-tier in O24 because it touches the wire path. **Candidate transport-plugin targets:** OpenWrt routers, Raspberry Pi / other SBCs, packet-radio modems, satellite modems, Tor (carrier for O69 Nostr), I2P (anonymising overlay), generic USB-CDC links. **Form-factor fit:** aligns with O55's prolonged-disconnection design target — fixed-position infrastructure on intermittent power is exactly what runs reliably across weeks or months. **Priority:** native-relay-on-hardware (OpenWrt, SBC, MCU) is explicitly the lowest priority tier (see priority floor under "Open items / known gaps"); phone-side TransportPlugin shim ships first. |
| O55 | **`[DECISION]`** Design target is prolonged-disconnection, not short outages** | Several items implicitly assumed brief disconnection scenarios (hours, a day). The actual design target is reliable operation across weeks or months of degraded or absent infrastructure, with intermittent access to power and no access to external services. Implications across the backlog: (a) **O33 battery framing** — the right envelope is duty-cycled operation over weeks on intermittent solar / handcrank / shared charging, not an 8-hour active-use day. Measurement target is sub-2%/hour at LOW_POWER scan + advertise-only + no Wi-Fi Direct discovery. A plugged, window-mounted phone as a permanent relay matters more than handheld active use. (c) **O23 storage quotas** — must handle months of accumulated messages without OOM; eviction policy is the load-bearing piece. (d) **O26 plugin distribution over the mesh** — when external app stores are not reachable, the mesh itself is the software distribution channel. (e) **O49 reproducible builds** — over time, users will run binaries whose origin they cannot directly verify; reproducibility lets a recipient confirm "this binary matches that source." (f) **O54 transport plugins** — fixed infrastructure on intermittent power is the right form factor; handheld phones are auxiliary. |
| O56 | **`[DECISION]`** LineageOS / de-Googled-Android compatibility — sustaining audit** | Baseline (as of c9f4473): codebase is clean. No Play Services, GMS, Firebase, FCM, SafetyNet, Play Integrity, Maps, or Push runtime deps. Build-time `com.google.devtools.ksp` is fine (it's Kotlin Symbol Processing, runs at build, not at runtime). OEM-aware paths (`DeviceQuirks.kt`) detect process-killer behaviour by package name — does not require those packages to be present. **Sustaining rules** to keep this state: (1) Never add a Play Services or Firebase dependency. (2) **O44 Keystore work must `KeyInfo.isInsideSecureHardware()`-gate, never hard-require StrongBox** — older LineageOS devices fall back to software Keystore and that path must keep working. (3) Any future WebView use → system WebView (Bromite/Mulch/system Chromium fork), never a bundled Chromium. (4) BLE paths should tolerate older bluedroid quirks; avoid features that require Android 13+ stack behaviour without a fallback for older LineageOS builds. (5) On any new permission requirement, verify the permission exists on AOSP without GMS — some "Android" permissions are GMS-only and unavailable on LineageOS. CI smoke check (future): build against a no-GMS classpath to fail any accidental Play Services import. |
| O57 | **`[PART]`** Device modes: Mobile / Static / Free** | User-settable behaviour mode, manual or auto-triggered by plug state. **Mobile (default):** device assumed in motion; conservative BLE/Wi-Fi-Direct duty cycle; topology breadcrumbs through this node decay fast; low routing-weight as an anchor. **Static:** auto-on when plugged in, or manual. Device treated as still for the duration; eligible to be a routing anchor; breadcrumbs through this node persist longer; appears in route advertisements (O31) by default. **Free:** auto-on when plugged in AND screen off, or manual. Dedicates bandwidth/CPU/battery — aggressive scanning, longer gossip sessions, eager relay, subsumes Static. Manual override always wins over auto-trigger. The device deliberately does not try to detect "I'm airborne / in a car / window-mounted / on FM broadcast" — same three modes cover every form factor, the user (or plug state) declares behaviour, the routing engine consumes it. Integrates with O54: `isUserCarried` is the per-transport version of the same signal; the mode toggle is the per-device version. Free-mode + plugged-in = natural infrastructure node without a different app build. **Mode-change pulses (entry and exit):** entering Static or Free fires the O30 self-presence beacon; *leaving* Static or Free fires the symmetric demotion pulse so peers downgrade routing weight immediately rather than waiting for stale signals to decay. Without the exit pulse, a phone that goes Free → Mobile (unplug + pocket) keeps attracting routed DMs as a ghost anchor for minutes-to-hours. Pulses are signed self-presence messages with a `mode: Mobile | Static | Free` field, INFRASTRUCTURE traffic class, short TTL. |
| O58 | **`[DECISION]`** Tiered routing-visibility for topology signals** | Each topology signal that improves routing efficiency carries a privacy cost. Rough efficiency vs baseline pure-flood: Tier 1 alone delivers ~50–80% bandwidth reduction (zero marginal privacy cost); Tier 1+2 adds ~10–20% (moderate per-handshake leak); Tier 3 adds ~5–10% (heavy broadcast leak). **T1+T2 is the sweet spot** for most users; T3 only justifies its cost for nodes that have already accepted being visible (Free mode). Sorted ascending: **Tier 1 (local-only, free):** reverse path from recent inbound from X, DM-ACK return path, outbound delivery success, TopologyTracker overlap (computed locally, only the overlap *fraction* shared), priority-peer declarations, co-presence history, **local contact-list consultation** (each forwarder checks its own contacts for hints about where the recipient is reachable — does not need broadcast contact-graph hints). No marginal leakage beyond gossip baseline. Default-on. **Tier 2 (direct-peer, moderate):** "recent senders" bloom in HELLO, hop-distance distribution in messages. Leaks to one peer per handshake — sybil aggregation attack exists but per-handshake cost is small. Default-on with bloom-size tuning; opt-out per-contact for paranoid users. **Tier 3 (broadcast, heavy):** route advertisements with explicit userId list (O31) and self-presence beacons (O30 refined). **Dropped from Tier 3:** broadcast contact-graph hints (Tier 1 contact-list consultation covers the routing case for free) and beacons-about-other-users (presence doesn't tell you *which path* — Tier 1 does — and beaconing others' presence without consent is the heaviest leak with the smallest gain). Self-only Tier 3 means the node opting into Tier 3 is opting into *its own* visibility, not anyone else's. Sybils in range still collect these from every passing self-advertising node. **Default-off**; opt-in per-contact; explicit "this is visible to the mesh" disclosure when enabling. Paired with O57 modes: Free-mode is the natural opt-in level for Tier 3 ("I want to be a routing anchor, I accept being visible"). Mobile-mode users stay at Tier 1+Tier 2 by default. A user staying at Tier 1 only gets routing significantly better than today's pure-flood DMs with no privacy regression — same posture, much better bandwidth. |
| O59 | **`[DECISION]`** Next-hop routing not source routing — design decision recorded** | DMs do not carry their traversed path. Each node makes the next-hop decision using its own local view (O58 signals); the message itself carries only id, sender, type, payload, signature, and the `routedHops`/`floodedHops` TTL counters (O32). Source-routing — where the message header lists the chain of hops — is rejected because it leaks the path to every intermediate observer and to anyone who later seizes a relay's stored ciphertext. Next-hop routing keeps each hop's leakage scoped to that hop's local neighborhood. This is the structural difference from Tor (which uses source routing inside the onion) and the reason Rumor cannot match Tor-grade anonymity even with perfect crypto — but also the reason in-mesh observers learn less than they would under a path-recording design. |
| O60 | **`[DECISION]`** Briar comparison: explicit non-claims** | Briar achieves "an in-mesh observer learns nothing about who talks to whom" by *not having a mesh* — pairwise contact-only transport, no relay through strangers. Rumor cannot match that floor while having a relay mesh; the mesh is the value proposition. **Rumor matches Briar on:** content confidentiality (X25519+AES-GCM), sender authenticity (Ed25519), receiver-FS (once O38 lands), identity continuity (O41), pinned-key contact recognition (O21). **Rumor falls below Briar on:** sender-recipient unlinkability (DMs carry `recipientId`; O53 sealed-sender reduces but doesn't eliminate because BLE adjacency leak publishes userId on every transmission for Ed25519 verification), social-graph privacy under sybil observation (a sybil swarm of ~$1000 worth of phones can map who-talks-to-whom from gossip patterns even with zero shared topology signals — the patterns themselves leak, independent of O58 tiers). This gap is fundamental to the use case rather than fixable. Honest framing: "Rumor matches Briar on content security and identity authenticity, falls below Briar on observer-in-the-mesh privacy because the mesh exists; we trade some of Briar's privacy ceiling for the relay-network value." Do not claim parity with Briar on the social-graph axis; do not claim parity with Tor on the anonymity axis. |
| O62 | **`[DECISION]`** Define Mobile / Static / Free modes precisely before auto-fire wiring** | O57 sketched the three modes but leaves a lot of behavior undefined. Before implementing the BroadcastReceiver wiring (plug events, screen on/off, manual toggle), pin down per-mode: (a) BLE scan mode + duty cycle (LOW_POWER vs BALANCED, scan_window / scan_interval ms), (b) Wi-Fi Direct discovery cadence (continuous, periodic with backoff, or disabled), (c) gossip session length (one-shot vs persistent with keepalive), (d) relay batcher window (delay range), (e) breadcrumb-cache decay rate (1h, 24h, etc), (f) presence-beacon rate (every N minutes, or driven by mode-change only), (g) routing-weight multiplier as a breadcrumb candidate (Mobile=1×, Static=3×, Free=10×?), (h) storage quota fraction available to relay vs local-user content. Each mode should have a single source-of-truth `ModeProfile` data class that everything consumes — no scattered `if (mode == Free) ...` checks throughout the codebase. Pre-implementation gate: no code that branches on mode lands until this profile is recorded here. |
| O63 | **`[DECISION]`** iOS via shared `:core` (KMP), not a separate app** | If iOS ever ships, do it by promoting `:core` to Kotlin Multiplatform with `commonMain` + `jvmMain` + `androidMain` + `iosMain` (and `linuxMain` for headless relay nodes per O54). A separate-but-interoperable iOS app is rejected: two protocol implementations diverge silently, and the wire is the only place a bug is unrecoverable post-ship. Hard constraints, in order — do not skip ahead: (1) **Define stable storage interfaces in `:core` before any SQLDelight migration.** Today `MessageRepository` / `ContactRepository` / `RouteRepository` / `BreadcrumbRepository` / `BlocklistRepository` / `TransferRepository` / `ChunkRepository` / `ScheduledMessageRepository` exist as core interfaces — keep it that way. DAOs (Room today, SQLDelight tomorrow, Core Data never) must not leak past the adapter layer. Anything in `:core` that imports a DAO type is the bug. (2) **`:core` compiles unchanged on JVM, Android, Linux, iOS.** Protocol, routing, scheduler, crypto, chunker, blocklist, RBSR, breadcrumb cache, dedup, wire-format — all `commonMain`. Platform deltas (Keystore vs Keychain, Room vs SQLDelight vs in-memory, BLE stack, time source, secure random) hide behind `expect/actual`. The simulator already proves the storage interfaces are honest — if it compiles against in-memory repos, it'll compile against SQLDelight repos. (3) **Plugin loading needs a more dynamic path before ecosystem ships** (per O15+O24+O26). DEX-only is Android-only by construction; iOS forbids runtime code load entirely. For cross-platform ecosystem the plugin contract has to be expressible without arbitrary code execution on the host: declarative manifests + a constrained capability surface (message filters, display transforms, envelope codecs declared as data, scheduled-message templates). Native-code plugins stay Android-APK-tier; the declarative tier runs on iOS and locked-down Android profiles. iOS-specific work when the time comes: SQLDelight Native driver, CryptoKit-backed `actual` for Ed25519/X25519/AES-GCM (or stick with BouncyCastle-Kotlin in commonMain — slower but one impl), Core Bluetooth wrapper for BLE peripheral+central, MultipeerConnectivity as Wi-Fi-Direct analog (no true equivalent — iOS won't hold a Wi-Fi-Direct group open). None of that is worth doing until (1) and (2) are done; (1) and (2) are worth doing regardless of iOS because they also unlock O54 (Linux relay nodes on routers/SBCs). (4) **No Android feature regression when iOS lands.** Adding iOS must never remove or weaken anything that works between two Android peers. iOS may ship a strict subset (no Wi-Fi Direct, part-time relay, no DEX plugin loader on App Store) — that is acceptable. What is NOT acceptable: cutting capabilities from Android because iOS cannot host them. Inter-Android exchanges may carry more than Android↔iOS or inter-iOS exchanges; the protocol stays interoperable across all three pairings by negotiating per-session through HELLO `supportedFeatures` (the same machinery G10 already uses for RBSR). Reviewer test for every change touching a cross-platform feature: "does this still work for two Android peers talking to each other?" If no, reject. iOS limitations are negotiated *down* per session, never enforced *upward* onto Android. |
| O65 | **`[DECISION]`** Wire has two layers: `RumorMessage` (content) and `GossipPacket` (transport) — do not collapse** | `RumorMessage` is the signed content unit (id, sender, payload, sig, `_ext`) — what dedup keys on, what gets stored, what gets relayed. One stable schema; additive evolution via `_ext` (O37). `GossipPacket.{Hello, Offer, Want, MessageBatch, BloomFilter, Rbsr, ...}` is what crosses a single hop — may carry zero, one, or many `RumorMessage`s plus session-control bytes (HELLO challenges, bloom, RBSR frames) that are *not* signed messages and must not be forced into `RumorMessage` shape. **Rule:** content evolution → `RumorMessage._ext` + new `MessageType`. Transport evolution → new `GossipPacket` subtype + HELLO `supportedFeatures` capability negotiation. Inventing a `MessageType` for what's really a transport-control frame (or vice versa) is the trap. Both layers already exist in the codebase; this row exists so the boundary isn't blurred in future work. |
| O66 | **`[DECISION]`** RBSR perf characterization (informs O42 promote-to-default gate)** | Not a blanket "negligible." Three regimes: **(a) Steady-state, small symmetric diff (≤dozens):** RBSR is *cheaper* than bloom — handful of 32-byte XOR fingerprints, no false-positive risk, no `expectedItems` to guess. Decisive bandwidth win once the diff exceeds ~50 items. **(b) Cold-start (one peer has zero):** degenerates into "send the id list" — same as bloom+offer-batching today. No win, no loss. **(c) Partition-heal (both sides thousands of items, large diff):** the interesting case. Each round bisects keyspace by 16; G17's bump to `MAX_RBSR_ROUNDS=12` makes the cap a recursion safety (16¹² leaf ranges), not a budget. CPU per round is O(items-in-range) for the XOR fingerprint — noise. Real cost is **latency** (each round = one round-trip), which is exactly what scenario 14 was hitting. G17 added `ExchangeMetrics.rbsrRoundsUsed` so the promote-to-default gate is "convergence ≤4 rounds on realistic heal traces" measured from data, not vibes. Until that gate passes, `LOCAL_SUPPORTED_FEATURES = emptyList()` stays — opt-in per session via simulator toggle. |
| O67 | **`[PART]`** Keyword filters as signed shareable lists** — **wire layer + Android persistence + DI shipped; UI + default lists open.** Pieces shipped: model (KeywordFilterList signed wire type + FilterEntry + FilterAction.{WARN,BLOCK} + MatchKind.{SUBSTRING_CI,SUBSTRING_CS,WORD_CI} + FilterSubscription); matcher (per-render hot path, BLOCK>WARN, publisher+subscriber allowlists compose, disabled subs skipped); publisher (signs with local Ed25519, monotonic version); verifier (publisherId re-derived from supplied pubkey, Ed25519 verify); subscriber (subscribe/unsubscribe/applyList with monotonic version + ONE_TIME gates, persists publisherKey + lastAppliedVersion inside FilterSubscription); KeywordFilterListRepository + FilterSubscriptionRepository commonMain interfaces; gossip bridge wired into MessageType.KEYWORD_FILTER_PUBLISH (TrafficClass.TRANSFER_SETUP, MAX_BROADCAST_HOPS ceiling, relayed like other infrastructure broadcasts). **Android Room adapters** in `:app` use a JSON-blob storage shape (publisherId PK + json TEXT) — entries / actionOverrides / allowlists are not Room-native types so per-row JSON beats a child-table refactor for two repos we never query inside; rationale documented in `KeywordFilterEntities.kt`. Adapters log+null on deserialize failure (mirror of BlocklistVerifier's "verify-fails-returns-null" pattern). Schema bumped v5 → v6. DI wired in AppModule. **Simulator in-memory stubs** in `InMemoryRepos.kt`. 30 tests total on the surface (12 matcher + 8 publisher/verifier + 10 subscriber). The G6 Koin DI verify test caught a real wiring issue with a default-valued lambda param on the gossip bridge mid-batch — dropped the `onListApplied` hook entirely (UI re-reads `subscribedListsForMatcher()` per render anyway); documented inline that a future hook should be a SharedFlow, not a constructor lambda. Domain tag `rumor-keyword-filter-v1:`. **Not done:** UI list editor (compose/edit/sign/share); built-in default lists (slurs / NSFW text / gore-keyword fallback — content sourcing TBD); auto-intake-by-publisher toggle; long-press "always show from this sender" exception UI; diff variant if real-world list churn justifies it; gossip-bridge tests with a real GossipEngine fixture. Original spec retained:** (text content only — image surfaces are O81; separate data type from blocklists, shared distribution infra only) | **Keyword filter lists are NOT blocklists.** They are a distinct data type and a distinct user-facing surface. They share *only* the signing / sign-verify / publish / subscribe / diff distribution machinery from `BlocklistVerifier`; the schema, semantics, UI, and user model are all separate. Don't conflate them in code or in product copy. Filter entry shape: `{pattern: String, action: BLOCK | WARN, warnLabel: String?}`. Multiple subscribed lists merge; strictest applicable label wins for any matched message. **Two actions, orthogonal to the keyword set:** **WARN** shows the message with a warning banner / collapsed-by-default ("before you tap, this message contains/may contain X"); **BLOCK** prevents the message from being rendered in the UI at all (still relayed normally per the relay-path-never-touches-blocklist rule, just not displayed locally). Ships with built-in default lists: (a) **slurs** (English first — localisation gap flagged honestly), default BLOCK; (b) **NSFW text keywords** (sexual content), default WARN; (c) **graphic violence / gore text keywords**, default WARN. User can promote WARN → BLOCK or demote BLOCK → WARN per list during onboarding and any time after. Custom lists subscribable. **Per-userId filter exception (important for community use):** every list supports `userIdAllowlist: Set<String>` so the user can specify "don't apply this filter to messages from this contact." Communities reclaiming slurs internally need this; misclassified senders need a single-tap "filter exception" path; recovery from a bad subscribed-list ban-wave needs this. UI surface: long-press a filtered message → "always show messages from this sender." **Auto-intake by publisher (power-mod feature):** mark a publisher's pubkey as "auto-apply everything they sign." When a new filter list signed by that publisher arrives over gossip, it's pulled in without prompt. Useful for community moderators / curators who maintain canonical lists. Opt-in per publisher; disclosed at toggle time as "you are delegating filter decisions to <publisher>." **Honest leak note:** subscribing to a list means the gossip layer sees you holding their signed snapshot; other peers can ask you for it. That advertises which lists you stand by. Two mitigations: (a) accept as design tradeoff (these are public commitments anyway), (b) per-list "private mode" where you hold the list but refuse to gossip it. **Distinction matters:** the four content surfaces are (a) text keywords [O67], (b) image NSFW [O81], (c) image gore [O81 — model gap noted, ships text-keyword-only at first], (d) sender identity [BlockManager / O78]. Each carries an independent WARN/BLOCK control. Don't conflate them in UI. Required for App Store 1.2 user-generated-content posture; useful on Android too. |
| O68 | **`[TODO/UI]`** Report-user / report-content UI + local report log** | Per-message "report" action in thread view; per-user "report" action in contact details. The report action does three local-visible things in sequence: (a) auto-add reported user to local blocklist with the chosen reason (ties into existing `BlockManager.block(reason=…)`); (b) append to a local report log surfaced in Settings → Reports for the user's own records; (c) **within a moderated Room (O79), the report is delivered as an `@-mention` to the Room's moderators** so they can act (remove / kick / ban per Room policy). There is no central maintainer to send reports to and no upstream submission — Rumor has no server. The Room-mod path is the actual moderation mechanism that scales; local-block + local-log is the personal-self-defense layer. Combined with O67 (keyword filters), O78 (public block reasons), O79 (per-Room mods), and O81 (image classifiers), this is the full content-moderation surface — each piece is local and user-controllable; none of them rely on a central service. |
| O69 | **`[TODO/UI]`** In-app full-wipe ("forget me") flow** | App Store 5.1.1 requires in-app account deletion. Rumor has no cloud account, but the UI must still expose a one-tap "delete identity and all data": wipe Ed25519 keypair, Room DB, SharedPreferences, transfer cache, log ring buffer, plugin scoped storage. Double-confirm with a 5-second undo timer before commit. **Required: emit a signed `IDENTITY_RETIRED` broadcast before wiping**, signed by the about-to-be-deleted Ed25519 key, telling contacts the identity is retired. Recipients update the contact record to "retired" status (display greyed, no further messages expected from this userId, blocked from re-establishing under the same userId since the holder is supposedly gone). This is the *only* legitimate use case for an old-key-signed identity message after G9's deprecation (see G9 row): O41 / G9 identity-rotation has been deprecated because key-compromise turns rotation into permanent impersonation. The retirement case is safe because there's no successor key to be hijacked into — old key signs "I am gone," nothing transfers. On success, app returns to onboarding as if first-launched. |
| O70 | **`[TODO/UI]`** Explicit user-controlled scan interval, with measured battery-cost annotations** | Replace the binary LOW_POWER / BALANCED duty-cycle slider with explicit intervals (15s, 30s, 1min, 5min, 15min, manual-only). Each interval renders an estimated battery cost ("≈3%/hr") from O33 measurements — honest numbers, not hand-waved. The reviewer-friendliness angle: Apple is far happier with "user chose this scan rate" than "app decided to drain battery." Maps onto O62 ModeProfile so each mode has its own default interval. Depends on O33 measurements landing first; until then, label estimates as approximate. |
| O71 | **`[TODO/UI]`** Local-only broadcast feed UI (display filter, no protocol change)** | A "discovery" tab that shows BROADCAST traffic from non-contacts in the device's current radio range, scored by recency and hop count. Pure display layer — does NOT change protocol behaviour; the network already carries this traffic identically to contact broadcasts. Per O36 this is explicitly allowed ("A local-only display filter — 'show me broadcasts from non-contacts in my immediate area' — is a UI feature and is fine to ship"). Cross-reference O36 in the code so future audits don't mistake it for a violation. Combined with O67 keyword filters and O68 reporting, this gives the public-feed experience users expect without re-introducing the global-broadcast-room failure mode. Apple-friendly framing: "your neighbourhood feed," not "public chat." |
| O72 | **`[TODO/CODE]`** Nostr relay fallback when internet is reachable** | Bitchat-style: when an internet connection appears (Wi-Fi, cellular), opportunistically forward messages to Nostr relays over Tor (or I2P), and ingest matching events back. Extends Rumor's mesh-only reach to cross-region delivery — mesh in city A talks to mesh in city B via Nostr-over-Tor without either mesh having continuous internet. Different threat model from the in-mesh case (relays are public, observers many), so it gets its own design pass before implementation. Concrete decisions owed: which Nostr event kinds Rumor uses (custom NIP?), how userId↔npub maps, whether Rumor messages are carried as encrypted `_ext` blobs inside otherwise-standard events. Tor integration via tor-android (Briar uses it) or Arti once mature. Cross-references O54 — Nostr is effectively a transport plugin. |
| O73 | **`[TODO/CODE]`** mDNS/Bonjour LAN transport (Android + iOS)** | First-class transport alongside Wi-Fi Direct and BLE. Uses Network Service Discovery on Android and Bonjour on iOS; both speak standard mDNS so Android↔iOS interop is genuine. Throughput is 10–50× BLE when applicable. Activates only when two peers are on the same SSID (no captive portal, no isolation mode). Caveats: every peer publishes a service record, so the metadata-leak profile is marginally worse than BLE adv — document. Sits at the same layer as `WifiDirectTransport` and `BleDiscoveryManager` in `TransportConfig`. Bonus: this is the highest-throughput interop path between Android and iOS once Wi-Fi Direct is off the table on iOS. |
| O74 | **`[TODO/CODE]`** Submit to non-Play Android stores** | Huawei AppGallery, Xiaomi GetApps, Samsung Galaxy Store, Amazon Appstore, Aurora Store mirroring, IzzyOnDroid F-Droid mirror, Uptodown, GetJar, SlideME, APKPure, 1Mobile, Tencent MyApp (China-only). The known major stores are listed here but there is also a long tail of regional and niche stores worth a broader research pass before shipping — every additional store with low marginal effort is reach the F-Droid+Play baseline doesn't get. Per-store review climate for P2P/mesh/E2E apps in 2026 is unknown and varies sharply by jurisdiction. Chinese App Store (Apple China) and Tencent MyApp are flagged separately as probably-infeasible for an uncensorable E2E mesh app without significant feature removal; document as a non-goal unless someone is willing to ship a heavily-stripped China variant. |
| O75 | **`[TODO/HW]` — LOWEST PRIORITY** `:relay-firmware` MCU relay-only spec | Microcontroller-class Rumor relay: ESP32 / RP2040 / nRF52, optionally paired with the NoLoRa SPI/I2S harmonic-emission trick for battery-free transmit. Relays signed broadcasts + signed DMs (carrying opaque ciphertext); never originates, never bridges, never runs plugins, never holds keys, cannot be a DM endpoint. Capability bits in HELLO: minimum relay-only subset. ~512–4096 entry RAM dedup ring, no persistence across reboot, no RBSR / no bloom (pure flood-with-TTL, the original Rumor model). Ed25519 verify is the heaviest op (~12ms on Cortex-M0+). Sibling Cargo/PlatformIO project, not Gradle. Hard prerequisite: a written wire-format spec (`docs/wire-format.md`) so a non-JVM implementation can be byte-compatible — currently the wire format is implicit in `core/wire/`. Explicitly lowest priority: phone-side software ships first; this lands once the protocol surface is locked and there's a real hardware experimenter asking for it. |
| O76 | **`[PART]`** Per-payload compression + 6-bucket padding (text only), UI-transparent chunking above ceiling — **compress + pad pipeline shipped; wire integration open.** `core/wire/PaddingBuckets.kt` defines the six buckets (64 / 256 / 1024 / 4096 / 16384 / 65536) + `Pad.pad` + `Unpad.unpad`. Zero-fill (TLS 1.3 pattern, NOT random — the AEAD wrap is what gives indistinguishability; sensor-entropy "upgrades" rejected, rationale in file docstring). `core/platform/Compression.kt` is the expect/actual shim — raw RFC-1951 deflate (no zlib wrapper); JVM actual uses `java.util.zip.Deflater` / `Inflater` with `nowrap=true`; iOS actual stubs `NotImplementedError` pending libcompression cinterop (same xtool/Mac gate as the rest of the iOS port). Inflate respects `maxOutputBytes` (zip-bomb defense at the compression layer; the application layer covers file-format expansion via O14). `core/wire/CompressedPaddedCodec.kt` composes the two ends — `encodeForWire(plaintext) → Encoded(bytes, bucketIndex, originalLength)` and `decodeFromWire(bytes, originalLength, maxOutputBytes) → ByteArray?`. Pure, no IO, no state — caller passes the result into AES-GCM. **Tests (24 total for the O76 surface):** 9 `PaddingBucketsTest` (bucket boundaries, exact-fit, oversized→null, round-trip, tamper, ≤4× overhead bound), 9 `CompressionTest` (round-trip on text / empty / random / 64 KB; truncated and garbage rejected; zip-bomb cap; zero/negative cap rejected), 7 `CompressedPaddedCodecTest` (round-trip on chat / empty / redundant-shrinks / every bucket; truncated originalLength rejected; oversized originalLength rejected; receiver-cap honoured). **Not done (wire integration in `GossipEngine`):** `_ext` carries `compressed: Boolean` + `bucketIndex: Byte`; HELLO `supportedFeatures: ["compression-v1"]` capability negotiation; `composeBroadcast` / `composeDirect` invoke `encodeForWire` only when peer advertises capability AND payload is TEXT (skip BINARY/FILE per CRIME/BREACH); receiver runs `decodeFromWire` on inbound; chunker fallback above 64 KB with seamless TextAssembler stitch (no "fragment 3 of 5" UI). The `originalLength` field rides in AEAD-protected associated data so it can't be tampered with by a relay. Original spec retained below for the open work. — Original spec: Today every message goes on the wire as raw plaintext (broadcasts) or raw ciphertext (DMs), uncompressed. For text-heavy traffic (chat, status updates, broadcast feed) typical content compresses to 30–50%. Worth taking — but only for `MessagePayload` of TEXT contentType, never for BINARY/FILE which is already compressed (re-running gzip on JPEG burns CPU for no gain). Pipeline order is **compress → pad → encrypt → sign → chunk**: zstd or deflate the plaintext; pad with zeros up to the smallest bucket that fits in `{64 B, 256 B, 1 KB, 4 KB, 16 KB, 64 KB}`; AEAD over the padded blob; outer Ed25519 as today. Six buckets ≈ 2.6 bits length leak per message vs full byte-length of raw plaintext today. Worst-case overhead capped at ~4× (content just over half a bucket). Buckets chosen for the real content distribution: quick replies / acks fit in 64 B without 50× overhead; typical chat in 256 B; paragraphs in 1 KB; blog-length posts in 16 KB without auto-chunking; only true outliers above 64 KB chunk. Top bucket exceeds the 16 KB INFRA/REALTIME ceiling so anything above 16 KB demotes to BULK class anyway. **UI seamlessness — non-negotiable.** User composes a message of any length in one input box; compression + bucket selection are invisible. Compressed payloads up to 64 KB ride a single wire message. Above 64 KB the existing `Chunker` (file-transfer path) splits into a TEXT-typed thread with sequence metadata; recipient's TextAssembler stitches them back into a single logical message before display. The user types, the recipient reads — no UI ever exposes "your message was chunked" or "this is the 3rd of 5 fragments." Wire-format addition routes through O37's `_ext`: a new boolean `compressed: true` on the wrapper plus a bucket-id byte; absence means uncompressed (back-compat with peers that don't advertise the capability via HELLO `supportedFeatures: ["compression-v1"]`). **CRIME/BREACH constraint:** never share a compression context across messages, and never mix attacker-controlled bytes with secret bytes inside the same compression pass — current DM model is safe (sender controls 100% of plaintext) but any future group-chat envelope (O52) or any header that mixes a session token with user content must audit before turning compression on for that path. Padding bytes are zeros (TLS 1.3 pattern); the AEAD makes ciphertext-pad indistinguishable from anything else regardless of pre-encryption content — mixing in OS sensor entropy (gyro, temp, camera) is a footgun, not an upgrade: a CSPRNG cannot be made stronger by XORing low-entropy sources, and reading sensors adds permission prompts, battery drain, and a fingerprinting vector. `SecureRandom` (kernel-seeded from boot-time hardware entropy + interrupt jitter) is what every well-reviewed protocol uses; trust it. Honest framing: bandwidth win on text, no win on media (covered separately by O19 pre-send compression UX), modest fingerprinting reduction (6 buckets instead of every length distinguishable). |
| O77 | **`[TODO/SIM]`** Massive long-uptime scenario (500 nodes, 24-hour sim, sustained churn) | Today's scenarios cap at 200 nodes / 120 s sim-time and never trigger storage eviction, bloom degradation at scale, or the kind of sustained-churn protocol exercise the O55 prolonged-disconnection design target requires. Predicted-but-unproven behaviors that only a massive sim can surface: (a) `MessageRepository.evictOldest` actually fires — does the eviction policy keep a coherent working set or churn the freshest content out? (b) Static bloom `falsePositiveRate` against per-node sets of 10–60k messages — does over-offering wreck bandwidth? (c) `MAX_MESSAGES=50000` per node × 500 nodes ≈ 12 GB worst-case heap — does the storage policy hold? (d) Multi-hour partition heal with churn during the partition — does anything converge? (e) Hub bottlenecks at scale — at what node count does hub-spoke fairness collapse under queue pressure? **Recommended scenario shape:** 500 nodes, static mesh topology with k=6 connections per node, 24 sim-hours, gentle steady-state traffic (1 broadcast/node/10 sim-min ≈ 72k broadcasts total), rolling partitions every ~30 sim-min healing 10 min later, breadcrumbs ON, RBSR OFF (until per-round cost optimization lands — G17). **Assertions:** heap stays under 4 GB; per-node message count stays under 60k after eviction kicks in; cross-partition delivery ratio stays >0.6; no node crashes; deterministic-replay within ±5% (now that O12 clock injection is in place, this should hold even at scale). **Operational requirements:** wall-clock cap raised from 240 s default to 1800 s (30 min); JVM `-Xmx16g`; bundle file will be ~70 MB (one per scenario); single scenario per run, not bulk sweep. Probably needs a dev box with ≥32 GB RAM to run comfortably. Deferred until a fresh hardware target exists — phone-class dev machines won't fit. When run, hand `result.json` summaries to a reviewer; the full trace is too big to read into chat context. Probably surfaces 1–3 real protocol issues invisible at 200-node scale — that's the point. |
| O78 | **`[TODO/CODE]`** Signed public block-reasons with ±1 attestations — visual rating only, no mechanical effect** | A user blocking someone may optionally publish a signed `BlockReason { blockedUserId, reasonText, reasonTags[], publishedAtMs }` (Ed25519 by the blocker), gossiped as INFRASTRUCTURE traffic. Other users browsing a contact see "blocked by N users — top reasons: spamming (38), harassment (12)" plus, more prominently, "**three of your regular contacts blocked this person**". **±1 attestations** are sibling signed messages anchored to `(blocker, blocked, reasonHash)` so each signer can only ±/− a given reason once; signature replay is impossible. **This is a UI signal, not a filter input.** Visual highlighting only — contacts' ±1s render larger / brighter / labeled "your contacts"; stranger ±1s render small / aggregated; blocked-by-you ±1s render hidden or greyed. **No mechanical filter behavior depends on counts**, so Sybil attacks against the count are irrelevant: a bad actor +1-flooding their own block does nothing except inflate a number the display already devalues for stranger sources. The earlier Sybil-mitigation analysis (graph-distance weighting, PoW, co-presence proof) is no longer needed because there's nothing to defend — the count is informational, not enforcement. **Opt-in per-block** — default unpublished; the publish toggle in the block UI surfaces the metadata-leak ("publishing this reason makes your identity visible to everyone seeing this contact"). **Account-deletion erasure:** O69's `IDENTITY_RETIRED` broadcast cascades to retract published BlockReasons + ±1s signed by the deleted userId; peers honour and remove from local view. Old key is the proof. **No quorum-of-third-parties erases someone else's content.** That remains a censorship primitive and explicitly off the table. Cross-references O67 (signed list machinery — same publish/sign/diff), O68 (combined posture for App Store 1.2), O79 (Rooms also have block lists), O27 (deanonymization — published reasons are an opt-in identity leak by design). |
| O79 | **`[TODO/CODE]`** Rooms — unified open / invite / password / closed-membership shape (subsumes O52 group chats; allowed by default per O36)** | The primitive is called a **Room**. One protocol primitive serves the full spectrum from open public chats to closed-membership groups — the difference is membership policy and per-message crypto. **Wire shape (one schema, four membership modes):** room creation signed by creator (`roomId = SHA-256(name || createdBy || createdAtMs)`); membership policy `{ open | invite | password | closed }`; posting policy `{ member_only | mod_approved | anyone_with_mod_removal }`. **Open**: anyone can join by roomId; messages are plaintext. **Invite**: joining requires a signed `RoomInvite { roomId, invitedUserId, roomKey, expiresAtMs, modSig }` DM from a moderator carrying the room's symmetric key. **Password**: room key derived from `Argon2id(password, roomId-as-salt)`; anyone who knows the password can derive K; supports printed-on-flyer / shared-by-voice deployment. **Closed**: explicit member list, K distributed per-member via signed key DMs (per O52 crypto). All modes addressed-to-roomId; **non-subscribers receive nothing for that roomId** (relay drops; not just UI filter). **Media attachments policy is the Room creator's call** — when someone creates a Room they decide whether to allow image/voice/video/file attachments. The only hard exception is the **global Room** (the singular shared roomId, if exposed): it has no creator and cannot be moderated at scale, so media is off there at the protocol layer (see O36). Every other Room inherits whatever its creator picked at Room-creation time. **Moderators:** room creator is mod-by-default; can grant additional mod keys signed by themselves; mods can grant other mods (transitive but each grant signed and revocable). Alternative path: member-elected mods via signed votes, top-N become mods, configurable quorum / rotation cadence. **Moderation actions:** signed `RoomAction { roomId, target, action: REMOVE_MESSAGE | KICK_USER | BAN_USER, reason, modSig }`. Peers honour the action if `modSig` verifies against the current mod set. Anti-abuse integration: Room can subscribe to O67 filters as policy; Room can publish its own block list (sibling of personal blocklist, signed by Room mods) so banned users' content is dropped at every member's client. **@-mention notifications** become a first-class wire surface: new `MessageType.MENTION` with `mentionedUserId`, INFRASTRUCTURE-tier, surfaces in the mentioned user's notification feed (covers "@ a mod" UX for moderated Rooms, plus general DM @-mentions, plus O68's report-to-mod delivery). **UX-only differences between "Room as group chat" and "Room as public space":** member-list visibility (group chat shows roster; open Room may not); join/leave events (group chat surfaces them; open Room may not); thread vs feed presentation. These are display-layer choices, not protocol-layer distinctions. Per-Room creator-mod or elected-mod with explicit remove/kick/ban actions is the content-moderation primitive that scales without a central authority — each Room's creator (or elected mods) is the human-in-the-loop for that Room. Every user can leave or join any Room at will; the global Room is text-only because it cannot have a creator-mod. **Open design questions:** moderator key rotation when a mod leaves / is compromised (NOT borrowing O41/G9 shape since that's deprecated — design a manual-confirm-by-Room-members flow); conflict resolution when two mods take contradictory actions (lean: last-action-wins with public audit log); Room forks when policy is contested (any user can fork; same membership-list seed, new roomId); author edit / delete within Rooms (covered by O40 relay-deletion + new "allow author delete" Room-policy bit). **Status:** Significant design work; one of the larger backlog rows. |
| O80 | **`[PART]`** User-customisable mode-transition schedule | Core data model + matcher shipped: `core/mode/ModeProfile.kt` carries an ordered `List<Rule>` where each Rule is `(Condition, targetMode, optional per-rule overrides)`. Conditions cover battery-above/below threshold, charging state, screen on/off, network (Wi-Fi/cellular/offline), time-of-day range (wraps across midnight), weekday set, and AND-composition. `ModeProfile.evaluate(state)` returns the first matching rule's target mode or the default. `ModeProfileTest` covers each condition + a realistic profile. **Not done:** the Android orchestrator that assembles fresh `DeviceState` from `BatteryManager.BATTERY_PROPERTY_CAPACITY` + plug events + screen receivers and re-evaluates on every change; hysteresis (5% gap on battery thresholds to prevent flapping) at the orchestrator level; firing G12 mode-change pulses on effective-mode transitions; the rule-editor UI. Hooks `BatteryManager.BATTERY_PROPERTY_CAPACITY` + plug events + screen state via standard receivers; periodic check every 60s plus event-driven re-evaluation. UI surfaces transitions silently by default; settings shows last-N transitions for diagnosis. May ship one "starter template" the user can edit as a kindness, but no enforced defaults. Cross-references O33 (battery measurements help users pick threshold values), O57 (modes), O62 (single source of truth for mode behaviour), G12 (mode-change pulse mechanism). |
| O81 | **`[TODO/CODE]`** Local-only on-device image content classifiers — NSFW (still + video), gore (video; still TBD) as shipped-default LOCAL_ONLY plugins** | **Image-content companion to O67 (text-pattern filter).** Two surfaces covered separately — NSFW (sexual content) and gore (graphic violence) — each its own classifier, each its own user-facing WARN/BLOCK control. **Shipped out of the box** as LOCAL_ONLY plugins (O24): cannot reach the network, cannot register a DM envelope, cannot compose outbound messages — loader denies. Cannot leak the user's images anywhere by construction. Runs on every inbound IMAGE-type message before display; sets per-message `contentWarning: { nsfw, gore }` flags the UI consumes. **WARN vs BLOCK are orthogonal user controls:** WARN = blurred-thumbnail-with-tap-to-reveal plus label ("contains sexual content" / "contains graphic violence"); BLOCK = never display the image, drop the attachment locally. Default-WARN for both at first install; promotable to BLOCK per-surface during onboarding. **Stacking / strictness slider:** user can enable multiple classifiers per surface and combine with `any-flags ⇒ flag` (OR) or `weighted-sum ⇒ flag if score > threshold`. UI slider: `off / high-confidence-only / moderate / anything-flagged`. Default: lightest model only (cheapest APK cost); user opts into stacking heavier/stricter via slider. **Per-surface model status (research per `docs/RESEARCH_NOTES.md` §8):** **NSFW stills:** (a) **AdamCodd/vit-base-nsfw-detector** (HuggingFace, Apache 2.0) — ViT-based, ONNX export available, more accurate than Open NSFW, recommended default. (b) **Yahoo Open NSFW** via `tensorflow-open_nsfw` → TFLite (BSD 3-Clause), ~22 MB, model from 2016, acceptable as a stackable second opinion. (c) **NudeNet v3** ONNX (Apache 2.0), granular body-part detection, best accuracy, requires Kotlin/ONNX-runtime port. **NSFW video:** MobileNet-TSM single-frame mode on key frames as a stopgap. **Gore stills: research gap — no production-ready open-source classifier verified.** Three fallbacks until a real still-gore model emerges: (i) MobileNet-TSM single-frame inference (degraded accuracy), (ii) Roboflow community-trained models with disclosed accuracy caveats, (iii) text keyword filter via O67 as the only ship-able default. Ship gore-still as "WARN-via-keywords-only until classifier matures" with honest disclosure. **Gore video:** **MobileNet-TSM** (PMC9621415, paper-bundled GitHub source + trained model + Android demo), ~8.5 MB, real-time on-device, uses temporal frames. **Recommended starting bundle:** AdamCodd ViT (NSFW stills) + MobileNet-TSM (NSFW + gore video) + O67 keyword filter (text + gore-still fallback). Combined ~30 MB. **Model distribution is post-install P2P, not bundled in the APK** — keeps install size slim and lets users opt into specific classifiers. On first launch (or first inbound image), prompt user to download whichever classifiers they want. Models are signed blobs (publisher pubkey + version + SHA-256 + capability declaration). Once one peer has a model, others request it over the mesh via the same chunked-transfer path as O26 plugin sharing; the mesh itself is the distribution channel. Cold-start fetch via plain HTTPS from a published mirror (model author's site, IPFS, GitHub releases) is the only network exception, and only on user-initiated install — after that, mesh re-distribution covers updates. Model verification: same signing infrastructure as plugins (O26 community attestation), with publisher key pinning on first install. Bundled APK installs zero classifiers by default; user explicitly opts in. **Explicit non-decisions:** (1) **Never** Google SafetyCore (system-service, Play-Services-adjacent, breaks F-Droid posture per O56). (2) **Never** Firebase AutoML even though `nipunru/nsfw-detector-android` uses it — same posture violation. (3) **Never** any cloud-API fallback. **Update model on plugin update only** (no runtime download — that would be a network leak). **Pre-ship requirements:** model card published per classifier (training data sources, known biases incl. demographic error-rate gaps — Open NSFW has documented issues with darker skin tones; surface in onboarding copy honestly); on-device-only enforcement at LOCAL_ONLY plugin tier verified by loader; capability disclosure popup before enable (O15). Cross-references O24 (LOCAL_ONLY plugin tier — canonical use case), O67 (text-pattern sibling and the only stills-gore filter until a model matures), O68 (combined App Store 1.2 posture), O56 (no Google system service dependency). **Sources:** [tensorflow-open_nsfw](https://github.com/mdietrichstein/tensorflow-open_nsfw), [AdamCodd/vit-base-nsfw-detector](https://huggingface.co/AdamCodd/vit-base-nsfw-detector), [MobileNet-TSM violence detection (PMC9621415)](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC9621415/), [SafetyCore risks (arXiv 2509.06371)](https://arxiv.org/pdf/2509.06371). |
| O82 | **`[DECISION]`** Median-clock consensus — NOT a core protocol feature; each use case has a better local fix | The proposal was Bitcoin-style network-adjusted-time at HELLO so code that wanted "a shared clock" could read a median across recent peers. **Rejected on review:** every use case Rumor actually has is better served by a local primitive that doesn't require trusting peer-reported clocks. (a) Relay-deletion-on-ACK (O40) → TTL since `receivedAtMs`; each relay decides locally. (b) Scheduled messages (O22) → fire on sender's own clock; no consensus needed. (c) Room mod-action ordering (O79) → Lamport / vector clocks (per-node monotonic counters), the textbook distributed-systems answer. (d) Anti-replay windows → `receivedAtMs` delta or sequence-number window. (e) "What time is it?" without internet → real UX question but it's a UI/utility need, not protocol; ships as an opt-in plugin or sister app (think mesh-NTP-replacement), never in `:core`. The median-clock approach is sound math chasing a problem that doesn't exist here — and it adds Sybil surface (peers can lie about their clocks to drag the consensus) for no actual win. Row kept as a decision so the temptation to add it recurs against the recorded rationale. |
| O83 | **`[TODO/CODE]`** Desktop targets: macOS, Linux, Windows as first-class relay nodes** | Mac, Linux, and Windows are explicit relay-node targets, on equal footing with Android. **Motivation:** computers have larger batteries (laptops), continuous power (desktops, mini-PCs, NUCs), better antennas, no OS-level background-execution restrictions, and most users have at least one. Computers are well-suited to be always-on relay nodes while phones are carry-around participants. **KMP work covers all three desktop targets in one pass:** `commonMain` → `jvmMain` already works on JVM-supporting Linux / macOS / Windows (the same JAR runs on all three). Adding `macosArm64` / `macosX64` / `linuxX64` / `linuxArm64` / `mingwX64` native targets is a separate pass after Phase 1c shims, but the JVM-on-desktop story is shippable today (just needs packaging: `.dmg` / `.deb` / `.AppImage` / `.msi`). **Transport coverage:** mDNS over Wi-Fi (O73) is the cross-platform path; Bluetooth via desktop BLE stacks (`bluez` on Linux, IOBluetooth on macOS, WinRT on Windows). No Wi-Fi Direct on desktops; mDNS + Bluetooth covers it. **Trade-off vs phone targets:** computers are heavier and less portable but more reliable and higher-bandwidth. Desktop nodes are infrastructure; phone nodes are participants. **Distribution:** matches the F-Droid + GitHub-releases-for-Obtainium pattern from O74 — desktop releases ride GitHub releases too. Cross-references O54 (transport plugin tier — desktops can host bridges and additional transports), O55 (prolonged-disconnection design target — a plugged window-mounted machine is the form factor that survives intermittent power), O63 (iOS is thin-client; macOS is the full-capability sibling). |
| O87 | **`[CLOSED]`** see G27 | `PluginContext.signWithLocalKey(bytes): ByteArray` shipped — `IdentityManager` was already threaded into `PluginContextImpl` so no plumbing was needed; just 4 actual lines in the impl. Hard rule "signs ONLY plugin-produced bytes" documented inline. Hard prereq for O20 Keystore now in place. |
| O88 | **`[TODO/UI]`** In-thread plugin display widget hook** | A `@Composable PluginDisplay(message: RumorMessage)?` declared by plugins that want to render their own MessageType inline in `ThreadScreen`. Falls back to default rendering (plain `[binary payload]` or hex-dump) if no plugin claims the type. Required for any payment/attestation companion that wants to show structured content ("$5 sent", "credential verified") inside the chat thread rather than driving the user out to a separate app. Lower priority than O87 because the companion can ship without it (separate-app UX is the v1 fallback). Filed by the O84 audit. |
| O86 | **`[DECISION]`** Crypto-as-payments is not part of Rumor core — separate companion app only** | A "mesh crypto" carrier (Cashu, Lightning, generic value rail) is useful when external infrastructure is degraded (per O55), but it does not live in Rumor core. Rationale: (a) money-transmission regulations (FinCEN MSB / EU MiCA / per-jurisdiction) may attach to whatever "facilitates" transactions — keeping payment semantics in a separate app keeps Rumor unambiguously a transport carrier; (b) conflating identity / messaging / payments into one process creates a single audit surface where a compromise in any layer affects all; (c) Nostr's success at carrying value rails came from being just a relay protocol with separate payment apps on top (Damus + Cashu, Amethyst + Bitcoin etc.). **What Rumor exposes (per O84):** identity + signing + opaque-blob DM + cross-network bridging. **What lives in the separate "mesh crypto" companion:** UTXO / balance tracking, payment composition, wallet UX, regulatory disclosure. The two apps coexist via APK-tier companion (O24) or shared-userId bound-service pattern. |

### Design decisions recorded

- **Latency NOT used for routing.** On BLE/Wi-Fi Direct, measured latency is mostly discovery timing. Ranking is by `bytesRelayed DESC, sessionCount DESC`. `latencyMs` stored for diagnostics only.
- **Relay path never sees blocklist.** Blocking is a display filter only — nodes relay everything regardless of blocklist state. This is intentional: if nodes stopped relaying blocked senders, a coordinated block could erase a person from the entire mesh, turning individual preferences into network-level censorship. Charlie needs Bob's emergency broadcast even if Alice blocked Bob. Your device's resources serve the mesh, not your social preferences. The residual CPU/storage cost of ingesting high-rate blocked senders is addressed separately by per-sender rate limiting (O16).
- **`BLOCKLIST_PUBLISH` is TRANSFER_SETUP, not INFRASTRUCTURE.** Full snapshots can exceed 16 KB; only incremental `BLOCKLIST_DIFF` stays at INFRASTRUCTURE tier.
- **Bridge traffic never re-relayed (today).** Re-signing a bridged message with the bridge's Rumor key would make every foreign-network message look like the bridge node originated it, and would let the bridge launder content it never received. Current invariant: BRIDGED → no relay, full stop. **Cost:** the bridge only reaches its direct Rumor peers; Charlie three hops away in the Rumor mesh never sees Bob's Meshtastic message. This is more restrictive than the design needs to be — see O17 (BRIDGE_VOUCHED trust level) for the path to extending bridged-content reach without re-introducing the laundering risk.
- **Bridges today are broadcast-only.** Both current bridge implementations (Meshtastic, MeshCore) bridge BROADCAST traffic only; DM bridging is unimplemented (O5). The DmEnvelope framework (G5/O5a) supports encrypted DM bridging via Architecture B (envelope passthrough) — the framework is in place; the per-bridge envelope code is what's missing. For BROADCASTs, Rumor hands the radio plaintext and the radio applies channel encryption with its own PSK; inbound, the radio gives us plaintext via `MeshPacket.decoded` (Meshtastic) or after firmware decryption (MeshCore). We never touch channel keys for broadcasts.
- **Encrypted bridging — two architectures, pick one before designing O5.**

  **Architecture A — decrypt-and-re-encrypt (bridge is an endpoint).** The bridge holds both crypto sessions. Sender E2E-encrypts to the bridge's Rumor pubkey; bridge decrypts; bridge re-encrypts to the LoRa contact's PKC key. Pure-relay DMs (Rumor → Rumor that happen to traverse a bridge node) stay E2E because the bridge has no key for messages it isn't the addressed recipient of. **Crypto is sound; trust model changes — the bridge operator can read bridged DMs.** Works for any LoRa stack regardless of envelope format. Required UI before ship (HARD blockers, not nice-to-have):
    1. Bridged contacts must render with a distinct "via <bridge>" label everywhere they appear (compose, contact list, thread header). Never as if they were direct Rumor contacts.
    2. The compose screen for a bridged contact must display a persistent banner: "Messages to this contact pass through <bridge owner>'s device and are readable there." No dismiss.
    3. SAS / safety-number verification must include a three-leg fingerprint (you ↔ bridge ↔ them) instead of the two-leg flow used for native Rumor contacts.
    4. Identity rotation: if the bridge node's Rumor key changes, every bridged contact must be re-verified — bridge-key change ≠ peer-key change but UI must treat it as either.

  **Architecture B — envelope passthrough (bridge sees ciphertext only).** Rumor's DM envelope for bridged contacts becomes byte-compatible with the target network's DM envelope. The bridge forwards opaque ciphertext both ways and never holds plaintext. Preserves real E2E across the bridge. Curve alignment exists for **both** bridges — Meshtastic PKI uses X25519 directly; MeshCore uses Ed25519 for identity and ECDH from the X25519-converted key, same as Rumor. The differences are only in the AEAD wrapper, not in the curve:
  - Rumor native: X25519 ECDH + AES-GCM
  - Meshtastic envelope: X25519 ECDH + AES-CCM + Meshtastic framing
  - MeshCore envelope: ECDH (Ed25519→X25519) + AES-128-ECB + HMAC-SHA256 (Encrypt-then-MAC) + MeshCore framing

  Requires per-bridge:
    1. A second DM-encryption code path in `GossipEngine.composeDirect` selected by recipient userId prefix (`meshtastic:*` → Meshtastic envelope; `meshcore:*` → MeshCore envelope; native → existing X25519+AES-GCM).
    2. Bridge advertises discovered remote NodeInfo / contacts as synthetic Rumor contacts whose `publicKey` field holds the remote pubkey (32 bytes for both — Meshtastic X25519 directly, MeshCore Ed25519 which we convert at ECDH time).
    3. Rumor's Ed25519 signature is dropped for bridged DMs — neither target envelope carries an outer signature, only an AEAD/MAC tag. Acceptable because authenticity is enforced by the AEAD/MAC plus the recipient's knowledge of which key sent it.

  Default recommendation: **Architecture B for both bridges.** Worth the per-bridge envelope code to preserve real E2E. Fall back to Architecture A only if a future bridge has no usable ECDH path. Both architectures still require the bridged-contact UI labelling from A1 above — even passthrough has a bridge in the path, and users need to know which contacts are bridged so a compromised bridge can't quietly substitute its own key for a peer's.

  **Framework: pluggable `DmEnvelope` (O5a).** To avoid hardcoding "if recipient starts with meshtastic: do X, if meshcore: do Y" into core, route DM crypto through a registered envelope. Each bridge plugin owns its envelope; core only knows the interface.

  ```kotlin
  // core/plugin/DmEnvelope.kt
  interface DmEnvelope {
      val recipientPrefix: String                  // e.g. "meshtastic:"
      val envelopeId: String                       // stable wire-format id, persisted on the message
      val selfAuthenticating: Boolean              // true → engine skips outer Ed25519 sig
      fun encrypt(recipientUserId: String, recipientPubKey: ByteArray, plaintext: ByteArray): ByteArray
      fun decrypt(senderUserId: String, senderPubKey: ByteArray, ciphertext: ByteArray): ByteArray?
  }

  // additions to PluginContext
  fun registerDmEnvelope(envelope: DmEnvelope)
  fun injectBridgedDm(
      recipientUserId: String,
      senderUserId: String,
      senderPubKey: ByteArray,
      ciphertext: ByteArray,
      envelopeId: String,
  )
  ```

  Compose path: `GossipEngine.composeDirect` consults the registry by recipient prefix; falls back to the existing native envelope when none matches. Inbound path: bridge plugin calls `injectBridgedDm` with an opaque blob plus the envelope id; engine routes to the recipient's inbox without ever holding plaintext; the recipient's UI looks up the envelope by id and decrypts at read time.

  This is a prerequisite for O5 if we want more than two bridges without forking core every time. Even with just Meshtastic + MeshCore today it is the right shape — two hardcoded branches in `composeDirect` would be the same wrong direction as the original Hilt → Koin "just rewire it where you need it" pattern we already abandoned. Implement O5a first, then O5 for each bridge is a small, isolated addition.

  **Security constraints for O5a (non-negotiable — without these, the framework is a regression).**

  1. **Source-gated `selfAuthenticating`.** Honored **only** for `MessageSource.LOCAL_BRIDGE` (messages arriving via `injectBridgedDm`). Over peer transport, every message must pass Ed25519 verification regardless of any envelope-id claim. This is the same rule that gates `BRIDGE_UNSIGNED` today (commit f898874); the lesson generalises directly. A peer transport claiming `selfAuthenticating` is treated as a corrupt frame and dropped.
  2. **Envelope id is derived, not asserted on the wire.** Recipient looks up the envelope by recipient userId prefix locally. An envelope-id field on the message exists only as a sanity assertion at the bridge boundary — never as the decryption selector. Prevents downgrade attacks where an attacker injects a weak envelope id to coerce decryption with attacker-controlled assumptions.
  3. **One envelope per prefix; registry is append-only per plugin lifecycle.** `registerDmEnvelope` throws on prefix collision. Plugin teardown unregisters atomically. Prevents prefix-squatting plugins from intercepting another bridge's DMs.
  4. **Bridged DMs inherit BRIDGED trust → never re-relayed.** Same invariant as broadcast bridge traffic today.
  5. **Replay protection is the envelope's responsibility.** Document this as a requirement. Native Rumor envelope uses dedup + sequence; bridged envelopes inherit the target network's mechanism (Meshtastic packet id + timestamp; MeshCore sender_timestamp tracking). A reviewer adding a new envelope must explicitly state how replay is prevented.
  6. **Trust model unchanged from existing plugins.** A user-enabled plugin already has full access to broadcast plaintext via `observeIncoming`. Adding DM handling raises the leakage surface only for users who enable a malicious bridge plugin — same gate (PluginCatalog toggle) as today. The framework does NOT widen the trust boundary; it just gives well-behaved bridges a way to preserve E2E that they previously didn't have.

  If any of these six constraints can't be honored by a proposed envelope, fall back to Architecture A for that bridge.
